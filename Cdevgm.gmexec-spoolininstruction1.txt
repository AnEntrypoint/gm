{"prompt":"Edge cases + failure scenarios: rs-plugkit (wasm orchestrator). For every mutable surface/operation/path: empty input, boundary (first/last, size zero vs 1, max int), overflow (unbounded growth, resource exhaustion), reentry (concurrent access, re-entrance), partial failure (one of N completes), degenerate input (nil, wildcard, escape char, cycle), race condition. What is NOT handled? What assumes success? Failure path logic? List finding + root cause per item."}
