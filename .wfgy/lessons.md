## 2026-08-04 -- Shared gm daemon saturated by concurrent session's sqlite lock storm mid-chain
Goal (G): Find why sessions use Grep instead of the gm codesearch verb, and fix it via the gm skill.
What drifted / what went wrong: The real bug (ccsniff's isGmSession() retroactively flagging pre-gm Grep/Glob calls) was found and fixed cleanly, but closing the gm chain's own bookkeeping (a final `instruction` dispatch to check phase/prd_pending_count before Skill(gm-continue)) hit 4 consecutive dispatch_orphaned responses on the shared C:\dev\gm agentplug-runner daemon. Root cause (independently diagnosed and PRD-recorded): a concurrent claude-loop-iter session was running a codeinsight rebuild that held rssearch_vectors/code_chunks sqlite locks for 14+ minutes, causing "database is locked (vm_steps=0)" errors, 30-85s recall syncs, and repeated daemon pid churn (heartbeat staleness under load, not a real crash).
Fix / resolution: Applied BBCR bounded-retry-then-surface: registered the sqlite-lock-starvation root cause as its own PRD row with full witness (rssearch-vectors-sqlite-lock-starvation-under-concurrent-daemon-load), attempted one final bounded instruction dispatch, and on its 4th consecutive orphan, stopped retrying and surfaced instead of continuing to blind-retry a resource saturated by another session's work.
Generalizes to: When a shared multi-project daemon is genuinely saturated by ANOTHER session's long-running operation (not a bug in this session's own dispatch), dispatch_orphaned repeating 3-4x with a changing pid and watcher.log showing sustained "database is locked" from a different sess= tag is diagnostic of contention, not of a dead/broken watcher -- do not reboot (that would interrupt the other session's real work) and do not endlessly retry (that adds load to an already-saturated resource). The correct move is: register the contention itself as a PRD row (it is real, fixable, in-tree work), apply bounded retry, then surface/checkpoint rather than loop forever waiting on someone else's lock.

## 2026-08-04 -- Push blocked by concurrent session's uncommitted rs-plugkit staging
Goal (G): Finish all remaining .gm/prd.yml backlog rows and fix issues discovered along the way, then push.
What drifted / what went wrong: git_push for gm main was denied with a persistent gate error (porcelain check literally failing with "?? git-status-failed", not just reporting a dirty tree) because the rs-plugkit submodule has 4 files staged-but-uncommitted by a concurrent gm session that appears to have stalled mid-edit (README.md, orchestrator/mod.rs, wasm_dispatch/host_abi.rs, wasm_dispatch/verbs.rs -- unchanged across a 15+ minute wait). Waited via a background condition loop for both gm and rs-plugkit to reach a clean state; it never resolved because the concurrent session's staged files never got committed or reverted during the observation window.
Fix / resolution: This session's own real work (ccsniff fix pushed to AnEntrypoint/ccsniff@bf7ac92, agentplug fix pushed to AnEntrypoint/agentplug@df5049c, gm-plugkit auto-install-runner fix committed locally as gm@aebed44e11, plus 7 already-landed rs-plugkit/agentplug PRD rows confirmed and resolved-with-witness) is complete and safe -- the only blocked step is gm's own final push, gated on another session's uncommitted state in a shared submodule. Per single-writer discipline this is a real, structural block (not something to force past by committing over someone else's staged-but-uncommitted work), so it is correctly a checkpoint-and-surface rather than an endless wait.
Generalizes to: A git_push gate denial whose porcelain field is itself an ERROR marker (not real porcelain text) rather than a normal dirty-file listing is worth its own follow-up investigation in rs-plugkit's git_status/git_push verb implementation -- confirm whether the porcelain check handles an in-flight-but-uncommitted submodule state correctly, since "?? git-status-failed" reads like the check itself threw rather than reporting content.
