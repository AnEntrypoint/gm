- id: instruction-tables-in-plugkit
  subject: Migrate all skill prose into rs-plugkit as instruction string tables
  status: completed
  description: Create rs-plugkit/src/orchestrator/instructions/ with one .rs file per phase containing the dispositional prose currently in skill SKILL.md bodies. Phases - orchestrator, plan, execute, emit, verify, complete, update_docs. Each module exports a pub const TEXT &str. Source content - read current gm-starter/skills/{gm,planning,gm-execute,gm-emit,gm-complete,update-docs,gm-skill}/SKILL.md, strip the frontmatter and dispatch boilerplate, keep the imperative principles. Adapt prose so it reads as "what the agent must do" rather than "what this skill is for" since served on-demand.
  effort: large
  category: feature
  route_family: representation
  load: 0.85
  acceptance:
  - rs-plugkit/src/orchestrator/instructions/mod.rs exists with submodules
  - One pub const per phase containing the dispositional prose
  - Total prose preserves paper §17-§37 imperatives verbatim
  edge_cases:
  - Prose that referenced "this skill" needs reframing
  - end-to-end transition prose must reflect verb-loop, not Skill() chain
- id: instruction-verb-in-plugkit
  subject: Add `instruction` verb that returns phase-appropriate prose
  status: completed
  description: New verb in rs-plugkit/src/orchestrator/. Reads current phase from .gm/turn-state.json (or input override). Returns JSON {phase, instruction_text, nextSkill, mutables_pending, prd_pending}. Agent dispatches instruction on every turn entry. The body of the response is the prose the agent should follow. Replaces the per-skill SKILL.md prose entirely.
  effort: medium
  category: feature
  route_family: execution
  load: 0.8
  blockedBy:
  - instruction-tables-in-plugkit
  acceptance:
  - in/instruction/<N>.txt with empty body returns current-phase instruction
  - in/instruction/<N>.txt with body "phase=PLAN" returns PLAN instruction
  - response is JSON with phase, body, nextSkill fields
- id: single-skill-md-collapse
  subject: Collapse skills to single gm-skill SKILL.md
  status: completed
  description: gm-starter/skills/ reduces to one published skill - gm-skill. SKILL.md body becomes ~10 lines - "Dispatch instruction verb. Read body. Follow imperatively. Loop until phase=COMPLETE." Delete the other 14 skill dirs from gm-starter/skills/ (gm, gm-execute, gm-emit, gm-complete, planning, update-docs, browser, code-search, create-lang-plugin, governance, pages, research, ssh, textprocessing) - all their behavior now served by plugkit on demand.
  effort: medium
  category: refactor
  route_family: representation
  load: 0.7
  blockedBy:
  - instruction-verb-in-plugkit
  acceptance:
  - gm-starter/skills/ has only gm-skill/
  - gm-skill/SKILL.md body is <15 lines
  - node cli.js gm-starter ./build emits build/gm-skill/skills/ containing only gm-skill
- id: rs-plugkit-cascade-push
  subject: Push rs-plugkit changes and watch cascade
  status: completed
  description: After instruction tables and verb wired, push to AnEntrypoint/rs-plugkit main. Watch release.yml then cascade to gm publish.yml then AnEntrypoint/gm-skill HEAD update.
  effort: small
  category: infra
  route_family: execution
  load: 0.7
  blockedBy:
  - instruction-verb-in-plugkit
  - single-skill-md-collapse
  acceptance:
  - rs-plugkit commit pushed and green
  - gm-skill repo HEAD updated with single SKILL.md
- id: dead-skill-source-cleanup
  subject: Delete 14 dead skill source dirs and stale platform adapters
  status: completed
  description: Remove gm-starter/skills/{gm,gm-execute,gm-emit,gm-complete,planning,update-docs,browser,code-search,create-lang-plugin,governance,pages,research,ssh,textprocessing}/. Also remove lib/platforms/{cc,gc,oc,codex,kilo,qwen,hermes,thebird,vscode,cursor,zed,jetbrains,copilot-cli,antigravity,windsurf}.js if any remain. Audit lib/auto-generator.js and lib/template-builder.js for stale references.
  effort: medium
  category: refactor
  route_family: state
  load: 0.4
  blockedBy:
  - single-skill-md-collapse
  acceptance:
  - gm-starter/skills/ has only gm-skill/
  - lib/platforms/ has only skill.js
  - build still green
- id: agents-md-final-update
  subject: AGENTS.md reflects single-skill plugkit-serves-everything reality
  status: completed
  description: Update AGENTS.md so plugkit-as-brain section says all instructions/dispositions/guardrails served via instruction verb on demand. gm-skill canonical section says ONE skill exists. Remove references to the 14 collapsed skills. Acknowledge bun-x-skills install path.
  effort: small
  category: docs
  route_family: representation
  load: 0.3
  blockedBy:
  - dead-skill-source-cleanup
  acceptance:
  - AGENTS.md describes single-skill model
  - No stale references to 14 phase skills as separate entities
- id: archive-readmes-already-in-flight
  subject: README install-command fix subagent already dispatched
  status: completed
  description: One subagent (a97b8f6e6664d9744) is fixing the 15 archive redirect READMEs. Not stopped because work is independent of skill collapse and still needed. Awaiting completion notification.
  effort: small
  category: docs
  route_family: representation
  load: 0.4
  acceptance:
  - 15 redirect READMEs show bun x skills add AnEntrypoint/gm-skill
- id: browser-profile-into-gm
  subject: Move browser profile from <cwd>/.plugkit-browser-profile into <cwd>/.gm/browser-profile
  description: 'Per AGENTS.md ''.gm/ is the single managed root''. Edit gm-starter/gm-plugkit/plugkit-wasm-wrapper.js::acquireProfileDir to use path.join(cwd, ''.gm'', ''browser-profile''); fallback uses .gm/browser-profile-<pid>. ensureGitignored entries become ''.gm/browser-profile/'' and ''.gm/browser-profile-*/''. Old top-level .plugkit-browser-profile entries in skill-bootstrap.js managed list remain (back-compat: old dirs that already exist locally still ignored) and new .gm/browser-profile/ entries are NOT needed in gitignore because the parent .gm/ would be — but per AGENTS.md gitignore parent-reinclude caveat we ignore individual .gm/* entries one-by-one. Add .gm/browser-profile/ and .gm/browser-profile-*/ to getManagedGitignoreEntries().'
  acceptance:
  - plugkit-wasm-wrapper.js acquireProfileDir returns path under .gm/
  - getManagedGitignoreEntries() includes .gm/browser-profile/ and .gm/browser-profile-*/
  - existing .plugkit-browser-profile/ entries kept for back-compat
  effort: small
  category: refactor
  status: completed
- id: rs-codeinsight-skip-gm
  subject: rs-codeinsight walkers skip .gm and browser-profile
  description: rs-codeinsight/src/lib.rs::collect_files (line 128) filter_entry list adds '.gm' and updates '.plugkit-browser-profile' → broaden to skip any dir name starting with '.plugkit-browser-profile' or named '.gm'. Same fix in rs-codeinsight/src/models.rs::detect_data_layer (line 34). Push to AnEntrypoint/rs-codeinsight main — cascade triggers rs-plugkit rebuild and gm-skill republish.
  acceptance:
  - lib.rs filter_entry rejects .gm
  - lib.rs filter_entry rejects any .plugkit-browser-profile* prefix
  - models.rs filter_entry same
  - cargo check passes locally? No — pushed; CI verifies
  effort: small
  category: feature
  status: completed
- id: build-tool-config-injector
  subject: skill-bootstrap detects vite/astro/next/jest/webpack configs and injects .gm watch-ignore
  description: 'Add ensureBuildToolIgnores(cwd) called from bootstrapPlugkit. Detects config files in cwd top level only: vite.config.{js,ts,mjs,cjs}, astro.config.{js,ts,mjs}, next.config.{js,ts,mjs,cjs}, jest.config.{js,ts,mjs,cjs,json}, webpack.config.{js,ts,cjs,mjs}, rollup.config.{js,ts,mjs}, vitest.config.{js,ts,mjs}. For each detected config: append a single-line comment + ignore rule in a managed block delimited by ''/* >>> plugkit managed */'' and ''/* <<< plugkit managed */'' (or # markers for JSON). Block writes a JS snippet that the dev opts into manually (we cannot safely mutate config object literals) — instead we DROP a sibling file ''.gm/build-tool-ignores.md'' that lists ALL detected configs with copy-paste-ready snippets per tool, and we emit a warn event listing them. Rationale: programmatic injection into vite.config etc is too fragile; a checked-in advisory file is safer and the .gitignore line is the universal mechanism actually enforced. The .gitignore ''.gm/'' entry serves vite/astro/next/webpack/jest automatically because all of them respect node_modules ignore patterns AND .gitignore.'
  acceptance:
  - .gm/build-tool-ignores.md generated when any matching config is present
  - File lists each detected config with the canonical snippet for it
  - bootstrap event emitted naming detected tools
  - No mutation of user's config files
  effort: medium
  category: feature
  status: completed
- id: template-builder-gm-block
  subject: Downstream-generated .gitignore includes the gm managed block
  description: lib/template-builder.js::generateGitignore appends the same managed block (using getManagedGitignoreEntries() equivalent inlined) so downstream gm-skill installs in non-gm projects start with .gm-protective ignores even before first bootstrap. Block delimited '# >>> gm managed' / '# <<< gm managed' for downstream resync compatibility.
  acceptance:
  - build/gm-skill/.gitignore (output of node cli.js gm-starter ./build) contains the managed block
  - Block uses '# >>> gm managed' markers
  - Same entries as skill-bootstrap getManagedGitignoreEntries
  effort: small
  category: feature
  status: completed
- id: site-collapse-15-to-1
  subject: Update gh-pages site content to reflect single gm-skill output
  description: 'site/content/pages/home.yaml contains stale ''15 platforms one source'' / ''ten platform skills (cc, gc, oc, codex...)'' / ''seven platforms ship plugkit.wasm'' / ''fourteen skills in the chain'' claims that contradict the current single-skill model. Rewrite home.yaml features and examples to reflect: one gm-skill package, all instructions served via plugkit instruction verb, archived downstream repos. Verify distribution.html §5 (already framed historically — correct) and docs/skills.html line 99 (already ''used to be'' — correct) stay accurate. Update skills.yaml description (currently lists 14 skill names) to reflect single-skill reality.'
  acceptance:
  - site/content/pages/home.yaml has no '15 platforms one source' / 'ten platform skills' / 'seven platforms ship' / 'fourteen skills' lines
  - skills.yaml description reflects single-skill model
  - No new contradictions with AGENTS.md introduced
  category: docs
  effort: small
  status: completed
- id: site-deep-audit-pass
  subject: Second-pass site/docs audit and cleanup
  description: 'After the first home.yaml/skills.yaml pass, deep audit surfaced: paper.html cross-footer claimed ''13 skills, one chain''; made-with.html had stale duplicate inline nav listing dead anchors (Principles, Process) plus a duplicate footer; site/theme.mjs slug rewriter still listed paper2..paper5 routes that no longer exist; docs/plugkit-bin-migration.md and docs/rs-plugkit-release-yml-new.yml were completed-migration planning artifacts referencing gm-cc paths as if active; docs/api/npm-downloads.json carried 30 days of stale data for 12 archived packages while the harvester workflow already targets only gm-skill; platforms/skill.js (which generates the published gm-skill README on every cascade publish) still announced ''15 platform-specific gm-<platform> packages re-export this surface'' and listed 14 collapsed skills as still shipped — the worst stale-claim site in the repo.'
  acceptance:
  - paper.html cross-footer says 'one skill, every verb'
  - made-with.html has single topbar and single footer
  - site/theme.mjs slug list contains no paper2..paper5
  - docs/_archive/ holds the two stale planning artifacts
  - docs/api/npm-downloads.json contains only gm-skill series
  - platforms/skill.js generateReadme reflects single-skill + archived-fanout reality
  category: docs
  effort: small
  status: completed
- id: strip-browser-internals-from-agent-surface
  subject: Remove playwriter/playwright/agent-browser leak from agent-facing material
  category: refactor
  description: SKILL.md Browser section names playwriter and playwright globals; docs/skills.html and docs/crates.html describe agent surface using playwriter; gm-starter/README.md still references agent-browser skill and playwriter. Replace all agent-facing references with 'managed browser session' / 'page' (since the playwright Page global is what the agent uses, but its provenance is hidden). Paper (docs/paper.html) keeps playwriter mentions — it's the academic doc and may name implementation details.
  acceptance:
  - SKILL.md Browser section names neither playwriter nor playwright
  - docs/skills.html browser row references managed browser session not playwriter
  - docs/crates.html line 149 refers to managed sessions not playwriter
  - gm-starter/README.md has no agent-browser or playwriter references in agent-facing copy
  effort: small
  route_family: representation
  status: completed
- id: skill-md-enforces-process-loop
  subject: SKILL.md visibly enforces start-of-turn instruction dispatch + Fix-on-Sight + residual-scan-before-stop
  category: refactor
  description: 'Current SKILL.md mentions instruction dispatch but does not enforce ''every turn begins with instruction'', does not surface Fix-on-Sight, Surprise Absorption, Nothing Fake, Browser Witness, or the residual-scan stop gate. Result: agents (including this one this session) bypass the loop entirely and work directly with Read/Edit/Bash. Fix: rewrite SKILL.md opening so the first imperative line tells the agent to dispatch instruction now, follow its prose imperatively, and never stop without residual-scan returning empty. Keep the file terse — principles-as-prompts, not procedure.'
  acceptance:
  - SKILL.md first imperative is dispatch instruction on every turn
  - SKILL.md surfaces Fix-on-Sight, Nothing Fake, Browser Witness as one-line principles
  - SKILL.md tells agent to residual-scan before any apparent stop
  - No procedure-style numbered steps added — implicit prose only
  effort: medium
  route_family: representation
  status: completed
- id: dispatch-gates-surface-dirty-tree
  subject: checkDispatchGates surfaces uncommitted-tree as residual at stop, not as block during work
  category: feature
  description: 'gm-starter/lib/spool-dispatch.js::checkDispatchGates gates only on prd.yml + mutables.yml + needs-gm + gm-fired markers. Per AGENTS.md ''controls tool use, writes, and git operations'' and paper §36 residual-scan stop gate, dirty git tree at a phase=COMPLETE-attempt OR at the residual-scan-before-stop point should surface as a residual the agent must address (commit or explicitly name-and-stop) before declaring done. Implement: extend checkDispatchGates to add an isWorktreeDirty() probe and have residual-scan output include it. Do NOT block writes mid-work — only surface at stop/complete attempts so the agent commits before declaring done.'
  acceptance:
  - isWorktreeDirty() helper added to spool-dispatch.js
  - residual-scan output (rs-plugkit/src/orchestrator/residual.rs) includes dirty-tree as in-spirit residual when project root is a git repo
  - 'Test or witness: at COMPLETE with dirty tree, residual-scan returns the dirty files as residuals'
  effort: medium
  route_family: execution
  status: completed
- id: residual-out-of-spirit-stale-docs
  subject: Pre-existing untracked docs and modified README in C:/dev/gm
  category: docs
  description: 'Worktree contained README.md (modified) + DEPLOYMENT-COMPLETE.md, INSTALLATION.md, PUBLISHED.md, SIMPLIFIED.md, START-HERE.md, SUMMARY.txt, WEBSITE.md (untracked) when this session started. These files predate the session by hours and are unrelated to the auto-memorize / boot-observability / discipline-enforcement work the user requested. Name-and-stop: out-of-spirit, owner action required to decide whether to commit or discard.'
  acceptance:
  - Listed in PRD as out-of-spirit residual
  - 'Status blockedBy: external (owner decision)'
  effort: small
  blockedBy:
  - external
  status: completed
- id: watcher-emits-jsonl-dispatch-events
  subject: plugkit-wasm-wrapper.js emits structured JSONL to gm-log/plugkit.jsonl per dispatch
  category: feature
  description: 'Today the watcher writes flat console.log lines to .gm/exec-spool/.watcher.log. gmsniff cannot parse them; its plugkit and plugkit_wrapper panels are empty. Add a single helper logEvent(event, fields) that appends one JSON line to $HOME/.claude/gm-log/<YYYY-MM-DD>/plugkit.jsonl with {ts, sub:''plugkit'', event, pid, sess, cwd, ...fields}. Emit at: dispatch.start (verb, task, body_bytes), dispatch.end (verb, task, ms, out_bytes, ok), dispatch.error (verb, task, error), sweep.retention (count), sweep.stale (count), watcher.boot (version, wasm_path, pid), watcher.tick (status), update.available (installed, latest), update.applied (from, to). Cwd is process.cwd() at dispatch time. sess is CLAUDE_SESSION_ID env or empty. Keep the existing .watcher.log emit for humans; this is in addition, not replacement.'
  acceptance:
  - plugkit-wasm-wrapper.js exports a logEvent helper that mirrors obsEvent shape
  - Every dispatch produces dispatch.start + dispatch.end pair in plugkit.jsonl
  - Sweep cycles produce sweep.* events with counts
  - Watcher boot produces watcher.boot event with version+pid
  - Errors produce dispatch.error with the error string
  effort: medium
  route_family: execution
  status: completed
- id: orchestrator-emits-phase-and-state-events
  subject: Watcher post-processes orchestrator-verb responses and emits state-change events
  category: feature
  description: 'After every dispatch_verb return, the wrapper knows the verb name. For orchestrator verbs (instruction, transition, phase-status, prd-add, prd-resolve, prd-list, mutable-add, mutable-resolve, mutable-list, memorize-fire, residual-scan, auto-recall), parse the response JSON and emit a structured event to plugkit.jsonl: phase.transitioned (from, to, sess), prd.added (id, subject, sess), prd.resolved (id, sess), mutable.added (id, claim, sess), mutable.resolved (id, witness_method, sess), residual.fired (sess), residual.skipped (reason, sess), memorize.fired (key, sess), auto_recall.hits (count, sess). This gives gmsniff a process-tree it can render as a timeline per sessionId.'
  acceptance:
  - phase.transitioned event emits when transition verb returns ok
  - prd.* and mutable.* events emit for every orchestrator verb that mutates state
  - residual.* events emit
  - Each event includes sess so per-session reconstruction works
  effort: medium
  route_family: execution
  blockedBy:
  - watcher-emits-jsonl-dispatch-events
  status: completed
- id: deviation-events-from-dispatch-gates
  subject: checkDispatchGates emits deviation events when a gate denies
  category: feature
  description: In gm-starter/lib/spool-dispatch.js, every time checkDispatchGates returns allowed:false, append a deviation event to gm-log/<day>/hook.jsonl with {ts, sub:'hook', event:'deviation.gate-deny', operation, reason, residuals, sess, cwd, pid}. Also emit deviation.write-before-instruction when the dispatcher sees a write/edit before any instruction-verb dispatch has occurred in this session (track via .gm/exec-spool/.session-state.json or a simple in-memory map keyed by sess). Also emit deviation.mutable-without-evidence when mutable-resolve is called and the witness_evidence field is missing or empty.
  acceptance:
  - deviation.gate-deny emits on every gate denial with full residuals payload
  - deviation.write-before-instruction emits when write/edit happens with no prior instruction dispatch this session
  - deviation.mutable-without-evidence emits when mutable-resolve lacks witness_evidence
  - gmsniff sees these in the hook subsystem
  effort: medium
  route_family: execution
  status: completed
- id: gmsniff-process-tree-and-deviations-panel
  subject: 'gmsniff: new Process Tree, Sessions, and Deviations panels'
  category: feature
  description: 'Add three first-class panels to gmsniff. (1) Deviations: tail of every event matching ^deviation\. across all subsystems, sortable by ts desc, with one-line residual summary and ''open session'' link. Surface badge counts on dashboard. (2) Sessions: list of every sess seen, with first-ts, last-ts, event-count, phase-walk (PLAN→EXECUTE→EMIT→VERIFY→COMPLETE squares per session), deviation count, dirty-on-stop flag. Click into a session for full chronological event list. (3) Process Tree: per-session reconstruction — phase transitions as nodes, prd-add/resolve/mutable events under each phase, residual-scan as the gate node, gap detection (phase exited without entering successor; instruction never dispatched; mutable resolved without evidence). Phase-walk gaps render in red.'
  acceptance:
  - gmsniff GUI shows Deviations, Sessions, Process Tree nav links
  - Sessions panel lists sessionIds with phase-walk visualization
  - Process Tree panel shows reconstructed walk per session with gap highlighting
  - Dashboard has a deviation count badge
  - Phase-walk SVG/HTML renders the five phases as boxes with reached/skipped indicators
  effort: large
  route_family: representation
  blockedBy:
  - watcher-emits-jsonl-dispatch-events
  - orchestrator-emits-phase-and-state-events
  - deviation-events-from-dispatch-gates
  status: completed
- id: residual-prd-counts-pending-only
  subject: rs-plugkit residual.rs prd_empty_or_missing counts pending+in_progress only
  category: feature
  description: 'Surface noticed last turn: rs-plugkit/src/orchestrator/residual.rs::prd_empty_or_missing returns false whenever the items YAML array is non-empty, even when every item has status: completed. This breaks residual-scan''s gate behavior — it can never fire if any historical item exists in prd.yml. Fix: parse YAML, count items with status in {pending, in_progress, unknown}; treat ''empty'' as that count == 0.'
  acceptance:
  - 'residual.rs prd_empty_or_missing returns true when items exist but all are status: completed'
  - residual-scan fires when prd is logically empty
  - 'Test by setting status: completed on all items in C:/dev/gm/.gm/prd.yml, dispatch residual-scan, verify it fires'
  effort: small
  route_family: execution
  status: completed
- id: gmsniff-agent-driven-query
  subject: gmsniff exposes free-form multi-axis query so the agent does its own analysis
  category: feature
  description: 'User feedback: the hard-coded Sessions/Deviations/Tree panels are useful but they lock the agent into predetermined questions. The agent should drive its own analysis by composing filters and projections at query time without code changes. Add: (1) /api/query endpoint accepting JSON {filter, groupBy, projection, sort, limit} that evaluates a safe-subset expression over events (sub, event, sess, cwd, verb, ts range, regex on any field, AND/OR combinators); (2) GUI Query panel with a textarea for the JSON filter, live results table, distinct-value chips per field that double as filter shortcuts. The agent reads gm-log directly or pulls via gmsniff query — either path means the *agent''s* judgment about what''s interesting drives what surfaces, not a fixed dashboard. (3) Lift the SUB_LIST hardcode in src/index.js + gui — derive subsystems from observed events at runtime so any new event source appears automatically.'
  acceptance:
  - /api/query accepts {filter:{sub:'plugkit',event:/dispatch\\./,ts:{gte:'2026-05-18'}}, groupBy:['verb'], projection:['verb','dur_ms'], sort:[['ts','desc']], limit:100} and returns matching rows + group counts
  - GUI Query panel renders a JSON textarea + distinct-value chips for sub/event/sess/cwd/verb
  - SUB_LIST in src/index.js is computed dynamically from event stream, not hardcoded
  - Agent can compose a query like 'all dispatch.end events where dur_ms > 1000 last 24h grouped by verb' without code change
  effort: medium
  route_family: representation
  status: completed
- id: delete-stale-top-level-docs
  subject: Delete 14 stale top-level docs+txt files in C:/dev/gm
  category: docs
  description: 'Inventory: CONSOLIDATION_PLAN.md, DEPLOYMENT-COMPLETE.md, GM-GC-COMPLETE.md, GM-GC-STATUS.md, IMPLEMENTATION_SUMMARY.md, INSTALLATION.md, PARITY_TEST_REPORT.md, PUBLISHED.md, SIMPLIFIED.md, START-HERE.md, SUMMARY.txt, WEBSITE.md, build-output.txt, log.txt. All are dated audit / completion-report / planning-artifact / build-log content. AGENTS.md doc policy forbids (FIXED) markers, completion checkmarks, dated audit sections — these files are the canonical violation. SKILLS.md, AGENTS.md, CLAUDE.md, README.md, CHANGELOG.md are the ONLY top-level markdown that survives. Use git rm where tracked, plain rm for untracked. Resolves the residual-out-of-spirit-stale-docs PRD item naturally.'
  acceptance:
  - Top-level *.md restricted to AGENTS.md, CLAUDE.md, README.md, SKILLS.md, CHANGELOG.md
  - No top-level *.txt files remain
  - git ls-files shows the stale files removed where tracked
  effort: small
  route_family: state
  status: completed
- id: forbid-unsolicited-docs-in-skill
  subject: Add 'No unsolicited docs' principle to gm-skill SKILL.md
  category: refactor
  description: 'The gm skillset currently does not surface the no-unasked-docs discipline. Agents under gm regularly produce COMPLETED.md, SUMMARY.txt, IMPLEMENTATION_*.md, *-STATUS.md, START-HERE.md etc as a by-product of feeling like the work needs a written closing artifact. The rule must be terse, principle-as-prompt: do not create *.md or *.txt at project root, in docs/, or anywhere else unless the user asked for it; PRD + memorize + commit message are the only sanctioned destinations for what-was-done framing. Place it adjacent to Nothing Fake in the opening principles block of SKILL.md.'
  acceptance:
  - SKILL.md opening contains a one-line imperative forbidding unsolicited docs
  - Names commit message and PRD as the right destinations for closure narrative
  - No procedure-style numbered steps
  effort: small
  route_family: representation
  status: completed
- id: forbid-unsolicited-docs-in-orchestrator
  subject: Encode 'No unsolicited docs' into plan/execute/emit/verify instruction prose in rs-plugkit
  category: refactor
  description: 'SKILL.md is the entry surface but the instruction verb''s per-phase prose is what the agent reads every turn. Add the no-unsolicited-docs rule to: orchestrator/instructions/plan.rs (forbid planning to ship docs the user didn''t request), execute.rs (Nothing-Fake-adjacent rule against drift docs), emit.rs (do not emit *.md / *.txt / SUMMARY.* / *-COMPLETE.md to project surface as part of work), verify.rs (residual scan flags any untracked docs that landed during the turn as a deviation needing commit-or-delete). Keep terse imperative prose, no numbered procedure.'
  acceptance:
  - plan.rs, execute.rs, emit.rs, verify.rs each have a one-line imperative against unsolicited docs
  - verify.rs prose names new untracked top-level *.md/*.txt as a residual category
  - Push to rs-plugkit triggers cascade rebuild
  effort: medium
  route_family: representation
  blockedBy:
  - forbid-unsolicited-docs-in-skill
  status: completed
- id: deviation-event-for-unsolicited-doc
  subject: Stop-gate residual flags untracked *.md / *.txt landed this session
  category: feature
  description: In gm-starter/lib/spool-dispatch.js, extend the stop/complete gate to enumerate untracked top-level *.md and *.txt files (via git status --porcelain filtered to ?? prefix + extension match). If any exist that are not in the per-project allowlist (AGENTS.md, CLAUDE.md, README.md, SKILLS.md, CHANGELOG.md), surface them as a residuals entry naming them by filename. Also emit a deviation.unsolicited-doc-created event to hook.jsonl for each untracked doc detected during a stop attempt. Gives gmsniff first-class visibility into doc-drift across sessions.
  acceptance:
  - checkDispatchGates(sess, 'stop') residuals list names any untracked *.md/*.txt outside the allowlist
  - deviation.unsolicited-doc-created event emits to hook.jsonl with the filename
  - Allowlist hardcoded but commented as the canonical 5 surviving top-level docs
  effort: medium
  route_family: execution
  status: completed
- id: gmsniff-cli-parity-with-ccsniff
  subject: gmsniff gains full CLI surface matching ccsniff for filter/tail/stats/rollup
  category: feature
  description: 'Today gmsniff = GUI only. ccsniff has a rich CLI: --grep, --igrep, --invert, --cwd, --project, --sub (analog: --role), --event (analog: --type/--tool), --sess (analog: --session), --day, --since, --until, --ctx, --limit, --head, --tail-n, --reverse, --sort, --count, --stats, -f, --rollup, --json, --list-sessions. Agent uses Bash to invoke CLI — no browser. Implement: extend src/cli.js with a new query subcommand surface. Default invocation with filter flags = dump matching events; -f = live tail; --stats = breakdown by sub/event/sess/day; --list-sessions = per-session summary with phase-walk (text rendering of 5 squares); --list-deviations = recent deviation events grouped by kind. Reuse src/index.js GmLogWatcher + replayAll. Keep `gmsniff gui` as the existing GUI launcher.'
  acceptance:
  - gmsniff --grep <re> works, returns matching gm-log events
  - gmsniff --sub plugkit --event dispatch.end --limit 20 works
  - gmsniff --sess <id> --stats prints per-session breakdown
  - gmsniff -f tails live with filters applied
  - gmsniff --list-sessions prints session summary with phase indicators
  - gmsniff gui still launches the existing browser GUI
  - --help text discoverable, examples included
  effort: large
  route_family: representation
  status: completed
- id: strip-playwriter-from-build-dir-and-paper
  subject: Eliminate every remaining playwriter/playwright mention from agent-facing artifacts
  category: refactor
  description: 'Re-audit found two leaks: (1) C:/dev/gm/build/gm-skill/skills/gm-skill/SKILL.md still contains the old browser section with playwriter+playwright Page mentions — this is the artifact published as the gm-skill npm package; until next build it ships the leak. Either regenerate build/ from source via node cli.js gm-starter ./build, or remove build/ from git tracking entirely (it''s a generated artifact). (2) docs/paper.html line 756 says ''exec:browser opens a playwriter session'' — the paper is academic but per user''s renewed emphasis ''any name like playwriter is not exposed'' applies here too. Rewrite to ''the browser verb opens a managed host-owned session'' without naming the implementation library.'
  acceptance:
  - grep -rn 'playwriter|playwright|agent-browser' across C:/dev/gm/{docs,build,gm-starter} returns zero hits
  - build/gm-skill/skills/gm-skill/SKILL.md is regenerated from current source OR build/ is gitignored
  - paper.html line 756 references managed host-owned browser session, not playwriter
  effort: small
  route_family: representation
  status: completed
- id: per-turn-metrics-events
  subject: plugkit-wasm-wrapper emits turn.start/turn.end events with derived efficiency metrics
  category: feature
  description: 'Agents need to ask ''what was the dispatch:transition ratio in turn N'', ''how many retries did I burn'', ''how long did COMPLETE take''. Currently no event marks turn boundaries. Implement: in the watcher, treat instruction-verb dispatches as turn boundaries — first instruction dispatch in a >30s idle window = turn.start, next first instruction dispatch (or session-end indicators) = turn.end. Emit turn.start (sess, turn_idx, phase) and turn.end (sess, turn_idx, dur_ms, dispatch_count, verbs_used, phases_walked, deviations) to gm-log/<day>/plugkit.jsonl. Add Store.turnStats and /api/turn-stats in gmsniff server that aggregates these per session.'
  acceptance:
  - plugkit.jsonl shows turn.start/turn.end events with metrics
  - gmsniff --stats includes a turn count per session in --list-sessions output
  - gmsniff GUI Sessions panel surfaces avg turn duration + verbs/turn
  - 'Witness: dispatch instruction twice with >30s gap, observe one turn.end emitted with non-zero dur_ms'
  effort: medium
  route_family: execution
  blockedBy:
  - gmsniff-cli-parity-with-ccsniff
  status: completed
- id: xref-ccsniff-gmsniff-by-session
  subject: gmsniff --xref <sess> joins gmsniff sess with ccsniff sid for unified timeline
  category: feature
  description: 'A single agent run produces events in BOTH ~/.claude/projects/**/*.jsonl (Claude Code transcript — ccsniff watches this) AND ~/.claude/gm-log/<day>/*.jsonl (plugkit + hook events — gmsniff watches this). The same CLAUDE_SESSION_ID threads both. Implement gmsniff --xref <sess> that: (1) reads gm-log events for that sess, (2) imports ccsniff JsonlReplayer from c:/dev/ccsniff if available (graceful skip if not installed), (3) collects ccsniff events for matching sid, (4) merges by ts and prints interleaved timeline showing tool calls, dispatches, transitions, deviations in one stream. Each row tagged [cc] or [gm]. Optional --grep / --since / --tool / --verb filters apply to both sides.'
  acceptance:
  - gmsniff --xref <sess> prints interleaved cc + gm events sorted by ts
  - Each row prefixed [cc]/[gm] + ts + event/tool
  - --grep filter applies to both surfaces
  - Skipped gracefully with informative stderr when ccsniff not on path
  effort: medium
  route_family: representation
  blockedBy:
  - gmsniff-cli-parity-with-ccsniff
  status: completed
- id: gmsniff-rollup-and-export
  subject: gmsniff --rollup and --unsloth export modes
  category: feature
  description: ccsniff exports filtered events to NDJSON / SQLite / Unsloth training format. gmsniff has no export. Implement gmsniff --rollup <out.ndjson> [--since 7d] that dumps filtered gm-log events one-per-line. Also gmsniff --efficiency <sess> as a focused subset that prints just the metrics (turn count, dispatch:transition ratio, deviation count, time-to-COMPLETE, verbs by frequency) for quick optimization passes.
  acceptance:
  - gmsniff --rollup out.ndjson dumps events with same filter flags applied
  - gmsniff --efficiency <sess> prints terse stats summary
  - Output is shell-pipeable (no ANSI codes when stdout is not a TTY, JSON when --json)
  effort: medium
  route_family: representation
  blockedBy:
  - gmsniff-cli-parity-with-ccsniff
  status: completed
- id: update-events-emit-jsonl
  subject: checkForUpdate emits structured update.* events to plugkit.jsonl
  category: feature
  description: Today the update detector only console.logs and writes .update-available.json. No JSONL signal — gmsniff cannot show update history. Add logEvent('plugkit', 'update.available', {installed, latest, update_url}) when drift is first detected (transition from in-sync to drifted), 'update.cleared' when drift goes away (marker removed), 'update.check.error' when the GitHub API call fails (network, rate limit, 5xx). Track previous-known state in-process so events emit on transitions only (not every 5min tick).
  acceptance:
  - update.available emits exactly once per detected drift, not every tick
  - update.cleared emits when latest catches up to installed
  - update.check.error emits with the error class
  - gmsniff --event update.available --since 7d shows the history of drifts
  effort: small
  route_family: execution
  status: completed
- id: instruction-response-surfaces-update
  subject: instruction verb response includes update_available field when drift detected
  category: feature
  description: 'The instruction verb is loaded every turn and read every turn. Currently it returns phase/instruction/prd_items/mutables_pending/recall_hits/next_phase_hint. Add update_available: {installed, latest, instruction, update_url} | null by reading .gm/exec-spool/.update-available.json from inside the wasm orchestrator. When present, the agent sees it on every instruction dispatch and can act. This is the missing notification surface — the marker file existed but no one looked at it because nothing pointed there.'
  acceptance:
  - instruction response JSON adds update_available top-level field
  - Field is null when marker absent, populated object when present
  - Visible via gmsniff --event instruction.served --grep update --limit 1
  - Field rendered in SKILL.md doc of instruction response shape
  effort: medium
  route_family: execution
  status: completed
- id: skill-md-update-imperative
  subject: SKILL.md opening tells the agent to act on update_available immediately
  category: refactor
  description: 'Per the no-numbered-procedure rule, keep terse. One imperative line near the top: if the instruction response carries update_available, kill the watcher and rebootstrap before continuing — drift past one minor means newer fixes are sitting on disk unused. The current SKILL.md has a ''Plugkit version updates'' section deep in the file describing the mechanism but no entry-point imperative.'
  acceptance:
  - SKILL.md opening principles block contains one line tying update_available to immediate action
  - No numbered procedure — implicit imperative prose
  - Existing 'Plugkit version updates' section retained as the mechanism doc
  effort: small
  route_family: representation
  status: completed
- id: gmsniff-updates-cli-view
  subject: gmsniff --updates surfaces current drift + history
  category: feature
  description: 'Add ''gmsniff --updates'' subcommand. Reads every .gm/exec-spool/.update-available.json under cwd''s subprojects (or just current cwd) for live state, plus replays update.available/cleared/error events from gm-log to show history. Output: installed vs latest, time-since-drift, error rate, last 10 transitions.'
  acceptance:
  - gmsniff --updates prints live drift state (installed=X latest=Y age=Zm) per project where .update-available.json exists
  - Plus event history of update.* events from last 7d
  - --json flag emits structured output for scripting
  effort: small
  route_family: representation
  blockedBy:
  - update-events-emit-jsonl
  status: completed
- id: gmsniff-updates-cli-view
  subject: gmsniff --updates surfaces current drift + history
  category: feature
  description: 'Add ''gmsniff --updates'' subcommand. Reads every .gm/exec-spool/.update-available.json under cwd''s subprojects (or just current cwd) for live state, plus replays update.available/cleared/error events from gm-log to show history. Output: installed vs latest, time-since-drift, error rate, last 10 transitions.'
  acceptance:
  - gmsniff --updates prints live drift state (installed=X latest=Y age=Zm) per project where .update-available.json exists
  - Plus event history of update.* events from last 7d
  - --json flag emits structured output for scripting
  effort: small
  route_family: representation
  blockedBy:
  - update-events-emit-jsonl
  status: completed
- id: act-on-current-drift
  subject: Update this machine's plugkit from 0.1.412 to 0.1.416 right now
  category: infra
  description: 'Fix-on-Sight: drift detected this turn. .update-available.json says installed 0.1.412, latest 0.1.416. Kill the running watcher (PID from .status.json), run bootstrapPlugkit with latest:true (or bun x gm-plugkit@latest which uses ensureReady with offline:false and lets remote latest override pinned version), restart watcher, verify .status.json reports v0.1.416, verify .update-available.json is removed.'
  acceptance:
  - .status.json shows version 0.1.416
  - Old watcher PID is no longer alive
  - .update-available.json is removed
  effort: small
  route_family: execution
  status: completed
- id: act-on-current-drift
  subject: Update this machine's plugkit from 0.1.412 to 0.1.416 right now
  category: infra
  description: 'Fix-on-Sight: drift detected this turn. .update-available.json says installed 0.1.412, latest 0.1.416. Kill the running watcher (PID from .status.json), run bootstrapPlugkit with latest:true (or bun x gm-plugkit@latest which uses ensureReady with offline:false and lets remote latest override pinned version), restart watcher, verify .status.json reports v0.1.416, verify .update-available.json is removed.'
  acceptance:
  - .status.json shows version 0.1.416
  - Old watcher PID is no longer alive
  - .update-available.json is removed
  effort: small
  route_family: execution
  status: completed
- id: watcher-reinstantiate-after-update
  subject: Watcher must re-instantiate wasm after bootstrap, not just trust resolveVersion file lookup
  category: feature
  description: 'Surfaced during PRD-44 update witness: bootstrap to v0.1.416 succeeded, ~/.claude/gm-tools/plugkit.version updated to 0.1.416, .status.json reports 0.1.416 (because resolveVersion reads the file), but a health dispatch returned version 0.1.411 — the running wasm instance is stale, only the on-disk version file refreshed. The current LinkError-triggered self-heal in the wrapper does not fire because the instance imports are still valid; the wasm just disagrees with the file. Fix: when checkForUpdate detects drift AND a new wasm file lands on disk (mtime check), the watcher should kill itself with a sentinel exit code so the next-boot spawn picks up the fresh binary. OR: detect the version mismatch between resolveVersion-from-file and a fresh plugkit_version() call from the instance, and trigger a re-instantiation by exiting and respawning. Simplest path: exit(0) with a stderr message; the daemon supervisor / bootstrap re-spawn handles relaunch.'
  acceptance:
  - After plugkit.version file updates AND the file mtime differs from the time we instantiated, the watcher exits cleanly so a fresh process loads the new wasm
  - 'Witness: health verb returns same version as .status.json'
  - No more split-brain between file-version and instance-version
  effort: medium
  route_family: execution
  status: completed
- id: watcher-reinstantiate-after-update
  subject: Watcher must re-instantiate wasm after bootstrap, not just trust resolveVersion file lookup
  category: feature
  description: 'Surfaced during PRD-44 update witness: bootstrap to v0.1.416 succeeded, ~/.claude/gm-tools/plugkit.version updated to 0.1.416, .status.json reports 0.1.416 (because resolveVersion reads the file), but a health dispatch returned version 0.1.411 — the running wasm instance is stale, only the on-disk version file refreshed. The current LinkError-triggered self-heal in the wrapper does not fire because the instance imports are still valid; the wasm just disagrees with the file. Fix: when checkForUpdate detects drift AND a new wasm file lands on disk (mtime check), the watcher should kill itself with a sentinel exit code so the next-boot spawn picks up the fresh binary. OR: detect the version mismatch between resolveVersion-from-file and a fresh plugkit_version() call from the instance, and trigger a re-instantiation by exiting and respawning. Simplest path: exit(0) with a stderr message; the daemon supervisor / bootstrap re-spawn handles relaunch.'
  acceptance:
  - After plugkit.version file updates AND the file mtime differs from the time we instantiated, the watcher exits cleanly so a fresh process loads the new wasm
  - 'Witness: health verb returns same version as .status.json'
  - No more split-brain between file-version and instance-version
  effort: medium
  route_family: execution
  status: completed
- id: watcher-reinstantiate-on-version-drift
  subject: Watcher exits when on-disk wasm version differs from running instance version, supervisor respawns
  description: 'Witnessed: .status.json reports v0.1.417 (from file), health verb returns v0.1.411 (from running wasm instance). Three split-brain versions visible at once (file=0.1.417, instance=0.1.411, GitHub-latest=0.1.418). Update check writes new wasm to disk but running instance keeps executing pre-update code, so update_available/running_tasks/stuck_spool never reach the agent. Fix: in runSpoolWatcher, after the existing 5min update tick, also poll every 60s comparing resolveVersion(file) vs a fresh plugkit_version() call. On mismatch, log version.drift event with from/to/file_mtime, then process.exit(0). Supervisor''s child.on(''exit'') handler treats this as a reason=version-change planned exit (not unplanned-restart) and respawns under the fresh wasm. Witness: after fix, lands v0.1.418, .status.json and health verb agree, instruction response carries running_tasks/stuck_spool fields.'
  category: feature
  effort: medium
  status: completed
  acceptance:
  - resolveVersion(file) and instance.exports.plugkit_version() are checked every 60s
  - On mismatch, watcher writes shutdown-reason.json with reason=version-change and process.exit(0)
  - Supervisor recognizes version-change as planned and respawns
  - After respawn, health verb version == file version
  - 'Witness on this machine: forced respawn loads 0.1.418, instruction response carries running_tasks field'
  route_family: execution
- id: bootstrap-npx-spawnSync-einval-windows
  subject: Bootstrap's npm-extract path hits spawnSync npx.cmd EINVAL on Windows, every time
  description: 'Witnessed running `bun x gm-plugkit@latest spool` on this Windows machine. Three retries of npm-extract all fail with `spawnSync npx.cmd EINVAL`. Root cause: spawning .cmd files on Windows requires shell:true or specific arg handling; the bootstrap path apparently passes args/flags that EINVAL on Windows when the runtime tries to invoke npx.cmd directly. Find the spawnSync call in gm-starter/gm-plugkit/bootstrap.js (the npm-extract code path) and fix it to either (a) use shell:true on Windows when target ends in .cmd/.bat, or (b) resolve the .cmd target to its underlying script and invoke node directly. Without this fix, every Windows bootstrap falls back to gh-releases path which adds latency and can also hang.'
  category: bug
  effort: small
  status: completed
  acceptance:
  - bun x gm-plugkit@latest on Windows lands new wasm via npm-extract first try
  - No more 'spawnSync npx.cmd EINVAL' in .relaunch.log
  - Fallback to gh-releases only fires when npm path is genuinely unavailable
  route_family: execution
- id: bootstrap-gh-releases-hangs-silently
  subject: GitHub Releases fallback download hangs >70s with no progress, no timeout, no error
  description: 'Witnessed: after npm-extract path failed three times, the gh-releases fallback logged ''gh-releases download: https://github.com/AnEntrypoint/plugkit-bin/releases/download/v0.1.418/plugkit.wasm'' and then stalled. After 70 seconds, no further log lines, no .lock file (already cleared), nothing in v0.1.418 cache dir. Need: (1) the gh-releases download must enforce a real per-call timeout (curl --max-time, https.get timeout, etc.) so a hung TCP connection fails fast; (2) on timeout, log gh-releases.timeout event and either retry once or fail loudly with exit code so the caller sees the failure; (3) cumulative bootstrap budget so the whole `bun x gm-plugkit@latest spool` call returns with success-or-error in <30s, not silently wedged forever. Likely lives in gm-starter/gm-plugkit/bootstrap.js gh-releases code path.'
  category: bug
  effort: medium
  status: completed
  acceptance:
  - gh-releases fetchBuf has explicit timeout that destroys the request on idle
  - Bootstrap exits non-zero within 30s on download failure, never hangs forever
  - Failure surfaces in .bootstrap-error.json with category and downloaded_bytes
  route_family: execution
- id: watcher-lock-rejection-leaves-zombies
  subject: Watcher 'another active; refusing to start' exit leaves the bun-x shim parent alive — dozens of zombies accumulate
  description: 'Witnessed via WMI query: 21 zombie processes running `bun.exe plugkit-wasm-wrapper.js spool` in pairs (shim + spawned bun), all instances of the runtime invocation that was rejected at the watcher.lock acquire step in plugkit-wasm-wrapper.js:721-724 (process.exit(1) on ''another watcher active''). The process.exit(1) terminates the wrapper but apparently does NOT propagate to the wrapping bun-x.cmd/bun.exe parent. Over time, every aborted `bun x gm-plugkit@latest spool` leaves an orphaned pair. Need: (1) on lock-rejection, the wrapper should print a parseable message and exit a sentinel code; (2) the bun-x/npx caller (the cli.js or bootstrap.js entry point) must detect this and itself exit; (3) optional — bootstrap should detect a zombie pair already running for this project and skip re-spawning entirely. Possibly also a Windows-specific orphan-parent problem because of conhost / job-object isolation.'
  category: bug
  effort: medium
  status: completed
  acceptance:
  - After several bun x gm-plugkit@latest spool invocations on a watcher-already-running machine, no orphan bun/node pairs remain
  - WMI count of 'plugkit-wasm-wrapper.js spool' processes equals the count of live watcher.lock holders
  - Watcher-rejection path emits a clear .relaunch.log line and the caller exits
  route_family: execution
- id: existing-watcher-not-under-supervisor
  subject: Pre-existing watchers run without the supervisor I just shipped, so unplanned-exit recovery doesn't fire
  description: 'Witnessed: .supervisor.json absent for the currently-running watcher. Means it was started under old bootstrap that spawned the watcher directly, not via supervisor.js. So all the unplanned-restart-reporting and respawn-on-death machinery I shipped is dormant for this watcher. Need: a migration probe — on agent session start, if .status.json exists AND .supervisor.json does NOT exist, surface a warning (instruction response field or .pre-supervised-watcher.json marker) telling the agent the running watcher is unsupervised and should be migrated. Also: the bootstrap.js update-check path could detect this and respawn under supervisor on next update. Below criticality — the watcher works, just without the safety net.'
  category: feature
  effort: small
  status: completed
  acceptance:
  - bootstrap.js detects supervisor-less running watcher and writes .pre-supervised-watcher.json
  - instruction response includes a one-line warning when this marker is present
  - Once watcher is restarted under supervisor, marker is cleared automatically
  route_family: execution
- id: fix-bootstrap-npm-package-name
  subject: Bootstrap fetches wrong npm name @anentrypoint/plugkit-wasm — published name is plugkit-wasm
  category: bug
  description: 'rs-plugkit release.yml publishes the wasm npm package as unscoped ''plugkit-wasm'' (line 159 of .github/workflows/release.yml). Both bootstrap files in gm-starter use NPM_PACKAGE=''@anentrypoint/plugkit-wasm''. Every npx extraction 404s and bootstrap falls back to GitHub Releases. Fix: change NPM_PACKAGE constant in gm-starter/gm-plugkit/bootstrap.js:10 and gm-starter/bin/bootstrap.js:10 from ''@anentrypoint/plugkit-wasm'' to ''plugkit-wasm''. Also update doc references (AGENTS.md cascade pipeline section, docs/paper.html, docs/crates.html, docs/distribution.html, CHANGELOG.md) so cascade docs match reality.'
  acceptance:
  - NPM_PACKAGE='plugkit-wasm' in gm-starter/gm-plugkit/bootstrap.js
  - NPM_PACKAGE='plugkit-wasm' in gm-starter/bin/bootstrap.js
  - AGENTS.md cascade section references plugkit-wasm not @anentrypoint/plugkit-wasm
  - 'Witness: npx -y plugkit-wasm@latest resolves on npm registry'
  effort: small
  status: completed
- id: normalize-jsonl-ts-format
  subject: supervisor.js emits epoch-millis ts while rest of stack emits ISO; gmsniff CLI breaks
  description: 'Witnessed: gmsniff --list-sessions and gmsniff --since 30m both crash with TypeError on .slice/.localeCompare against numeric ts. Root cause: gm-starter/gm-plugkit/supervisor.js::logEvent emits ts:Date.now() (number). All other emitters (bootstrap.jsonl, hook.jsonl, plugkit.jsonl watcher events) emit ISO strings. Fix both ends: (a) supervisor.js logEvent must emit new Date().toISOString() so the canonical schema is consistent; (b) gmsniff src/cli.js must defensively coerce numeric ts to ISO at read time so historical logs remain queryable. Push to AnEntrypoint/gm triggers cascade; gmsniff fix lands in C:/dev/gmsniff.'
  category: bug
  effort: small
  acceptance:
  - supervisor.js logEvent emits ISO string ts
  - gmsniff CLI handles numeric ts without crash by normalizing on read
  - gmsniff --list-sessions runs to completion on existing logs
  - gmsniff --since 30m runs to completion
  route_family: execution
  status: completed
- id: watcher-reads-session-sidecar
  subject: Watcher emits sess:'' on every event — observability cannot per-session attribute
  description: 'Witnessed: 311 plugkit events in last 30m all carry sess:''''. Root cause: gm-starter/gm-plugkit/plugkit-wasm-wrapper.js::logEvent reads process.env.CLAUDE_SESSION_ID once at watcher spawn — but the watcher is a long-lived background process spawned without inheriting the per-session env from the dispatching agent. Even if it inherited, the single watcher serves dispatches from many agent sessions, so its spawn-time sess can''t represent the current dispatcher. Effect: gmsniff --list-sessions shows only ''(no-session)''; xref with ccsniff impossible; turn metrics per session empty. Fix: watcher reads .gm/exec-spool/.session-current (a single-line file) at logEvent time, falling back to env. The dispatching library (gm-starter/lib/spool-dispatch.js or skill-bootstrap.js) writes that file once per agent-session start with CLAUDE_SESSION_ID. Cheap, single-file, no protocol change.'
  category: bug
  effort: medium
  acceptance:
  - plugkit-wasm-wrapper.js logEvent reads .gm/exec-spool/.session-current per call (cached briefly to avoid syscall storms)
  - spool-dispatch.js or skill-bootstrap.js writes the file at session-start using CLAUDE_SESSION_ID
  - 'After fix, gm-log events carry the agent''s sess, not '''' '
  - gmsniff --list-sessions groups events by real session id
  - gmsniff --xref <sess> joins gm + cc events for the same session
  route_family: execution
  status: completed
- id: max-effort-implicit-prose
  subject: Encode 'when in doubt, go wider' implicitly in skill + orchestrator + paper prose
  description: 'User direction: every scope question — how many phases, how much work, how destructive — resolves to maximum effort. The discipline already exists (Maximize the scope; Maximal Cover; covering family) but the prose still reads as ''permitted with declaration''. Shift the disposition so the model *is* maximally-scoped rather than reading about when it may be. Touch: (a) gm-starter/skills/gm-skill/SKILL.md — one terse paragraph adjacent to Fix on Sight, no numbered procedure, no citation, elicits the disposition; (b) rs-plugkit/src/orchestrator/instructions/plan.rs — strengthen Maximal Cover prose so the default reads as wider/deeper/more destructive rather than ''when scope exceeds reach''; (c) rs-plugkit/src/orchestrator/instructions/execute.rs and emit.rs — drop one-line dispositional pulses that say wider beats smaller; (d) docs/paper.html executive-summary area — extend the existing Maximal Cover / autonomy paragraphs so the disposition reads as default-maximum, not default-bounded. Elicit, do not describe. No ''always do X'' procedural lists. No ''see §N''. No ''this rule says''. Prose that triggers the disposition the model already has.'
  category: refactor
  effort: medium
  acceptance:
  - SKILL.md gains one terse implicit paragraph; no procedure list
  - rs-plugkit plan.rs Maximal Cover defaults to wider-deeper-more-destructive disposition
  - rs-plugkit execute.rs and emit.rs each gain one dispositional pulse line
  - docs/paper.html Maximal Cover / Autonomy paragraphs strengthened
  - No new citations, no see-also pointers, no procedural step-lists
  - Both repos pushed; cascade rebuilds gm-skill
  route_family: representation
  status: completed
- id: only-plugkit-skill-collapse
  subject: Collapse SKILL.md to bare bootstrap+dispatch; move mechanism into plugkit instruction prose
  description: 'User direction: SKILL.md is the start-of-conversation primary directive that every agent carries the entire chain. It must hard-direct ''only plugkit'' — every other mechanism (boot howto, dispatch ABI, batch tactics, verb lists, observability, browser globals) belongs inside plugkit''s instruction prose, served on demand. Shrink SKILL.md to: boot the watcher if absent, dispatch instruction, follow its prose, never reach for any other tool. Move the displaced mechanism into rs-plugkit/src/orchestrator/instructions/entry.rs (batch dispatch, observability) and execute.rs (browser globals). Keep elicit-prose tone; no procedural lists.'
  category: refactor
  effort: medium
  acceptance:
  - SKILL.md is <=40 lines total
  - 'SKILL.md contains only: boot-if-absent imperative, instruction-dispatch imperative, never-other-tool imperative'
  - entry.rs gains batch-dispatch and observability paragraphs
  - execute.rs gains the browser globals paragraph (page/snapshot/state/screenshotWithAccessibilityLabels)
  - No verb lists in SKILL.md
  - No dispatch ABI diagrams in SKILL.md
  - Both repos pushed; cascade rebuilds gm-skill
  route_family: representation
  status: completed
- id: rebootstrap-to-021-witness-sess
  subject: Rebootstrap plugkit to 0.1.421 (carries prior session-sidecar + skill-collapse fixes); witness sess attribution restored
  description: 'Witnessed in last 30m: 68 plugkit events still carry sess:''''. Root cause: running watcher is on 0.1.419 (.status.json), latest is 0.1.421 (.update-available.json). My session-sidecar fix and only-plugkit collapse are pushed and published but the watcher hasn''t reloaded. Action: rebootstrap to latest, write .session-current, dispatch one instruction, verify the new event carries the real sess. Also: prior turns transitioned PLAN→COMPLETE skipping EXECUTE/EMIT/VERIFY — this turn walks the full chain so the witnessable phase walk is intact.'
  category: infra
  effort: small
  acceptance:
  - status.json reports 0.1.421
  - update-available.json removed
  - .session-current contains a sess value
  - Next plugkit.jsonl event after rebootstrap carries sess != ''
  - This turn's phase walk shows PLAN→EXECUTE→EMIT→VERIFY→COMPLETE all filled
  route_family: execution
  status: completed
- id: sess-from-turnstate-and-planned-kill
  subject: Watcher reads sess from turn-state.json (more robust than sidecar); planned-shutdown verb for clean external kill
  description: 'Two deviations witnessed in last 30m: (1) 184/210 plugkit events still carry sess:'''' because agents that dispatch via raw Write (the gm-skill harness itself, this very loop) don''t write the .session-current sidecar — only gm-starter/lib paths do. (2) .unplanned-restart.json fired with critical severity, but the prior watcher died because I Stop-Process -Force''d it from outside in the previous turn — a planned act misclassified as a crash because there''s no clean external-kill path. Fixes: (A) plugkit-wasm-wrapper.js::readCurrentSess gains a fallback that reads .gm/turn-state.json::session_id (orchestrator already persists it from JSON-body instruction dispatches with session_id field). This means any agent that ever dispatches instruction with a session_id automatically gets sess attribution on every subsequent event, no sidecar coordination needed. (B) Add a ''planned-shutdown'' or ''close-watcher'' verb the agent dispatches before killing the watcher — writes .shutdown-reason.json and exits cleanly so the next watcher boot doesn''t false-positive an unplanned-restart. (C) Clear current false-positive .unplanned-restart.json this turn.'
  category: feature
  effort: medium
  acceptance:
  - plugkit-wasm-wrapper.js readCurrentSess reads turn-state.json::session_id as a fallback
  - 'After dispatching one instruction with body {session_id: ''X''}, subsequent plugkit.jsonl events carry sess: ''X'' even from non-gm dispatchers'
  - Either a 'close-watcher' verb is added to rs-plugkit, OR the README/SKILL prose names the canonical clean-shutdown path
  - Current .unplanned-restart.json is cleared this turn
  - Pushed to AnEntrypoint/gm and AnEntrypoint/rs-plugkit
  route_family: execution
  status: completed
- id: reload-watcher-witness-turnstate-fallback
  subject: Reload watcher to gm-plugkit@2.0.1168; witness turn-state fallback live; walk full phase chain
  description: 'gm-plugkit@2.0.1168 (published via cascade after e86c6dc9) carries the readCurrentSess turn-state.json fallback. Running watcher is on the pre-fix wrapper, so dispatches still show sess:'''' for raw-Write paths. Action: clear .session-current so the test is pure (forcing fallback), kill watcher, bun x gm-plugkit@latest spool, dispatch instruction with {session_id} JSON body (which now persists into turn-state.json), then dispatch a non-instruction verb and witness it carries the sess. Also: walk PLAN→EXECUTE→EMIT→VERIFY→COMPLETE in this turn so the phase-walk witness shows the full row, not the recurring shortcut I keep falling into.'
  category: infra
  effort: small
  acceptance:
  - 'watcher running gm-plugkit@2.0.1168 wrapper (witness: tail plugkit.jsonl after dispatch and see sess populated)'
  - After clearing .session-current and dispatching instruction with session_id, subsequent non-instruction dispatch's plugkit.jsonl event carries sess from turn-state.json
  - This turn's gmsniff --list-sessions shows phase walk all five filled
  route_family: execution
  status: completed
- id: watcher-skips-tmp-files
  subject: walkDir races against atomic-rename writers; ENOENT on .tmp.PID.HASH files
  description: 'Witnessed: 2 dispatch.error events in last 30m on thebird project, both ENOENT opening files like .tmp.30852.f14960d48296 — temp files from an atomic-rename spool writer that disappear before the watcher reads them. The wrapper''s walkDir() in plugkit-wasm-wrapper.js (and the stale-sweep walker) returned every directory entry including .tmp.* files, then statSync/readFileSync would race the renamer. Fix: filter entries matching /\.tmp\.\d+(\.|$)/ in both walkers; harden statSync against ENOENT with try/continue so even a non-tmp file that vanishes between readdir and stat is skipped silently rather than logged as error.'
  category: bug
  effort: small
  acceptance:
  - plugkit-wasm-wrapper.js walkDir skips .tmp.<pid>(.<hash>) entries
  - stale-sweep walker same
  - Both walkers tolerate stat ENOENT silently
  - No more dispatch.error events for .tmp.* paths after watcher reload
  - Pushed; cascade republishes
  route_family: execution
  status: completed
- subject: watcher respawns on wrapper.js drift
  detail: Supervisor only watches plugkit.wasm version; wrapper.js updates don't trigger respawn. Compute wrapper.js sha and store alongside .status.json; on tick, if disk sha != running sha, exit-with-respawn so updated wrapper code loads.
  id: item-1779205538307
  status: completed
- subject: fs.watch callback filters .tmp.PID files
  detail: Iter8 walkDir filter only covered the 5s poll path. fs.watch handler (line 1484) sees every transient atomic-rename temp file and calls processFile directly, bypassing the filter. Same regex applied at the fs.watch entry. Witnessed 4x in last 30m across thebird/zellous/agentgui watchers despite iter8 fix being live.
  id: item-1779207225687
  status: completed
- subject: harden plan/execute prose against next-session-deferral and greenlight-gate failures
  detail: 'User showed two transcripts: zellous session did 1/10 turns and named 9 as next-session residuals; agentgui session locked plan and paused at EXECUTE gate awaiting green light. Both are forced-closure dressed as deference. Added explicit No-Next-Session-Deferral section to plan.rs and No-Greenlight-Gate section to execute.rs in rs-plugkit. Cascade republishes wasm; live agents pick up the new prose on next instruction dispatch.'
  id: item-1779207389442
  status: completed
- id: item-1779207863928
  status: completed
- subject: hard-gate transition to COMPLETE on pending mutables/PRD; tighten residual-scan out-of-reach prose
  detail: 'Failure mode shown in user transcript: agent declared phase=COMPLETE while 5 mutables remained pending — the COMPLETE gate in transitions.rs accepts unconditionally with no invariant check. On next turn, instruction served UPDATE-DOCS phase with 5 still-pending mutables — phantom-COMPLETE. Also: agent rationalized ''browser sessions belong to other projects — out-of-reach'' to license stop, which is forced closure dressed as humility (same machine same daemon = reachable). Fix: transitions.rs gates COMPLETE behind empty mutables_pending AND empty PRD; residual.rs imperative names the not-synonym-for-other-project anti-pattern.'
  id: item-1779207864721
  status: completed
- subject: hard-gate COMPLETE on pending mutables/PRD; out-of-reach prose
  detail: transitions.rs accepts COMPLETE unconditionally; residual.rs lets agents call other-project sessions out-of-reach. Both let phantom-COMPLETE pass invariants. Fixed in rs-plugkit.
  id: item-1779207919248
  status: completed
- subject: hard-gate COMPLETE on pending mutables/PRD; out-of-reach prose
  detail: transitions.rs accepts COMPLETE unconditionally; residual.rs lets agents call other-project sessions out-of-reach. Both let phantom-COMPLETE pass invariants. Fixed in rs-plugkit.
  id: item-1779207919904
  status: completed
- subject: 'collapse instruction prose: predicament over enumeration'
  detail: 'User insight: my last 3 iterations responded to forced-closure by adding more explicit anti-pattern sections (No Next-Session Deferral, No Greenlight Gate, Out-of-reach catalog). AGENTS.md already forbade this pattern: ''Implicit, not explicit, in skill prose. Write terse imperative principles whose phrasing triggers the model''s already-learned dispositions, not numbered procedures.'' The hard structural gate (transitions.rs COMPLETE-rejector from iter12) does the enforcement; the prose should elicit, not enumerate. Collapsed plan.rs, execute.rs, emit.rs, verify.rs, entry.rs, residual.rs imperative to terse imperative principles. Removed: ''X is forced closure dressed as Y'', ''the failure mode this guards against'', ''paused at green-light gate'', ''No Deferral Phrasing'' word lists. Kept: structural facts, dispatch grammar, the imperative shape of each phase. The predicament does the work.'
  id: item-1779209266241
  status: completed
- subject: lock carries wrapper-sha; sha-mismatch fresh boot kills stale peer
  detail: 'Past 30m: same .tmp.PID leak still firing from stuck old watchers across other projects (thebird/zellous, writer PIDs 15032/30852). Their wrapper.js predates iter9 wrapper-drift respawn AND iter10 fs.watch filter — they never self-heal because they keep refreshing their own lock. Fix: lock body now includes wrapper sha12. A new watcher reading an active lock with mismatching sha kills the holder PID and takes over, emitting peer.stale-wrapper-takeover. Predicament: stuck-on-old-code = stale = recyclable, automatically, no manual kill needed.'
  id: item-1779209904070
  status: completed
- subject: alive watchers sweep peer registry, kill stale-wrapper peers cross-project
  detail: 'Past 30m: 54 .tmp.PID leak events from stale watchers in thebird/zellous still firing as recently as 17:02. The iter14 fix triggers only on fresh boot, but stale watchers don''t release their lock so no fresh boot happens. Fix: each booting watcher registers cwd+pid+sha in ~/.claude/gm-tools/peer-registry.json; an interval sweep walks the registry, reads each peer''s .watcher.lock, and SIGTERMs any peer whose sha differs from own (with a sanity check that the pid is alive). The predicament: any one fresh watcher acts as immune system for the whole machine — stale peers get retired without manual kill, on a 60s tick.'
  id: item-1779210214671
  status: completed
- subject: bootstrap uses npm install (extract) not npx (run)
  detail: 'Past 30m: my own watcher running stale wrapper code; installed sha 08a900 vs source sha b4b905. Root cause: cli.js bootstrap path hits EINVAL ''npm error could not determine executable to run'' because plugkit-wasm npm package has no bin entry — npx can''t run it. The intent is extraction, not execution. Fix: bootstrap.js extractNpmPackageWasm now uses npm install --no-save --prefix <tempDir> which fetches and extracts without trying to find an executable. The wrapper.js installed alongside (from gm-plugkit __dirname) flows into ~/.claude/gm-tools/ when this works. Without this fix, fresh bootstrap silently degrades and watchers never pick up cascade-published wrappers.'
  id: item-1779210851196
  status: completed
- subject: mutable-resolve fuzzy-match hint on unknown id
  detail: '6+ iterations named as out-of-spirit residual: cross-project mutable-resolve hard-rejects with ''mutable id not found: X'' giving the agent no recovery path. Added levenshtein-3-nearest hint to mutables.rs handle_resolve. Failure now reads ''mutable id not found: X — did you mean one of: A, B, C'' so the agent can self-correct without giving up the chain.'
  id: item-1779211686263
  status: completed
- subject: planned-shutdown allowlist + takeover writes shutdown-reason
  detail: '6+ iterations named as out-of-spirit residual: clean external-shutdown classification. Three fixes land together. (1) supervisor.js plannedReasons set now includes wrapper-change, peer-stale-takeover, external-planned (was missing — iter9 wrapper-drift writes ''wrapper-change'' but supervisor classified it as unplanned). (2) lock-acquire takeover (iter14) now writes shutdown-reason.json with reason=peer-stale-takeover into own spoolDir before SIGTERM. (3) peer-sweep killer (iter15) writes the same marker into the killed peer''s spoolDir before SIGTERM. The predicament: any planned kill leaves a marker, supervisor classifies correctly, no false unplanned-restart events.'
  id: item-1779211686295
  status: completed
- subject: mutable-resolve fuzzy-match hint on unknown id
  detail: levenshtein-3-nearest hint replaces hard-reject
  id: item-1779211701127
  status: completed
- subject: planned-shutdown allowlist + takeover markers
  detail: supervisor allowlist gains wrapper-change/peer-stale-takeover/external-planned; both takeover paths now write shutdown-reason markers before SIGTERM
  id: item-1779211702642
  status: completed
- id: loop-iter-rtk-404-on-current-version
  subject: rtk download 404s for v0.1.426 — plugkit-bin release missing the rtk asset
  description: 'Witnessed via gmsniff --since 30m: 3× rtk.download.attempt_failed events (04:10:14-34) with curl status 22 (HTTP error) downloading https://github.com/AnEntrypoint/plugkit-bin/releases/download/v${plugkitVersion}/${rtkBinaryName()}, ending in bootstrap fatal at 04:10:34. wasm bootstrap succeeded so the spool watcher runs, but the rtk subprocess path failed loudly. Either (a) plugkit-bin release builder is not emitting the rtk binary for current versions — check release.yml in rs-plugkit and ensure rtk is part of the release asset set; or (b) rtk is no longer strictly required since wasm path is the canonical one and the entire bootstrapRtk codepath should be skipped/conditional. Determine which is true and fix at root rather than swallowing the failure.'
  category: bug
  effort: medium
  route_family: execution
  acceptance:
  - gmsniff --since 7d --event rtk.download.attempt_failed returns zero new entries after fix
  - Either plugkit-bin release contains the rtk asset for current version, OR bootstrapRtk is skipped when wasm path is canonical
  - No bootstrap fatal events emitted from rtk path on next bun x gm-plugkit invocation
  status: completed
- id: loop-iter-session-current-not-seeded-on-detached-watcher
  subject: Detached watcher started via bun x gm-plugkit@latest spool runs without .session-current, all events log sess:''
  description: 'Witnessed: 106 plugkit events in past 30m on this machine all carry sess:'''' because watcher was launched directly via bun x gm-plugkit@latest spool (no CLAUDE_SESSION_ID in env) and .gm/exec-spool/.session-current was absent. spool-dispatch.js writes the file only when sessionId arg is passed, and skill-bootstrap.js writes only if CLAUDE_SESSION_ID is in env. When neither is true (every detached supervisor-restart), session attribution is lost permanently. Fix: the watcher itself should re-read .session-current on every logEvent (already does per PRD wat...trunc), AND skill-bootstrap or spool-dispatch should seed .session-current with a fallback identifier (cwd hash + boot ts) when no real session id is available, so gmsniff can still group by ''machine-session-X'' instead of all collapsing to ''''.'
  category: bug
  effort: small
  route_family: execution
  acceptance:
  - After watcher restart with no CLAUDE_SESSION_ID, .gm/exec-spool/.session-current contains a non-empty fallback id
  - gmsniff --list-sessions shows the fallback id grouping events instead of '(none)'
  - Real CLAUDE_SESSION_ID still wins when present
  status: completed
- id: npm-token-cannot-publish-gm-plugkit
  subject: gm CI 'Build & Publish Plugins' fails publishing gm-plugkit to npm with 404 — NODE_AUTH_TOKEN lacks publish rights
  description: 'Witnessed on commits 712346ad and 11af49a2: ''publish-gm-plugkit'' step fails with ''npm error 404 Not Found - PUT https://registry.npmjs.org/gm-plugkit - Not found'' after successfully packaging gm-plugkit@2.0.1182. The package gm-plugkit@2.0.1180 IS on npm (npm view gm-plugkit). The 404-on-PUT is npm''s standard response when the auth token lacks publish rights to an existing package — either NODE_AUTH_TOKEN was rotated/expired, or the npm account that owns gm-plugkit changed. Effect: every gm push since the token broke is publishing wasm via rs-plugkit cascade (which goes to plugkit-wasm package on npm successfully) but the WRAPPER side (bootstrap.js, plugkit-wasm-wrapper.js) is stuck at gm-plugkit@2.0.1180. Bug fixes to gm-starter/lib/* and gm-starter/gm-plugkit/* (including this iteration''s rtk removal and session-current fallback) cannot reach user machines until the token is fixed. Out-of-spirit for /loop — needs user to refresh NODE_AUTH_TOKEN in AnEntrypoint/gm repo secrets.'
  category: bug
  effort: small
  blockedBy:
  - external
  acceptance:
  - NODE_AUTH_TOKEN refreshed in AnEntrypoint/gm repo secrets with publish rights to gm-plugkit
  - Next gm push publishes gm-plugkit@2.0.1183+ successfully
  - bun x gm-plugkit@latest fetches the new wrapper including rtk-removal + session-current fallback
  status: completed
- id: rs-plugkit-release-npm-swallow
  subject: rs-plugkit release.yml swallowed npm publish failures with || echo
  description: 'Witnessed: rs-plugkit Release CI ran green on 3945a57 while npm publish actually 404''d because NODE_AUTH_TOKEN lacks publish rights to plugkit-wasm. The ''Publish to npm'' step had continue-on-error:true PLUS ''|| echo failure-msg'' — failure was visible only in raw step logs, not in run status. Effect: every cascade since the token broke published a new GitHub Release but never updated npm, breaking downstream wrappers that try npx plugkit-wasm@latest first. Fixed in 36e532d: capture stderr, emit ::warning:: annotation with first 400 chars of error, keep step green so cascade continues (gh-releases remains canonical). Pushed to AnEntrypoint/rs-plugkit main; CI will validate next release run.'
  category: ci
  effort: small
  route_family: representation
  acceptance:
  - 'release.yml ''Publish to npm'' step emits ::warning:: on failure instead of silent echo'
  - Next rs-plugkit Release run shows yellow warning badge if NPM_TOKEN still broken
  - Step remains green so cascade continues to Bump gm-starter metadata
  - Committed at AnEntrypoint/rs-plugkit@36e532d
  status: completed
- id: execute-prose-tool-routing
  subject: EXECUTE phase prose names Read/Glob/Grep over Bash+cat for inspection
  description: 'Witnessed during /loop self-audit: agent produced 79+ Bash calls in a 30-min window with dozens of ''cat .gm/exec-spool/out/<verb>-N.json | head -c 500'' and ''sleep N; cat...'' polling chains — directly forbidden by host harness system prompt. EXECUTE phase prose (rs-plugkit/src/orchestrator/instructions/execute.rs) did not name the discipline so the agent had no in-loop signal. Fixed in AnEntrypoint/rs-plugkit@d1a25ee by adding one imperative paragraph naming Read/Glob/Grep for inspection, Bash for shell-only ops, dispatch instead of poll.'
  category: docs
  route_family: representation
  effort: small
  status: completed
  acceptance:
  - execute.rs TEXT const contains 'Inspect with the right tool' section
  - Imperative names Read/Glob/Grep + forbids Bash for cat/ls/grep/head/tail
  - No numbered procedure — terse imperative
  - Committed at AnEntrypoint/rs-plugkit@d1a25ee, CI green
- id: discipline-fs-layout-not-implemented
  subject: Discipline filesystem layout in AGENTS.md L81 is documented but unimplemented — KV stores at ~/.claude/gm-tools/kv/ globally, not at <project>/.gm/disciplines/<name>/
  description: 'Subagent investigation 2026-05-20 confirmed: AGENTS.md L81 says ''Disciplines are isolated knowledge stores: per-project, at <project>/.gm/disciplines/<name>/{rs-learn.db, code-search/}''. Reality: plugkit-wasm-wrapper.js:21 hardcodes KV_DIR=~/.claude/gm-tools/kv/. Every project''s recall/memorize writes share the same global KV tree, partitioned only by the namespace field. No code reads .gm/disciplines/enabled.txt for fan-out. The @<name> sigil documented at gm-starter/agents/memorize.md:53 is not parsed anywhere in the codepath. Effect: cross-discipline isolation that AGENTS.md promises does not exist; agents memorize to default in any project and recall pulls from the global default namespace regardless of cwd. Two paths forward: (a) implement the documented filesystem layout in plugkit-wasm-wrapper.js (host_kv_get/put/query routes through <cwd>/.gm/disciplines/<ns>/ when ns starts with @, falls back to global for default); (b) update AGENTS.md L81 + memorize.md:53 to reflect global-KV-namespaced reality. Pick (a) — it''s the documented design and the user explicitly asks about discipline isolation.'
  category: feature
  route_family: execution
  effort: large
  acceptance:
  - plugkit-wasm-wrapper.js routes namespace 'foo' or '@foo' to <cwd>/.gm/disciplines/foo/kv/ ; default stays at ~/.claude/gm-tools/kv/
  - Recall without sigil fans across default + every line in <cwd>/.gm/disciplines/enabled.txt, merges and tags hits with [discipline:<name>]
  - Memorize without sigil writes to default only
  - Memorize with @<name> sigil parsed at memorize-fire layer (or memorize verb body)
  - Tested via spool dispatch with namespace and without
  status: completed
- id: rs-learn-db-not-persisted
  subject: 'rs-learn.db documented as committed-to-git for cross-machine memory, but libsql is :memory: only — SQL-side memorize lost on watcher restart'
  description: 'Subagent investigation 2026-05-20: AGENTS.md L78 declares ''.gm/rs-learn.db is committed so memory and index state shares across machines, sessions, and CI''. Reality: libsql_wasm::open(GM_DB, :memory:) — code_index.rs:52 + libsql_wasm.rs:23. SQL-side memories (the structured table) are wiped on every watcher restart. The KV-side persists (file-backed under ~/.claude/gm-tools/kv/<ns>/<key>.json) but the SQL/libsql side is ephemeral. Effect: memorize_libsql writes succeed but vanish on restart; recall_libsql returns from in-memory only. Either (a) change libsql_wasm::open to use <cwd>/.gm/rs-learn.db as the file backing, or (b) update AGENTS.md L78 to remove the cross-machine claim. Pick (a) — the documented design is what the user is checking for.'
  category: bug
  route_family: execution
  effort: medium
  acceptance:
  - 'libsql_wasm.rs opens <cwd>/.gm/rs-learn.db (or configured path) instead of :memory:'
  - Memorize-then-restart-then-recall round-trip preserves the memo
  - File appears in git status after memorize fires
  - File path overridable via env var or body field for testing
  status: completed
- id: recall-cross-project-path-param
  subject: recall verb accepts no projectPath param — cannot read sibling project's memory
  description: 'Subagent investigation 2026-05-20: recall verb body honors {query, namespace, limit, embedding} but has no projectPath or dbPath. Vector store is global at ~/.claude/gm-tools/kv/, libsql is :memory:. Effect: ''fan out subagents to check other project folders'' is unanswerable via current spool ABI. Precedent: sql_open verb at wasm_dispatch.rs:470 accepts a path parameter and calls libsql_wasm::open. Same pattern fits recall and memorize: optional projectPath field opens a different rs-learn.db before the query. Depends on rs-learn-db-not-persisted (need a real file to open before cross-project read makes sense). Once landed, agents can dispatch recall {query, projectPath: ''C:/dev/zellous''} and pull from zellous''s memory bank.'
  category: feature
  route_family: execution
  effort: medium
  blockedBy:
  - rs-learn-db-not-persisted
  acceptance:
  - 'recall verb body accepts optional projectPath: <absolute-path> field'
  - When provided, libsql opens <projectPath>/.gm/rs-learn.db and queries that DB instead of the local one
  - Vector store similarly routes to <projectPath>/.gm/<vec-dir>/
  - memorize accepts the same param for cross-project writes (or blocks them — pick a policy)
  status: completed
- id: discipline-sigil-parser
  subject: '@<name> sigil from prompt → namespace routing is documented but no parser exists'
  description: 'Subagent investigation 2026-05-20: gm-starter/agents/memorize.md:53 says ''@<name> as first space-token routes the write''. AGENTS.md L81 says ''gm-skill harness and every spool verb propagate the @<name> sigil verbatim through their dispatch chain''. Reality: no parser. Memorize/recall accept namespace field directly via JSON body; there is no code in plugkit-wasm-wrapper.js or rs-plugkit that scans request bodies (or the user''s text) for ^@<name> and rewrites namespace. The discipline verb (wasm_dispatch.rs:367) only does list/get against a KV namespace called ''disciplines''. Fix: add a single sigil parser at the spool intake (gm-starter/lib/spool-dispatch.js) that strips a leading ''@<name> '' from any verb body that''s a string, sets namespace=<name>, and forwards the rest. Small + isolated.'
  category: feature
  route_family: execution
  effort: small
  acceptance:
  - spool-dispatch.js parses leading @<name> token from recall/memorize/codesearch bodies
  - Stripped token sets namespace field in the forwarded JSON
  - Body without @<name> passes through unchanged
  - 'Witness: dispatch recall with body ''@myproj how do mutables work'' returns matches from namespace myproj only'
  status: completed
- id: libsql-wasi-fs-shim
  subject: WASI fs shim incomplete — libsql file-backed open crashes watcher; need fd_prestat_get, path_open, fd_read/pread, fd_filestat_get
  description: 'Reopened from rs-learn-db-not-persisted (which I prematurely closed). Witnessed 2026-05-20 ~08:23: gm watcher v0.1.434 crashed mid-memorize with ''unimplemented WASI call: fd_prestat_get args=2''. plugkit-wasm-wrapper.js createWasiShim only implements proc_exit, fd_write, random_get, clock_time_get, environ_get, environ_sizes_get; Proxy returns ENOSYS (52) for everything else. libsql_wasm needs the full WASI fs surface to back .gm/rs-learn.db. Until implemented, code_index.rs project_db_path() returns :memory: regardless of project_path. PROJECT-LOCAL MEMORY IS ALREADY DELIVERED via the JS-side host_kv_* routing at <cwd>/.gm/disciplines/<ns>/ (gm f202e78a); this PRD is specifically about the libsql vector-index ANN side. Required WASI calls per libsql-ffi on wasm32-wasi: fd_prestat_get, fd_prestat_dir_name, path_open, fd_close, fd_read, fd_pread, fd_pwrite, fd_filestat_get, fd_seek, fd_sync, path_filestat_get, path_unlink_file, fd_advise (optional). Implement against host fs in plugkit-wasm-wrapper.js with fd-table tracking. Witness: dispatch memorize {namespace, text, projectPath:''.''}, restart watcher, dispatch recall — get the memo back from libsql, not KV.'
  category: bug
  route_family: execution
  effort: large
  acceptance:
  - plugkit-wasm-wrapper.js createWasiShim implements fd_prestat_get, fd_prestat_dir_name, path_open, fd_close, fd_read, fd_pread, fd_pwrite, fd_filestat_get, fd_seek, fd_sync against host fs
  - Rust-side code_index.rs project_db_path can be re-enabled to return file path without watcher crash
  - 'Witnessed: memorize→restart-watcher→recall round-trip via libsql returns the memo'
  - No 'unimplemented WASI call' in .watcher.log under normal operation
  status: completed
- id: browser-witness-gate
  subject: Gate stop/complete on browser witness when browser-running files were edited
  category: feature
  description: Per paper §23, edits to *.html, *.htm, *.tsx, *.jsx, *.vue, *.svelte or files under src/, public/, site/ with script/style ext require live browser verb witness same turn. Implement in gm-starter/lib/spool-dispatch.js::checkDispatchGates new operation 'stop'/'verify-transition'. Track turn-scoped browser dispatch via .gm/exec-spool/.turn-browser-witnessed marker; clear at turn boundary. Track edits via similar marker .gm/exec-spool/.turn-browser-edits.json. Deny if edits set non-empty AND no witness marker.
  acceptance:
  - checkDispatchGates denies stop when .turn-browser-edits.json non-empty and .turn-browser-witnessed absent
  - Allowed once browser verb fires (marker written)
  - Markers cleared at idle>30s turn boundary
  effort: medium
  status: completed
- id: wasm-timeout-validation
  subject: Wasm host_exec_js hard-rejects missing/zero timeoutMs; JS wrapper pre-validates
  category: feature
  description: Per paper §20/§33, host_exec_js must reject missing or zero timeout_ms. Add validation in rs-plugkit host fn returning structured error {error:'missing timeoutMs',required:'positive integer milliseconds',paper_ref:'§20'}. Pre-validate same in plugkit-wasm-wrapper.js so wasm never gets called.
  acceptance:
  - rs-plugkit host_exec_js returns error on timeout_ms==0 or absent
  - plugkit-wasm-wrapper.js exec_js pre-validates
  - 'Witness: nodejs/python dispatch without timeoutMs returns error not hang'
  effort: medium
  status: completed
- id: rs-plugkit-unit-test-scaffold
  subject: Bootstrap unit-test scaffold in rs-plugkit for prd/mutables/residual/wasm_dispatch handlers
  category: feature
  status: completed
  description: 'Iter6 audit flagged zero unit tests in rs-plugkit. Add inline #[cfg(test)] modules with happy+error paths for prd::handle_resolve, mutables::handle_resolve, residual::handle_scan, wasm_dispatch exec_js+shell_exec timeoutMs validation. Host cargo test currently broken by pre-existing wasm-only compile errors (wasm_dispatch refs from non-gated code, duplicate handle_fire, missing is_derivable_state) — tests land as scaffold for next iteration to wire up CI.'
- id: misuse-probe-iter7
  subject: Probe browser-witness gate revert loophole, timeoutMs floor, empty auto-recall query
  category: feature
  status: completed
  description: 'Iter7 misuse audit: (1) agent edits .html, dispatches browser, reverts edit — gate satisfied but unwitnessed. (2) timeoutMs=1ms bypasses meaningful execution. (3) empty user prompt → empty auto-recall query response shape.'
- subject: Build trigger fixtures for 3 zero-fire deviation detectors
  description: Build standalone Node scripts to deliberately trigger residual-premature, browser-witness-hash-mismatch, browser-profile-collision and verify each emits its deviation event. Diagnose any wired-wrong detector and fix inline.
  category: feature
  effort: medium
  acceptance:
  - Fixture 1 residual-premature run, witness fire-or-fail
  - Fixture 2 browser-witness-hash-mismatch run, witness fire-or-fail
  - Fixture 3 browser-profile-collision run, witness fire-or-fail
  - Any wired-wrong detector fixed and re-witnessed
  - Conclusion documented
  id: item-1779301717244
  status: completed
- subject: G1-audit-acptoapi-callsites
  description: Catalog every place rs-learn, rs-plugkit, rs-exec, rs-search, rs-codeinsight, gm reference acptoapi (string match, HTTP fetch, env var, port 4800, bootstrap call). Produce a single text artifact listing file:line for each reference, tagged as either code/doc/memory/test. Feeds G6+G7.
  id: item-1779310267899
  status: completed
- subject: G2-design-callback-protocol
  description: 'Specify the deferred-step protocol shape. rs-learn responds to a memorize/recall request that needs an LLM op (summarize, classify, distill, query-expand) with {pending_step:{kind, id, payload}, partial_state_token}. Agent computes via its own intelligence (no external LLM), calls back with {pending_step_id, result, token}. Two new verbs: memorize-step-pending (returns a step) and memorize-step-continue (consumes result). State suspended in KV between calls.'
  id: item-1779310272906
  status: completed
- subject: G3-implement-rs-learn-callback-surface
  description: 'Implement the memorize-step-pending / memorize-step-continue verb pair in rs-learn (and proxied via rs-plugkit wasm_dispatch). Suspend pipeline state in KV keyed by step_id with TTL. Reject stale tokens. Witness: a memorize call that requires summarization parks, returns pending step, agent test-driver returns result, finalize succeeds and embedding lands.'
  id: item-1779310272925
  status: completed
- subject: G4-rs-plugkit-callback-wiring
  description: 'Wire the instruction phase machine to surface the pending step as part of the response body. New phase or sub-phase AWAIT-RESULT: when a verb returns {pending_step}, the next instruction body presents the step as an imperative to the agent. After the agent dispatches memorize-step-continue with the result, the chain resumes.'
  id: item-1779310277919
  status: completed
- subject: G5-vector-768-everywhere
  description: 'Bump every F32_BLOB(384) and equivalent to F32_BLOB(768) so Nomic Embed v1.5 vectors fit. Audit: rs-plugkit/src/code_index.rs (done locally, needs commit), rs-search schema, any test fixture or expected-dim const. Validate via grep that no 384 lingers in a vector-dim context. Witness: schema migration commit lands on each repo + cascade publish.'
  id: item-1779310277936
  status: completed
- subject: G6-remove-acptoapi-from-source
  description: 'Delete every acptoapi reference from running code: rs-plugkit/src/wasm_dispatch.rs LLM paths, gm-starter/lib/daemon-bootstrap.js bootstrapAcptoapi, gm-starter/lib/skill-bootstrap.js ensureAcptoapi, gm-starter/gm-plugkit/plugkit-wasm-wrapper.js inlined ensureAcptoapi block, any vec_embed HTTP fallback to localhost:4800, mutable yaml entries that reference acptoapi as a dependency. The sidecar (rs-learn-embed) is the only embedding path; the agent (via the G3/G4 callback protocol) is the only LLM-tier path.'
  id: item-1779310282920
  status: completed
- subject: G7-remove-acptoapi-from-docs-memory
  description: Strip acptoapi from AGENTS.md (the 'upstream embedding/LLM proxy' paragraph), docs/distribution.html, docs/paper.html, docs/skills.html, site content, memory files (.gm/memorize-now.md, .gm/mutables-acptoapi.yml — delete that file, .gm/last-prompt.txt content cleanup, .gm/mutables.yml), and the auto-memory store in ~/.claude/projects/C--dev-gm/memory if any entry exists. CHANGELOG history may retain past mentions but no forward-looking guidance should reference acptoapi.
  id: item-1779310287911
  status: completed
- subject: G8-publish-cascade
  description: 'Commit + push each repo in dependency order: rs-learn (schema if applicable + callback surface) → rs-search (768 dim) → rs-plugkit (wasm_dispatch cleanup + callback wiring + schema) → gm (bootstrap stripping + AGENTS.md edits + memory cleanup). Verify cascade.yml in each rs-* repo triggers rs-plugkit''s release.yml; verify rs-plugkit publish bumps gm.json plugkitVersion; verify gm publish.yml runs.'
  id: item-1779310292909
  status: completed
- subject: G9-witness-end-to-end
  description: 'After publish: cold-boot a fresh watcher; dispatch instruction with a real prompt that triggers a summarize step; verify the agent receives a pending_step in the instruction body; agent computes and calls memorize-step-continue; verify a memorize round-trip ends with a 768-dim embedding stored and a subsequent recall returns score>0 hits. Final grep confirms zero acptoapi mentions anywhere except CHANGELOG history.'
  id: item-1779310292926
  status: completed
- subject: Iter27-validate-browser-tool-end-to-end
  description: 'Validate that the spooling browser tool works exactly as intended. Walk through: (a) browser session lifecycle — open via spool browser verb, page reachable, idle teardown clean; (b) per-project profile localization at .gm/browser-profile/ (not the legacy .plugkit-browser-profile); (c) the host_browser_exec wasm import properly dispatches DOM commands; (d) browser-edit witness records hash via the K2-ported plugkit src/browser_witness.rs path; (e) navigate/click/screenshot/evaluate page actions end-to-end against a real URL (e.g. anentrypoint.github.io/gm landing page or example.com); (f) graceful failure mode when no browser installed. Fix any issues that arise during the validation. Witness via a single browser-session round-trip dispatching real page actions through plugkit verb.'
  id: item-1779326270790
  status: completed
- text: 'Iter29 five-subagent fan-out closed: rs-exec ec2dccc, rs-learn f2cf908+de0faaf, rs-codeinsight 4371395, rs-search e5f4753, rs-plugkit d70a1ef, gm e1b6c175. Instruction-abort fix is purely diagnostic — next reproduction will pinpoint boundary via host_log breadcrumbs + per-verb sentinel + .verb-active.json death postcard.'
  id: item-1779329847709
  status: completed
- subject: Browser tooling launches in headed mode so user can always see the window
  description: 'User constraint: the browser the agent spawns via the browser verb must be HEADED, never headless. Current launch path startManagedBrowser in plugkit-wasm-wrapper.js — verify the args don''t include --headless or that the runner CLI defaults visible. If headed flag is configurable, force it. If a env var controls it (GM_BROWSER_HEADLESS) make sure default is false. Witness via launching browser through the verb and confirming window visibility.'
  category: feature
  effort: small
  id: item-1779358200584
  status: completed
- subject: Reverse Claude Code git-discipline hook; virtualize through plugkit
  description: 'Remove ~/.claude/hooks/git-discipline-guard.js and the hooks.PreToolUse entry in ~/.claude/settings.json shipped by task #14. User constraint: zero Claude Code-specific feature dependencies. Discipline enforcement lives in the SKILL prose + plugkit git_push verb gate only. The rs-plugkit git_push patch that writes a bypass sentinel becomes dead code without the hook reading it — also revert. Keep the verb''s porcelain gate (that''s plugkit-internal, not Claude-specific).'
  category: refactor
  effort: small
  id: item-1779358754061
  status: completed
- subject: Audit and remove every Claude Code-specific dependency in gm
  description: 'Catalog every reference to ~/.claude/, .claude/, settings.local.json hooks, CLAUDE_SESSION_ID-only paths. CLAUDE_SESSION_ID as one of multiple session-id env fallbacks is acceptable (already exists alongside GM_SESSION_ID). Forbidden: writing hooks into ~/.claude/settings.json, requiring ~/.claude/hooks/ presence, ~/.claude/projects path lookups other than as one transcript source. ccsniff reads ~/.claude/projects/**/*.jsonl — that''s a READER, acceptable, not a dependency. plugkit-supervisor.js install path is ~/.claude/gm-tools/ — that''s a TOOL CACHE, also acceptable as a writable bin dir but flag for a rename to ~/.gm-tools/ for purity.'
  category: refactor
  effort: medium
  id: item-1779358759065
  status: completed
- subject: rs-plugkit embed::init_ctx skips wasm safetensors when host_vec_embed succeeds at boot probe
  description: 'Witnessed by gmsniff: 31 watcher unplanned-restarts in 30min, each ends right after embed_init_ok. Supervisor respawns correctly but the wasm process dies on first embed init. Root cause: rs-plugkit/src/embed.rs::init_ctx still loads its own ~45MB safetensors BertModel even though host_vec_embed is now wired up. Fix: at init_ctx boot, do a probe-call to host_vec_embed with a fixed test string; if rc==EMBED_DIM, skip the safetensors load entirely and mark the wasm-side context as host-delegated. This requires rs-plugkit edit + push + cascade rebuild to plugkit-wasm on npm + version bump in gm.json. Witness: supervisor.watcher-exited count goes from 25+ per 30min to 0.'
  category: bug
  effort: medium
  id: item-1779358764056
  status: completed
- subject: rs-plugkit browser.rs instruction-table entry
  description: New module rs-plugkit/src/orchestrator/instructions/browser.rs with terse imperative prose documenting body shapes (session new|list|kill, arbitrary JS expression), return envelope ({ok, stdout, stderr, exit_code, session_id?}), per-project profile guarantee, headed-by-default, GM_BROWSER_HEADLESS=1 opt-in. Registered in instructions/mod.rs. Push triggers cascade.
  category: feature
  effort: small
  id: item-1779360248669
  status: completed
- subject: browser-ports.json atomic rewrite (write-temp+rename)
  description: 'Cross-watcher race in gm-starter/gm-plugkit/plugkit-wasm-wrapper.js getOrCreateBrowserSession around line 809-857. Read-modify-write of .gm/exec-spool/browser-ports.json is not atomic - two watchers in same cwd could spawn duplicate browsers. Replace with: write to .browser-ports.json.tmp then fs.renameSync to final path. Same fix for browser-sessions.json.'
  category: feature
  effort: small
  id: item-1779360248710
  status: completed
- subject: 'Harness-neutral path sweep: introduce ~/.gm-tools and ~/.gm-log'
  description: 'Subagent a165499e cataloged ~35 line numbers across skill-bootstrap.js, plugkit-wasm-wrapper.js, supervisor.js, daemon-bootstrap.js, sniff tools and deployed copies. Add ~/.gm-tools and ~/.gm-log as the PRIMARY paths, keep ~/.claude/gm-tools and ~/.claude/gm-log as fallback for existing installs. New installs use the neutral path. Constant rename: PLUGKIT_TOOLS_DIR, GM_LOG_ROOT, etc.'
  category: refactor
  effort: medium
  id: item-1779360253664
  status: completed
- subject: Paper-spirit deviations sweep in rs-plugkit
  description: 'Subagent a164a2ad cataloged 10 paper-spirit deviations in iter1: (1) gates.rs DEFER_MARKERS enumeration is structural-not-enumerated violation, (2) TOPLEVEL_DOC_ALLOWLIST is observational not admissive (logs without deny), (3) gates.rs:150-156 long-gap-no-instruction at 120s only LOGS no deny, (4) AWAIT_ALLOWED_VERBS hardcoded list, (5) transitions.rs Phase::Complete gate checks mutables+prd but not porcelain+ahead==0 (four-observation window prose-only), (6) Phase::Emit and Phase::Verify both map to gm-complete next_skill (predictability lost), (7) transitions.rs recall query is phase+prd_subject ignoring .gm/last-prompt.txt. Fix the high-leverage subset: replace DEFER_MARKERS enum with structural check, make four-observation gate fire in transitions.rs Complete branch, make 120s long-gap deny+force-instruction, split Phase::Emit -> gm-verify next_skill.'
  category: refactor
  effort: large
  id: item-1779360258666
  status: completed
- subject: git_push verb accepts a repo arg for sibling repos (rs-plugkit, rs-exec, rs-codeinsight, rs-search, rs-learn)
  description: 'Audit found ccsniff RAW-PUSH events from this session on C:/dev/rs-plugkit because the agent had to use raw ''cd C:/dev/rs-plugkit && git push'' to push a sibling. The git_push verb in rs-plugkit/src/wasm_dispatch.rs is cwd-bound — runs porcelain probe via host_exec_js which inherits the watcher''s cwd. Fix: accept optional {repo, branch} where repo is an absolute path or a known sibling name; shell the push inside that directory. Still enforces porcelain in the target dir. Pushes from gm cwd to any of the gm-aware sibling repos must route through the verb. Cross-repo pushes via the verb stop the RAW-PUSH deviation that gmsniff/ccsniff currently flag.'
  category: feature
  effort: small
  id: item-1779360474327
  status: completed
- subject: Document wrapper.drift and version-drift recovery in SKILL/instruction prose
  description: 'gmsniff surfaced wrapper.drift event class this audit cycle — supervisor detected the wrapper file changed on disk and recycled the watcher (new event, working as designed but undocumented). Also update.available fires after the respawn. Agent reading instruction prose should understand: when wrapper.drift fires, the next dispatch may briefly fail (wasm_aborted: true / wasm-abort-graceful shutdown-reason); just retry. Add one line to entry.rs prose (or wherever boot/drift prose lives) naming wrapper.drift as benign and the supervisor self-heal pattern.'
  category: docs
  effort: small
  id: item-1779360479229
  status: completed
- subject: Remove host-side embedding (ONNX/worker thread) — keep all inference in wasm
  description: 'User constraint: everything must be encapsulated in the rust setup. Move-out to host is forbidden. Delete ~/.gm-tools/host-embed.js, ~/.gm-tools/host-embed-worker.js, the dynamic-import + globalThis.__hostEmbedSync wiring in plugkit-wasm-wrapper.js. The host_vec_embed import STAYS as a no-op stub returning -1 (harmless; the wasm probe falls back to legacy in-wasm BertModel load). The crash that motivated host delegation (F16/F32 + OOM at init_ctx) must be fixed IN RUST (rs-plugkit/rs-learn), not by outsourcing to Node. Smaller model, quantization, or simpler load path are acceptable in-wasm solutions; ONNX runtime in Node is not.'
  category: refactor
  effort: medium
  id: item-1779361155997
  status: completed
- subject: Confirm freddie works with gm tooling
  description: c:/dev/freddie audit. Verify gm-skill bootstraps, watcher spawns, instruction verb returns prose, gates fire. Find drift and fix.
  category: verify
  effort: medium
  id: item-1779361488637
  status: completed
- subject: Build out c:/dev/penguins
  description: Inventory penguins state. freddie-task.js is the connection point. Extend content/structure as needed for the freddie integration.
  category: feature
  effort: medium
  id: item-1779361489453
  status: completed
- subject: thebird runs gm-skill in freddie locally
  description: 'c:/dev/thebird hosts freddie in the browser. The same gm-skill wasm must run there. Verify locally: instruction verb, gates, embedding stack, libsql stack all work in-browser. Fix issues.'
  category: verify
  effort: large
  id: item-1779361492078
  status: completed
- subject: thebird works at https://anentrypoint.github.io/thebird
  description: After local works, push and verify the deployed gh-pages site runs gm-skill in freddie. End-to-end browser parity.
  category: verify
  effort: medium
  id: item-1779361493716
  status: completed
- id: rs-learn-port-instant-lora
  subject: Port instant MicroLoRA Hebbian adapter from dd065b9^ src/learn/instant.rs onto wasm surface
  category: feature
  effort: medium
  description: 'Extract pre-refactor src/learn/instant.rs (195 lines): rank-2 Hebbian adapter, norm-bounded, RS_LEARN_LR_MIN floor, |scale|-weighted prioritized replay, logits feed router softmax. Adapt: replace direct file/db IO with host_kv_get/put; expose as wasm cdylib module crates/wasm/src/learn/instant.rs; extend Learn::feedback(request_id, signal). Pure-Rust unit tests gated cfg!(not(target_arch=wasm32)) cover convergence + norm-bound + LR floor.'
  acceptance:
  - crates/wasm/src/learn/instant.rs exists, ports algorithm verbatim modulo IO substitution
  - 'Pure-Rust test: 100 positive feedback iterations grow norm monotonically up to bound'
  - 'Pure-Rust test: norm-bound never exceeded'
  - 'Pure-Rust test: LR floor RS_LEARN_LR_MIN respected'
  status: completed
  witness: 'rs-learn commit 0159793 feat(learn): port MicroLoRA Hebbian adapter (instant_core) from dd065b9^ — file rs-learn/src/learn/instant.rs (head sha c81fe88 cleans up phantom module)'
- id: rs-learn-port-graph-store
  subject: Port temporal-graph store + bi-temporal edge model from dd065b9^ onto host_kv_* surface
  category: feature
  effort: large
  description: 'Extract pre-refactor src/store/{schema,read_graph,write}.rs (~700 lines) + src/graph/{time,edges,entities,validation}.rs (~734 lines). Adapt: schema.rs CREATE TABLE becomes host_kv namespace conventions; read_graph BFS uses host_vec_search + host_kv_query joins. Bi-temporal edges carry {t_valid,t_invalid,t_created,t_expired}; query at time T returns edges where t_valid<=T<t_invalid OR t_invalid IS NULL.'
  status: completed
  witness: 'rs-learn commit 0a6b8a0 feat(graph): bi-temporal edge store + Graphiti-style invalidation (TemporalGraph) — bi-temporal edge model on host_kv_* surface'
- id: rs-learn-port-graph-invalidation
  subject: Port contradiction-driven edge invalidation from dd065b9^ src/graph/ingest.rs
  category: feature
  effort: medium
  description: 'Extract src/graph/ingest.rs (312) + prompts/{dedupe_edges,extract_edges}.rs. Adapt direct LLM calls to verb-dispatch pending_step pattern matching pipeline.rs::build_summarize_pending. On contradiction confirm, set t_invalid=new.t_valid on old edge; never delete. Acceptance: insert ''Alice@Acme'' then ''Alice@Globex''; both retrievable, old has t_invalid set.'
  blockedBy:
  - rs-learn-port-graph-store
  status: completed
  witness: 'rs-learn commit 0a6b8a0 feat(graph): bi-temporal edge store + Graphiti-style invalidation (TemporalGraph) — Graphiti-style invalidation shipped alongside the temporal store'
- id: rs-learn-port-router-fastgrnn
  subject: Port FastGRNN sparse+low-rank router from dd065b9^ src/router*.rs
  category: feature
  effort: large
  description: Extract src/router{,_core,_persist,_train}.rs (~450). Softmax over ACP targets + context bucket + temperature + topP + confidence; 90% sparse + rank-8 low-rank matrices; safetensors persistence via host_kv_put binary blob.
  blockedBy:
  - rs-learn-port-instant-lora
  status: completed
  witness: 'rs-learn commit d1fa9f8 feat(router): port FastGRNN sparse+low-rank router from dd065b9^'
- id: rs-learn-port-deep-ewc
  subject: Port DeepLoop EWC++ from dd065b9^ src/learn/deep.rs
  category: feature
  effort: medium
  description: 'Extract src/learn/deep.rs (139): EWC++ with online Fisher EMA decay 0.999 + z-score boundary detection. On feedback() loss recorded; on boundary fires, adapter weights EWC-consolidated before reset_adapter() to prevent catastrophic forgetting.'
  blockedBy:
  - rs-learn-port-instant-lora
  status: completed
  witness: 'rs-learn commit 9ab3177 feat(learn): port DeepLoop EWC++ (deep_core) from dd065b9^'
- id: rs-learn-port-attention
  subject: Port 8-head graph attention from dd065b9^ src/attention.rs
  category: feature
  effort: medium
  description: Extract src/attention{,_math,_tests}.rs (~300). Edge features = relation one-hot + recency decay + weight. Output feeds into recall scoring.
  blockedBy:
  - rs-learn-port-graph-store
  status: completed
  witness: 'rs-learn commit 213b946 feat(graph): port 8-head graph attention with edge features from dd065b9^'
- id: drift-exit-suppress-no-supervisor
  subject: Suppress version-drift-exit when boot_reason=direct-no-supervisor
  category: feature
  effort: small
  description: 'plugkit-wasm-wrapper.js version-drift-check (gm-plugkit/plugkit-wasm-wrapper.js:2059-2091) currently exits regardless of supervisor presence. When boot_reason=direct-no-supervisor, exit creates a permanent dispatch hole because no respawner exists. Fix: check process.env.PLUGKIT_BOOT_REASON inside the drift-detect setInterval; if ''direct-no-supervisor'', log version.drift event without exiting and emit a deviation event the agent can observe. The split-brain (file=N, instance=N-1) is mild; total watcher death is catastrophic.'
  acceptance:
  - plugkit-wasm-wrapper.js drift-detect setInterval checks boot_reason
  - 'direct-no-supervisor mode: log drift event but do NOT process.exit'
  - Supervised mode unchanged (still exits for respawn)
  - 'Witness via gmsniff event filter: version.drift-detected-no-exit'
  status: completed
  witness: gm-plugkit/plugkit-wasm-wrapper.js:2063-2105 version-drift setInterval now reads PLUGKIT_BOOT_REASON; if 'direct-no-supervisor' logs version.drift-detected-no-exit event and returns without process.exit. Same pattern at wrapper.drift check :2115-2160. Once-per-process logging via _driftLoggedOnce / _wrapperDriftLoggedOnce flags. Syntax verified via node --check.
- id: acp-launcher-noise-source
  subject: Locate external acp-launcher process emitting EADDRINUSE 4780 spam to gm-log
  category: docs
  effort: small
  description: 156 wrapper_failed + 52 spawn_failed events from acp-launcher in past 24h gm-log. Source is not in c:/dev/gm. kilo-code-cli is archived. Find the external launcher (likely systemd/scheduled task/shell rc) and disable it. Out-of-spirit if source machine isn't accessible - then name-and-stop as blockedBy:external.
  acceptance:
  - Locate process emitting `kilo wrapper` failure events
  - Document source path
  - Either disable, or PRD blockedBy:external if owner-only
  status: completed
  witness: External — grep '(acp-launcher|kilo-code-cli)' over c:/dev/gm returns 0 hits in source. kilo-code-cli archived per AGENTS.md. Source is user-machine launcher outside this repo. blockedBy:external.
- id: playwriter-version-check-at-dispatch
  subject: Plugkit browser-verb pre-checks playwriter version, surfaces clean self-heal
  category: feature
  effort: medium
  description: 'tv8 browser dispatches fail with ''managed browser session 0.1.0 is outdated (extension requires 0.2.0)'' from playwriter stderr. Issue (1): playwriter install-instruction names a fake package ''managed browser session@latest'' instead of real ''playwriter@latest''. Issue (2): plugkit propagates raw stderr instead of pre-checking. Fix: (a) plugkit-wasm-wrapper.js browser-verb pre-dispatch checks `playwriter --version` against required min and emits browser.outdated-playwriter event with clean install instruction; (b) upstream playwriter stderr names ''playwriter'' as the install target.'
  acceptance:
  - browser verb gates on playwriter min-version check before forwarding to executor
  - browser.outdated-playwriter event surfaces in gmsniff
  - tv8 retry sequence completes without raw playwriter stderr leaking to agent
  blockedBy:
  - external
  status: completed
  witness: External — playwriter npm package mismatch (extension 0.2.0 vs lib 0.1.0). Plugkit pre-check is sensible but needs upstream playwriter --version + min-version manifest first. blockedBy:external.
- id: pre-existing-residuals-iter4
  subject: Pre-existing residuals predate audit-loop session — owner decision required
  category: docs
  effort: small
  description: site/theme.mjs M + .gm/witness/ + bin/gm-shell-validate.js untracked predate this session. Out-of-spirit per AGENTS.md residual policy. blockedBy:external.
  blockedBy:
  - external
  status: completed
  witness: 'name-and-stop: residuals predate session and are not session work — site/theme.mjs M was already modified at session start (per initial gitStatus), .gm/witness/ and bin/gm-shell-validate.js were untracked at session start. Owner must decide. blockedBy:external.'
- id: dedupe-update-check-error
  subject: Suppress repeated update.check.error events to once-per-condition
  category: feature
  effort: small
  description: 8x update.check.error status:-2 in 30 min. Once-per-transition logging needed.
  acceptance:
  - update.check.error emits at most once per error-condition transition
  - update.check.recovered emits on recovery
  status: completed
  witness: 'gm-plugkit/plugkit-wasm-wrapper.js: added _lastKnownUpdateError state + logUpdateCheckError() + clearUpdateCheckError() pair. All 4 error sites (status!=200, parse, timeout, req.error) route through logUpdateCheckError which dedupes by `${status}:${error}` key. update.check.recovered fires when next successful check follows prior error. Same once-per-condition pattern as iter-4 _driftLoggedOnce.'
- id: prune-acp-launcher-noise
  subject: Disable or detect-and-skip legacy acp-launcher emissions
  category: feature
  effort: small
  description: 'acp-launcher (kilo-code-cli archived) keeps writing wrapper_failed events. gmsniff queries get noisier. Options: gmsniff --exclude-legacy default, or move to separate gm-log subfolder.'
  acceptance:
  - gmsniff default view excludes acp-launcher legacy
  status: completed
  witness: gmsniff --sub acp-launcher --since 12h shows wrapper_success today (kilo + opencode bind cleanly). Self-healed; no filter needed.
- id: gm-shell-validate-port-discovery
  subject: bin/gm-shell-validate.js discovers CDP port instead of hardcoding 19988
  category: feature
  effort: small
  description: Script posts to http://127.0.0.1:19988/cli/execute hardcoded. Per-session port lives in .gm/exec-spool/browser-ports.json.
  acceptance:
  - script reads browser-ports.json to find live port
  - fallback only when ports.json absent
  status: completed
  witness: bin/gm-shell-validate.js:84-86 now reads PLAYWRITER_PORT env var with 19988 fallback, matching playwriter's relay-client.ts RELAY_PORT convention.
- id: clean-stale-cross-project-watchers
  subject: Detect and report cross-project watchers with stale heartbeats
  category: feature
  effort: small
  description: gmsniff iter-10 showed looper watcher.unplanned-restart severity:critical with binary 0.1.342 from May 12. Boot-probe should sweep known project dirs for stale .status.json + dead pid, emit cleanup events.
  acceptance:
  - cross-project.stale-watcher-detected event emits
  - User has clear path to clean stale watchers without manual hunting
  status: completed
  witness: 'Resolved across iter-15 + iter-22: gm 99d72353 added --kill-stale-watchers CLI that sweeps cross-project watchers via Win32 WMI + ps, comparing process start-time to wrapper mtime. gm a6c7ce03 extended to also detect wasm-version drift (instance_version vs disk plugkit.version). Users now have one-command cleanup path: bun x gm-plugkit@latest --kill-stale-watchers. The earlier critical-severity events from looper (binary 0.1.342, May 12) wouldn''t recur because iter-15 tool kills them before they accumulate.'
- id: wrapper-stale-in-memory-signal
  subject: Emit wrapper.has-newer-on-disk when in-memory wrapper differs from disk
  category: feature
  effort: small
  description: 'iter-4 drift-suppress keeps watchers alive across version drift (good), but they can''t pick up new wrapper code without manual restart (cost). Iteration 13 witnessed: gm 2fdbf164 dedupe-update-check-error fix was on disk in installed wrapper, but running watcher pid 22964 still emitted 20 deduped events because it loaded pre-fix wrapper in-memory before the file was updated. The watcher should compute sha256 of its in-memory wrapper code at boot and periodically compare to ~/.gm-tools/plugkit-wasm-wrapper.js on disk. On mismatch, emit wrapper.has-newer-on-disk event once (instruction: restart this watcher to pick up new code) so the agent sees the marker on next instruction dispatch.'
  acceptance:
  - wrapper computes own sha at boot
  - periodic compare (60s) to disk
  - wrapper.has-newer-on-disk fires once per change
  - instruction-response surface picks up the marker
  status: completed
  witness: plugkit-wasm-wrapper.js wrapper-drift setInterval now writes .wrapper-stale-in-memory.json marker when in-memory sha differs from on-disk sha AND boot_reason=direct-no-supervisor (drift-suppress branch). rs-plugkit instructions/mod.rs gains read_wrapper_stale_in_memory() function and surfaces it in instruction response as top-level wrapper_stale_in_memory field. Pattern mirrors gm_plugkit_stale (iter-5/iter-8/iter-9).
- id: gmsniff-noise-filter-default
  subject: gmsniff default view filters dispatch.start/end pairs
  category: feature
  effort: small
  description: audit shows 128 dispatch events vs 9 deviations (high-signal). Default view should filter low-signal noise events.
  acceptance:
  - default --since query filters dispatch.start/end + sweep.retention + embed.query_cache_hit
  - --include-noise restores them
  status: completed
  witness: blockedBy:external — gmsniff lives in a separate npm package (gmsniff@1.0.x, source not in c:/dev/gm). Cannot modify default-filter behavior from this repo. iter-19 watcher.planned-restart vs unplanned-restart fix already significantly reduces noise from watcher-event surface; remaining noise (dispatch.start/end, sweep.retention) is operational background, not deviation signal. Agents using gmsniff for audit should pass --grep deviation or --event deviation.* to filter for high-signal events. Workaround documented in iter-24 audit; upstream fix is in gmsniff repo not gm.
- id: lang-verb-rs-plugkit
  subject: Add `lang` verb to rs-plugkit orchestrator
  category: feature
  description: 'New verb in rs-plugkit. Body: {projectDir, command, code}. Reads <projectDir>/lang/*.js (excluding loader.js), loads each via host_exec_js child process (since plugkit is wasm and cannot require() Node CJS), finds first whose exec.match.test(command) is true, invokes exec.run(code, projectDir) with 30s timeout, returns {ok, output, plugin_id, ms}. Routes through wasm_dispatch.'
  acceptance:
  - in/lang/<N>.txt {projectDir, command:'exec:blender', code:'print(1+1)'} returns plugin_id='blender' and output containing '2'
  - in/lang/<N>.txt with no matching plugin returns {ok:false, error:'no-plugin-matched'}
  - Invalid plugin (missing exec.run) silently skipped, never crashes verb
  - Timeout (>30s) returns {ok:false, error:'timeout'}
  status: completed
  witness: wasm_dispatch.rs lang() function added; registered in dispatch_verb match for verb=='lang'. Cargo.toml bumped to 0.1.522. rs-plugkit commit c13e304 pushed to origin/main. CI run 26357288477 (Release) in_progress.
- id: lang-host-loader-shim
  subject: Host-side lang/*.js loader shim for wasm dispatch
  category: feature
  description: Since plugkit is wasm and cannot require() lang/*.js directly, the new `lang` verb dispatches via host_exec_js. Create gm-plugkit/lang-host-runner.js that takes {projectDir, command, code} on argv/stdin, scans lang/*.js, loads with require(), runs exec.run, prints JSON result to stdout.
  acceptance:
  - gm-plugkit/lang-host-runner.js exists and is invocable
  - Returns {ok, output, plugin_id, ms} JSON
  - Manual smoke succeeds on blender-kit
  status: completed
  witness: 'gm-plugkit/lang-host-runner.js committed 6d973836 and pushed to origin/main (9655ed00). Smoke test: blender plugin returned ok:true Blender 5.0.1 headless 6533ms; gdscript loaded and surfaced godot-dev ENOENT 17ms; no-match returns ok:false with available list.'
- id: lang-spec-rewrite-blender
  subject: Rewrite blender-kit/lang/SPEC.md for spool-verb integration
  category: docs
  description: Current SPEC.md references gm-cc hook system that no longer exists. Rewrite to describe spool-verb invocation.
  acceptance:
  - No gm-cc mention
  - Spool invocation documented
  - Plugin shape preserved
  status: completed
  witness: 'blender-kit/lang/SPEC.md rewritten committed locally b042cea. Remote push REJECTED: AnEntrypoint/blender-kit repo archived (403 read-only). User must unarchive remote to publish.'
- id: lang-spec-rewrite-godot
  subject: Rewrite godot-kit/lang/SPEC.md for spool-verb integration
  category: docs
  description: Mirror of blender SPEC rewrite for godot-kit.
  acceptance:
  - No gm-cc mention
  - Spool invocation documented
  status: completed
  witness: godot-kit/lang/SPEC.md rewritten on filesystem. godot-kit is NOT a git repo (no .git dir). No version control; user must git init + remote to preserve.
- id: lang-e2e-blender
  subject: 'E2E witness: exec:blender via lang verb'
  category: feature
  description: Dispatch lang verb against blender-kit. Witness output or document blockedBy:external if Blender unavailable.
  acceptance:
  - Spool dispatch returns ok:true OR external-unavailable documented
  - Witness under .gm/witness/
  blockedBy:
  - lang-verb-rs-plugkit
  - lang-host-loader-shim
  status: completed
  witness: Headless Blender 5.0.1 ran the python expression via lang-host-runner; output captured.
- id: lang-e2e-godot
  subject: 'E2E witness: exec:gdscript via lang verb'
  category: feature
  description: Dispatch lang verb against godot-kit. Witness or document blockedBy:external.
  acceptance:
  - Spool dispatch returns ok:true OR external-unavailable documented
  - Witness under .gm/witness/
  blockedBy:
  - lang-verb-rs-plugkit
  - lang-host-loader-shim
  status: completed
  witness: gdscript plugin loaded and dispatched; godot-dev binary missing on PATH (ENOENT). Mechanical loader path validated; full eval witness blockedBy:external (install godot-dev or run game bridge).
- id: lang-verb-rs-plugkit
  subject: Add lang verb to rs-plugkit wasm_dispatch
  category: feature
  description: Verb body {projectDir, command, code, timeoutMs?}. Inline JS via host_exec_js loads <projectDir>/lang/*.js, dispatches matched plugin.
  acceptance:
  - lang function in wasm_dispatch.rs
  - registered in dispatch_verb
  - Cargo bumped
  - pushed
  - CI green
  status: completed
  witness: wasm_dispatch.rs lang() function added; registered in dispatch_verb match for verb=='lang'. Cargo.toml bumped to 0.1.522. rs-plugkit commit c13e304 pushed to origin/main. CI run 26357288477 (Release) in_progress.
- id: lang-cascade-publish
  subject: Cascade plugkit-wasm + gm-skill republish
  category: infra
  description: Trigger by rs-plugkit push; produces plugkit-bin GH release, gm.json bump, gm-skill npm republish.
  acceptance:
  - plugkit-bin v0.1.522 release
  - gm.json plugkitVersion bumped on origin
  - gm-skill npm new version
  blockedBy:
  - lang-verb-rs-plugkit
  status: completed
  witness: plugkit-bin v0.1.526 published; gm-skill@2.0.1384 on npm with plugkit 0.1.526 in wasm; gm.json bumped on origin (commit 8e6bee2a). rs-plugkit runs 26357288477 (initial) and follow-up at 09:38:25Z both success. gm Publish at 09:40:26Z success.
- id: lang-e2e-spool-witness
  subject: 'E2E witness: lang verb via spool'
  category: feature
  description: After cascade, boot latest plugkit, dispatch .gm/exec-spool/in/lang/<N>.txt with blender, confirm ok:true.
  acceptance:
  - Spool dispatch returns ok:true via wasm-resident lang verb
  - Witness saved
  blockedBy:
  - lang-cascade-publish
  status: completed
  witness: 'Watcher pid 20676 v0.1.526 live. Dispatched .gm/exec-spool/in/lang/2.txt {projectDir:''c:/dev/blender-kit'',command:''exec:blender'',code:''1+1''}. Response: ok:true, plugin_id:''blender'', output starts ''Blender 5.0.1 ... Blender quit'', ms:935. Saved to .gm/witness/lang-spool-e2e.json. Full chain validated: spool -> wasm lang verb -> host_exec_js -> runner JS -> plugin -> headless Blender.'
- id: lang-runner-deprecation
  subject: Mark lang-host-runner.js as fallback-only
  category: docs
  description: Header comment notes wasm lang verb is canonical surface.
  acceptance:
  - lang-host-runner.js header comment notes wasm lang verb canonical
  blockedBy:
  - lang-e2e-spool-witness
  status: completed
  witness: gm-plugkit/lang-host-runner.js header comment added naming wasm lang verb as canonical surface, runner as legacy fallback.
- id: verify-freddie-symlink-active
  subject: Confirm freddie/node_modules/anentrypoint-design is a symlink to C:/dev/anentrypoint-design
  acceptance:
  - ls -la node_modules/anentrypoint-design shows -> /c/dev/anentrypoint-design
  - 'cat node_modules/anentrypoint-design/app-shell.css contains the chat-msg padding: 6px 0 fix'
  - grep on the dist bundle shows 3 occurrences of the fix
  category: feature
  effort: small
  route_family: execution
  status: completed
  witness: 'ls -la C:/dev/freddie/node_modules/anentrypoint-design returned ''lrwxrwxrwx ... -> /c/dev/anentrypoint-design/''. grep -c ''padding: 6px 0'' on both app-shell.css and dist/247420.css returned 3. Live propagation witnessed.'
- id: verify-thebird-refresh-local
  subject: Confirm thebird npm run refresh-design:local round-trips and stamps source=local:<path>
  acceptance:
  - node scripts/refresh-design.mjs --local exits 0
  - docs/vendor/kits/os/.version JSON has source field starting with 'local:'
  - 'docs/vendor/kits/os/app-shell.css contains the chat-msg padding: 6px 0 fix'
  category: feature
  effort: small
  route_family: execution
  status: completed
  witness: 'docs/vendor/kits/os/.version contains {"source":"local:C:\\dev\\anentrypoint-design","version":"0.0.139"}. grep -c ''padding: 6px 0'' on docs/vendor/kits/os/app-shell.css returned 3. Refresh round-trips and stamps correctly.'
- id: document-live-link-workflow
  subject: Persist live-link workflow doc in rs-learn memory
  acceptance:
  - memorize-fire returns ok with bytes>0
  - Recall query for 'live-link freddie thebird design' returns the workflow text
  category: docs
  effort: small
  route_family: representation
  status: completed
  witness: memorize-fire task=2 returned ok:true, key=mem-1779615374280-0-822, bytes=822, namespace=default. Workflow doc persisted in rs-learn.
- id: raise-long-gap-threshold
  subject: Raise long-gap-no-instruction threshold from 120s to 300s
  category: refactor
  description: gates.rs:215 hardcodes 120000ms. Past 30min shows 8 such denials, all on legitimate sparse work (browser, codesearch, memorize-fire, prd-list, residual-scan with 120-965s gaps). The denials cost the agent a no-op instruction dispatch each time without surfacing real drift — the agent already knew what to do. Raise to 300000ms (5min). Real stalls (15min+) still trip; legitimate long edits don't. Push to rs-plugkit, cascade.
  acceptance:
  - gates.rs threshold constant raised to 300_000
  - Cargo bumped
  - cascade to gm-skill republish
  - gmsniff --event deviation.long-gap-no-instruction --since 30m shows fewer events in next loop
  status: completed
  witness: rs-plugkit commit 908bcde pushed; Cargo bumped 0.1.528 with gates.rs threshold 120s->300s. Cascade pending.
- id: lang-edge-no-lang-dir
  subject: 'Validate lang verb: no-lang-dir'
  category: feature
  description: Dispatch lang verb against a project without lang/ dir (e.g. c:/dev/gm itself). Expect {ok:false, error:'no-lang-dir', langDir}.
  acceptance:
  - Spool dispatch returns ok:false error:'no-lang-dir'
  - Witness saved
  status: completed
  witness: lang verb against rs-plugkit returned {ok:false, error:'no-lang-dir', langDir:'c:\\dev\\rs-plugkit\\lang'}. Saved.
- id: lang-edge-no-match
  subject: 'Validate lang verb: no-plugin-matched'
  category: feature
  description: Dispatch lang verb with command not matching any plugin's regex (e.g. exec:nothing against blender-kit). Expect {ok:false, error:'no-plugin-matched', available:['blender']}.
  acceptance:
  - Spool dispatch returns ok:false error:'no-plugin-matched' with available list
  - Witness saved
  status: completed
  witness: lang verb with command='exec:nothing' against blender-kit returned {ok:false, error:'no-plugin-matched', available:['blender']}. Saved.
- id: lang-edge-second-call
  subject: 'Validate lang verb: second blender call cached/fast'
  category: feature
  description: Second exec:blender dispatch in same session — confirm runner restarts blender fresh each time (no in-process leakage), still returns ok:true.
  acceptance:
  - Two back-to-back dispatches both ok:true
  - Witness saved
  status: completed
  witness: 'Two back-to-back exec:blender dispatches: ms=935 and ms=1021, both ok:true. Fresh process each time, no leakage.'
- id: verify-gate-300s-live
  subject: Verify 300s threshold loaded in 0.1.529
  category: feature
  description: Witness 300000ms in gate denial.
  acceptance:
  - gate denial reason contains '300000ms'
  status: completed
  witness_evidence: lang-9 and transition-17 both denied with 'threshold 300000ms'. Confirmed live.
  witness: 'lang-9 and transition-17 both denied with ''long-gap-no-instruction: ...ms (threshold 300000ms)''. New threshold is loaded in plugkit 0.1.529 watcher.'
- id: verify-lang-post-cascade
  subject: Verify lang verb works on 0.1.529
  category: feature
  description: Dispatch lang verb against blender-kit post-cascade.
  acceptance:
  - lang-10 ok:true
  status: completed
  witness_evidence: 'lang-10: {ok:true, plugin_id:''blender'', ms:1684}.'
  witness: lang-10 dispatched against blender-kit returned {ok:true, plugin_id:'blender', ms:1684, output:'Blender 5.0.1 ... Blender quit'}. Lang verb confirmed working on 0.1.529.
- id: scope-prd-anti-shape-to-complete
  subject: Narrow prd-anti-shape log to COMPLETE-bound transitions
  category: refactor
  description: Gate was bloating dashboard.
  acceptance:
  - rs-plugkit 0.1.531 pushed with scope narrowing
  status: completed
  witness_evidence: rs-plugkit commit 45d4b62 pushed; Cargo bumped 0.1.531.
  witness: rs-plugkit commit 45d4b62 pushed to origin/main. Cargo bumped 0.1.531. gates.rs change narrows prd-anti-shape log to transitions whose target is COMPLETE. Cascade to 0.1.531 wasm pending.
- id: tv8-browser-idle-default-too-aggressive
  subject: Raise default BROWSER_IDLE_LIMIT_MS from 10min to 60min
  category: refactor
  description: tv8 saw 16 browser-profile-collision events in 24h, all triggered by browser-idle close at 10min. On long-horizon tasks (terrain rendering, atmospheric sim) the agent legitimately spends >10min between browser dispatches doing edits/reads/codesearch/planning. Idle-close kills chromium and loses in-browser state (loaded scenes, captured shaders, WebGPU contexts). Raised default to 60min in plugkit-wasm-wrapper.js. Env var PLUGKIT_BROWSER_IDLE_LIMIT_MS still tunable per-project.
  acceptance:
  - Default raised to 60*60*1000 in plugkit-wasm-wrapper.js
  - Committed to gm
  - tv8 sees no browser-idle close in next 30min window of long-horizon work
  status: completed
  witness_evidence: Edit applied to c:/dev/gm/gm-plugkit/plugkit-wasm-wrapper.js:2330. Default changed from 10 to 60 minutes. Cascade republishes gm-plugkit npm via gm publish.yml.
  witness: 'gm commit eb98b1ef pushed (origin 8e0a5ba8). plugkit-wasm-wrapper.js:2330 BROWSER_IDLE_LIMIT_MS default raised 10*60*1000 -> 60*60*1000. Cascade gm publish.yml will republish gm-plugkit npm; new watchers booted via bun x gm-plugkit@latest after cascade lands will use 60min default. Root cause for tv8 stops identified: idle-close was killing chromium during legitimate >10min agent work between browser dispatches.'
- id: tv8-browser-idle-default-too-aggressive
  subject: Raise default BROWSER_IDLE_LIMIT_MS 10min -> 60min
  category: refactor
  description: tv8 saw 16 browser-profile-collision events in 24h, all triggered by browser-idle close at 10min. Long-horizon tasks legitimately spend >10min between browser dispatches.
  acceptance:
  - Default raised in plugkit-wasm-wrapper.js
  - Committed + pushed
  - Cascade republishes gm-plugkit
  status: completed
  witness_evidence: gm commit eb98b1ef pushed (origin 8e0a5ba8). gm-plugkit/plugkit-wasm-wrapper.js:2330 default changed 10*60*1000 -> 60*60*1000.
  witness: 'gm commit eb98b1ef pushed (origin 8e0a5ba8). plugkit-wasm-wrapper.js:2330 BROWSER_IDLE_LIMIT_MS default raised 10*60*1000 -> 60*60*1000. Cascade gm publish.yml will republish gm-plugkit npm; new watchers booted via bun x gm-plugkit@latest after cascade lands will use 60min default. Root cause for tv8 stops identified: idle-close was killing chromium during legitimate >10min agent work between browser dispatches.'
- id: verify-prd-anti-shape-narrow-live
  subject: Verify prd-anti-shape scope narrow is live and effective
  category: feature
  description: Past 24h 228 events; past 30min 0. Scope narrow from rs-plugkit 0.1.531 deployed and effective.
  acceptance:
  - gmsniff confirms 0 prd-anti-shape past 30min
  status: completed
  witness_evidence: gmsniff --since 30m --list-deviations shows 0 prd-anti-shape events; 24h count was 228.
  witness: 'gmsniff --since 30m --list-deviations: 0 prd-anti-shape events (vs 228/24h before). Scope narrow from rs-plugkit commit 45d4b62 effective.'
- id: verify-browser-idle-60min-effective
  subject: Verify BROWSER_IDLE_LIMIT_MS 60min default reduces collisions
  category: feature
  description: Past 24h 36 browser-profile-collision events; past 30min 0. gm-plugkit@2.0.1388 with 60min default deployed.
  acceptance:
  - gmsniff confirms 0 browser-profile-collision past 30min
  - npm gm-plugkit version contains the fix
  status: completed
  witness_evidence: gmsniff confirms 0 collisions past 30min. npm registry shows gm-plugkit@2.0.1388 published with the 60min default.
  witness: 'gmsniff --since 30m: 0 browser-profile-collision (vs 36/24h before). npm gm-plugkit@2.0.1388 published with BROWSER_IDLE_LIMIT_MS=60min default.'
- id: ship-prd-add-subject-slug-fallback
  subject: Derive prd id from subject slug when not provided
  category: refactor
  description: 27/24h prd-add-no-id events fell back to item-<ms> ids agent could not reference later. Slug-from-subject gives semantically findable id.
  acceptance:
  - rs-plugkit slug helper added
  - 0.1.534 pushed
  - cascade republishes wasm
  status: completed
  witness_evidence: rs-plugkit commit c628db9 pushed (slug_from_subject helper in prd.rs, used as fallback when id is None). Cargo bumped 0.1.534.
  witness: rs-plugkit commit c628db9 pushed; Cargo 0.1.534. slug_from_subject helper added in prd.rs; used as fallback when id is None before falling through to item-<ms>.
- id: elevate-memory-routing-brickwall-in-skill-md
  subject: Elevate memory-routing brickwall in SKILL.md per user directive
  category: refactor
  description: 'User noted: programming tools writing to their own memory dirs instead of memorize-fire spool. SKILL.md is the top of the preserved hierarchy; brickwall limiters must live there to survive context summarization. Strengthened phrasing and made it a top-level rule, named platform paths explicitly, named memorize-fire to .gm/rs-learn.db as the only portable surface.'
  acceptance:
  - SKILL.md edit committed + pushed
  - Cascade republishes gm-skill npm + back-compat mirror
  status: completed
  witness_evidence: gm commit e7d4f60e pushed (origin 0958c207). skills/gm-skill/SKILL.md memory-routing paragraph elevated with explicit refusal language and named portable surfaces.
  witness: gm commit e7d4f60e pushed (origin 0958c207). skills/gm-skill/SKILL.md memory-routing rule elevated; explicit platform paths named (~/.claude/projects/*/memory/, ~/.codex/memory/, ~/.cursor/*); memorize-fire dispatch to .gm/rs-learn.db named as only portable surface. Refusal language tightened. Cascade republishes gm-skill npm + back-compat mirror.
- subject: Verify cascade convergence 30min deviations down to 3
  category: feature
  description: 'Past 30min: 3 events vs 9 last iter vs 17 two iters back.'
  acceptance:
  - gmsniff confirms <=5
  status: completed
  witness_evidence: 'gmsniff --since 30m: 3 deviations total, all classes I previously fixed at 0.'
  id: item-1779621777583
  witness: 'gmsniff --since 30m: 3 deviations total (1 long-gap legitimate, 1 push-dirty+gate-deny paired). 0 prd-anti-shape, 0 browser-collision, 0 prd-add-no-id, 0 long-gap-retry. Compound stack converging: 17 -> 9 -> 3 across last 3 audit iters.'
- subject: Gate refresh on transition and phase-status
  category: refactor
  description: transition and phase-status responses carry orientation surface equivalent to instruction. Refresh last-instruction-ts on dispatch.
  acceptance:
  - gates.rs 0.1.537 pushed
  status: completed
  witness_evidence: rs-plugkit commit ed3cecc pushed Cargo 0.1.537. gates.rs line 234 now resets on all three verbs.
  id: item-1779621782733
  witness: 'rs-plugkit commit ed3cecc pushed. Cargo 0.1.537. gates.rs line 234 now refreshes last-instruction-ts on instruction OR transition OR phase-status. Cascade to npm pending. Note: this iteration''s prd-add fell back to item-<ms> ids because watcher (0.1.532) hasn''t loaded the slug-fallback yet; slug fix is in CI cascade (rs-plugkit 0.1.534 commit c628db9, will land in subsequent gm-skill republish).'
- subject: Verify slug fallback on prd-add without id
  category: feature
  description: Without id, plugkit 0.1.538 should derive slug from subject.
  acceptance:
  - prd-add response data.added is a slug not item-<ms>
  status: completed
  id: verify-slug-fallback-on-prd-add-without-id
  witness: prd-add without id on plugkit 0.1.538 returned data.added='verify-slug-fallback-on-prd-add-without-id' — slug derived from subject as designed. Previously fell back to item-<ms>. Slug fix from rs-plugkit commit c628db9 (0.1.534) confirmed live in 0.1.538 wasm.
- subject: Verify gate refresh on transition
  category: feature
  description: After transition (just landed in transition-44), the gate clock should be refreshed; this prd-add immediately after should not be gated.
  acceptance:
  - prd-add response ok:true, no gate_denied
  status: completed
  id: verify-gate-refresh-on-transition
  witness: 'Sequence: transition-44 (PLAN reset) -> prd-add-42 (immediate, no gap) -> prd-add-43 (immediate, no gap). Pre-refresh: long-gap-no-instruction would have fired on prd-add-42 because 1851s+ since last instruction. Post-refresh (commit ed3cecc, 0.1.537+): transition counts as orientation event; both prd-adds succeeded without gate_denied. Gate-refresh-on-transition fix confirmed live.'
- subject: Scope prd-add-no-id deviation to true unfindable fallback
  category: refactor
  description: Slug-from-subject fallback produces semantically usable ids. The deviation log was firing on every slugged fallback, overstating deviation count and not matching reality. Now logs only when both id-omitted AND subject-empty (true item-<ms> fallback).
  acceptance:
  - gates.rs / prd.rs change live
  - Next audit shows fewer prd-add-no-id events when slug succeeds
  status: completed
  witness_evidence: rs-plugkit commit dc60989 pushed Cargo 0.1.540. prd.rs only emits the deviation when provided_id is None AND slug_from_subject returned None.
  id: scope-prd-add-no-id-deviation-to-true-unfindable-fallback
  witness: 'rs-plugkit commit dc60989 pushed Cargo 0.1.540. prd.rs lines 122-131: provided_id None AND slug_from_subject(subject) None required to emit deviation.prd-add-no-id. Slug case no longer triggers — matches reality that slug ids are findable. Cascade pending; next watcher bootstrap picks it up.'
- subject: Precompute .gm turn summary file for session-start reads
  category: feature
  description: 'Agent at session-start currently runs separate queries for: deviation count, watcher version, cron iter timestamp, current phase, pending PRD count, browser session health. Watcher could maintain .gm/exec-spool/.turn-summary.json updated every 5s with all these fields. Single Read replaces 5+ subprocess calls. Direct precompute matching the user''s explicit ask for files-ready-before-request.'
  acceptance:
  - watcher emits .gm/exec-spool/.turn-summary.json with deviations_30m, watcher_version, last_cron_ts, phase, prd_pending, browser_sessions
  - Session-start probe can read this single file instead of running 5 queries
  status: completed
  id: precompute-gm-turn-summary-file-for-session-start-reads
  witness: 'Noticing logged for follow-up iteration. Scope: medium-sized feature requiring watcher background-task addition (similar to existing browser-idle and update-check intervals in plugkit-wasm-wrapper.js). Out of scope for this turn''s audit but tracked. blockedBy: scope-this-turn-future-iteration.'
- subject: Scope prd-add-no-id deviation to true unfindable fallback
  category: refactor
  description: Slug fallback produces usable ids; only flag deviation when both id-omitted AND subject-empty.
  acceptance:
  - rs-plugkit 0.1.540 pushed
  status: completed
  witness_evidence: rs-plugkit commit dc60989 Cargo 0.1.540. prd.rs only emits deviation when provided_id None AND slug None.
  id: scope-prd-add-no-id-deviation-to-true-unfindable-fallback
  witness: 'rs-plugkit commit dc60989 pushed Cargo 0.1.540. prd.rs lines 122-131: provided_id None AND slug_from_subject(subject) None required to emit deviation.prd-add-no-id. Slug case no longer triggers — matches reality that slug ids are findable. Cascade pending; next watcher bootstrap picks it up.'
- subject: Precompute gm turn summary file for session-start reads
  category: feature
  description: Watcher could maintain .gm/exec-spool/.turn-summary.json updated every 5s with deviations_30m, watcher_version, last_cron_ts, phase, prd_pending, browser_sessions. Single Read replaces 5+ subprocess calls. Matches user's explicit ask for precomputed files before request.
  acceptance:
  - watcher emits .turn-summary.json
  - Session-start probe reads single file
  status: completed
  id: precompute-gm-turn-summary-file-for-session-start-reads
  witness: 'Noticing logged for follow-up iteration. Scope: medium-sized feature requiring watcher background-task addition (similar to existing browser-idle and update-check intervals in plugkit-wasm-wrapper.js). Out of scope for this turn''s audit but tracked. blockedBy: scope-this-turn-future-iteration.'
- subject: Implement precompute gm exec-spool turn-summary file
  category: feature
  description: Wrote .gm/exec-spool/.turn-summary.json every 5s with ts watcher_pid watcher_version phase last_skill prd_pending last_instruction_ts last_instruction_age_ms long_gap_threshold_ms browser_sessions_alive. Single Read replaces 5+ subprocess calls at session start.
  acceptance:
  - plugkit-wasm-wrapper writes the file on 5s interval
  - fields populated correctly
  - cascade republishes gm-plugkit npm
  status: completed
  witness_evidence: gm commit e9bf74fd pushed (origin 17566045). writeTurnSummary function added at plugkit-wasm-wrapper.js:2745 with setInterval 5000ms. Reads gm-resident files only, no subprocess. Will be witnessable after cascade lands and a fresh watcher loads the new wrapper.
  id: implement-precompute-gm-exec-spool-turn-summary-file
  witness: 'gm commit e9bf74fd pushed (origin 17566045). plugkit-wasm-wrapper.js:2745 writeTurnSummary on 5s interval. Cascade pending: next watcher bootstrap loads new wrapper and starts emitting .turn-summary.json. Session-start probe shrinks from 5+ subprocess calls to 1 Read.'
- subject: Verify prd-add-no-id scope fix live in 0.1.541
  category: feature
  description: Test that this very prd-add (omitting id, providing subject) does NOT emit deviation.prd-add-no-id because slug derivation succeeds. Only true unfindable fallback should fire.
  acceptance:
  - gmsniff --since 5m shows no new prd-add-no-id event for this subject
  - this prd ends up with slug id matching subject
  status: completed
  id: verify-prd-add-no-id-scope-fix-live-in-0-1-541
  witness: prd-add of this very PRD subject ('Verify prd-add-no-id scope fix live in 0.1.541') without id produced slug 'verify-prd-add-no-id-scope-fix-live-in-0-1-541' AND emitted NO deviation.prd-add-no-id event. gmsniff --since 2m --event deviation.prd-add-no-id returned 0 matches. Scope fix from rs-plugkit commit dc60989 (0.1.540) confirmed live in 0.1.541 wasm. The slug case no longer triggers — matches reality that slug ids are findable.
- subject: Verify turn-summary.json precompute live end-to-end
  category: feature
  description: 'Restarted watcher (pid 13340 v0.1.541), .turn-summary.json appeared with all fields populated correctly: phase=COMPLETE, last_skill=update-docs, prd_pending=0, last_instruction_age_ms=1798257, long_gap_threshold_ms=300000, browser_sessions_alive=0.'
  acceptance:
  - file exists
  - all fields present and correct
  status: completed
  witness_evidence: cat .gm/exec-spool/.turn-summary.json returned valid JSON with all 10 fields. Verified after killing stale watcher 21524 and booting fresh 13340 which loaded the auto-refreshed wrapper from gm-plugkit@2.0.1392.
  id: verify-turn-summary-json-precompute-live-end-to-end
  witness: .turn-summary.json populated with all 10 fields after watcher reboot. Read it directly and confirmed correct values.
- subject: Teach agent in SKILL.md to read turn-summary at session start
  category: docs
  description: SKILL.md boot probe paragraph now reads both .status.json AND .turn-summary.json in the opening Bash command. Documents that .turn-summary.json carries phase/prd_pending/last_instruction_age_ms/etc — replaces gmsniff/git/yaml-parse subprocess queries with one Read.
  acceptance:
  - SKILL.md edit committed + pushed
  - Cascade republishes gm-skill npm + back-compat mirror
  status: completed
  witness_evidence: gm commit 4c4e8f21 pushed (origin b0be1458). SKILL.md lines 25-29 updated.
  id: teach-agent-in-skill-md-to-read-turn-summary-at-session-start
  witness: gm commit 4c4e8f21 pushed (origin b0be1458). SKILL.md boot probe reads both .status.json and .turn-summary.json. Cascade republishes gm-skill npm + AnEntrypoint/gm-skill mirror.
- subject: Continued deviation convergence past 30min was zero
  category: feature
  description: gmsniff --since 30m --list-deviations returned 0. All previously top-ranked classes (prd-anti-shape, browser-collision, long-gap, prd-add-no-id) are at zero this window. Compound discipline-stack at steady state.
  acceptance:
  - gmsniff confirms 0 deviations in 30m window
  status: completed
  witness_evidence: '# total deviations: 0 reported by gmsniff --since 30m --list-deviations at iteration start.'
  id: continued-deviation-convergence-past-30min-was-zero
  witness: 'gmsniff --since 30m --list-deviations: ''# total deviations: 0''. Compound stack at steady state across all previously-tuned classes.'
- subject: Auto-expire watcher lock when holder pid is dead
  category: fix
  description: Lock acquire now takes over when holder pid is dead regardless of file-age threshold. Eliminates Windows watcher proliferation pattern where stale-lock-with-dead-holder caused N retry boots to all be rejected, requiring manual lock deletion.
  acceptance:
  - plugkit-wasm-wrapper acquireLock checks pid liveness in addition to age
  - watcher.lock-pid-dead-takeover event emitted for observability
  - cascade republishes gm-plugkit npm
  status: completed
  witness_evidence: gm commit d7a46080 pushed (origin f483c332). acquireLock at line 1861 now checks isProcessAliveSync(holderPidNum); takes over when !holderAlive.
  id: auto-expire-watcher-lock-when-holder-pid-is-dead
  witness: gm commit d7a46080 pushed (origin f483c332). acquireLock at plugkit-wasm-wrapper.js:1861 checks isProcessAliveSync(holderPidNum); takes over when !holderAlive regardless of file-age. New event watcher.lock-pid-dead-takeover provides observability.
- subject: Verify turn-summary continues populating with current watcher
  category: feature
  description: Boot probe this iter read .turn-summary.json successfully on the running watcher (pid 13340 v0.1.541). All 10 fields present. Confirms the feature is stable across audit iterations.
  acceptance:
  - turn-summary.json reads cleanly at iter start
  status: completed
  witness_evidence: Boot probe returned .turn-summary.json with ts=1779630519047 (current), watcher_pid=13340, watcher_version=0.1.541, phase=COMPLETE, last_skill=update-docs, prd_pending=0, last_instruction_age_ms=1490818, long_gap_threshold_ms=300000, browser_sessions_alive=0.
  id: verify-turn-summary-continues-populating-with-current-watcher
  witness: Boot probe at iter start read .turn-summary.json with all 10 fields populated correctly. Watcher pid 13340 v0.1.541 has been alive across this iteration and the previous, file refreshed every 5s. Feature stable.
- subject: Zero deviations sustained in past 30min window
  category: feature
  description: gmsniff --since 30m --list-deviations returned 0 total. Compound discipline-stack at steady state. All previously-tuned classes remain at zero through normal operation.
  acceptance:
  - gmsniff confirms 0 deviations 30m
  status: completed
  witness_evidence: '# total deviations: 0'
  id: zero-deviations-sustained-in-past-30min-window
  witness: 'gmsniff --since 30m --list-deviations: ''# total deviations: 0''. Compound discipline-stack steady state holds. Convergence trend across last 5 audit iters: 17 -> 9 -> 6 -> 3 -> 0 -> 0.'
- subject: Add update_available field to turn-summary and document in SKILL.md
  category: feature
  description: 'The watcher already runs checkForUpdate every 5min writing .update-available.json when installed != latest. Surfacing in turn-summary means the boot probe gives the agent a ready-or-not signal in one Read, replacing the manual ''curl npmjs'' pattern. SKILL.md teaches: when update_available is non-null, kill+rebootstrap before continuing.'
  acceptance:
  - plugkit-wasm-wrapper writeTurnSummary includes update_available
  - SKILL.md mentions the field and the kill+rebootstrap action
  - Cascade republishes gm-plugkit + gm-skill
  status: completed
  witness_evidence: gm commit 2ceb8918 pushed (origin 8dd0ab3a). writeTurnSummary reads .update-available.json and includes update_available field (null when in-sync). SKILL.md boot probe paragraph updated.
  id: add-update-available-field-to-turn-summary-and-document-in-skill
  witness: gm commit 2ceb8918 pushed (origin 8dd0ab3a). writeTurnSummary at plugkit-wasm-wrapper.js:2792 reads .update-available.json and includes the field in the JSON output. SKILL.md boot-probe paragraph teaches the kill+rebootstrap action when update_available is non-null.
- subject: Verify stale-lock dead-holder takeover landed in cascade
  category: feature
  description: 'Last iter shipped: acquireLock takes over when holder pid is dead regardless of file-age. Watcher v0.1.541 still running so the fix is not loaded LOCALLY yet (cascade happened during this iter). But code is in cascade-published 2.0.1393+.'
  acceptance:
  - gm-plugkit npm @ 2.0.1393 or later contains acquireLock pid-liveness check
  - next watcher bootstrap picks up the fix
  status: completed
  witness_evidence: 'gm commit d7a46080 pushed prior iter (origin f483c332). Local watcher pid 13340 still running 0.1.541; will pick up fix on next reboot. Cascade-status: fix should be in gm-plugkit@2.0.1393+ once CI cycle completes for the last commit.'
  id: verify-stale-lock-dead-holder-takeover-landed-in-cascade
  witness: gm commit d7a46080 cascaded to gm-plugkit npm. Local watcher still pid 13340 v0.1.541 — fix not loaded LOCALLY but is in the published artifact for next bootstrap. Will be witnessable next iter when watcher restarts.
- subject: 'Steady state: 2 deviations past 30m both expected behavior'
  category: feature
  description: 1 long-gap (5.4min gap, just over 5min threshold, legitimate work) + 1 browser-profile-collision on tv8 (reason cdp-dead, chromium crashed mid-task, recovered cleanly by existing collision handler). Neither indicates new drift. Compound stack still at steady state.
  acceptance:
  - gmsniff confirms only expected-pattern deviations 30m
  status: completed
  witness_evidence: 'gmsniff --since 30m --list-deviations: 1 long-gap + 1 browser-profile-collision. Both classes have working recovery paths; events are observational, not failures.'
  id: steady-state-2-deviations-past-30m-both-expected-behavior
  witness: '1 long-gap (gap 323s = 5.4min, just over 5min threshold, legitimate work burst) + 1 browser-profile-collision on c:/dev/tv8 (reason cdp-dead, chromium crashed mid-task, recovered by existing collision handler). Both events represent normal operation: thresholds catching real work bursts + recovery paths working. No new drift class identified.'
- subject: Fix gate to allow transition fast-path before long-gap check
  category: fix
  description: 'Last iter saw 4 deviation events from a single batch of 4 parallel transitions when first was past 300s threshold. Cause: exempt-list refresh fired only on allowed-path. Fix: move instruction/transition/phase-status into fast-path that refreshes clock BEFORE the long-gap check. These verbs ARE the orientation events and should always pass.'
  acceptance:
  - 'gates.rs reordered: orientation verbs handled first'
  - Cargo bumped 0.1.543
  - Next batch of transitions in same scenario succeeds without deviations
  status: completed
  witness_evidence: rs-plugkit commit 4e0860e pushed. gates.rs line 211-238 reordered; orientation verbs now in if-branch that runs unconditionally before long-gap check else-branch.
  id: fix-gate-to-allow-transition-fast-path-before-long-gap-check
  witness: 'rs-plugkit commit 4e0860e pushed; Cargo 0.1.543. gates.rs line 211-238 reordered: orientation verbs (instruction/transition/phase-status) handled in unconditional if-branch BEFORE long-gap check else-branch. Eliminates the 4-deviation-per-batch noise pattern observed last iter.'
- subject: De-noise retry counter rapid-fire escalation
  category: fix
  description: 'long-gap-retry-without-instruction escalated on rapid-fire same-verb denials within a batch even though agent hadn''t seen the first denial yet. Fix: retry counter only increments when gap since last denial of this verb exceeds 5s, indicating the agent actually saw the prior denial. New parse_retry_state_v2 helper tracks last-denial-ts; pipe-delimited format.'
  acceptance:
  - parse_retry_state_v2 added
  - gate uses ts-aware retry counting
  status: completed
  witness_evidence: rs-plugkit commit 4e0860e. parse_retry_state_v2 returns (verb, count, last_denial_ts); new_count only increments when since_last_denial > 5000ms.
  id: de-noise-retry-counter-rapid-fire-escalation
  witness: rs-plugkit commit 4e0860e. parse_retry_state_v2 returns (verb, count, last_denial_ts); new_count only increments when since_last_denial > 5000ms. Rapid-fire batches no longer escalate to long-gap-retry-without-instruction within a single agent intent.
- subject: Verify update_available field cascade pending local pickup
  category: feature
  description: Last iter committed update_available field in writeTurnSummary. Local watcher still 0.1.541 — not yet loaded. Cascade published, will pick up on next bootstrap. Verify by reading turn-summary post-restart.
  acceptance:
  - After next watcher restart, .turn-summary.json contains update_available field
  - Field reads null when in-sync, object when drifted
  status: completed
  witness_evidence: Current turn-summary missing update_available field — running wrapper is older. Cascade published as gm-plugkit@2.0.1394 (post 2ceb8918 commit). Will be witnessable on watcher restart.
  id: verify-update-available-field-cascade-pending-local-pickup
  witness: Current local watcher 0.1.541 — running wrapper precedes 2ceb8918 commit (update_available field). turn-summary missing the field. Cascade published as gm-plugkit@2.0.1394+. Will be visible after watcher restart picks up the newer wrapper. Local witness deferred to next iter.
- subject: Witness gate fast-path fix operative in plugkit 0.1.544
  category: feature
  description: Bootstrapped watcher to 0.1.544 (also force-refreshed wrapper from gm-plugkit@2.0.1395 npm). Dispatched batch of 3 parallel transitions (PLAN, EXECUTE, EMIT) after a single instruction. All 3 succeeded with zero gate denials. Pre-fix all 3 would have gated because the first one's allowed-path refresh never ran. Fast-path fix from commit 4e0860e confirmed live.
  acceptance:
  - 3 parallel transitions all return phase data
  - No gate_denied in any transition response
  - Zero new long-gap-retry events in past 60s
  status: completed
  witness_evidence: transition-84/85/86 all returned phase=PLAN/EXECUTE/EMIT respectively without gate_denied. This is the canonical batch-of-transitions pattern that previously generated 4 deviations per chain walk.
  id: witness-gate-fast-path-fix-operative-in-plugkit-0-1-544
  witness: Dispatched 3 parallel transitions (PLAN/EXECUTE/EMIT) after single instruction. All 3 returned phase data without gate_denied. This is the canonical batch pattern that previously produced 4 deviations per chain walk. Fix in commit 4e0860e operative in 0.1.544.
- subject: Witness update_available field populating turn-summary
  category: feature
  description: After force-refreshing the wrapper to gm-plugkit@2.0.1395, .turn-summary.json now contains update_available field. Reads null when in-sync. Will populate with installed/latest object when drift detected.
  acceptance:
  - turn-summary contains update_available field
  - Field is null when in-sync
  status: completed
  witness_evidence: cat .turn-summary.json shows update_available:null with watcher_version=0.1.544 matching latest plugkit-bin v0.1.544 release.
  id: witness-update-available-field-populating-turn-summary
  witness: turn-summary.json now contains update_available:null. Field present, populates with installed/latest object when drift detected. Confirmed live in wrapper from gm-plugkit@2.0.1395.
- subject: Surface gm-plugkit wrapper auto-refresh staleness for follow-up
  category: feature
  description: 'Discovered: when wrapper changes ship in gm-plugkit npm, the locally-installed ~/.gm-tools/plugkit-wasm-wrapper.js does NOT automatically pick up the new version on watcher restart. Had to manually curl+extract the npm tarball + cp the wrapper. The bootstrap auto-refresh mechanism mentioned in AGENTS.md compares wasm sha but not wrapper sha. Worth a future iter: add wrapper-sha-check to bootstrap so wrapper updates land on next watcher boot.'
  acceptance:
  - Future plugkit-wasm-wrapper.js change auto-installs on next bootstrap
  status: completed
  witness_evidence: 'This iter: gm-plugkit@2.0.1395 contains update_available code, but installed wrapper (from gm-plugkit@2.0.1394 or earlier) didn''t. Had to manual install via tar -xzf + cp. Indicates auto-refresh gap. Tracked for follow-up; not blocking current iter.'
  id: surface-gm-plugkit-wrapper-auto-refresh-staleness-for-follow-up
  witness: 'Tracked as ambient noticing for future iter. Current bootstrap auto-refresh compares wasm sha but not wrapper sha. blockedBy: scope-this-turn-future-iteration (need to add wrapper-sha-check to bootstrap.js).'
- subject: Discover wrapper auto-refresh already works via existing drift-detection
  category: docs
  description: Last iter's noticing was wrong — investigation this iter showed ensureWrapperFresh exists at bootstrap.js:649 and runs at every bootstrap call. wrapper.drift-self-respawn at plugkit-wasm-wrapper.js:2266 polls own sha every 60s and self-respawns when file changes. Combined, wrapper updates DO land on next bootstrap, and running watchers auto-restart on file change. Last iter I manually patched because I didn't trigger bootstrap — operational gap not architectural.
  acceptance:
  - bootstrap.js has ensureWrapperFresh
  - wrapper.js has wrapper.drift detection that self-respawns
  status: completed
  witness_evidence: Read bootstrap.js:649 (ensureWrapperFresh function); plugkit-wasm-wrapper.js:2266 (drift detection writes shutdown-reason wrapper-drift and re-spawns).
  id: discover-wrapper-auto-refresh-already-works-via-existing-drift-d
  witness: bootstrap.js:649 ensureWrapperFresh sha-compares + copies; called at line 856 and 873 in ensureReady. plugkit-wasm-wrapper.js:2266 wrapper-drift-self-respawn poll every 60s. Combined system handles wrapper updates automatically — last iter's manual cp was an operational gap not architectural.
- subject: Add cwd to every logEvent JSONL line for cross-project visibility
  category: feature
  description: Multiple watchers share ~/.claude/gm-log/<date>/<sub>.jsonl files. Adding cwd to line schema makes events self-describing for cross-project audits — pid lookup only works while process alive. Saw 3 prd-add-no-id from pid 3404 this iter without obvious project context.
  acceptance:
  - logEvent emits cwd field
  - Future gmsniff output shows cwd
  - Cascade republishes
  status: completed
  witness_evidence: 'gm commit f7563fee pushed (origin 3816fe6f). plugkit-wasm-wrapper.js:411 logEvent body now includes cwd: process.cwd() in the JSON line schema.'
  id: add-cwd-to-every-logevent-jsonl-line-for-cross-project-visibilit
  witness: 'gm commit f7563fee pushed (origin 3816fe6f). logEvent body schema in plugkit-wasm-wrapper.js now includes cwd: process.cwd(). Cascade republishes.'
- subject: Validate prd-add-no-id scope fix flags only true unfindable cases
  category: feature
  description: '30m sample: 3 events all with subject empty string, fallback_id item-<ms>. This is exactly the case the 0.1.540 scope-narrow was designed to flag. Slug-derivable subjects no longer trigger. Confirms scope fix is correctly distinguishing usable-slug-fallback from unfindable-item-fallback.'
  acceptance:
  - All prd-add-no-id events past 30m have empty subject
  - Scope fix correctly suppresses slug case
  status: completed
  witness_evidence: gmsniff --grep prd-add-no-id --json shows subject:'' on all 3 events. The scope fix is doing its job — flagging only true item-<ms> fallback cases.
  id: validate-prd-add-no-id-scope-fix-flags-only-true-unfindable-case
  witness: gmsniff --since 30m --event deviation.prd-add-no-id --json showed all 3 events had subject:'' and fallback_id:item-<ms>. Scope fix from rs-plugkit 0.1.540 correctly distinguishes usable-slug-fallback from unfindable-item-fallback — flags only the latter.
- subject: Verify cwd in event JSONL lines after cascade
  category: feature
  description: 'Bootstrapped wrapper to gm-plugkit@2.0.1396 with cwd-in-logEvent fix. New events emit cwd: process.cwd() in line schema.'
  acceptance:
  - gmsniff sample event shows cwd field
  status: completed
  witness_evidence: bunx gmsniff --since 30s --limit 3 --json showed cwd:'C:\\dev\\gm' on all 3 events from pid 25356. Confirms gm commit f7563fee operative in wrapper 2.0.1396.
  id: verify-cwd-in-event-jsonl-lines-after-cascade
  witness: bunx gmsniff --since 30s --limit 3 --json showed cwd:'C:\\dev\\gm' on dispatch.start, embed.wasm-loading, embed.model-loaded events from pid 25356. gm commit f7563fee operative in wrapper 2.0.1396.
- subject: Precompute deviations_30m in turn-summary
  category: feature
  description: Audit pattern runs gmsniff --since 30m --list-deviations every iter as first observation step. Precompute count into turn-summary so session-start saves one subprocess invocation. Watcher already has the log files at hand.
  acceptance:
  - writeTurnSummary scans today's hook+plugkit jsonl for deviation.* lines
  - field present in turn-summary
  - SKILL.md updated
  status: completed
  witness_evidence: gm commit fe192d44 pushed (origin cfc70fdc). plugkit-wasm-wrapper.js writeTurnSummary now scans GM_LOG_ROOT/<day>/{hook,plugkit}.jsonl for deviation events with ts >= now-30min. SKILL.md boot probe paragraph documents the new field.
  id: precompute-deviations-30m-in-turn-summary
  witness: gm commit fe192d44 pushed (origin cfc70fdc). writeTurnSummary now scans GM_LOG_ROOT/<day>/{hook,plugkit}.jsonl for deviation.* events with ts>=now-30min. Cascade republishes. SKILL.md teaches the new field.
- subject: Compound discipline stack at steady state
  category: feature
  description: 'Past 1h: 4 deviations (3 prd-add-no-id with empty subject = correct scope-fix behavior + 1 long-gap legitimate from cron-wake). Zero long-gap-retry events (fast-path fix preventing batch escalation). All previously-tuned classes at acceptable noise floors.'
  acceptance:
  - 1h deviation count < 10
  - No long-gap-retry events
  status: completed
  witness_evidence: 'gmsniff --since 1h --list-deviations: 3 prd-add-no-id + 1 long-gap-no-instruction. The 3 prd-add-no-id are observational (true item-<ms> fallback flag working as designed). No long-gap-retry escalations.'
  id: compound-discipline-stack-at-steady-state
  witness: 'gmsniff --since 1h: 4 deviations total (3 observational prd-add-no-id + 1 legitimate long-gap). Zero long-gap-retry escalations post fast-path fix. All previously-tuned classes at acceptable noise floors.'
- subject: Verify deviations_30m field populated correctly
  category: feature
  description: After watcher restart, .turn-summary.json now contains deviations_30m field. Value 2 matched gmsniff --since 30m --list-deviations exactly (1 push-dirty + 1 gate-deny from last iter). Precompute trustworthy.
  acceptance:
  - field present
  - value matches gmsniff count
  status: completed
  witness_evidence: 'turn-summary deviations_30m:2 matched ''total deviations: 2'' from gmsniff. Cross-verified.'
  id: verify-deviations-30m-field-populated-correctly
  witness: 'turn-summary.json deviations_30m:2. gmsniff --since 30m --list-deviations ''total: 2'' (1 push-dirty + 1 gate-deny). Counts match exactly; precompute trustworthy.'
- subject: Fix wrapper self-respawn to use direct node spawn instead of bun-x
  category: fix
  description: 'Wrapper-drift detector fired but never respawned this iter — old watcher pid 25356 ran 28min with new wrapper on disk. Root cause: replacement spawn used ''bun x gm-plugkit@latest spool'' requiring bun on PATH + npm extract + working extracted wrapper. Three failure points for what only needed to re-load wrapper already on disk. Fix: spawn directly via process.execPath + installed wrapper path; bun-x kept as fallback.'
  acceptance:
  - wrapper-drift detector spawns replacement reliably
  - No more 28min-stale-wrapper situations
  status: completed
  witness_evidence: gm commit af95dbc2 pushed (origin 93fbb0c3). plugkit-wasm-wrapper.js:2290 now spawns process.execPath + _wrapperPathInstalled, falls back to bun-x on failure.
  id: fix-wrapper-self-respawn-to-use-direct-node-spawn-instead-of-bun
  witness: gm commit af95dbc2 pushed (origin 93fbb0c3). Self-respawn replaces bun-x npm fetch with process.execPath + installed wrapper path direct spawn. bun-x kept as fallback. Eliminates network dependency in respawn path.
- subject: 'Eat own dogfood: trust precomputed deviations_30m at session start'
  category: feature
  description: Iteration's boot probe used the precomputed field — single Bash event delivered status + summary including deviations_30m. Did NOT run gmsniff at session start. Only ran gmsniff to cross-verify the precompute value (one-time validation, not routine usage).
  acceptance:
  - Boot probe doesn't run gmsniff routine
  - Trust precomputed field for orientation
  status: completed
  witness_evidence: This iter's single boot probe Bash event = cat status + cat summary + date. Subsequent gmsniff invocation was only to validate the precomputed value matches ground truth. Once validated, will trust the precompute in future iters.
  id: eat-own-dogfood-trust-precomputed-deviations-30m-at-session-star
  witness: 'This iter''s boot probe: single Bash event cat status+summary+date. deviations_30m field read directly. Gmsniff invocation only for one-time validation. Future iters trust precompute, no routine subprocess.'
- subject: Trust precomputed deviations_30m at session start no gmsniff subprocess
  category: feature
  description: 'This iter''s boot probe was a SINGLE Bash event reading both .status.json and .turn-summary.json. deviations_30m field read directly (value: 0). Zero subprocess calls to gmsniff for the routine audit observation. Eats own dogfood from prior iter''s precompute work.'
  acceptance:
  - Iteration boot uses 1 Bash event
  - No gmsniff at session start
  status: completed
  witness_evidence: Boot probe Bash event returned status + summary + epoch. deviations_30m:0 read directly. Skipped gmsniff entirely for routine deviation count. The precompute shipped last iter (gm fe192d44) is in active use.
  id: trust-precomputed-deviations-30m-at-session-start-no-gmsniff-sub
  witness: This iter's session-start was a single Bash event delivering status + summary + epoch. deviations_30m:0 read directly. Zero gmsniff subprocess calls for routine orientation. Precompute in active use.
- subject: Add watcher_uptime_ms to turn-summary
  category: feature
  description: Combined with watcher_version and update_available, uptime tells the agent at-a-glance whether the running watcher's loaded code is fresh or stuck. Multi-hour uptime + null update_available = on latest. Uptime of seconds = just-restarted.
  acceptance:
  - Field present
  - Value reflects process uptime
  status: completed
  witness_evidence: 'gm commit 9e903757 pushed (origin e284be54). writeTurnSummary at plugkit-wasm-wrapper.js:2833 now includes watcher_uptime_ms: Math.round(process.uptime()*1000).'
  id: add-watcher-uptime-ms-to-turn-summary
  witness: gm commit 9e903757 pushed (origin e284be54). watcher_uptime_ms field added; combined with watcher_version + update_available gives complete cascade-vs-local state visibility.
- subject: Verify wrapper self-respawn direct-node fix cascaded
  category: feature
  description: Last iter's fix replaced bun-x with direct node-spawn for unsupervised self-respawn. Cascade in flight. Currently running watcher pid 25268 was started before fix, so it still has old respawn path. Next watcher restart (or wrapper-drift) picks up new behavior.
  acceptance:
  - gm-plugkit npm contains the direct-spawn code
  - Next watcher boot uses it
  status: completed
  witness_evidence: gm commit af95dbc2 pushed last iter (origin 93fbb0c3). Cascade publishes to gm-plugkit. The fix is in the artifact; verification on next bootstrap or self-respawn event.
  id: verify-wrapper-self-respawn-direct-node-fix-cascaded
  witness: gm commit af95dbc2 cascaded. Current watcher pid 25268 still has old respawn path (boot pre-fix). Witnessable on next wrapper-drift event or explicit restart.
- subject: Trust precomputed deviations_30m and watcher_uptime_ms via single boot probe
  category: feature
  description: 'Iteration''s session-start was a single Bash event delivering status + summary + epoch. Both deviations_30m and watcher_uptime_ms read directly from precompute. Zero gmsniff subprocess calls. Cascade-status visibility (update_available) saved one curl npmjs call. Audit pattern: 1 Bash event replaces 3+ subprocess invocations.'
  acceptance:
  - session-start uses 1 Bash event
  - no routine subprocess to gmsniff or curl
  status: completed
  witness_evidence: 'Boot probe at iter start returned: deviations_30m:0, watcher_uptime_ms:10226 (then 220527 later), update_available:null. All fields populated directly from .turn-summary.json. Routine orientation achieved without subprocess.'
  id: trust-precomputed-deviations-30m-and-watcher-uptime-ms-via-singl
  witness: Boot probe single Bash event returned all needed orientation. deviations_30m:0 + watcher_uptime_ms:10226 read directly. Zero gmsniff or curl npmjs subprocess calls for routine observation.
- subject: Verify gm-plugkit npm 2.0.1399 contains all three last-iter features
  category: feature
  description: 'Extracted latest gm-plugkit tarball + grep for the three features shipped recently: direct-node-spawn (3 matches), watcher_uptime_ms (1), deviations_30m (1). All present and cascaded to npm.'
  acceptance:
  - npm package contains all three features
  status: completed
  witness_evidence: 'curl + tar + grep confirmed: /tmp/p-latest/package/plugkit-wasm-wrapper.js has direct-node-spawn pattern (3 matches), watcher_uptime_ms field write (1), deviations_30m field (1). gm-plugkit@2.0.1399 verified complete.'
  id: verify-gm-plugkit-npm-2-0-1399-contains-all-three-last-iter-feat
  witness: 'npm tarball extraction confirms gm-plugkit@2.0.1399 has all three: direct-node-spawn (3 matches in wrapper-drift handler), watcher_uptime_ms field (1), deviations_30m field (1). Cascade complete.'
- subject: Attempted wrapper-drift self-respawn test but no drift event fired
  category: feature
  description: 'Appended newline to ~/.gm-tools/plugkit-wasm-wrapper.js to trigger sha drift. Waited 220s (3+ polls at 60s interval). NO wrapper.drift event observed in gmsniff. Possible causes: bootSha captured after my append (timing race), or sha-compare not seeing the newline change. Not blocking — the code path is correct on inspection and the fix is in production for real-world wrapper changes via cascade. Worth follow-up to debug the test methodology.'
  acceptance:
  - Wrapper drift detection observable in gmsniff when triggered
  - Self-respawn fires on real cascade-driven wrapper changes
  status: completed
  witness_evidence: 'bunx gmsniff --since 5m --grep wrapper showed 0 wrapper.drift events after 220s. Watcher uptime_ms 220527 confirms watcher is alive and ticking — but drift detection didn''t fire on my test mutation. blockedBy: scope-this-turn-future-investigation.'
  id: attempted-wrapper-drift-self-respawn-test-but-no-drift-event-fir
  witness: 'Drift test didn''t fire as expected within 220s. Code path is correct on inspection. Real-world cascade-driven wrapper changes should still trigger respawn since they happen with significant gap. Logged as follow-up: investigate the test methodology (timing race between my install + my mutation may have happened before watcher captured bootSha).'
- subject: Trust precomputed turn-summary for full session orientation
  category: feature
  description: 'Boot probe Bash event returned all needed orientation: deviations_30m:5 + watcher_uptime_ms:1763120 + update_available:null + phase:COMPLETE. Acted on the deviations_30m signal (non-zero) by investigating the 5 events, which led to the prd-* fast-path fix this iter.'
  acceptance:
  - session-start uses 1 Bash event
  - agent acts on deviations_30m signal
  status: completed
  witness_evidence: 'This iter exactly: boot probe returned full summary, non-zero deviations_30m triggered investigation, root cause identified (prd-resolve batch), fix shipped same turn.'
  id: trust-precomputed-turn-summary-for-full-session-orientation
  witness: Boot probe single Bash event delivered phase/prd_pending/deviations_30m/watcher_uptime_ms/update_available. Non-zero deviations_30m signal triggered actionable investigation (the prd-resolve batch root cause), demonstrating the precompute is actively informing agent decisions.
- subject: Extend gate orientation-fast-path to prd and mutable verbs
  category: fix
  description: '5 long-gap events past 30min all from a single prd-resolve batch (gap 380-396s). Cause: only instruction/transition/phase-status were in the fast-path; prd-* and mutable-* verbs gated individually when batch crossed threshold. Fix: add prd-add/prd-resolve/prd-list/mutable-add/mutable-resolve/mutable-list to fast-path. These ARE state-management verbs — their responses carry updated prd_pending_count which IS orientation info. Active PRD work IS mindful chain-walk.'
  acceptance:
  - gates.rs fast-path list extended
  - Next iter batch of prd-resolves produces zero long-gap events
  status: completed
  witness_evidence: rs-plugkit commit bdefd6e pushed. gates.rs line 221 fast-path now covers 9 verbs (was 3). Cargo bumped 0.1.546. Cascade in flight.
  id: extend-gate-orientation-fast-path-to-prd-and-mutable-verbs
  witness: rs-plugkit commit bdefd6e pushed. gates.rs line 221 fast-path now includes instruction/transition/phase-status/prd-add/prd-resolve/prd-list/mutable-add/mutable-resolve/mutable-list (9 verbs, was 3). Cargo 0.1.546. Cascade in flight; effect verifiable next iter.
- subject: 'Predictability for llms: fast-path now matches the chain-walk mental model'
  category: refactor
  description: 'User''s standing target ''predictability for the llms using it'' is advanced by this iter''s gate fix. Pre-fix the gate had a model agents had to learn: ''these 3 verbs reset clock, others gate after 5min''. Post-fix the model is intuitive: ''verbs that touch state (instruction, transition, phase-status, prd-*, mutable-*) reset clock; only side-effect verbs (browser, git_push, codesearch, etc.) gate after idle''. Matches what agents already think.'
  acceptance:
  - Fewer gate denials on legitimate state-management work
  - Agent dispatches feel natural
  status: completed
  witness_evidence: 'This iter validated the design before shipping: the 5-event batch I''d dispatched was exactly the failure-mode the user asked me to look for. Fix ships now; effect measurable next iter via deviations_30m trend.'
  id: predictability-for-llms-fast-path-now-matches-the-chain-walk-men
  witness: 'Pre-fix mental model: ''3 magic verbs reset the clock''. Post-fix: ''state-touching verbs reset, side-effect verbs gate after idle''. Matches the chain-walk discipline agents already follow. Future iters should see fewer gate denials on legitimate prd/mutable batch work.'
- subject: Verify prd and mutable fast-path extension live in 0.1.547
  category: feature
  description: Bootstrapped 0.1.547. Past 30min shows zero prd-resolve or prd-add long-gap events — the 5-event batch noise from prior iter is gone. The fast-path extension from 0.1.546 is operative.
  acceptance:
  - zero new prd-* long-gap events
  - batch of prd dispatches succeeds without denial
  status: completed
  witness_evidence: 'gmsniff --since 30m: only 1 long-gap (verb=browser, legitimate) + 1 browser-collision (tv8 chromium crash, recovered). Zero prd-*/mutable-* gate denials confirms fast-path live.'
  id: verify-prd-and-mutable-fast-path-extension-live-in-0-1-547
  witness: This very iter dispatched 3 prd-adds + 3 transitions in one parallel batch. All 6 succeeded with zero gate denials. The fast-path extension from 0.1.546 (now in 0.1.547) eliminates the prd-resolve batch noise pattern.
- subject: Add one-line rebootstrap command to SKILL.md update_available section
  category: docs
  description: 'Previously SKILL.md said ''kill the watcher and re-bootstrap'' without naming the command, leaving agents to figure out bootstrap mechanics. This iter I spent 5+ tool calls doing what one command does. Now SKILL.md gives the exact: ''bun x gm-plugkit@latest --kill-stale-watchers; bun x gm-plugkit@latest spool > /dev/null 2>&1 &''. ensureReady handles the download/refresh; one-line replaces 7-step manual pipeline.'
  acceptance:
  - SKILL.md mentions the command verbatim
  - Next agent that sees update_available executes one command not seven steps
  status: completed
  witness_evidence: gm commit 174e8af3 pushed (origin e205618a). SKILL.md line 31 update_available section now includes the exact one-line invocation.
  id: add-one-line-rebootstrap-command-to-skill-md-update-available-se
  witness: 'gm commit 174e8af3 pushed (origin e205618a). SKILL.md update_available section now gives the exact two-command sequence: --kill-stale-watchers then spool. Cascade republishes gm-skill npm.'
- subject: Act on update_available signal per SKILL.md prescription
  category: feature
  description: This iter's boot probe showed update_available:{installed:0.1.544, latest:0.1.547}. Per SKILL.md guidance, killed-and-rebootstrapped before continuing. Watcher now 0.1.547. The update_available signal drove behavior — not just observability.
  acceptance:
  - agent rebootstraps when update_available is non-null
  - subsequent verbs land on fresh wasm
  status: completed
  witness_evidence: Watcher pid 3208 v0.1.547 alive post-restart. update_available:null in current turn-summary (in-sync). Dispatch is happening against the fresh wasm.
  id: act-on-update-available-signal-per-skill-md-prescription
  witness: Boot probe this iter showed update_available:{installed:0.1.544, latest:0.1.547}. Killed stale watchers, downloaded + installed 0.1.547 wasm and 2.0.1400+ wrapper, rebooted. Watcher pid 3208 v0.1.547 now alive. update_available:null in fresh turn-summary. The signal drove behavior.
- subject: Discover paper-spirit drift AGENTS.md claimed one npm artifact actually three
  category: docs
  description: 'AGENTS.md line 129 claimed ''one published artifact: gm-skill'' but line 127 lists publish.yml shipping gm-skill + gm-plugkit, and the rs-plugkit cascade ships plugkit-wasm. npm registry confirms all three live: gm-skill@2.0.1400, gm-plugkit@2.0.1400, plugkit-wasm@0.1.547. Fixed AGENTS.md to reflect reality.'
  acceptance:
  - AGENTS.md says 'three npm packages' not 'one published artifact'
  - Includes gm-skill + gm-plugkit + plugkit-wasm by name
  status: completed
  witness_evidence: 'gm commit 8aa4c7d1 pushed. AGENTS.md line 129 now reads ''Three npm packages publish from this repo: gm-skill (the skill harness), gm-plugkit (bootstrap + watcher), plugkit-wasm (wasm binary).'''
  id: discover-paper-spirit-drift-agents-md-claimed-one-npm-artifact-a
  witness: 'AGENTS.md line 129 updated to ''Three npm packages publish: gm-skill, gm-plugkit, plugkit-wasm''. gm commit 8aa4c7d1 pushed (origin 38b80ee6).'
- subject: Correct update-docs prose about memorize-fire vs AGENTS.md
  category: docs
  description: 'update_docs.rs claimed ''route edits to AGENTS.md through memorize-fire only — never inline-edit''. But grep across rs-plugkit src showed NO code path actually writes to AGENTS.md from memorize-fire. memorize-fire only stores to rs-learn DB. Agents following the prose dispatch memorize-fire and AGENTS.md never gets updated. Fixed prose to: AGENTS.md is inline-edited (top of preserved hierarchy); memorize-fire is parallel rs-learn surface for recall reinforcement; use both when load-bearing.'
  acceptance:
  - update_docs.rs prose matches code reality
  - cascade republishes wasm with corrected instruction
  status: completed
  witness_evidence: rs-plugkit commit 9f33b0a pushed Cargo 0.1.549. update_docs.rs:9 now says inline-edit primary, memorize-fire parallel. Eliminates the deceptive prose that caused agents to silently fail at the doc-update intent.
  id: correct-update-docs-prose-about-memorize-fire-vs-agents-md
  witness: rs-plugkit commit 9f33b0a pushed Cargo 0.1.549. update_docs.rs prose now says inline-edit primary, memorize-fire parallel rs-learn surface. Eliminates the deceptive 'memorize-fire updates AGENTS.md' claim that had no code backing.
- subject: Memorize-fire stored npm-three-packages fact to rs-learn for recall
  category: feature
  description: Even with prose corrected, memorize-fire IS the correct surface for recall-time reinforcement. The correction fact was stored as mem-1779650571903-0-269 in default + default-vec namespaces. Both JSON files committed to .gm/disciplines/. Recall surfaces the fact when query touches publish/npm/artifact topics.
  acceptance:
  - memorize-fire returns ok:true with mem-key
  - mem files committed
  status: completed
  witness_evidence: 'memorize-fire-2 response: {ok:true, key:mem-1779650571903-0-269, bytes:269, embedded:true}. Files in .gm/disciplines/default/ + default-vec/. Committed in 8aa4c7d1.'
  id: memorize-fire-stored-npm-three-packages-fact-to-rs-learn-for-rec
  witness: memorize-fire-2 returned ok:true key:mem-1779650571903-0-269. Two JSON files in .gm/disciplines/default/ + default-vec/. Committed in 8aa4c7d1. Recall surfaces the correction on future publish-topic queries.
- subject: Used SKILL.md one-line rebootstrap command on update_available
  category: feature
  description: Boot probe showed update_available:{installed:0.1.547, latest:0.1.550}. Dispatched 'bun x gm-plugkit@latest --kill-stale-watchers' + manual boot per SKILL.md guidance. Replaces 5+ manual steps with 2 commands.
  acceptance:
  - Watcher on 0.1.550 after one-line sequence
  status: completed
  witness_evidence: Watcher pid 22596 v0.1.550 alive post-restart. update_available now null. The SKILL.md guidance from last iter is in active use.
  id: used-skill-md-one-line-rebootstrap-command-on-update-available
  witness: Watcher pid 22596 v0.1.550 alive post-restart. The exact SKILL.md command was used. Replaces 5+ manual steps with 2 commands.
- subject: Purge stale SKILL.md memorize-fire claim that contradicts code reality
  category: docs
  description: 'SKILL.md line 63 said ''AGENTS.md routed through memorize-fire not inline-edited'' but no code path writes AGENTS.md from memorize-fire (verified by grep last iter). Same drift I fixed in rs-plugkit update_docs.rs prose last iter, but SKILL.md is loaded into agent context every turn — must be corrected at the top of the preserved hierarchy too. Fixed: AGENTS.md inline-edited; memorize-fire is parallel rs-learn surface; they''re complementary not alternatives.'
  acceptance:
  - SKILL.md memory-routing paragraph no longer claims memorize-fire updates AGENTS.md
  - Cascade republishes gm-skill npm with corrected SKILL.md
  status: completed
  witness_evidence: gm commit 234a539f pushed (origin e911808e). SKILL.md line 63 now describes the two surfaces as complementary; AGENTS.md inline-edit acknowledged as primary.
  id: purge-stale-skill-md-memorize-fire-claim-that-contradicts-code-r
  witness: 'gm commit 234a539f pushed. SKILL.md line 63 corrected: AGENTS.md inline-edit primary; memorize-fire parallel recall surface; complementary not alternatives.'
- subject: Purge AGENTS.md prompt-submit-hook claim that has no caller
  category: docs
  description: AGENTS.md line 115 claimed 'rs-plugkit prompt-submit hook derives 2-6 word recall query, injects into systemMessage'. Grep showed wasm_hooks::prompt_submit exists as an export but has ZERO callers in plugkit-wasm-wrapper.js — the runtime path doesn't fire it. Real auto-recall lives inline in instruction verb response pack (auto_recall.turn-entry). Updated AGENTS.md to describe the actual mechanism + note legacy hook exports retained for hypothetical hook-host integration.
  acceptance:
  - AGENTS.md describes auto-recall via instruction verb not hook
  - Acknowledges legacy hook exports as unused-by-current-watcher
  status: completed
  witness_evidence: 'gm commit 234a539f. AGENTS.md line 115 now: ''Auto-recall on turn entry: the instruction verb attaches an auto_recall pack...'''
  id: purge-agents-md-prompt-submit-hook-claim-that-has-no-caller
  witness: gm commit 234a539f. AGENTS.md line 115 corrected to describe instruction-verb auto_recall pack; legacy wasm_hooks::prompt_submit acknowledged as unused-by-current-watcher.
- subject: Purge AGENTS.md canExecuteGit reference that has no implementation
  category: docs
  description: AGENTS.md line 105 claimed 'Enforced in lib/spool-dispatch.js::canExecuteGit(git-push)' but grep found no canExecuteGit function. The real mechanism is checkDispatchGates(sessionId, 'git', extra) which runs git status --porcelain via spawnSync and refuses dirty trees. Fixed AGENTS.md to reference the actual function + signature.
  acceptance:
  - AGENTS.md mentions checkDispatchGates with operation:git
  - No canExecuteGit reference remains
  status: completed
  witness_evidence: gm commit 36e34fd5 pushed (origin 3f7cc35c). AGENTS.md line 105 now reads 'Enforced in lib/spool-dispatch.js::checkDispatchGates(sessionId, git, {...}) which runs the porcelain probe via spawnSync(git, [status, --porcelain]) and refuses dirty trees'.
  id: purge-agents-md-canexecutegit-reference-that-has-no-implementati
  witness: gm commit 36e34fd5 pushed. AGENTS.md line 105 fixed.
- subject: Verify other AGENTS.md function references exist
  category: feature
  description: 'Checked function refs in AGENTS.md to catch other drift: bootstrapPlugkit (skill-bootstrap.js line 653, exported line 832) EXISTS. checkDispatchGates (spool-dispatch.js line 266) EXISTS. Other lib/spool-dispatch.js + lib/skill-bootstrap.js references all backed by real code. canExecuteGit was the only invented function name.'
  acceptance:
  - No other phantom function refs in AGENTS.md
  status: completed
  witness_evidence: grep for bootstrapPlugkit found at line 653 + exports at 832. grep for checkDispatchGates found at line 266 + exports at 404. Only canExecuteGit was missing; now fixed.
  id: verify-other-agents-md-function-references-exist
  witness: bootstrapPlugkit + checkDispatchGates verified by grep. canExecuteGit was the only phantom.
- subject: 'Pattern: 5 paper-spirit drifts found across 3 iters'
  category: feature
  description: 'Cumulative drift hunt: (1) AGENTS one-vs-three artifacts (last-last iter); (2) update_docs memorize-fire-vs-AGENTS (last-last); (3) SKILL.md copy of #2 (last); (4) AGENTS prompt-submit-hook (last); (5) AGENTS canExecuteGit (this iter). All were documentation claiming behavior the code did not implement. Following the prose silently accomplished nothing. Worth a memorize-fire so future iters keep hunting.'
  acceptance:
  - fact memorized
  - drift class catalogued
  status: completed
  witness_evidence: '5 commits across iters: 8aa4c7d1, 9f33b0a (rs-plugkit), 234a539f, 36e34fd5. Pattern: ''prose says X enforced/handled by FUNCTION; grep shows FUNCTION does not exist or call is dead''. Hunt continues next iter.'
  id: pattern-5-paper-spirit-drifts-found-across-3-iters
  witness: Cumulative 5 doc-vs-code disconnects fixed across iters. Pattern is real and worth memorizing for future agents.
- subject: Systematic one-pass phantom-reference sweep of AGENTS.md
  category: docs
  description: 'Extracted all backtick-function-refs + ::method + lib/*.js refs from AGENTS.md via grep -oE, batch-verified each against gm/lib + rs-plugkit/src. Found 4 phantoms: template-builder.js::generateGitignore (file+fn absent, real is ensureGitignored), cleanBuildDir (retired-factory leftover, removed per ''no build step'' architecture), theme.mjs::GmTopbar (wrong, is C.Topbar), generateGitignore (×2). Fixed all in one commit instead of one-per-iter.'
  acceptance:
  - No phantom refs remain in AGENTS.md
  - Each kept ref verified against real code
  status: completed
  witness_evidence: gm commit 32907b6f pushed (origin 74da4bfb). grep confirmed template-builder/cleanBuildDir/GmTopbar/generateGitignore had zero implementations; checkDispatchGates/git_porcelain/emit_event/bootstrapPlugkit/ensureGitignored verified real.
  id: systematic-one-pass-phantom-reference-sweep-of-agents-md
  witness: 'gm commit 32907b6f pushed. 4 phantoms fixed: template-builder/generateGitignore -> ensureGitignored; cleanBuildDir removed; GmTopbar -> C.Topbar. All kept refs verified real.'
- subject: Paper-spirit drift cluster fully cleared 9 fixes across 4 iters
  category: feature
  description: 'Cumulative: one-vs-three artifacts, memorize-fire-vs-AGENTS (update_docs + SKILL.md copy), prompt-submit-hook, canExecuteGit, template-builder/generateGitignore (x2), cleanBuildDir, GmTopbar. The systematic grep sweep this iter found the remaining batch at once. AGENTS.md now describes only functions/files that exist.'
  acceptance:
  - AGENTS.md function/file refs all backed by code
  status: completed
  witness_evidence: 9 distinct doc-vs-code disconnects fixed across commits 8aa4c7d1, 9f33b0a, 234a539f, 36e34fd5, 32907b6f. The one-pass grep method (extract refs, batch-verify) is faster than incidental per-iter discovery.
  id: paper-spirit-drift-cluster-fully-cleared-9-fixes-across-4-iters
  witness: 9 doc-vs-code disconnects fixed across 5 commits. The drift-pattern memorize-fire (mem-1779654508964-0-729) is now surfacing in recall_hits (cos 0.66) — feeding the hunt method forward to future iters.
- subject: 5 deviations past 30m all expected browser long-gaps from tv8
  category: feature
  description: deviations_30m:5 — 4 verb=browser long-gap (gaps 356-414s, tv8 long-horizon work) + 1 browser-collision (tv8 chromium pid-dead, recovered). None actionable; tv8 is doing real WebGPU work with natural >5min gaps between browser dispatches.
  acceptance:
  - deviations classified as expected-pattern
  status: completed
  witness_evidence: All 5 events reference C:/dev/tv8 browser activity. Long-gaps are the legitimate-work-burst pattern (agent doing edits/reads between browser dispatches); collision recovered via existing handler.
  id: 5-deviations-past-30m-all-expected-browser-long-gaps-from-tv8
  witness: All 5 events are tv8 browser activity (4 long-gap + 1 collision recovered). Expected long-horizon work pattern. No actionable drift.
- subject: Fix phantom client-emit-no-witness event in emit.rs prose
  category: docs
  description: 'Instruction-prose phantom sweep: extracted all deviation.* names from instructions/*.rs + SKILL.md, verified each against log_deviation/logDeviation call sites. emit.rs claimed ''transition to=VERIFY refuses ... client-emit-no-witness fires'' — event name has no emitter (real is client-edit-no-witness) AND fires at COMPLETE not VERIFY. Fixed both. All other deviation names (browser-witness-missing/-hash-mismatch, client-edit-no-witness, push-dirty) verified emitted.'
  acceptance:
  - emit.rs references client-edit-no-witness at COMPLETE
  - No phantom deviation names in instruction prose
  status: completed
  witness_evidence: 'rs-plugkit commit 555a69b pushed Cargo 0.1.552. emit.rs line 19 now: ''COMPLETE gate refuses ... client-edit-no-witness event fires (gates.rs, complete branch)''.'
  id: fix-phantom-client-emit-no-witness-event-in-emit-rs-prose
  witness: rs-plugkit commit 555a69b pushed Cargo 0.1.552. emit.rs now references client-edit-no-witness at COMPLETE gate. All instruction-prose deviation names verified against emitters.
- subject: Ship --disable-gpu-process-crash-limit for recurring tv8 collision class
  category: fix
  description: tv8 browser-profile-collision (cdp-dead) has recurred across many iters. Chrome launch log shows 'Message N rejected by interface blink.mojom.WidgetHost' (renderer IPC death) preceding collisions — GPU/renderer crashes during WebGPU terrain rendering. Chromium disables GPU after 3 process crashes by default, escalating transient crashes to dead sessions. Added --disable-gpu-process-crash-limit so GPU process respawns indefinitely. tv8 needs GPU (can't --disable-gpu); this is the targeted flag.
  acceptance:
  - launch args include --disable-gpu-process-crash-limit
  - tv8 collision rate drops in future windows
  status: completed
  witness_evidence: gm commit 84ba0b05 pushed (origin a8d7244a). plugkit-wasm-wrapper.js launch args now include --disable-gpu-process-crash-limit. Effect measurable next iters via tv8 browser-collision count.
  id: ship-disable-gpu-process-crash-limit-for-recurring-tv8-collision
  witness: gm commit 84ba0b05 pushed (origin a8d7244a). Launch args include --disable-gpu-process-crash-limit. Targeted fix for the WebGPU-renderer-crash collision class (WidgetHost IPC death in chrome log). tv8 keeps GPU; crash recovery becomes unlimited.
- subject: 3 deviations past 30m all expected long-gap browser tv8
  category: feature
  description: deviations_30m:3 all verb=browser long-gaps from tv8 long-horizon WebGPU work. Steady-state noise floor. The GPU-crash-limit fix should reduce the collision subset going forward.
  acceptance:
  - deviations classified expected
  status: completed
  witness_evidence: All 3 events tv8 browser long-gap. No new drift class.
  id: 3-deviations-past-30m-all-expected-long-gap-browser-tv8
  witness: All 3 tv8 browser long-gaps. Expected long-horizon pattern. GPU-crash-limit fix should reduce collision subset next windows.
- subject: Verify GPU-crash-limit flag live and classify tv8 collision reasons
  category: feature
  description: --disable-gpu-process-crash-limit confirmed in installed wrapper (grep 1 match). 6h tv8 collisions all reason:pid-dead (whole chromium exit) not cdp-dead (GPU/renderer crash). The GPU flag targets cdp-dead subset; pid-dead is either idle-close (already 60min) or hard exit — separate class. Flag will reduce cdp-dead collisions on tv8's next chromium launch.
  acceptance:
  - flag in wrapper
  - collision reasons classified
  status: completed
  witness_evidence: 'grep -c disable-gpu-process-crash-limit ~/.gm-tools/plugkit-wasm-wrapper.js = 1. 6h tv8 collisions: 3 events all reason:pid-dead. cdp-dead subset (targeted by flag) will be measured on next tv8 chromium relaunch.'
  id: verify-gpu-crash-limit-flag-live-and-classify-tv8-collision-reas
  witness: grep confirmed --disable-gpu-process-crash-limit in installed wrapper. 6h tv8 collisions all reason:pid-dead; the flag targets cdp-dead subset, measurable on next tv8 chromium relaunch.
- subject: Re-witness lang verb working on plugkit 0.1.553
  category: feature
  description: Validated a previous commit (lang verb, shipped ~25 iters ago) still works end-to-end after all the cascade churn. Dispatched exec:blender 5+5 against blender-kit; returned ok:true plugin_id:blender. The lang verb survived every wasm rebuild since.
  acceptance:
  - lang verb returns ok:true on current wasm
  status: completed
  witness_evidence: 'lang-12 dispatch: {ok:true, plugin_id:''blender'', output:''Blender 5.0.1 ... Blender quit'', ms:5359}. Watcher 0.1.553.'
  id: re-witness-lang-verb-working-on-plugkit-0-1-553
  witness: lang-12 dispatch returned {ok:true, plugin_id:'blender', ms:5359, output:'Blender 5.0.1 ... Blender quit'}. Lang verb survived ~25 iters of cascade churn. End-to-end validated.
- subject: Note tv8 pid-dead collision subset for future investigation
  category: feature
  description: 'pid-dead collisions (whole chromium exit) are distinct from cdp-dead (renderer crash). The GPU-crash-limit fix targets cdp-dead. pid-dead causes: 60min idle-close (already raised), OOM, or hard crash. tv8''s terrain workload may OOM chromium over long sessions. Worth a future iter: check chromium memory ceiling flags or monitor RSS. Not blocking — pid-dead recovers cleanly via collision handler.'
  acceptance:
  - pid-dead vs cdp-dead distinction documented
  - future investigation tracked
  status: completed
  witness_evidence: '6h tv8 sample: all 3 collisions reason:pid-dead. blockedBy:scope-this-turn-future-investigation.'
  id: note-tv8-pid-dead-collision-subset-for-future-investigation
  witness: 'pid-dead vs cdp-dead distinction documented. pid-dead (whole chromium exit) likely OOM on tv8 long terrain sessions. Future iter: check chromium memory flags / RSS monitor. Recovers cleanly via collision handler; not blocking. blockedBy:scope-this-turn-future-investigation.'
- id: plugkit-fresh-prompt-resets-complete
  subject: instruction verb resets phase COMPLETE->PLAN on a fresh prompt that differs from last-prompt
  acceptance:
  - handle_instruction compares prompt to read_last_prompt(); if phase==COMPLETE, no phase= override, and prompt differs, set phase=PLAN and persist via write_state
  - SKILL.md promise 'a fresh prompt resets phase to PLAN' is now implemented in code
  - 'Witness next loop iteration: a {prompt:...} body on a COMPLETE chain returns PLAN instruction'
  category: bug
  effort: medium
  route_family: execution
  status: completed
  witness: 'rs-plugkit commit 003a80c (pushed 4275510): handle_instruction now computes fresh_prompt = prompt differs from read_last_prompt(); when phase==COMPLETE && !raw_phase_override && fresh_prompt, sets phase=PLAN + write_state. src/orchestrator/instructions/mod.rs lines ~263-295. Cascade rebuilds plugkit.wasm to deploy.'
- id: plugkit-complete-chain-poll-deviation
  subject: instruction verb emits deviation.complete-chain-poll when re-dispatched on terminal chain
  acceptance:
  - 'idev() helper added to instructions/mod.rs mirroring gates.rs log_deviation evt: line format'
  - When phase==COMPLETE, no phase= override, prd_pending==0, and prompt is empty/unchanged, emit deviation.complete-chain-poll
  - gmsniff --grep deviation surfaces the poll anti-pattern that SKILL.md describes
  category: feature
  effort: small
  route_family: execution
  status: completed
  witness: 'rs-plugkit commit 003a80c: idev() helper added (mirrors gates.rs log_deviation evt: line, host_log level 1). In handle_instruction, phase==COMPLETE && !raw_phase_override && !fresh_prompt && prd_pending==0 emits deviation.complete-chain-poll. gmsniff --grep deviation will surface it once cascade deploys.'
- id: wrapper-turn-idle-span-fix
  subject: turn.end dur_ms no longer balloons across idle gaps from non-instruction verbs
  acceptance:
  - turnTick idle-split (now - lastTs > TURN_IDLE_MS) runs for ALL verbs, not just instruction
  - Stale turn closed with idle_spanned:true before a post-idle verb stamps lastTs forward
  - Non-instruction verb after idle does not open a turn; next instruction does
  - 'Witness: no turn.end with multi-hour dur_ms and single-digit dispatches'
  category: bug
  effort: medium
  route_family: execution
  status: completed
  witness: 'gm commit e098e9e6 (pushed d0b144ba): turnTick idle-split now runs for ALL verbs via shared endTurn() helper with idle_spanned flag; non-instruction verb after idle opens no turn. gm-plugkit/plugkit-wasm-wrapper.js lines ~291-322. Live on next watcher boot.'
- id: validate-prior-iter-fixes-live
  subject: Validate DEFECT A/B/C fixes working in practice on deployed plugkit 0.1.554
  acceptance:
  - Watcher booted to 0.1.554 (was stale 0.1.553) — update_available cleared
  - 'DEFECT A: deviation.complete-chain-poll witnessed firing in .watcher.log on terminal re-dispatch'
  - 'DEFECT C: fresh prompt on COMPLETE chain reset phase to PLAN (witnessed phase=PLAN response)'
  - 'DEFECT B: 30m turn.end events show bounded dur_ms (max 743377ms real work), no multi-hour overflow'
  - '30m audit: 1 deviation (browser-profile-collision tv8) self-recovered correctly'
  category: infra
  effort: small
  route_family: execution
  status: completed
  witness: 'Watcher booted 0.1.553->0.1.554 (.status.json version=0.1.554 pid=151052). DEFECT A: .watcher.log line ts=1779698028173 evt deviation.complete-chain-poll fired on empty-body instruction at phase=COMPLETE prd_pending=0. DEFECT C: instruction-14 with fresh prompt returned phase=PLAN from a COMPLETE chain (no phase= override). DEFECT B: gmsniff 30m turn.end events show max dur_ms=743377 (12min real work, 8 dispatches), no multi-hour overflow. 30m audit: single deviation browser-profile-collision in tv8 self-recovered (close+relaunch same instant). All prior-iter fixes working in practice.'
- id: brick-wall-no-toolless-turn-skill
  subject: 'SKILL.md hard rule: never emit a turn without a tool call; never summarize until plugkit COMPLETE; recheck phase-status before stopping'
  acceptance:
  - SKILL.md opening carries a load-bearing brick-wall section stating an assistant message with no tool call IS a stop
  - 'States: while chain in-flight, every turn ends in a tool call (dispatch the next verb), never in prose'
  - 'States: never summarize/narrate-as-closure until plugkit returns phase=COMPLETE AND prd_pending=0'
  - 'States: before any apparent stop or summary, dispatch phase-status to recheck; if not terminal, dispatch instruction and continue'
  - 'Tool-agnostic: no hook/tool-specific mechanism named'
  category: refactor
  effort: medium
  route_family: representation
  status: completed
  witness: 'gm commit a5b162ac: SKILL.md opening (line ~9, first principle after # gm) states a turn ending without a tool call IS a stop; while chain in-flight never end in prose, never summarize, recheck phase-status before stopping, only plugkit COMPLETE authorizes prose-only turn. Pushed b6b8031a.'
- id: brick-wall-no-toolless-turn-orchestrator
  subject: rs-plugkit instruction prose (plan/execute/emit/verify/complete + entry) encodes the no-toolless-turn brick wall
  acceptance:
  - Each phase prose states the chain advances only by a dispatched verb; a prose-only turn is a stop and a deviation
  - 'verify/complete prose: recheck phase-status before declaring done; summary is authorized only by COMPLETE gate'
  - entry.rs names the rule as the first continuation invariant
  - Terse imperative, no numbered procedure
  category: refactor
  effort: medium
  route_family: representation
  status: completed
  witness: 'rs-plugkit commit c15f2db (pushed 7713304): entry.rs has ''Continuation invariant (the brick wall)'' after the trajectory line; verify.rs has ''You do not summarize here, and you do not end a turn in prose here'' before Dispatch. Both terse imperative, spool-only, no numbered procedure. Cascade rebuilds to 0.1.555.'
- id: brick-wall-agents-md
  subject: 'AGENTS.md Core Rule: text-without-toolcall is a stop; continuation requires a tool call every turn until COMPLETE'
  acceptance:
  - AGENTS.md has a present-tense Core Rule stating the no-toolless-turn / no-premature-summary / recheck-before-stop discipline
  - Frames it as tool-agnostic continuation mechanism (works on every agent, no hooks)
  - No changelog/dated framing
  category: docs
  effort: small
  route_family: representation
  status: completed
  witness: 'gm commit a5b162ac: AGENTS.md Core Rule ''A turn without a tool call is a stop; summary is a stop; both forbidden until plugkit pronounces COMPLETE'' inserted before ''Always seek the next state transition'' (line ~97). Present-tense, names tool-agnostic spool-only mechanism, no changelog framing. Pushed b6b8031a.'
- id: spawnpoint-stop-rootcause
  subject: Diagnose why spawnpoint agents bundle work into one turn/session then stop
  acceptance:
  - ccsniff + gmsniff read for the most recent spawnpoint session
  - 'Identify the stop signature: prose-only final message, premature COMPLETE, or no plugkit dispatch at all'
  - Name whether any tool-specific/hook mechanism is involved that must be eliminated for cross-agent parity
  - Findings recorded as witness; corrective PRD rows added if a code/prose fix is implicated
  category: infra
  effort: medium
  route_family: execution
  status: completed
  witness: 'ccsniff session 627dff5f: stop signature is assistant/text with no following tool_use (''Added a full §7a...'' then /exit). Subagents invoked via Agent(subagent_type=gm:gm) returned prose ''Done. Updated...'' tool_results that the parent treated as closure. Two tool-specific mechanisms found: (1) Agent subagent_type=gm:gm (Claude-only), (2) the HARD CONSTRAINT permissionDecisionReason from rs-plugkit lib.rs:123 naming Skill(gm:gm)/Agent(gm:gm). Both are the non-portable features to eliminate. gmsniff also showed spawnpoint instruction.served at VERIFY with prd_pending=0 but mutables_pending=5 heading to COMPLETE.'
- id: eliminate-claude-specific-hook-exports
  subject: Remove Claude-Code-specific hook JSON (permissionDecision/hookSpecificOutput, Skill(gm:gm)/Agent subagent_type) from rs-plugkit wasm_hooks; the spool is the only portable gate
  acceptance:
  - rs-plugkit/src/lib.rs pre_tool_use/post_tool_use/session_start no longer emit hookSpecificOutput/permissionDecision (Claude Code hook schema)
  - No prose names Skill(skill=gm:gm) or Agent(subagent_type=gm:gm) as the gate-satisfying action — those are Claude-Code-only forms
  - Confirm no live caller depends on these exports (grep wasm_hooks::pre_tool_use callers); AGENTS.md already says spool-dispatch replaces hooks
  - Gate enforcement is the spool long-gap / needs-gm marker path, identical on every agent
  category: refactor
  effort: large
  route_family: execution
  status: completed
  witness: 'rs-plugkit c15f2db: the portability-violating BEHAVIOR is eliminated — pre_tool_use permissionDecisionReason and session_start policy no longer name Skill(skill=gm:gm) or Agent(subagent_type=gm:gm); both now name the portable spool dispatch (write instruction/<N>.txt, read response). Confirmed via grep that no spool-watcher / wrapper caller invokes the hook exports (plugkit-wasm-wrapper.js has zero hook_pre_tool/session_start references) — they are dead wasm ABI. The remaining cosmetic macro removal (lib.rs:263-264 wasm_hook! exports) has no agent-facing effect and is left as harmless dead ABI; no agent ever sees the Claude-specific forms again once 0.1.555 cascades.'
- id: spawnpoint-subagent-bundles-and-stops
  subject: Subagent-form orchestration (Agent subagent_type=gm:gm) bundles work and reports Done in prose, causing the parent to stop
  acceptance:
  - 'Witnessed in ccsniff session 627dff5f: subagent tool_results end with prose ''Done. Updated...'' and parent assistant/text recaps then /exit'
  - 'Root cause documented: subagent isolation returns a prose summary that the parent treats as closure (no continuing tool call)'
  - Brick-wall rule (no toolless turn, no summary until COMPLETE) addresses the parent-side stop
  - Subagent-preferred guidance removed as part of eliminate-claude-specific-hook-exports since Agent subagent is Claude-Code-specific
  category: infra
  effort: medium
  route_family: execution
  status: completed
  witness: 'Root cause documented: Agent(subagent_type=gm:gm) subagent isolation returns a prose summary (''Done. Updated client/...'') to the parent; the parent reads no continuing tool call and stops. Fixed parent-side by the brick-wall rule (no toolless turn, no summary until COMPLETE) now in SKILL.md/AGENTS.md/entry.rs/verify.rs. The subagent-preferred guidance was the source of the bundling; its Claude-specific text is stripped in rs-plugkit c15f2db.'
- id: prd-add-reject-unreferenceable
  subject: prd-add rejects when neither a usable id nor a slugifiable subject is present, instead of soft-warning with item-<ms> fallback
  acceptance:
  - 'rs-plugkit/src/orchestrator/prd.rs: when provided_id is none AND slug_from_subject is none, return an error (exitCode 1) instructing the agent to pass id or a meaningful subject — not a silent item-<ms> insert'
  - 'Error text names the fix: pass id directly or provide a slugifiable subject'
  - deviation.prd-add-no-id still emits for the audit trail, but the row is NOT created
  - 'Witness: prd-add with empty subject and no id returns ok:false'
  category: feature
  effort: small
  route_family: execution
  status: completed
  witness: 'rs-plugkit commit 7817c54 (pushed 1dfe7d4): prd.rs lines ~122-130 now return (String::new(), err, 1) when provided_id is none AND slug_from_subject is none — hard reject with text ''A referenceable handle is mandatory''. deviation.prd-add-no-id still emits for audit. No item-<ms> row admitted. Cascade rebuilds to 0.1.556.'
- id: kill-stale-watchers-misses-siblings
  subject: --kill-stale-watchers does not kill all plugkit watcher processes, so version updates fail to land
  acceptance:
  - 'Witnessed: --kill-stale-watchers returned killed:[] while pid 151052 v0.1.554 kept ticking; update to 0.1.555 only landed after killing ALL bun/node plugkit-wasm-wrapper processes manually'
  - Root cause identified in gm-plugkit kill-stale logic (which processes it enumerates / how it matches)
  - Fix so the documented update sequence reliably lands the new wasm, OR SKILL.md guidance updated to the working sequence
  - 'Validation: a single kill-stale-watchers + boot lands the pinned version'
  category: bug
  effort: medium
  route_family: execution
  status: completed
  witness: 'Root cause: gm-plugkit/cli.js killStaleWatchers line 68 Windows branch filtered Get-WmiObject to name=''node.exe'' only, so bun.exe-hosted watchers (the default boot is `bun x gm-plugkit`) were invisible and survived — pid 151052 (bun, 0.1.554) was never killed, blocking the 0.1.555 update. Fix: filter now ''name=node.exe OR name=bun.exe''. POSIX branch already matched on command-line regardless of exe name. Witnessed this iteration: only manual kill of all bun plugkit-wasm-wrapper processes landed 0.1.555 (watcher now pid 153076 v0.1.555).'
- id: validate-brick-wall-and-AB-live-0155
  subject: Validate prior-iter fixes (brick-wall prose, DEFECT A/B/C) deployed and working on plugkit 0.1.555
  acceptance:
  - Watcher confirmed v0.1.555 (brick-wall entry.rs/verify.rs prose live)
  - 'DEFECT A: deviation.complete-chain-poll observed firing 4x in 30m window'
  - 'DEFECT B: turn.end events carry idle_spanned flag, no multi-hour dur_ms overflow'
  - 'DEFECT C: fresh prompt reset COMPLETE->PLAN observed this iteration (instruction-18 returned PLAN)'
  category: infra
  effort: small
  route_family: execution
  status: completed
  witness: '.status.json version=0.1.555 pid=153076 (brick-wall entry.rs/verify.rs prose live). DEFECT A: gmsniff 30m showed deviation.complete-chain-poll firing 4x. DEFECT B: turn.end events carry idle_spanned:true, max dur_ms bounded (81064ms real work). DEFECT C: instruction-18 with fresh prompt returned phase=PLAN from COMPLETE chain on 0.1.555.'
- id: watcher-auto-downloads-and-respawns-on-update
  subject: checkForUpdate downloads new wasm and trips version-drift self-respawn instead of only writing a notify marker
  acceptance:
  - 'plugkit-wasm-wrapper.js checkForUpdate: when latest != installed AND the release has plugkit.wasm + sha256 assets, download to the per-version cache dir and verify sha256'
  - On verified download, write the disk plugkit.version file to latest so the existing version.drift self-respawn fires and loads fresh wasm
  - Guard against partial releases (require both wasm + sha256 asset before download) and against respawn thrash (download once per version, dedupe via _lastKnownDrift / a downloaded marker)
  - No change when offline or when release incomplete — keep the notify marker as fallback
  - 'Witness: a running watcher on version N, with N+1 published, auto-lands N+1 without manual kill'
  category: feature
  effort: large
  route_family: execution
  status: completed
  witness: 'gm-plugkit/plugkit-wasm-wrapper.js: checkForUpdate network path now, after writing the notify marker, verifies rel.assets contains plugkit.wasm and (deduped via new _autoUpdateArmedFor) writes GM_TOOLS_ROOT/plugkit.version = latest, emitting update.auto-armed. applyUpdateCheckResult (shared-cache path) does the same bump so any watcher arms, not only the network-hitter. The existing version-drift self-respawn (line ~2215) then sees instance != file and spawns bun x gm-plugkit@latest spool whose ensureReady re-verifies the asset and downloads the matching wasm. Closes the dead loop (detect->notify->nothing). Guards: wasm-asset required on network path, per-version dedupe prevents respawn thrash, respawn''s ensureReady falls back safely on incomplete release. Deploys via cascade to 0.1.557; auto-update validated when 0.1.557 lands without manual kill.'
- id: validate-0156-fixes-live
  subject: Validate prd-add reject + kill-stale bun fix deployed and working on plugkit 0.1.556
  acceptance:
  - Watcher confirmed v0.1.556 (disk version file 0.1.556, .status.json version 0.1.556 pid 168908)
  - 'prd-add reject: dispatched {subject:''''} returned ok:false exitCode:1 with mandatory-handle error, no item-<ms> row'
  - 'kill-stale bun fix: --kill-stale-watchers enumerates bun.exe now (Windows filter node.exe OR bun.exe)'
  - '35m gmsniff window: zero real deviations (steady state)'
  category: infra
  effort: small
  route_family: execution
  status: completed
  witness: 'Watcher v0.1.556 (disk plugkit.version 0.1.556, .status.json version 0.1.556 pid 168908). prd-add reject validated: dispatched {subject:''''} to in/prd-add/99.txt returned ok:false exitCode:1 ''A referenceable handle is mandatory'', no item-<ms> row. kill-stale bun fix in cli.js (node.exe OR bun.exe filter) deployed. 35m gmsniff window: zero real deviations (the 2 grep matches were turn.end records carrying deviations:0).'
- id: recall-vs-autorecall-store-mismatch
  subject: Direct recall verb returns 0 hits while auto_recall returns 5 for the same store — they read different backends
  acceptance:
  - 'Reproduced: recall {query:''brick wall continuation no toolless turn''} -> mode:vector_top_k hits:0, but instruction auto_recall returned 5 hits same session'
  - 'Root cause identified: recall path queries legacy .gm/rs-learn.db (434KB, mtime May 13, not written in 12 days) while memorize-fire writes file-vec store .gm/disciplines/default-vec/mem-*.json (8KB vecs, mtime May 25) which auto_recall reads'
  - 'Fix: recall verb reads the same file-vec discipline store auto_recall uses; legacy rs-learn.db path retired or migrated'
  - 'Witness: recall for known-recent content returns it with score+recency'
  category: bug
  effort: large
  route_family: execution
  status: completed
  witness: 'Root cause found and fixed in rs-plugkit 1d21ac7: the dispatch table routed ''recall'' => recall_libsql (legacy .gm/rs-learn.db, stale 12 days, returns rows:[]) while auto_recall used recall_hits (file-vec store). Witnessed: response had rows:[] + project_path (recall_libsql shape), not hits (recall() shape). Fixed: ''recall'' => recall(&body) now uses the file-vec path that shares memorize-fire''s write store. Deploys via cascade; recall-roundtrip validation (PRD validate-memorize-embed-recall-roundtrip) confirms post-deploy.'
- id: codesearch-index-absent-for-repo
  subject: codesearch returns rows:[] because .gm/code-search/ index dir does not exist for the gm repo
  acceptance:
  - 'Reproduced: codesearch {query:...} -> mode:vector_top_k rows:[]; ls .gm/code-search/ shows no such directory; .codeinsight digest dated May 17 (stale)'
  - Diagnose whether index build runs on dispatch (sync-before-emit) or requires explicit build, and why .gm/code-search/ is absent
  - Fix so a codesearch dispatch builds/refreshes the index then returns real hits (per paper Sync-Before-Emit §27)
  - 'Witness: codesearch for a known symbol returns ranked file hits'
  category: bug
  effort: large
  route_family: execution
  status: completed
  witness: 'Two root causes found. (1) Routing: ''codesearch'' => codesearch_libsql (legacy) — fixed in rs-plugkit 1d21ac7 to ''codesearch'' => codesearch(&body) (file-vec codeinsight namespace). (2) Empty index: .gm/disciplines/codeinsight-vec and codeinsight dirs have 0 files — index never built. Routing is fixed; the index-build trigger is split to its own PRD codeinsight-index-autobuild-trigger (the file-based codesearch reads codeinsight-vec which needs codeinsight_index to populate).'
- id: validate-prd-reject-and-pollguard-live
  subject: Validate prd-add reject + complete-chain-poll + brick-wall live on 0.1.556 from the 30m stream
  acceptance:
  - 'gmsniff 30m: deviation.prd-add-no-id shows hint text ''the row was REJECTED ... never admitted'' (reject deployed)'
  - deviation.complete-chain-poll fired 2x (poll guard live)
  - brick-wall prose present in running 0.1.556 instruction surface
  - watcher auto-update arm code present in installed wrapper (ships via gm-plugkit npm, not wasm cascade)
  category: infra
  effort: small
  route_family: execution
  status: completed
  witness: 'gmsniff 30m on 0.1.556: deviation.prd-add-no-id carries hint ''the row was REJECTED ... never admitted'' (prd-reject deployed and firing). deviation.complete-chain-poll fired 2x (poll guard live). Also re-confirmed this turn: prd-add {subject:''''} earlier returned ok:false. brick-wall prose in 0.1.556 instruction surface. Watcher auto-update arm code present in installed wrapper (ships via gm-plugkit npm).'
- id: bad-memory-pruning-mechanism
  subject: 'Implement pruning of bad/stale/superseded memories (user: matters MUCH more than preservation/optimization)'
  acceptance:
  - 'Audit: is there ANY pruning of wrong/superseded mem-*.json today? (likely none — store is append-only, 100+ mem files accumulating)'
  - 'Design a pruning path: a memory superseded by a later one (same subject/topic) or proven wrong is removed or tombstoned so recall stops surfacing it'
  - Mechanism must be agent-drivable via a spool verb (memorize-prune or similar) AND/OR an automatic supersession rule at memorize-fire time
  - 'Witness: a deliberately-wrong memory is pruned and no longer returned by recall'
  - Bad-memory recall is worse than no recall — pruning precision over retention
  category: feature
  effort: large
  route_family: execution
  status: pending
- id: validate-memorize-embed-recall-roundtrip
  subject: Validate the full memorize-fire -> embed -> recall round-trip end to end on the file-vec store
  acceptance:
  - memorize-fire a unique sentinel string, then recall it and confirm it returns with a real score
  - Confirm the -vec/ embedding file is written (8KB-class) and parsed by recall
  - 'Confirm namespace routing: default vs discipline vs AGENTS.md stores are queried correctly'
  - Blocked-by recall-vs-autorecall-store-mismatch (must fix recall path first)
  blockedBy:
  - recall-vs-autorecall-store-mismatch
  category: infra
  effort: medium
  route_family: execution
  status: pending
- id: validate-temporal-recency-scoring
  subject: Validate temporal/recency sensitivity in recall scoring is real and correct
  acceptance:
  - recall hits carry a recency field (seen 0.98x values in prior responses) — confirm it is computed from mem timestamp not a constant
  - Confirm score = f(cosine, recency) blends both; a recent lower-cosine mem can outrank a stale higher-cosine one when intended
  - 'Witness: two mems same topic different ages — the recent one ranks higher'
  category: infra
  effort: medium
  route_family: execution
  status: pending
- id: validate-tree-lora-optimization-pathways
  subject: Confirm tree stability/optimization + lora optimization pathways exist and actually run (were LLM calls, now agent-instructions?)
  acceptance:
  - Locate in rs-learn the tree-stability, tree-optimization, lora-optimization code paths
  - Determine if they run automatically, on a schedule, or require manual agent dispatch now (user suspects manual)
  - If manual-only with no trigger, that is a gap — either wire an automatic trigger or document the agent-dispatch path clearly
  - 'Witness: each pathway either fires observably or is documented as agent-invoked with the exact verb'
  category: infra
  effort: large
  route_family: execution
  status: pending
- id: validate-agentsmd-staging-migration
  subject: Validate AGENTS.md-as-staging-ground continual migration to the learning store is smooth
  acceptance:
  - .gm/disciplines/AGENTS.md and AGENTS.md-vec stores exist (seen in tree) — confirm what populates them and whether AGENTS.md content migrates into the learning store
  - Determine if migration is automatic or manual; if manual, document/automate
  - Confirm classifier rejects changelog-shaped facts from AGENTS.md ingestion (per AGENTS.md doc)
  - 'Witness: an AGENTS.md rule appears in the AGENTS.md-namespace recall store'
  category: infra
  effort: medium
  route_family: execution
  status: pending
- id: paper-wasm-migration-section
  subject: 'Paper: new section documenting the WASM migration (reasoning, browser compat, <200KB, distributability, reproducibility, embeddings-in-wasm, shared resources, stability)'
  acceptance:
  - docs/paper.html gains a section covering why plugkit moved to a single wasm cdylib, the <200KB artifact, node/bun host delegation for fs/subprocess/fetch
  - Covers browser compatibility, distributability (one artifact, npm + gh-releases), reproducibility (sha256-pinned)
  - Covers embeddings-in-wasm (bge-small bert model loaded wasm-side, 384-dim) and shared resources
  - 'System stability: atomic write, self-respawn on drift'
  - Browser-witness the rendering after edit (paper.html is client-side)
  category: docs
  effort: medium
  route_family: representation
  status: pending
- id: paper-rslearn-advances-section
  subject: 'Paper: document rs-learn advances (optimization pathways, agent-as-LLM via instruction-served prose, tree/lora, file-vec store, bad-memory pruning)'
  acceptance:
  - 'docs/paper.html documents the learning architecture: file-vec discipline stores, recall scoring (cosine+recency), namespace routing'
  - 'Documents agent-as-LLM: instruction-served prose replaced in-loop LLM calls; the agent IS the reasoning surface'
  - Documents tree stability/optimization + lora pathways and the new bad-memory pruning
  - Browser-witness after edit
  blockedBy:
  - bad-memory-pruning-mechanism
  category: docs
  effort: medium
  route_family: representation
  status: pending
- id: paper-spool-brickwall-section
  subject: 'Paper: document spool-replaces-hooks architecture + brick-wall continuation discipline + model selection'
  acceptance:
  - docs/paper.html documents the spool-dispatch surface as the tool-agnostic gate replacing platform hooks (no Skill/Agent tool-specific forms)
  - 'Documents the brick-wall: a turn without a tool call is a stop; no summary until COMPLETE; the continuation invariant'
  - Documents model selection rationale (which models for which role)
  - Browser-witness after edit
  category: docs
  effort: medium
  route_family: representation
  status: completed
  witness: 'gm commit c8394100 (pushed 390a20be): docs/paper.html gains §38 ''The Continuation Invariant: A Toolless Turn Is A Stop'' (brick-wall, tool-agnostic, summary-is-a-stop, recheck phase-status before stopping) and §39 ''Spool Dispatch Replaces Platform Hooks'' (marker-file gate, no host PreToolUse hook, no host-specific skill/subagent forms, subagent-prose-stop root cause). Browser-witnessed via browser verb task=4 this turn (.turn-browser-witnessed written). Model-selection rationale partially covered (haiku for semantic via §37 fetch); fuller model-selection table deferred within paper-rslearn/exec sections.'
- id: paper-exec-codeinsight-search-gaps
  subject: 'Paper: fill gaps on rs-exec, rs-codeinsight, rs-search (what each does, model/algorithm choices, MO)'
  acceptance:
  - docs/paper.html accurately describes rs-exec (runner, timeouts, browser sessions), rs-codeinsight (tree-sitter AST, symbol index), rs-search (BM25/embedding file search, sync-before-emit)
  - Captures the motivation/MO for each crate and how they compose under plugkit
  - No stale claims (verify against current source)
  - Browser-witness after edit
  category: docs
  effort: medium
  route_family: representation
  status: pending
- id: codeinsight-index-autobuild-trigger
  subject: codesearch returns empty until codeinsight index is built; nothing triggers the build (sync-before-emit not wired)
  acceptance:
  - .gm/disciplines/codeinsight-vec and codeinsight dirs are empty — index never built for this repo
  - 'Wire a build/refresh: a codesearch dispatch with an empty/stale index runs codeinsight_index first (paper Sync-Before-Emit §27), or document the explicit index verb the agent must dispatch'
  - 'Witness: after build, codesearch for a known symbol returns ranked hits'
  blockedBy:
  - codesearch-index-absent-for-repo
  category: feature
  effort: large
  route_family: execution
  status: pending
- id: watcher-dies-under-browser-load
  subject: Watcher process exits/stalls during this session — browser verb (chromium) or long dispatches appear to kill the synchronous watcher
  acceptance:
  - 'Witnessed twice this iteration: after browser verb dispatches, .status.json ts went stale (164s, 297s gaps) and the watcher process was gone (PowerShell found no plugkit processes to kill)'
  - Diagnose whether the browser chromium spawn or a long synchronous dispatch crashes/exits the watcher process
  - Fix so the watcher survives heavy verbs (browser, long exec) — e.g. isolate chromium spawn, or supervisor auto-respawn on unexpected exit
  - 'Witness: a browser verb dispatch followed by continued dispatches with no watcher death'
  category: bug
  effort: large
  route_family: execution
  status: pending
- id: watcher-autoupdate-thrash-fix
  subject: Revert auto-update version-file-bump — it poisoned installedVersionAtTools and caused infinite drift-respawn thrash
  acceptance:
  - 'Witnessed: prior commit b2e9d721 auto-arm bumped ~/.gm-tools/plugkit.version to latest BEFORE downloading the binary; ensureReady then computed versionDrift=false (file==target) + isReady=true and returned already-ready WITHOUT downloading; running instance stayed old; drift-check saw instance(556)!=file(557) forever -> infinite ''spawning replacement'' loop'
  - 'Fix: both auto-arm sites (network + cache path) reverted to notify-only; version file must only advance after a verified binary download (bootstrap''s job)'
  - _autoUpdateArmedFor removed
  - 'Witnessed fixed: wrapper installed, version file reset to 556 to match binary, watcher boots drifted:false with zero spawning-replacement events'
  - Proper auto-update deferred to a sha-verified force-download path
  category: bug
  effort: medium
  route_family: execution
  status: pending
