---
key: mem-1781466765815-3-1084
ns: default
created: 1781466765815
updated: 1781466974890
---

CORRECTION (2026-06-14, supersedes the pruned cwd-bound-git-verb finding): sibling-repo git MUTATE via the spool IS implemented in rs-plugkit src/wasm_dispatch.rs -- git_add (line ~1223), git_commit (~1242/1245), git_finalize (~1261) all read body {repo} (or {cwd}) and forward it to git_call_argv as the working directory. The empirical failure this fire (git_finalize {repo} -> event repo:null + nothing_to_commit; git_add {paths} -> staged:["-A"] ignoring paths) was because the DEPLOYED wasm 0.1.644 PREDATES this source -- not a missing feature. The rs-plugkit push 261c686 rebuilds wasm and will deploy sibling git-mutate support; after that cascade lands, commit a sibling via git_finalize {repo:"<abs>", message} through the spool (no raw Bash, no deviation.bash-git-bypass). Until the new wasm deploys, raw Bash git in the sibling (separate porcelain-probe events) remains the fallback for old-wasm sessions. Lesson: before concluding a 'missing verb feature', check the committed rs-plugkit source vs the running wasm version -- source is often ahead of the deployed binary.
