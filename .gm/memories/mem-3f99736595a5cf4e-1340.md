---
key: mem-3f99736595a5cf4e-1340
ns: default
created: 1781771194553
updated: 1781771352377
---

Audit cross-check lesson (the audit-loop WORKFLOW caught the inline pass's own search-discipline slip, demonstrating the workflow's independent-discovery value): the codesearch verb indexes ONLY the current cwd, so when the gm audit must inspect SIBLING-repo source (rs-plugkit/rs-learn/rs-exec/rs-codeinsight/rs-search), codesearch cannot reach it -- inspect sibling source via the Read tool on the known/guessed path (Read is NOT a search-discipline violation), and use codesearch+Read-region for call-site lookups; NEVER the native Grep/Glob tools, which trip ccsniff --search-discipline (native-search-grep/glob) in the audit cwd. STRUCTURAL: checkDispatchGates gates native search only on operation=='bash' (lib/spool-dispatch.js:391) AND is itself dormant in Claude Code (no PreToolUse hook, .claude/settings.json={}); the Grep/Glob dedicated tools bypass it entirely -- so search-discipline on the tool surface is enforced post-hoc by ccsniff + SKILL prose only, by design (hook-free portability), never a blocking gate. Running the audit-loop workflow as an independent cross-check returned 5/6 scans clean (cascade/deviations/version-source/residual/supervisor-health) AND caught 3 own-session native Grep/Glob self-deviations the inline pass rationalized -- the cross-check earns its cost on the recurring fire when budget allows.
