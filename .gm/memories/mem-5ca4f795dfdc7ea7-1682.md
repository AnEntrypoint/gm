---
key: mem-5ca4f795dfdc7ea7-1682
ns: default
created: 1784800856766
updated: 1784800856766
---

wasm_dispatch/verbs.rs history (rs-plugkit): memorize_prune candidates field falls back to the libsql rssearch_vectors result (vec_search_local) when host_vec_search is unimplemented -- both native runtimes stub it, so vector search always runs the real libsql vector_top_k directly, never the former host_vec_search stub. codesearch(): code chunk embeddings live in the code_chunks table with their OWN code_chunks_vec index -- they are NEVER mirrored into rssearch_vectors, whose "codeinsight" namespace a separate lookup searches. Measured live: code_chunks=305 rows while rssearch_vectors WHERE namespace='codeinsight'=0, so the vector half contributed nothing and every codesearch silently degraded to BM25-only despite a fully populated vector index. Fix: fall back to the store that actually holds the chunks, mapping its (path, line_start) rows onto the fusion ranker's ci-hash-idx key space via FusionCorpus::key_for_path_line. Symbol-awareness + git-overview parity fix (mcp-thorns comparison): mcp-thorns returns real file:line:name(params) symbol locations on every hit; gm's fusion previously baked path/name/line_start/line_end into an unparseable "path:ls:le name\nbody" text blob and only attached the git commit-overview line to the fused hits array, never to bm25_top10/vector_top10 -- build_hit() constructs every hit through one helper so all three arrays carry the same structured symbol object and overview field uniformly. vector_hits (the response field) inherited the same empty-namespace problem vector_top10 was fixed for -- report what fusion actually consumed (vector_top10, with its own fallback chain) rather than the raw always-empty namespace query.
