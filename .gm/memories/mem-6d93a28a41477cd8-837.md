---
key: mem-6d93a28a41477cd8-837
ns: default
created: 1779971834885
updated: 1779971946597
---

FIXED recurring fresh-prompt-reset gap (2026-05-28, mod.rs): a fresh {prompt:...} body reset phase to PLAN ONLY when phase==COMPLETE. When a prior /loop fire was interrupted before COMPLETE (concurrent-contention left it at EMIT/VERIFY/EXECUTE), the next fire fresh prompt was IGNORED - chain stuck mid-phase, agent had to manually transition to=PLAN every fire (hit 3 fires running). FIX: added a reset block before the COMPLETE branch - if fresh_prompt && !raw_phase_override && phase not PLAN/COMPLETE && prd_pending==0, reset to PLAN. Now a fresh request deterministically starts at PLAN from any stuck non-terminal carryover. Guarded on prd_pending==0 so an actively-in-flight chain with open work is not clobbered. The iter98 fix handled COMPLETE; this handles the non-terminal-carryover case. Predictability-regardless-of-LLM win.
