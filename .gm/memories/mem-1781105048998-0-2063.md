---
key: mem-1781105048998-0-2063
ns: default
created: 1781105048999
updated: 1781105154250
---

WATCHER-LIFECYCLE RESILIENCE TRIAD shipped (2026-06-10, gm d17aea88, outside-the-box session): three structural fixes for the #1 recurring campaign failure class (watcher/supervisor fragility blocking long-horizon-without-stopping), all witnessed same-day: (1) WEDGED-HOLDER TAKEOVER: supervisor.js acquireSingleInstance previously checked only pidAlive(holder) so an alive-but-wedged supervisor (stuck loop, dead watcher, no status progress) blocked every newcomer forever (witnessed: 12-proc manual kill required). Now reads .supervisor.json ts + spool .status.json ts, honors busy_until, both stale >45s = SIGTERM+taskkill holder, unlink lock, take over, emit supervisor.takeover-wedged. VALIDATED live in sandbox: decoy alive-pid holder with 120s-stale statuses, lock handed over. (2) VERB-ABORT RESPONSE: wrapper VERB ABORT branch previously only logged forensics; the orphaned dispatch (in-file consumed, prior watcher died pre-out-write) left the agent waiting forever (witnessed: lost git_finalize task 131). Now writes {ok:false,verb_aborted:true} to both out-name shapes so the agent Read returns a definite failure prompting verify-state+re-dispatch. (3) ATOMIC WASM REPLACE: copyWasmToGmTools (bootstrap.js) used copyFileSync and selfHealFromGithubReleases (wrapper) used writeFileSync directly onto the live ~/.gm-tools/plugkit.wasm - both truncate-then-write a 149MB file, so a crash/concurrent-load mid-write sees truncated/absent wasm (witnessed: disk_wasm_version=null self-heal crash-loop during the 0.1.638->639 cascade window). Now both write .partial-<pid> temp + renameSync with EEXIST/EPERM unlink+retry. DEPLOYMENT: ships via publish.yml npm publish on the d17aea88 push; running watchers pick the new wrapper up automatically via the supervisor wrapper-sha-drift recycle. DIAGNOSIS METHOD NOTE: all three fixes came from same-day witnessed incidents, not speculation - the audit-loop observability surface (watcher.log spawn reasons, kill-stale disk_wasm_version, supervisor refused-duplicate stderr) is what made the root causes legible.
