---
key: mem-1d2e17201b412cd0-1479
ns: default
created: 1780830286210
updated: 1780830359265
---

CASCADE STALLED RELEASE RESOLVED (iter33, 2026-06-07): the stalled rs-plugkit 0.1.636 Release (run 27021235843, failed 2026-06-05 at Fetch-embedding-weights) was RE-TRIGGERED via `gh run rerun 27021235843 --failed` and SUCCEEDED on retry (conclusion=success) - the embedding-weights fetch passed the 2nd time, confirming it is a TRANSIENT/FLAKY external-fetch failure, not a persistent breakage. So 0.1.636 now publishes to npm plugkit-wasm and the cascade auto-bump propagates plugkitVersion to gm.json. DURABLE OPERATIONAL RULE: when an audit finds Cargo.toml-version ahead of npm-published with a FAILED Release run whose Build same-commit succeeded, the corrective is `gh run rerun <id> --failed` on the rs-plugkit Release workflow (a CI re-run, admissible - not a local cargo build/publish which AGENTS.md forbids); the Fetch-embedding-weights step is the flaky point and usually passes on retry. This is reachable in-scope work, NOT blockedBy:external - the auditor has gh authority to re-trigger the CI. POSSIBLE STRUCTURAL IMPROVEMENT (noted, not shipped): add retry-with-backoff to the Fetch-embedding-weights step in rs-plugkit release.yml so a transient fetch failure self-heals without a manual re-run - that is an rs-plugkit CI change for a future fire. The audit-loop workflow's scan:cascade row caught this (first real cascade finding of the 33-fire campaign), validating the cascade-health check end-to-end: detect -> verify-live -> re-trigger -> confirm-success.
