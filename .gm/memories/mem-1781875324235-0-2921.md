---
key: mem-1781875324235-0-2921
ns: default
created: 1781875324236
updated: 1781998049452
---

{"facts":[{"key":"exec_js-abi-shape","text":"exec_js ABI: plugkit-wasm-wrapper.js host_exec_js returns writeWasmJson({ok, stdout, stderr, exit_code, timed_out}). rs-exec wasm_host.rs must parse this JSON struct -- reading bytes as raw stdout loses exit_code/stderr/timed_out. take_bytes must use slice::from_raw_parts borrow then .to_vec(), NOT Vec::from_raw_parts (double-free UB)."},{"key":"memorize-atomicity-pattern","text":"memorize atomicity invariant: vec-put must fire FIRST and be checked; if vec-put fails, abort before text-put. If text-put fails, rollback by vec-put with empty bytes. Without this, a vec-put failure creates orphaned text-only memories the index never sees."},{"key":"sql-parameterized-libsql-wasm","text":"SQL parameterization in libsql_wasm.rs: exec_params/query_params use sqlite3_bind_text with SQLITE_STATIC (None). Key: collect all CString params into Vec<CString> BEFORE binding so they stay alive for the duration. SQLITE_TRANSIENT may not exist in the ffi crate; SQLITE_STATIC with pre-collected CStrings is the safe pattern. Eliminates sql_quote format! injection risk in pipeline.rs."},{"key":"serde-yaml-gate-parse","text":"COMPLETE gate YAML parse: prd_has_open_items() and mutables_unresolved() in gates.rs must use structural serde_yaml parse, not string contains(). String search matches 'status: pending' in description fields causing false COMPLETE-gate positives. Pattern: serde_yaml::from_str::<Value> -> Sequence -> per-item .get('status').as_str() comparison."},{"key":"inbox-resilience-two-phase-parse","text":"Inbox resilience in rs-exec dispatch_pending: two-phase parse is required -- first serde_json::from_str::<Vec<Value>> (array shape), then per-item serde_json::from_value::<InboxTask> with skip-on-error. Single-pass from_str::<Vec<InboxTask>> fails the entire batch on one malformed task, silently dropping the inbox."},{"key":"rrf-merge-n-fusion","text":"RRF fusion in rs-search: rrf_merge_n(ranked_lists: &[Vec<String>]) is the canonical N-source Reciprocal Rank Fusion. fusion_search in wasm_host.rs must delegate to it rather than reimplementing inline (inline used f32+0-indexed rank, inconsistent with fusion.rs f64+1-indexed). Deduplication is automatic since all sources contribute to the same scores HashMap."},{"key":"kv-put-namespace-allowlist","text":"kv_put namespace allowlist in rs-plugkit wasm_dispatch.rs: KV_PUT_ALLOWED_NAMESPACES = [default, session, config, cache, user]. Write attempts to unlisted namespaces are rejected. This prevents arbitrary namespace proliferation and unintended kv surface expansion."},{"key":"fs-write-path-sandbox","text":"fs_write path sandbox in rs-plugkit wasm_dispatch.rs: normalized path (backslash to forward slash) must not contain '../', start with '/', or contain ':'. This blocks path traversal and absolute-path writes. The check runs on the normalized form to prevent bypass via mixed separators."}]}
