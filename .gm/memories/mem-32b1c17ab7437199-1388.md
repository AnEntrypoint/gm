---
key: mem-32b1c17ab7437199-1388
ns: default
created: 1782386022739
updated: 1782386158410
---

BROWSER about:blank-loudness fix (gm-method, plugkit-wasm-wrapper.js host_browser_exec): navigation was opt-in (url=/bare-URL prefix), so a bare expression on a never-navigated session evaluated against about:blank and looked like 'the tool only opens about:blank' -- a no-surprises violation. Fix: a blankProbe (`console.error('__GM_BLANK__')` when page.url()==='about:blank'||'') is injected into the profile/capture/bare eval paths ONLY when !startUrl; the wrapper reads the marker off runner stderr to set envelope.navigation_requested, landed_on_blank:true, and a hint to prefix url=, then scrubs the marker from stderr. Default navigating path byte-unchanged. Witnessed two-stage: browser-7 proved the probe emits __GM_BLANK__ on a blank page; exec_js-1 proved the envelope branch sets landed_on_blank+hint and strips the marker. Live watcher runs the old cached wrapper so behavior ships via cascade; witness the TRANSFORM against the real runner+real node rather than the live prefix. ALSO removed a false 'session=<id>\n<expr>' body shape from browser.md -- it was documented but never parsed by the wrapper (grep session= = 0) and ReferenceErrored at runtime; session is selected by the spool sessionId, not a body prefix. LESSON: a documented body shape with no wrapper parse is a runtime ReferenceError waiting to happen -- grep the wrapper for every shape the prose promises.
