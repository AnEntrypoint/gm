---
key: mem-1781791660967-0-983
ns: default
created: 1781791660968
updated: 1781791733917
---

RECALL-EMISSION path invariant: the structured 'recall' gm-log event (that gmsniff --recall-scores/--recall-misses/--recall-modes + --event recall reveal) emits at the orchestrator's ACTUAL recall paths in rs-plugkit/src/orchestrator/recall.rs -- recall::recall_hits (the instruction/transition recall_hits, via host_vec_search/host_kv_query) and handle_auto_recall (the turn-entry pack) -- NOT only code_index::recall_at, which the high-frequency recall traffic bypasses (auto_recall + recall_hits never call it; only the explicit recall verb might). emit_recall sets {sub:'rs_learn', query, hit:n_hits>0, top_score:hits[0].score, mode:vector_top_k|kv_query|auto_recall, n_hits, namespace}. Validate any recall-observability change via gmsniff --event recall > 0 in practice -- an emit at a path the traffic does not use produces zero events (the lesson: instrument the chokepoint the real traffic flows through, confirmed by the event count, not the function that looks canonical).
