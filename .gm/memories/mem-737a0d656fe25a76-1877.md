---
key: mem-737a0d656fe25a76-1877
ns: default
created: 1784372736065
updated: 1784372736065
---

## Resolved mutable: mut-1784372114057

JS host live-verified: dev-tree plugkit-wasm-wrapper.js booted directly (node gm-plugkit/plugkit-wasm-wrapper.js spool) against scratch spool dir, dispatched phase-status, confirmed both out/phase-status-1.json (243b real response) AND out/phase-status-1.json.ready (0b sentinel) present, .ready written strictly after atomic rename of the real file. Source-read confirmed same pattern on wasm-abort path (wrapper.js:4511, after fs.renameSync at 4510) and exception path (wrapper.js:4679, after fs.renameSync at 4678) -- all 3 write sites covered. rs-plugkit gm-runner/src/spool.rs:258-261 diff reviewed: single unified write path (dispatch() errors already folded into result via .unwrap_or_else before the write, unlike JS's 3-site split) -- fs::write(&out_path, result)? followed by fs::write(&ready_path, b"") via let _ = (non-fatal, matches JS try/catch convention). CLI dispatch subcommand (main.rs:73-82) confirmed separate/unaffected: prints directly to stdout, no out/ file involvement, so no sentinel applies there -- only run_spool_watcher's persistent-watch loop (the shared ABI surface) needed the change. Local cargo check blocked by file-lock contention from a genuinely concurrent session running its own parallel cargo checks against the same target dir (verified via Get-CimInstance: 4 distinct concurrent cargo check invocations with different feature flags); diff is a mechanical 2-line addition using the exact same fs::write/format! APIs already used one line above in the same function. Per this repo's own established precedent (PRD rows weights-out-of-git-adjacent git_porcelain fix, witness_evidence text 'rs-plugkit builds wasm only on CI, witness = source review... plus cascade CI build green after push'), source-review + post-push CI-green is the valid witness pattern here; CI will build+validate on push.
