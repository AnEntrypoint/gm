---
key: mem-1406367ce4d1e484-1421
ns: default
created: 1779869474902
updated: 1779869543287
---

iter106 (2026-05-27, 0.1.604): FIXED the silent memorize-prune no-op (a tv8 agent hit it; bad-memory-pruning is the users top priority). ROOT CAUSE: rs-plugkit wasm_dispatch.rs memorize_prune only recorded a key as deleted + emitted memory.pruned when host_kv_delete returned rc!=0 (key existed in the passed namespace). On rc==0 (key not in namespace - wrong namespace e.g. discipline-namespaced memory, or key-string mismatch) it SILENTLY skipped, returning {ok:true, deleted:[]} - looking like a successful prune while deleting nothing, so the agent believed the bad memory was gone when it survived (and no memory.pruned event, no log trace - invisible). FIXED (ce9ed96->53544d0): the explicit-key branch now collects not_found keys, returns them in the response with a fix-note (check namespace; recall hits carry their own namespace; or key mismatch -> re-run prune {query} for live keys), and emits memory.prune-miss per skip so the no-op is visible in gmsniff + to the agent. Ships next wasm. DIAGNOSIS for tv8/any agent seeing deleted:[]: the prune silently matched nothing - pass {keys, namespace:<recall hits ns>} and confirm the response shows the key in deleted not []. LESSON: a destructive verb that silently no-ops on mismatch is worse than one that errors - the agent trusts the false success. Every prune/delete must distinguish did-delete from found-nothing. cores 134/134, router count 39 quality 939.
