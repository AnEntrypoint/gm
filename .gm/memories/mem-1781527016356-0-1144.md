---
key: mem-1781527016356-0-1144
ns: default
created: 1781527016357
updated: 1781527072068
---

OWN SLEEP-POLL DEVIATION + CORRECTIVE (2026-06-15, caught by ccsniff --bash-discipline): waiting for a git_finalize response with a bash loop `for i in 1..N; do if [ -f out/git_finalize-N.json ]; then break; fi; sleep 2; done; cat ...` is the spool-poll anti-pattern (flagged sleep-poll). plugkit is synchronous and a slow verb advertises busy_until; the prescribed wait is: dispatch the verb (Write), Read the response in the same block, and if it is missing because the verb is mid-flight (git_finalize ~8s push) simply re-Read on the NEXT message - NEVER a bash sleep/for loop. The bash-discipline ENDORSED_POLL exemption (until/while) is for external-state polling, never for spool-response waits. Applies to every slow spool verb (git_finalize, browser spawn, codesearch index rebuild): direct Read + re-Read, no shell loop. VALIDATION same fire: supervisor boot_reason label fix shipped as gm-plugkit 2.0.1558 (publish.yml auto-bumped c71f42c1); the recurring ~30min watcher restart cadence is benign gm-plugkit self-stale auto-update driven by each fire's push, handled by supervisor + single-instance refuse-duplicate, deviations_30m 0.
