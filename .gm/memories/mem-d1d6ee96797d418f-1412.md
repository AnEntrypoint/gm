---
key: mem-d1d6ee96797d418f-1412
ns: default
created: 1787382564979
updated: 1787382564979
---

gm-method: supply-chain tell scanner detection classes (drained from AGENTS.md, single-tool: scripts/scan-supply-chain-tells.mjs). Detection classes: exact-match known-incident strings (campaign tag, hardcoded C2-lookup address, custom payload-smuggling header), structural patterns (blockchain-RPC-derived C2 address resolution, IP-from-bytes decode, detached+hidden-stdio child-process spawn, eval() fed by a network response, XOR-decode loops, require/module stashed onto global, a dense minified tail line appended to an otherwise normal readable file, backslash-u-escaped ASCII used to defeat plaintext grep, multiple public RPC endpoints raced with Promise.any for C2 resilience), Unicode confusables/invisible characters (zero-width spaces/joiners, bidi override characters -- the Trojan Source class -- soft hyphens, and common Cyrillic homoglyphs for Latin letters), and a statistical AI-prose-tell class over .md files (sentence-length burstiness, "it's not X, it's Y" and "X, Y, and Z" rhetorical constructs, an overused-vocabulary regex, explicit validation markers) yielding a heuristic ai_probability_score -- a signal for human-review-worthy docs, not a malicious-code finding. Source incident: an obfuscated C2 stager appended after a legitimate export default in a build config, delivered via a compromised automated release commit that also bumped a dependency and dropped .env from .gitignore.
