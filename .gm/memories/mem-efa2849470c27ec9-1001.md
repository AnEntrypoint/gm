---
key: mem-efa2849470c27ec9-1001
ns: default
created: 1784800841823
updated: 1784800841823
---

wasm_dispatch/verbs.rs history (rs-plugkit): embed_query previously collapsed three distinct failures (empty query, failed bert plugin call, response with no embedding field) into the same bare Value::Null -- search_with_recency reported any Null as "invalid query embedding", misattributing a failed EMBED to a malformed QUERY (the rssearch_vector_hits_failed event named the query even though bert was what failed). Still returns Null (callers treat it as "no vector half available", fall back to BM25) but now emits which of the three actually happened via emit_event before returning. vec_search_local is the native in-guest replacement for host_vec_search, which was a not_implemented stub in every runtime -- runs the identical libsql vector_top_k query rssearch_vector_hits already used successfully, so vector search returns real scored hits in-guest instead of round-tripping to an unimplemented host function; the stub is eliminated at its root since the guest owns the libsql path directly.
