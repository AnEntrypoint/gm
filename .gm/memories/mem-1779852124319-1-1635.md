---
key: mem-1779852124319-1-1635
ns: default
created: 1779852124321
updated: 1779852210076
---

iter84 (2026-05-27, 0.1.603): investigated the user-requested lora-optimization effectivity question and made a careful design judgment + one real cleanup. FINDING: the LoRA namespace-rerank (iter71-proved working with >=2 disciplines) is dormant in normal recall because there is no enabled.txt (recall fans default-only per AGENTS.md L85). There are 3 populated discipline namespaces: codeinsight (397 entries - the code-search index, consumed by the codesearch verb NOT recall, correct), AGENTS.md (9 entries - real facts overlapping default), validate (15 entries - SYNTHETIC TEST RESIDUE: gm-validate witness one/two/three with timestamps). JUDGMENT: default-only is largely CORRECT by design - codeinsight serves codesearch on its own path, AGENTS.md-discipline facts overlap default; enabling disciplines for recall would surface the code-index into memory recall (wrong channel). So the LoRA-dormant state is mostly by-design, NOT a bug - did NOT manufacture an enable-disciplines fix (Nothing Fake). The ONE real action: REMOVED the validate discipline (15 mem + 15 vec = 30 files, git rm --cached + delete) as stale test residue - bad-memory pruning at the discipline granularity. No code references the validate namespace. LESSON: bad-memory pruning applies at the DISCIPLINE level too, not just individual mem keys - a whole namespace of test-witness residue (gm-validate witness N) is prunable junk; and a dormant capability is not automatically a bug - check whether the dormancy is by-design (disciplines opt-in, each verb has its own channel) before activating. cores 110/110, router count 15 quality 851, deviations 0.
