---
key: mem-c13be6368bc2e13a-1780
ns: default
created: 1779849680167
updated: 1779849766471
---

iter80 (2026-05-27, 0.1.602): fixed paper drift contradicting the agent-as-llm MO. paper.html §37 + abstract + TOC described semantic work routing through a haiku-class model call offloaded to the host agent via the LLM callback protocol (and haiku calls are cheap) - but the agent-as-llm principle (AGENTS.md, verify.rs) is that plugkit/rs-learn NEVER call a separate model; the agent IS the model and does understand/classify/score/decide INLINE. haiku appeared nowhere in AGENTS.md or live code. The §37 title had been half-updated to Meaning Through fetch leaving the body inconsistent (title said fetch, body said haiku). FIXED all 4 refs: abstract clause, TOC entry (s37), §37 heading + hard-rule title + body (now: Meaning Through the Agent; semantic work IS the agents own inline judgment, plugkit is the harness and the agent IS the model it dispatched, fan out N items as N parallel dispatches), removed haiku-calls-are-cheap. Zero haiku refs remain. BROWSER-WITNESSED live: page.evaluate on file:///paper.html returned s37_title=37. Meaning Through the Agent..., has_agent_as_llm=true, no_haiku=true. Closed the browser session (browser_sessions_alive=0, avoiding the iter33 orphan-chrome wedge). LESSON 1: the browser verb body is RAW JS CODE executed with page available (await page.goto + page.evaluate, return value), NOT an action-newline-args protocol - the action+args shape gets mis-eval-wrapped. LESSON 2: when a framing is corrected (agent-as-llm, iter62), the PAPER is a separate surface that drifts - grep the paper for the old framing (haiku, separate model call, callback protocol) just like AGENTS.md/instruction-prose. cores 106/106, router count 11 quality 801 (climbed 611->801 across the run, agent-as-llm loop accumulating cleanly), deviations 0.
