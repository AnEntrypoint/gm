---
key: mem-ca378746b3e21aae-1078
ns: default
created: 1779977387633
updated: 1779977427089
---

ccsniff discipline-detector FP-refinement pattern (2026-05-28): a discipline detector that pattern-matches must strip its structural context or it false-positives. TWO fixes this arc: (1) glyph-discipline flagged its own GLYPH regex char-class definition -> stripGlyphCharClass blanks regex [...] bodies before testing (b32ae16). (2) search-discipline flagged every bash grep/find including <cmd>|grep stdout-filtering (gmsniff|grep, git ls-files|grep, byte-scan) which has no codesearch equivalent -> now flags only a search tool in the FIRST pipe-segment (reads tree directly), grep downstream of | is filtering not searching (fda5ff1->bc1cfb9). LESSON: when adding a discipline detector, the regex matches the rule-defining context too (its own definition, pipe-filtering, quoted strings) - strip that context like git-discipline strips quoted commit-message bodies, or the detector reports noise. Run all three each audit: bun x ccsniff@latest --git-discipline --search-discipline --glyph-discipline --stats --since <window>; gm sessions should now be ~0 after the FP fixes.
