---
key: mem-e5b3cbf57f15a034-564
ns: default
created: 1779969996118
updated: 1779969996118
---

memorize-prune verb full two-mode spec: explicit {key}/{keys:[...]} deletes exactly those mem keys (text + -vec embedding sibling) via the host_kv_delete host import; {query} returns review-only candidates (vector_top_k hits with keys) for the agent to judge, then re-dispatch with the stale {keys:[...]}. Query mode never auto-deletes by similarity (a blind similarity-delete is itself a bad-memory generator; destructive step stays under agent judgment). Emits memory.pruned per deletion. nonexistent key -> {deleted:[],not_found:[key]} (prune-miss fix iter106).
