---
key: mem-1781262434695-0-2357
ns: default
created: 1781262434696
updated: 1781262853183
---

String-externalization project (2026-06-12, in-flight EXECUTE) — LOCKED ARCHITECTURE + PROGRESS. GOAL: edit gm agent-facing prose without rebuilding Rust tooling. DECISIONS (user-approved): (1) scope = agent-facing prose ONLY (phase instruction TEXT + gate/deviation/residual messages the agent reads), NOT internal error/log strings; (2) single SHIPPED bundle, authored canonically in the gm repo, shipped via gm-plugkit, loaded by the wasm; compiled const stays as last-resort fallback (this is what keeps freddie/thebird unbroken); editing prose = edit gm bundle + JS-only gm-plugkit republish, NO Rust rebuild. MECHANISM (witnessed): resolver reads relative path .gm/instructions/<key>.md via pkfs::read_to_string -> host_read; host_read resolves project-relative paths in BOTH the Node watcher (disk) and thebird in-page wasm (IDB FS) — proven by existing host_read(\".gm/exec-spool/.session-current\") at wasm_dispatch.rs:853/1064. Wrapper provisions the shipped bundle into .gm/instructions/ at boot (always overwritten from shipped copy = 'shipped bundle only', no per-project override). DEP CRATES (rs-learn/rs-exec/rs-search/rs-codeinsight) have NO host_read (their wasm_host.rs lists host_kv/vec/bm25/git/exec/log/now only; rs-codeinsight has zero host imports) -> do NOT add host_read to them (expands the #[link(wasm_import_module=env)] link-trap surface); rs-plugkit MEDIATES (deps return stable key, rs-plugkit substitutes from bundle at dispatch boundary). DONE THIS RUN: created rs-plugkit/src/prose.rs resolve(key,default) (BOM-strip + CRLF-normalize + empty->fallback), added pub mod prose to lib.rs, rewired get_instruction (mod.rs:75) to return String via prose::resolve with the 7 phase consts as defaults (single caller mod.rs:320 -> format!+json!, type-safe). NOT YET: route gate msgs (gates.rs:279 long-gap, residual.rs:122/132/141/167) through resolve; author gm bundle; gm-plugkit ship+provision bundle; cascade ship; acceptance no-rebuild+fallback; freddie/thebird compat. NOT PUSHED YET (batch rs-plugkit changes into one push to avoid per-increment cascade). PRD ids: design-string-resolver, extract-plugkit-phase-text(done), extract-plugkit-gate-messages, author-gm-canonical-bundle, gm-plugkit-ship-bundle, acceptance-no-rebuild, acceptance-fallback, thebird-compat-inbrowser, standard-usage-audit + corner cases 93-107."
