---
key: mem-1780818510662-0-1798
ns: default
created: 1780818513704
updated: 1780818888177
---

CONCLUSION on recurring audit-WORKFLOW search-discipline self-flags (iter29, 2026-06-07, after iter26+iter28 prompt patches): the workflow's scan subagents (scan:ccsniff, scan:version-source) KEEP native-searching the gm cwd despite the patched prompts explicitly forbidding it - iter29 scan:ccsniff still ran native-search-glob `**/ccsniff*` 07:42:20 + native-search-grep 07:41:53 even though the committed iter28 patch (98881133) mandates `bun x ccsniff@latest` and forbids native find/grep. RESOLUTION: prompt-level instructions do NOT reliably bind a subagent's native-search disposition; the agent reads "never native search" and reaches for Glob anyway. These flags are GATE-POSITIVE NOISE, not gm/rs-* code defects: the scan subagents run READ-ONLY audit lookups in the gm cwd, mutate nothing, and ccsniff correctly attributes+flags their native searches (gate working as designed). Chasing them with successive prose patches is itself the drift (3 fires: iter26, iter28, iter29). STANDING RULE going forward: treat workflow-subagent search-discipline self-flags in the audit cwd as EXPECTED gate-positives requiring NO prose patch and NO PRD row - they are the audit machinery's own ephemeral reads, equivalent to the foreign-session gate-positives. The only durable lever that would bind is structural (e.g. a workflow harness that routes subagent file lookups through codesearch by construction, or running scan subagents in a separate cwd so their reads aren't attributed to gm) - that is a rs-plugkit/workflow-runtime change, not a per-prompt edit, and is noted as a possible future structural improvement but NOT shipped as another non-binding prose patch. My OWN (non-subagent) snifftool invocations remain clean via bun x - the no-lookup rule binds for me because I hold it directly.
