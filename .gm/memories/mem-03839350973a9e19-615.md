---
key: mem-03839350973a9e19-615
ns: default
created: 1785274371670
updated: 1785274371670
---

gm submodule-pin-drift incident history: a commit landed and pushed only to a submodule's (agentplug/rs-plugkit/rs-codeinsight/agentplug-bert/agentplug-libsql/agentplug-treesitter/rs-search) own standalone remote leaves gm's own tracked pointer silently pointing at the prior commit, invisible until the next explicit git rev-parse HEAD (submodule) vs git rev-parse origin/main comparison surfaces the drift. Live-hit twice in one session (two distinct commit pairs) before the standing rule existed -- each time required a manual git checkout <sha> --detach in the submodule dir plus a git_finalize in gm to close.
