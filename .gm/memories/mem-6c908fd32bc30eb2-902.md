---
key: mem-6c908fd32bc30eb2-902
ns: default
created: 1780314452054
updated: 1780314666770
---

ccsniff search-discipline FP-fix 2026-06-01: targetsOutsideCwd extended to recognize git -C <dir> and pushd (not just cd) as cwd-overrides, AND normPath now normalizes msys /c/dev form to windows c:/dev form so a git-bash path arg compares against a windows-form cwd (without it /c/dev/gm/sub never startsWith c:/dev/gm and reads cross-repo). New targetsSingleFile exempts grep PATTERN onefile.ext (a read-filter codesearch cannot serve, distinct from a tree search), strips shell redirects first, and # comment lines are skipped. Helpers extracted to ccsniff/src/discipline-helpers.js with 12 unit-test cases in test.js. Dropped this-session FP count 16->5 (5 remaining are genuine in-cwd multi-pattern symbol greps). FP-refinement class: a discipline detector must strip structural context (redirects, comments, cwd-overrides, path forms) or it false-positives legitimate cross-repo/single-file work.
