---
key: mem-1780383587024-6-1618
ns: default
created: 1780383589932
updated: 1780386845041
---

CASCADE-OUTAGE second-order cause FOUND+FIXED (2026-06-02 ~06:58 fire): wasi-sdk-24 installed locally (C:/dev/wasi-sdk, clang.exe), reproduced the EXACT CI release build: cargo build --release --target wasm32-wasip1 --no-default-features --features wasm --lib with full CI CFLAGS -> EXIT=0, Finished release profile in 3m15s, 0 errors (only 7 warnings). So the E0428 fix (4c4ed3f) was COMPLETE - the source on origin builds clean. There was NO second source error; the find-second-wasm-build-error premise was wrong. REAL second-order cause: Swatinem/rust-cache cache-on-failure:true saved the E0428-failed target/ incremental state under constant keys (release/build/check-wasm32-wasip1); every later run restored that poison and cargo clean -p rs-plugkit did not purge cross-crate fingerprints -> Build-WASM kept failing on already-fixed source. FIX (rs-plugkit 27eeea8): bumped all 3 cache keys to -v2 (abandon poison) + cache-on-failure:false (never re-poison), in build.yml + release.yml. Pushed -> fresh Release run on d8c5357 now in_progress with clean cache. VALIDATION PENDING: confirm Build-WASM success + publish-binaries ships plugkit-bin > v0.1.616 + gm.json bumps. METHODOLOGY: local wasi-sdk-24 reproduction of the exact CI wasm build is the definitive diagnostic when CI logs are admin-403; it distinguished a source error (none) from a cache-infra error (the real one). LESSON: a failed CI build with cache-on-failure:true poisons the constant-key cache and wedges all future runs even after the fix - always pair a constant cache key with cache-on-failure:false, or bump the key on the fixing commit.
