---
key: mem-12cff811d9944026-1056
ns: default
created: 1789755113577
updated: 1789755113577
---

rs-plugkit git_log (wasm_dispatch/verbs.rs) accepted path/paths/files in its body schema per gm-config/prose/entry.md's own documented contract but never read them -- only limit/count/range reached the git argv, so every path/paths dispatch silently returned the unfiltered top-N same as a bare call. Fixed: collect paths from body.paths (array) or body.files (alias) or body.path (single string), and when non-empty push -- <pathspec...> onto the git log argv after the range arg. Live-witnessed via isolated AGENTPLUG_HOME scratch dispatch (never touched the shared daemon): bare git_log unaffected, path/paths=AGENTS.md returns a real filtered set distinct from the unfiltered top-10, paths=[nonexistent-file] returns commits: []. git_diff (path only, no paths/plural) and git_show (paths for commit-diff limiting, per entry.md) have the same documented-but-never-implemented gap for their plural/paths forms -- not fixed in this pass, scope was git_log only; a follow-up PRD row should audit git_diff/git_show against their own doc claims the same way.
