---
key: mem-9463531836001a94-1228
ns: default
created: 1779841207025
updated: 1779841352160
---

iter70 (2026-05-27, 0.1.597->0.1.598): found+fixed the DEEPER LoRA re-rank bug - logit/namespace MISALIGNMENT. The iter69 softmax fix was correct but apply_adapter returned only logits with NO target labels, so recall.rs mapped logits[i] to namespaces[i] by HIT-discovery order while logits are in the ADAPTER target order - positionally wrong, applying the wrong weight per namespace. FIX (rs-learn cff127d: apply_adapter returns {logits,targets:core.targets}; rs-plugkit 8459370: recall.rs maps each hit-namespace to its logit BY LABEL via adapter_targets.position(ns), build green 0d9ea0f). Live reorder witness deferred to the 0.1.598 boot. TEST-HARNESS LESSON (cost a diagnostic cycle): inline node -e inside command-substitution mangles the JSON dispatch body - feedback dispatches landed malformed (feedback_count stayed 0), making the real misalignment bug look like a fix-failure. ALWAYS write spool dispatch bodies via standalone node fs.writeFileSync, never $(node...>file). BROADER LESSON: when a re-rank still no-ops after a calibration fix, check the INDEX/LABEL alignment between the score source and the items being ranked - positional assumptions across two independently-ordered lists are a silent-wrong class.
