---
key: mem-c5a45520f73ccb49-1711
ns: default
created: 1782175508576
updated: 1782175644287
---

TOOLING-SIDE PROFILE+DEBUG AFFORDANCES (2026-06 /gm-skill): made profiling/debugging zero-boilerplate at the verb level. (1) exec_js host (plugkit-wasm-wrapper.js host_exec_js ~1879) now returns duration_ms (Date.now() delta around spawnSync) on every response -- free node wall-time, additive/non-breaking. (2) browser host (host_browser_exec ~1989, mirrors the timeout= prefix parse) accepts an opt-in 'capture'/'profile' body prefix: it wraps the agent script with page.on('console'|'pageerror'|'requestfinished') capture + a post-script page.evaluate of navigation performance + an async IIFE (top-level await/return preserved), returning {result:<your return>, debug:{console,pageErrors,network,performance}}. Default path (no prefix) is byte-unchanged = non-breaking, browser-witness gate + existing scripts unaffected. The EXACT injected wrap string was witnessed live through the current browser verb (out/browser-213.json): result:{title:'Example Domain',cap:{ok:true}} + debug:{console:[CAP-PROBE log], pageErrors:['CAP-uncaught'], network:[{dur_ms:223,ttfb_ms:220}], performance:{load_ms:243,dcl_ms:242}}. SHIP: wrapper edits are gm-plugkit JS (ship via cascade ensureWrapperFresh + supervisor wrapper-sha-drift recycle, NO wasm rebuild); auto-wrap goes live post-cascade (running watcher uses the installed wrapper, so this session witnessed the wrap STRING not the live prefix). Instructions reinforced: browser.md 'capture\n<expression>' shape + zero-boilerplate paragraph; SKILL.md + execute.md name duration_ms + capture prefix; rs-plugkit execute.rs/browser.rs synced. KEY: an opt-in body prefix (like timeout=) is the safe way to add a verb affordance -- default unchanged bounds blast radius.
