---
key: mem-b21556c6ba17ff33-1711
ns: default
created: 1780503230617
updated: 1780503258187
---

Audit-loop fire (2026-06-03 ~16:13): HEADED-BROWSER-FIX chain closed. USER BUG REPORT: managed browser "was supposed to start in headed mode" but did not. ROOT CAUSE: the prior --no-startup-window fix (which killed the extra unused startup window, recall mem-1780437833973) suppressed the ONLY visible window in headed mode - startManagedBrowser:1043 headed branch pushed --no-startup-window so chromium opened zero visible windows. FIX (sha 25d82610, gm main): headed branch now pushes about:blank positional arg instead of --no-startup-window, so chromium opens exactly ONE visible window the playwriter session reuses (visibly headed AND no duplicate); headless branch (--headless=new) unchanged. This reconciles the two requirements the --no-startup-window fix had put in conflict. Deploy via cascade: push to gm -> publish.yml -> gm-plugkit npm bump -> supervisor reverts .gm-tools wrapper to published copy on sha-drift (source-resident edit is the only admissible deploy surface; hot-deploy to .gm-tools is reverted). LIVE WITNESS DEFERRED to post-cascade reboot - the new wrapper is not yet on disk; chrome /json/list will report 1 page about:blank (was 0). Deploy path proven end-to-end by the --no-startup-window cascade (recall mem-1780438304736: reached disk as bumped wrapper_sha 3bac69c866f6, witnessed steady). Chain walked PLAN->EXECUTE->EMIT->VERIFY->COMPLETE; PRD 291 total, 0 pending; witness commit a2c3f6eb pushed clean. Watcher alive at close (pid 15808, wrapper_sha 0b0c6a6d2612 - this is still the PRE-fix wrapper; the about:blank cascade had not yet redeployed). Next audit fire after reboot re-confirms wrapper_sha bump + single visible about:blank page to close the live-witness loop.
