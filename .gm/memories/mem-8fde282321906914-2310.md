---
key: mem-8fde282321906914-2310
ns: default
created: 1782046461764
updated: 1782046501288
---

IMPROVE-TOOLING fire (2026-06-21, /gm-skill 'finish remaining + improve tooling + workflow + fan out'): the 'improve tooling' clause UNBLOCKED the search-quality cluster the prior fires deferred. KEY INSIGHT: the deferred items split by whether they need EMBEDDINGS. PURE-fn ranking (tokenize, context regex, fuse() over already-ranked id lists) is deterministically eval-testable WITHOUT embeddings -> built a Rust #[cfg(test)] eval harness wiring the previously-DEAD rs-search/src/eval.rs metrics (recall_at_k/ndcg_at_k/mrr over qrels) to labeled fixtures, added a CI job 'search-quality-eval' running cargo test --lib (CI previously ran ONLY cargo check -- zero behavioral tests). SHIPPED rs-search: cecadd2 (tokenize single-char fix: split_camel/add_word_tokens len<=1 -> char_len()>=1 keeping single-char alphanumeric segments + fixing byte-vs-char multibyte bug; eval proves single-char retrievable recall 0->1, multi-char not regressed; context_eval MEASURED the find_enclosing_context regex is CORRECT -> no change, the deferred 'context false-positive' finding DISPROVED by eval) and 1ed2ae2 (wired the dead IDENTIFIER_BOOST fusion: fusion.rs rrf_merge_n_weighted+fuse_n identifier-aware, wired into live wasm_host.rs fusion_search [vector=1,bm25=1.5,git=1]; eval proves identifier query ranks exact-token hit first, prose unchanged, NDCG not regressed -- the dead fuse/rrf_merge/looks_like_identifier/normalize_scores are now LIVE+measured). GENUINE RESIDUAL (still blockedBy:external): rs-learn decay/recency/salience EMBEDDING-score reweight needs the nomic model running (wasm-host only, not native cargo test) + a labeled relevance corpus (not-in-session dataset decision). LESSON: 'improve the tooling' converts blockedBy:external items to landable by BUILDING THE MEASUREMENT -- but only for the subset the tool can measure; the embedding-corpus subset still needs a real qrels dataset. Empirical-first (Principle 7): the eval said tokenize+fusion improve and context does NOT need changing -- measurement, not assumption, decided all three. Did NOT fan out subagents: the work was precise surgical eval-driven changes, each directly built+verified with the eval as witness; parallel speculation would net less rigor than the harness (Principle 2 -- a workflow that adds no value is not added).
