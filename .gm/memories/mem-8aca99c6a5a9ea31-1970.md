---
key: mem-8aca99c6a5a9ea31-1970
ns: default
created: 1781774938052
updated: 1781775388760
---

CONCURRENT-AGENT productivity fix (2026-06-18, user /gm-skill request): the recurring foreign-session push-dirty/residual-premature LOOPS were caused by rs-plugkit gates.rs to_denial_json hardcoding next_dispatch='instruction' for EVERY denial -- a weak/concurrent agent hitting the COMPLETE stop-gate (dirty tree / open PRD) was told 'dispatch instruction for recovery' (generic), re-read prose, and re-attempted -> loop. FIX (gates.rs): added next_dispatch: Option<String> to GateVerdict + a with_next() helper; to_denial_json now uses self.next_dispatch.unwrap_or(await?memorize-continue:instruction); the COMPLETE stop-gate derives the FIRST-blocker recovery verb by dependency priority (open PRD->prd-resolve, mutables->mutable-resolve, dirty->git_finalize, missing residual-scan->residual-scan, client-edit->browser) via next_recovery.get_or_insert; bash-git-bypass->git_finalize. Long-gap stays 'instruction' (correct). So gate denials are now SELF-RECOVERING -- a concurrent agent dispatches the named verb and converges in mechanical steps instead of looping = predictability-regardless-of-LLM. rs-plugkit wasm change, ships via cascade on the rs-plugkit push; behavioral validation next fire (a foreign push-dirty denial should carry next_dispatch=git_finalize). VERIFIED CLEAN (no concurrent-contention fix needed): codeinsight index is per-cwd isolated (code_index.rs project_db_path derives a per-project db file, each watcher opens its own -> no cross-cwd table thrash); gm-log append is atomic-per-line (single fs.appendFileSync of line+\n across all writers, <4096B no interleave). DECLINED as over-engineering: a .pending busy_until out/ marker (the next_dispatch fix keeps agents moving, not polling) and an in-band fleet-awareness surface (watchers already independent + supervisor-coordinated). Local wasm cargo-check infeasible (heavy fresh dep graph); CI validates the gates.rs build; a failed build is contained (0.1.647 keeps serving, no outage).
