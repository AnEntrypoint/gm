---
key: mem-0373f036a50f05d9-1133
ns: default
created: 1779833561042
updated: 1779833749268
---

iter63 (2026-05-27, 0.1.592): CLOSED the LoRA consumption loop (iter62 open gap). rs-plugkit recall.rs recall_hits now re-ranks vec_search hits by the MicroLoRA scoring heads learned per-namespace relevance blended with cosine: final=cosine*(1+0.25*(sigmoid(ns_logit)-0.5)*2), factor in [0.75,1.25]. rerank_by_adapter (496d9ae, build SUCCESS de2929c) is STRICTLY no-op-safe: unchanged on <2 hits/namespaces, embed fail, absent adapter, untrained all-zero logits, or logit/namespace count mismatch - baseline recall provably unchanged until a matching trained head exists. The LoRA loop is now closed end-to-end at the architecture level: head TRAINS from trajectory quality (iter61 feedback) + recall CONSUMES it (this turn re-rank). NEXT REFINEMENT (noted): align the adapters train-time targets to the recall namespaces so the re-rank engages in practice (today apply_adapter loads the persisted adapter sized to whatever targets it was trained with; a namespace-targeted train makes the blend live). LESSON: ship the consumption hook no-op-safe first, then align train-targets - never let a new re-rank degrade untrained baseline.
