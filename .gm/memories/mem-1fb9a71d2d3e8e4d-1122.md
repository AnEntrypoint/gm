---
key: mem-1fb9a71d2d3e8e4d-1122
ns: default
created: 1779825493458
updated: 1779825655405
---

iter56 (2026-05-26, 0.1.590): closed BOTH recurring deviation classes structurally. (1) long-gap-on-boot fix WITNESSED deployed+working: gm-plugkit 2.0.1454 refreshed the installed wrapper on fresh bun-x boot, .gm/last-instruction-ts reset to boot-time (~29s pre-dispatch not stale), first instruction dispatched with NO long-gap denial. The per-iter boot false-deviation is gone. (2) ROOT-CAUSED + FIXED the complete-chain-poll noise (rs-plugkit 41b8326, build green 8e61015): it fired for empty-body cron/stop-hook probes on terminal chains (no session_id), inflating deviations_30m every COMPLETE without being agent action. Added && session_id_opt.is_some() so only AGENT re-polls flag; hook probes no longer count. Preserves the iter51 real-agent-poll detection. Together these two fixes mean deviations_30m now reflects genuine agent behavior, not boot/hook artifacts - operational-perfection signal cleaned. LESSON: distinguish hook-internal dispatches (empty sess) from agent dispatches (sess present) when counting deviations; and reset gap-clocks on the mechanism (boot) that caused the gap, not blame the agent.
