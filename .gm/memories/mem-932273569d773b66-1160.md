---
key: mem-932273569d773b66-1160
ns: default
created: 1779706064647
updated: 1779706110182
---

Loop iter (2026-05-25): shipped BAD-MEMORY-PRUNING (user's top priority). The file-vec store was append-only with NO delete primitive (no host_kv_delete, no fs_delete in wasm). Added: host_kv_delete host import (gm 6a4a56b2 wrapper — deletes key's text + -vec embedding sibling across enabled discipline dirs so no orphan embedding survives) + memorize-prune verb (rs-plugkit 1c0ca87). Two modes: explicit {key}/{keys:[...]} hard-deletes named mems + emits memory.pruned; {query} is REVIEW-ONLY returning vector_top_k candidates with keys for agent judgment, then re-dispatch with stale {keys}. Query mode never auto-similarity-deletes — a blind similarity delete is itself a bad-memory generator, so the destructive step stays agent-judged. Dispatch via spool verb memorize-prune. NEEDS cascade: the verb is in the wasm (rebuilding) and host_kv_delete is in the wrapper (installed to ~/.gm-tools); end-to-end dispatch validation gated on plugkit-wasm rebuild pairing both. STILL OPEN: tree/lora validation, codeinsight-index-autobuild (codeinsight-vec empty), AGENTS.md-staging migration, watcher-dies-under-browser-load, paper sections wasm/rslearn/exec.
