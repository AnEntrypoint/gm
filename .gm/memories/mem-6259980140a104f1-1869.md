---
key: mem-6259980140a104f1-1869
ns: default
created: 1779776178939
updated: 1779776244076
---

Loop iter28 (2026-05-26, plugkit 0.1.579 live): fixed a META-OBSERVABILITY bug that was corrupting the audit itself. ccsniff JsonlReplayer._push (src/index.js:128) stamped every streaming_progress event with Date.now() instead of the JSONL events real timestamp. CONSEQUENCE: --git-discipline (and all time-window consumers) showed scan-time not command-time; displayed ts drifted forward each invocation (06:06->06:07->06:07:50); --since 30m NEVER aged out historical deviations, so every audit window re-surfaced already-corrected violations as if fresh. This directly sabotages the users "study the past 30 minutes" workflow. FIX (ccsniff 9fc80de->167a266): thread Date.parse(e.timestamp) through _push as eventTs param (live-stream callers fall back to Date.now()). Validated: 1h window now shows real 05:37/05:39; 15m window ages out the 32m-old iter26 deviation (0 violations). LESSON: when an audit tool mis-timestamps, the audit loop chases ghosts — verify the observability layer timestamps by source-event time, not wall-clock. Also iter28: learn_status verb validated REAL on 0.1.579 (returns text_rows/vec_rows/balanced per namespace via host_kv_query, default 60/60, AGENTS.md 9/9 - NOT the old fixed note string; iter26 Nothing-Fake fix confirmed in practice). git_push rebase-on-rejection live (0.1.579). RECURRING version-transition fragility: cascade-publish -> bun x spool downloads+installs new wasm to ~/.gm-tools OK -> but the auto-respawn leaves NO live watcher (empty status.json); reliable recovery is manual node ~/.gm-tools/plugkit-wasm-wrapper.js spool. The respawn-after-install step is the persistent weak link across kill-stale/watcher-dies-on-publish/wrapper-refresh findings - all share: bootstrap branches skip the live-watcher-start when the daemon was just killed mid-transition. rs-learn 5 cores still unwired (named open advance).
