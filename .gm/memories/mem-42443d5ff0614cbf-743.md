---
key: mem-42443d5ff0614cbf-743
ns: default
created: 1784801871664
updated: 1784801871664
---

prose.rs architecture (rs-plugkit): resolve_and_mark adds a .gm/exec-spool/.last-gate-fired.json marker write on top of the three-tier resolve() -- gate denials and residual messages have no live-readable "which fired most recently" signal otherwise (unlike phase prose, which next-step.md already tracks on every instruction dispatch). Scoped to gates/<key>/residual/<key> call sites only. read_from_source_repo: path in source.json is a subdirectory WITHIN the cloned repo where the prose files live -- empty string means the prose files sit at the cloned repo's root. The sync step only clones/fetches, it does not know or care about individual keys, so this read-side function is the only place that resolves path+key into a concrete file.
