---
key: mem-1780063833645-6-1904
ns: default
created: 1780063841679
updated: 1780063915716
---

BROWSER BLANK-TAB INVESTIGATION (2026-05-29, gm-plugkit 2.0.1505): user reported the browser verb pops a blank tab then works in a separate tab (wasted resources). LIVE WITNESS FALSIFIED THE 2-TAB PREMISE - the current wrapper is ALREADY single-tab. Cold-spawn proof: deleted a session (chrome graceful-closed via the chrome-fix, ports.json->{}), fresh session new -> new chromium, queried raw CDP http://127.0.0.1:<port>/json BEFORE any goto -> PAGE TABS=1 (chrome://newtab/ only; the iframe + service_worker targets are not tabs). page.context().pages().length=1, browser.contexts()=1ctx/1page. goto reuses the same page (1->1, no new tab). Repeated session new returns the SAME pwSessionId (fast-reuse, no new tab). So playwriter 0.2.0 `session new --direct` ADOPTS chromium's existing initial NTP page rather than opening a second - no orphan blank tab exists to combine. playwriter session new --direct has no page-reuse CLI flag (only --host/--token/--browser/--direct) but its default --direct behavior already adopts the existing target. NO CODE SHIPPED (Nothing Fake): adding speculative close-orphan-blank code for a non-reproducing 2-tab case would net-grow maintenance and risk closing the working page. LIKELY EXPLANATION of the user's observation: either an older wrapper predating recent browser fixes, OR a headed-mode visual where chromium's NTP renders first and the agent's page.goto navigates THAT SAME tab (looks like a 2nd tab loading but is one tab navigating). DIAGNOSTIC FOR RECURRENCE: if PAGE TABS>=2 on a cold-spawn /json query is ever witnessed, the ready fix is to dispatch a playwriter eval post-attach (spawn-path only) closing extra about:blank pages via page.context().pages(), guarded never-close-last + only-about:blank + tolerate-failure. Also re-validated this fire: the chrome-fix session-delete graceful-close (ports.json cleared, chrome killed) works on 2.0.1505.
