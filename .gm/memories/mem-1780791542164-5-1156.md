---
key: mem-1780791542164-5-1156
ns: default
created: 1780791544230
updated: 1780811939161
---

Audit-loop WORKFLOW self-deviation (iter26, 2026-06-07): the audit-loop.js workflow's OWN scan:version-source subagent ran native Grep ("releases/latest" over C:\dev\gm, glob **/*.js) at 00:13:07 instead of codesearch -> ccsniff --search-discipline flagged it (1 violation, own-session 7d37a7ed). This is distinct from the iters22/23 auditor-reflex (my own bun-x invocations were CLEAN this fire - that rule held). The leak moved INSIDE the workflow: the scan:version-source prompt says "verify every version-resolution path in gm-plugkit/plugkit-wasm-wrapper.js, lib/skill-bootstrap.js, gm-plugkit/bootstrap.js" which makes the subagent reach for native Grep to inspect those files. STRUCTURAL FIX (durable, not memorize-only): edit .claude/workflows/audit-loop.js so the scan:version-source AND scan:residual prompts explicitly instruct the subagent to route all file/pattern lookup through the codesearch verb (or recall), NEVER native Grep/Glob/find, since the workflow runs inside the [gm] audit cwd and ccsniff scores its subagents' tool calls. The workflow that audits search-discipline must not itself violate search-discipline. Fix shipped iter26.
