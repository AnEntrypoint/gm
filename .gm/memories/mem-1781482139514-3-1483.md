---
key: mem-1781482139514-3-1483
ns: default
created: 1781482139516
updated: 1781482210076
---

AUDIT-WORKFLOW RUN + NATIVE-SEARCH GATE (corrected scope, 2026-06-15; supersedes pruned mem-1781473932058): ran the audit-loop workflow in practice (wf_c9f2c3ab-ab2, 6 scans, validates supervisor-health scan 7ad8f1c2 executes); it caught two of the auditor's own self-deviations - search-discipline native grep -rl + continuation deferred-intent - both via ccsniff post-hoc. FIX gm 44c1fac2: added deviation.native-search-bash to lib/spool-dispatch.js checkDispatchGates (blocks grep -r/-R/-rl/-rn, --recursive, standalone rg; detector tested 7/7 block + 9/9 allow incl pipe-grep/grep --color/rg --version). CORRECTED SCOPE (do NOT over-claim 'structural regardless of LLM'): checkDispatchGates is DORMANT in Claude Code - .claude/settings.json={} (no PreToolUse hook) and no caller invokes it for operation='bash', so the JS dispatch gates (native-search-bash, bash-git-bypass, spool-poll, write-before-instruction) fire ONLY in a harness that wires checkDispatchGates (gm's own CLI). In CC the live enforcement is SKILL prose + ccsniff post-hoc (the layer that actually caught the grep) + rs-plugkit gates.rs (spool VERBS only, not harness Bash). A harness Bash tool call is un-gateable without a per-harness hook. DECISION: not wiring a CC PreToolUse hook (every-tool-call blast radius would risk wedging the audit loop; non-portable; ccsniff post-hoc is the portable enforcement). The gate code is valid where wired; its CC value is defense-in-depth, not the primary enforcement.
