---
key: mem-1779615374280-0-822
ns: default
created: 1779615374282
updated: 1779615683039
---

Live-link workflow for anentrypoint-design consumers (freddie, thebird): edits in C:/dev/anentrypoint-design propagate without npm publish. (1) Source edit + `npm run build` regenerates dist/247420.{css,js}. (2) freddie via npm link: `cd C:/dev/freddie && npm run link-local-design` (idempotent); express static at /vendor/anentrypoint-design/* serves the symlinked dist. Revert via `npm unlink anentrypoint-design && npm install`. (3) thebird via copy-snapshot: `cd C:/dev/thebird && npm run refresh-design:local` runs `node scripts/refresh-design.mjs --local` (default path C:/dev/anentrypoint-design; override via --local /path or ANENTRYPOINT_DESIGN_LOCAL env). .version stamp records `source: local:<path>` vs `source: npm:<pkg>@latest`. Default `npm run refresh-design` still uses npm@latest for reproducible builds.
