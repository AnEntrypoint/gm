---
key: mem-8727054bbc11e613-944
ns: default
created: 1789377910343
updated: 1789377910343
---

gm MCP CONNECT_TIMEOUT (30s) root cause: the server registration was `npx -y github:AnEntrypoint/gm-mcp`, which re-resolves the git ref over the network and reinstalls on every connect -- measured 2026-09-14: 8.2s warm npm cache, 22.2s fully cold (npx --cache <empty>), vs 0.19s launching the same bundle locally (node ~/.gm-tools/gm-mcp-server.mjs) and 0.34s via the project-scope `node -e` homedir-resolving launcher. Fix: bin/gm-install.js vendors the bundle to ~/.gm-tools/gm-mcp-server.mjs, registers node <local path> (user scope ~/.claude.json) or the node -e launcher (committed .mcp.json), and migrates legacy npx entries; `npx github:AnEntrypoint/gm -g --mcp-only` repairs without reinstalling skill/runner. A running Claude Code session keeps the registration it connected with -- restart required. Measure cold start with a real MCP initialize over stdio (spawn command, write initialize, time first stdout line), never by guessing.
