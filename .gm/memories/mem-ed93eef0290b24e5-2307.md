---
key: mem-ed93eef0290b24e5-2307
ns: default
created: 1782733124377
updated: 1782733366267
---

idempotency contract per-verb convergence (gm-method, f∘f≡f): the gm spool dispatch layer is AT-LEAST-ONCE by design -- the in-memory processed Map (plugkit-wasm-wrapper.js processFile, keyed by verb/N relpath, unmarked right after the input file is unlinked) guards only concurrent double-pickup, NOT cross-time replay (re-Write same N after consume) nor watcher-restart. Correctness therefore rests on every state-mutating verb being individually CONVERGENT: memorize + memorize-fire use a content-hash key (mem-{fnv1a64(namespace|text)}-{len}) + a host_kv_read dedup early-return (returns deduped:true); git_finalize/git_commit return nothing_to_commit or already-pushed on a clean/ahead-0 tree; insert_edge (rs-learn temporal_core) kv.put overwrites by id and append_index dedup-guards before appending; invalidate_edge early-returns if already invalidated earlier; ensure_managed_gitignore (rs-plugkit) strips+rebuilds the managed block and gates the write on changed=cleaned!=original (fixed point in one step); codeinsight rebuilds only on digest-mismatch (same digest serves cache); publish.yml skips already-published npm versions and gates the plugkit.version commit-back on git status --porcelain. Read-only verbs (recall, codesearch, git_status/log/diff, instruction, phase-status, health, filter) recompute every dispatch and must NEVER be served from a cache (time/index-sensitive). exec_js and browser re-run on replay -- at-most-once-by-NATURE, since they execute arbitrary user code/actions; a content-sha dedup ledger was evaluated and REJECTED as net-additive (new state + failure modes to make already-convergent verbs 'more' idempotent, while unable to make exec_js/browser idempotent). HISTORICAL DEFECT now fixed: the `memorize` verb (wasm_dispatch.rs memorize_with_raw) used a timestamp+counter key (mem-{now}-{counter}-{len}) with no dedup -- non-idempotent, two rows for the same text -- diverging from memorize-fire's content-hash path; converged to the content-hash + dedup contract, dead MEMORIZE_COUNTER removed. test.js carries the real-services witness: memorize-fire(X) twice -> deduped:true with the same key. Witnessed by process-of-elimination: spool-probing out/memorize-50.json (timestamp key) vs out/memorize-fire-51.json (content-hash key) exposed the divergence.
