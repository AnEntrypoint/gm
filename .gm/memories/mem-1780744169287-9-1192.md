---
key: mem-1780744169287-9-1192
ns: default
created: 1780744169288
updated: 1780744271464
---

rs-learn self-report core internals (drained from AGENTS.md 2026-06-06): the agent IS the LLM rs-learn calls - every quality score, relevance call, prune decision, route outcome, loss signal is the agent deciding inline and reporting through the spool; rs-learn never reaches out to a separate judge model. The internals each self-report feeds: (1) MicroLoRA adapter trains from agent-self-reported trajectory-quality via learn{feedback, embedding, payload:{quality}} - a rank-2 learned scoring head reading a bge-small embedding, emits per-target logit B-(A-embedding), sona-style (scores targets from embeddings, does NOT reshape the vector); consuming its score in recall re-ranking is the open integration. (2) Distinct FastGRNN model-selection router takes learn{record_outcome, target:<model id>, quality}. (3) bad-memory pruning = agent judging a recall hit stale -> memorize-prune{key}. (4) deep core takes agent record_loss. (5) attention takes nudge_relation. None wait on a host-exposed signal because the agent already holds it. VERIFY closes the training loop. Encourage heavy recall+learn so judgments are grounded not guessed. Recall key: 'rs-learn self-report core internals'.
