---
key: mem-1780839388362-3-1684
ns: default
created: 1780839392357
updated: 1780839585993
---

ROOT-CAUSE FIX SHIPPED for AppleDouble pollution (iter36, 2026-06-07): the rs-codeinsight indexer's TWO file-walks now skip ._* AppleDouble files - src/lib.rs:150 (the language-file collector, added `if file_name.starts_with("._") { continue; }`) AND src/models.rs:32 (the ignore-crate walker's filter_entry, added `if name.starts_with("._") { return false; }`). So the indexer will NEVER emit phantom empty-chunk manifests for AppleDouble sidecars again. Version 0.3.7, cargo build passes, pushed to rs-codeinsight origin (97bcd7e..3ce30cb) = triggers the cascade rebuild. This is the ROOT cause the gm-side gitignore rule (iter34) was containing as a symptom; now fixed at source. PROCESS NOTES: (1) git_finalize {repo:"C:/dev/rs-codeinsight", message} works for sibling repos but its push got rejected because a concurrent CI auto-bump moved rs-codeinsight main to 0.3.6 - I rebased onto origin/main, bumped my version to 0.3.7 (above remote), resolved the Cargo.toml version conflict, and pushed via git_push {repo, branch}. (2) CAVEAT: after `git rebase --continue` with `git add` of a still-conflicted Cargo.toml, the conflict MARKERS got committed - I caught it via grep -c '<<<<<<<' (count 2), fixed via Edit tool to keep version 0.3.7 + strip markers, cargo-build-verified clean, amended. ALWAYS grep for conflict markers after a rebase --continue before pushing. (3) the long-gap gate fired twice during the multi-step rebase resolution (>5min each) - reset via instruction each time, correct recovery. rs-codeinsight version source: Cargo.toml is authoritative; concurrent CI auto-bumps it [skip ci] so an audit-session edit must rebase + bump above whatever the CI landed.
