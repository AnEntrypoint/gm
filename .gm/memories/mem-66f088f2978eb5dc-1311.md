---
key: mem-66f088f2978eb5dc-1311
ns: default
created: 1784525548086
updated: 1784525548086
---

gm-method: a stale gm-plugkit watcher (gm_plugkit_stale flagged, self-upgrade-respawn-exhausted -- e.g. 2.0.1972 unable to reach latest because bun/npx cache serves the old tarball) returns CORRUPT/MISLABELED spool verb responses: prd-resolve echoes unrelated 'resolved' ids that are not the id you passed (and are not even present in .gm/prd.yml), and git_finalize {repo:<sibling>} returns a sha+summary from the WRONG repo (a cached prior commit of the cwd repo, not the target). The underlying filesystem/git mutation may still be CORRECT despite the wrong response body. Recovery: never trust the stale watcher's response JSON -- verify the real outcome by DIRECT git inspection in each target repo (git log --oneline, git rev-parse HEAD, git status --porcelain, git merge-base --is-ancestor <sha> origin/main, git ls-tree origin/main). Also compounded by a concurrent second gm session driving the SAME cwd .gm store (single-writer hazard: prd_total_count and phase shift between two close-together instruction reads, ready_wave changes, unexplained .gm/prd.yml modification) -- do NOT race PRD bookkeeping into a contended+stale store; commit the sibling-repo deliverable via git verb (which still lands correctly on disk) and verify by direct git, rather than relying on the orchestration store's ledger.
