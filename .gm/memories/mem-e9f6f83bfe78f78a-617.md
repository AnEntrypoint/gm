---
key: mem-e9f6f83bfe78f78a-617
ns: default
created: 1779826965434
updated: 1779827039591
---

BEHAVIORAL NOTE (iter57): heavy EXECUTE stretches (multiple commits + CI build-watches + prune scans) routinely exceed the 300s long-gap threshold between instruction dispatches, tripping a LEGITIMATE long-gap-no-instruction (sess=agent, not boot/hook). Unlike the boot gap (fixed by last-instruction-ts reset), this is real agent idle-on-instruction during work. MITIGATION: interleave an instruction dispatch during long work stretches (after a commit+push cycle, before the next), to keep the gap clock fresh. The gate is correct; the discipline is to re-anchor on instruction more often during dense EXECUTE work.
