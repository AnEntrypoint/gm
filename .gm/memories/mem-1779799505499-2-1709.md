---
key: mem-1779799505499-2-1709
ns: default
created: 1779799505500
updated: 1779799590177
---

iter43 (2026-05-26): fixed two home-page (GH Pages) visual bugs, user-reported, browser-witnessed on the LIVE deployed page. BUG1 Star on GitHub CTA sat 19px lower than sibling buttons: root cause .work-detail-chips was display:block so the inline-flex buttons aligned by baseline and the accent .btn-primary baseline differs from .btn (witnessed: star top 467 vs siblings 448). FIX theme.mjs inline style .work-detail-chips{display:flex;flex-wrap:wrap;align-items:center} -> all tops 448. BUG2 install instructions side-by-side: root cause flatspace base .cli{display:flex} (a single-line CLI-widget layout: prompt+cmd+copy in a row) applied to our MULTI-line install block (12 cli-cmt/cli-line children) laid them horizontally; witnessed .cli display:flex flexDir:row. FIX theme.mjs .cli{display:block} -> children stack (witnessed cli_stacks true). KEY PATTERN: flatspace SDK base styles (.cli, .btn, .work-detail-chips) are loaded via <link> at theme.mjs:233 BEFORE the inline <style> at 234, so same-specificity overrides in the inline block WIN by source order - use the inline block to override flatspace base widget styles when reusing its classnames for a different layout. DEPLOY caveat: gh-pages.yml fired (gm Actions works) but FAILED at Set up job - codeload.github.com could not download actions/configure-pages@v5 (GH infra error B450, Failed to download archive). SAME GitHub Actions infra incident as the rs-plugkit release-trigger outage (iter41/42) - action+tarball delivery degraded GitHub-side. Source fix committed 0ce4ad48, re-ran the failed build; LIVE deploy blockedBy:external (GitHub codeload) until it recovers. rs-plugkit fixes (4283091,09148c2) still unbuilt for the same reason.
