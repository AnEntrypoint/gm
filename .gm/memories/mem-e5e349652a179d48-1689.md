---
key: mem-e5e349652a179d48-1689
ns: default
created: 1780392146568
updated: 1780392333465
---

SUPERVISOR WRAPPER-SHA-DRIFT RECYCLE added (2026-06-02 ~09:09 audit fire). PROBLEM: a published gm-plugkit wrapper-only fix (no wasm version bump, e.g. the stall-gate 214b0e77) lands in ~/.gm-tools via ensureWrapperFresh but a HEALTHY running watcher keeps the old wrapper until restart - --kill-stale-watchers only kills wasm-version-drifted watchers, and the wrappers peer-sha-takeover only fires for cross-cwd competing watchers (not the supervisors own same-cwd child, because findSupervisorPid returns early when a supervisor is already running, so no fresh-wrapper watcher boots to trigger the peer sweep). FIX: added wrapperSha12OnDisk() + a wrapper-sha-drift recycle to checkWatcherHealth in BOTH bin/plugkit-supervisor.js (gm-skill) and gm-plugkit/supervisor.js (gm-plugkit). After busy_until + heartbeat-stale guards, compares status.wrapper_sha vs on-disk ~/.gm-tools wrapper sha12; on mismatch logs supervisor.wrapper-sha-drift + kills the child; existing child.on(exit)->spawnWatcher respawn reads the fresh wrapper. Safe: null reported sha skips (no false recycle). node --check both OK. NOTE: depends on the wrapper actually populating status.wrapper_sha (=_ownWrapperSha12, first 12 of sha256 of the gmTools wrapper, written in writeStatus line 3096) - it was observed null on pid 19940, so verify _ownWrapperSha12 computes on fresh boots; if it stays null the drift check no-ops harmlessly but does not recycle. The immediate stall-gate fix was already made live this fire by manual taskkill /F /PID 19940 -> supervisor respawned pid 440 on the fixed wrapper (verified ~/.gm-tools wrapper has 3x _lastBusyUntil, mtime Jun 2 11:11). Ships via gm-skill + gm-plugkit publish.
