---
key: mem-1780651333068-0-1647
ns: default
created: 1780651336734
updated: 1780651456367
---

TRANSITION-MATRIX VALIDATION (2026-06-05, live on running wasm 0.1.635 + transitions.rs audit): The PLAN/EXECUTE/EMIT/VERIFY/COMPLETE matrix is: ALL forward edges legal, ALL back-edges (any->PLAN) legal, ONLY transition to=COMPLETE is gated (four-observation: worktree-clean AND remote-pushed AND prd-empty AND mutables-witnessed, each refusal names the unmet condition). transitions.rs handle() parses target and gates only Phase::Complete; every other target falls straight to set_phase_with_session, so EXECUTE->PLAN was ALWAYS legal. VALIDATED LIVE: transition PLAN->EXECUTE returned phase=EXECUTE (task 9320); transition EXECUTE->PLAN returned phase=PLAN, accepted, NO deviation (task 9321) - the primary-bug edge is gate-legal. So the EXECUTE->PLAN re-entry bug was NEVER legality - it was (a) EXECUTE instruction prose only named the back-edge for a new unknown as a terse clause while Re-expand-on-discovery pushed prd-add-and-stay, so a RESHAPING discovery (decision changing an existing rows scope) had no named move and the agent narrated re-scope and stranded; (b) prd::handle_add always pushed, so re-adding an existing id DUPLICATED the row (witnessed live: re-add id enumerate-paper-transition-matrix on 0.1.635 returned {added} not {rescoped}, grep -c id in prd.yml = 2). FIX: rs-plugkit PR #6 (https://github.com/AnEntrypoint/rs-plugkit/pull/6) adds the Planning-event re-entry section to execute.rs (additive discovery->prd-add+stay vs reshaping discovery->transition to=PLAN+re-cut), makes handle_add upsert by id ({rescoped} on existing id, preserves position+handle), documents re-scope contract in plan.rs. Ships via cascade.
