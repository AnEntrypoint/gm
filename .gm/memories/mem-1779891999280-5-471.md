---
key: mem-1779891999280-5-471
ns: default
created: 1779891999280
updated: 1779892119949
---

Gate enforcement layer (gm spool-dispatch architecture): the CLI layer (plugkit, rs-exec, downstream platforms) calls checkDispatchGates() before tool execution; on denial the reason text surfaces to the model. Bootstrap (lib/skill-bootstrap.js) handles daemon initialization and marker setup. Marker-driven dispatch replaces the hook event pump entirely - no session event callbacks needed. Orchestration state is tracked via marker files in .gm/ instead of hook events.
