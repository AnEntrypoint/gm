---
key: mem-474f9787e8638e91-1859
ns: default
created: 1780395529575
updated: 1780395581043
---

ROOT CAUSE of wrapper_sha=null FOUND+FIXED via instrumentation (2026-06-02 ~10:08 audit fire). The diagnostic logEvent I added last fire (8b671982) immediately revealed it on the next bun boot: gm-log watcher.own-wrapper-sha-failed pid=15208 error="require is not defined". plugkit-wasm-wrapper.js is ESM (import crypto/https/net/child_process at top) but used INLINE require() for node builtins in 10+ sites. Under bun (the watcher runtime - supervisor resolveRuntime prefers bun), require is NOT a global in ESM, so every inline require threw - and most sat in catch(_){} so the failure was SILENT. This broke, ONLY under the bun watcher: _ownWrapperSha12 (status.wrapper_sha=null -> the supervisor wrapper-sha-drift recycle I added 2f98ee4e was inert), _wrapperShaAtBoot + its 5s self-drift-restart check (the wrappers OWN restart-on-wrapper-change mechanism, DEAD), the synthetic-session cwd-hash (440), and the file-index sha (491). Node-run watchers worked (require available via CJS interop) which is why it was intermittent + hid for months. FIX: replaced all inline require(builtin) with module ESM imports - crypto x5 -> existing import crypto; net x3 -> _netModule; http x2 -> _httpModule (added import http); https x1 -> _httpsModule; child_process x4 -> _childProcess namespace (aliases avoid TDZ shadowing the named imports). grep require( node-builtins = 0. node --check AND bun parse both pass. Added AGENTS.md hard rule (ESM wrapper never inline-requires a builtin). METHODOLOGY: the silent catch(_){} hid this for ages - instrumenting the catch (last fire) cracked it in ONE boot. A silent catch on a value feeding a gate is a root-cause black hole; always log the error. Ships via gm-plugkit publish; once live, wrapper_sha populates -> supervisor recycle becomes functional -> the whole wrapper-only-fix-propagation chain works end to end.
