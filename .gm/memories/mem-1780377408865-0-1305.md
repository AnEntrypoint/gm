---
key: mem-1780377408865-0-1305
ns: default
created: 1780377408867
updated: 1780377472229
---

CODESEARCH ABSENT-DIGEST STALE FIX (2026-06-02, reported by a foreign session: codesearch surfaced deleted files exec_delegate.js/exec_power_up.js/src/add_satellite.js). ROOT CAUSE: the 2026-05-31 staleness fix (mem-1780234310513) added digest mismatch detection but wasm_dispatch.rs:524 wrote `let stale = match stored { Some(s) => s != current, None => false }` -- an index built by a pre-store_digest plugkit has NO __digest__ row, so stored_digest()=None, so stale=false, so it served deleted-file rows forever and never rebuilt (the empty-index autobuild also doesn't fire since the index is non-empty). Absent provenance is the MOST stale case, not the freshest. FIX (rs-plugkit 540c32c): None => true, plus reason=digest-absent emit (vs digest-mismatch) for observability. Validated locally: codesearch{rebuild:true} forces clear_codeinsight+index and the deleted symbols returned ZERO deleted-file rows. Loop guard: retry sets auto_indexed=true + index() stamps store_digest so None=>true cannot loop. git-fail edge: current_digest stamp-and-compare is symmetric (empty HEAD self-consistent), no thrash. rs-search was NOT implicated (pure ranking/fusion lib, no index-cache); report's 'rs-search must be fixed' was imprecise -- the staleness defect is entirely rs-plugkit code_index+wasm_dispatch.
