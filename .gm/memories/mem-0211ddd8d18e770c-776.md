---
key: mem-0211ddd8d18e770c-776
ns: default
created: 1783690753790
updated: 1783690753790
---

## Resolved mutable: playwriter-latest-hangs-this-env

Root-caused via direct execution: `node ~/.bun/install/cache/playwriter@0.4.0@@@1/bin.js --version` runs instantly and correctly, proving the playwriter binary itself is fine -- the hang is specifically bun's `x` subcommand wrapper in this environment. Fixed in gm-plugkit/plugkit-wasm-wrapper.js findBrowserRunner (new findCachedBunRunnerBin helper) to resolve the cached bin.js directly via node instead of routing through `bun x <pkg>@latest`, which also eliminates the needless re-resolve-every-dispatch behavior. Not narrated as user-facing fix; this is an internal runner-invocation defect discovered while investigating the browser session/profile symptoms, tracked as its own PRD row (browser-runner-bun-x-hangs).
