---
key: mem-5c649c8cadf4a231-1147
ns: default
created: 1784803522937
updated: 1784803522937
---

gm AGENTS.md drain: agentplug-runner self-update split into two independently-gated halves inside daemon.rs's main loop -- polls plugkit-bin's Releases API every 600s (runner_update_poll_interval, configurable via ~/.agentplug/daemon-config.json) for the wasm it serves; a version mismatch triggers download+verify+atomic-swap of the cached Module and a live in-process reload, no restart needed. The runner EXECUTABLE itself never self-updates via that poll -- self-update is bin/install.js re-run OR the takeover-handoff protocol (stage a verified <exe>.new via stage_runner_self_update, unconditionally every poll regardless of load; hand off via attempt_self_update_handoff only on a genuinely idle tick, spawning the staged binary as `takeover <version>` which pre-builds its own engine+plugins before signaling ready, at which point the OLD process releases ownership and exits). Full incident-level mechanics (IN_FLIGHT race fixes, ownership TOCTOU close, wasm-memory-monotonic reclaim, Instant-underflow panics) already in the recall store from the agentplug comment-sweep pass -- query 'agentplug daemon.rs' for the five detailed entries.
