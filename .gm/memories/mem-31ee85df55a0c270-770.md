---
key: mem-31ee85df55a0c270-770
ns: default
created: 1784800674564
updated: 1784800674564
---

code_index.rs history (rs-plugkit): call_plugin() routes libsql/bert/treesitter through host_plugin_call since these became out-of-process shared plugins; code_index.rs no longer defines its own private mod libsql_wasm (that was a drifted-stale shadow copy never used by pipeline.rs) -- it uses crate::libsql_wasm directly. clear_codeinsight_full() clears codeinsight/-vec/-manifest KV namespaces but must ALSO DELETE FROM code_chunks (the real SQL symbol table) -- a rebuild:true dispatch that only cleared KV manifests left code_chunks rows for long-deleted files (gm-plugkit/supervisor.js etc, git-rm'd in commits b03333811/68d3c1d93) surviving indefinitely across rebuilds, since the empty prior manifest gave the file-deletion-prune loop nothing to compare against.
