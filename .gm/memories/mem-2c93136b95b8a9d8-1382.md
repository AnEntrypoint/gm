---
key: mem-2c93136b95b8a9d8-1382
ns: default
created: 1780561462483
updated: 1780561620116
---

codeinsight thrash ROOT CAUSE + FIX (rs-plugkit src/code_index.rs, fetched 2026-06-04): current_digest() = git rev-parse HEAD + crc32(git_porcelain()). The crc32 over the FULL porcelain means ANY uncommitted change flips the digest - including transient runtime files (.gm/exec-spool/*, .gm/witness/, prd.yml churn, mem-*.json from memorize). index() then calls clear_codeinsight() and re-embeds ALL chunks of ALL ~98 files (~280s in-wasm bge), blocking the watcher heartbeat. Concurrent loop sessions perpetually dirty the tree so the digest never stabilizes -> perpetual full rebuilds. FIX OPTIONS (rs-plugkit, ships via cascade): (1) digest-stability - exclude transient/non-indexable paths from the porcelain hash in current_digest() (filter out .gm/, and any path whose ext is not in lang_for_ext), so only indexable-source changes invalidate the index; (2) gate rebuild on whether any indexable-extension file actually changed; (3) true incremental - per-file content-hash in code_chunks, re-embed only changed files (bigger rewrite). Option (1) is smallest/safest. CAVEAT: the digest also feeds sync-before-emit staleness; changing it must not let a real source edit serve a stale index. This is a focused rs-plugkit chain, not buildable/testable locally (cargo build forbidden, push-and-cascade only). Exact site: code_index.rs current_digest() + index() clear+rebuild path.
