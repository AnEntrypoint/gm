---
key: mem-59d5b49973c13e27-897
ns: default
created: 1789414976181
updated: 1789414976181
---

agentplug >= 0.1.136: the implicit cdp/browser page is keyed by the dispatching gm session, derived in agentplug-host/src/dispatch_origin.rs from a JSON body's session_id or the spool task id (<session>-<N>, or gm-mcp's <session>-<pid>-<unix_ms>-<counter>); the daemon sets it as a thread-local scope around each dispatch. Page precedence in browser::run: sessionId= line > JSON envelope sessionId > gm session > guest-resolved id > "default" (only when no gm session is derivable). session list reports owner_gm_session/owned_by_caller; owner persists in chrome.owner-gm-session sidecar. Steel sessions set claimFreshTarget so they never adopt another session's tab. serp (oxibrowser) still has one page per wasm instance, not partitioned. Live-verify runner changes by pushing agentplug main, waiting for release.yml, then dispatching plugin-refresh {runner:true} to make the daemon self-update.
