---
key: mem-ac76896759152c36-2069
ns: default
created: 1780038298292
updated: 1780038344554
---

Audit-loop iter (2026-05-29, 0.1.616): cron-fired audit. Stack steady-state CONFIRMED — gm + rs-plugkit/rs-exec/rs-codeinsight/rs-search all clean, ahead=0 (origin reflects HEAD). Zero plugkit deviation.* events in gm over 45m/6h; only 1 long-gap-no-instruction in 24h (05-28 07:13, predates the ec6a751/3cbb933 gate fix). VALIDATED the long-gap wait/sleep exemption (gates.rs:86 LONGGAP_EXEMPT=[health,auto-recall,wait,sleep], L93-98 two-condition fire, L299-301 time-based burst dedup, L390-440 six unit tests) — commit 3cbb933 sound. The 8 ccsniff --search-discipline violations this window are ALL in CONSUMER repos (guru-attendance x4, guru-gcs x1, tv8 x3 incl one until-grep spool-poll antipattern), NOT gm — these are downstream harness users misusing native grep; emit no plugkit deviation so only ccsniff catches them. OPEN WORK left for next iter (chain was mid-EXECUTE when harness Bash/Read output channel failed): (1) rs-learn .gitignore managed block was MISSING the runtime-junk entries causing 10 untracked residuals; applied Edit adding .gm/browser-profile/ .gm/exec-spool/ .gm/gm-fired-this-turn .gm/historical/ .gm/imported-claude-md-*.md .gm/mutables.yml .gm/prd.yml .gm/turn-state.json.corrupted-* .code-search/ to the block (kept .gm/rs-learn.db + .gm/code-search/ un-ignored). NEEDS: verify check-ignore, then commit+push rs-learn. (2) rs-learn/.gm/rs-learn.db shows ' M' — triage whether legitimate memory write or cross-project leak. (3) gates.rs has dead parse_retry_state (L62, only v2 used) + duplicate test (L391 vs L429 identical) — delete. (4) gates.rs/all rs-plugkit src carry ~530 rust comments; rs-plugkit AGENTS.md is @../gm/AGENTS.md so inherits 'No comments in code' — but mass-strip across gate/dispatch core needs its own dedicated CI-build-witnessed run, NOT a single audit window. LESSON: when the harness output channel fails mid-chain (Bash/Read return empty), do NOT fabricate witnesses; record state to memory and let the next cron iteration boot-probe fresh and recover. The cron job faa9e4ab fires every 30min.
