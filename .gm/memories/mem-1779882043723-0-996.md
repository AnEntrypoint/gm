---
key: mem-1779882043723-0-996
ns: default
created: 1779882043723
updated: 1779882363048
---

iter116 (2026-05-27): VALIDATED the busy_until fix LIVE. git_finalize ran 28.6s (the duration that VERB-ABORTed twice in iter114) and completed clean with NO abort; .status.json showed busy_until stamped ~82s ahead (the wrapper _writeStatusBusy(90000) before git network verbs) counting down through the push. Wrapper-side fix confirmed on fresh watcher 458504. DEPLOY NUANCE worth remembering: gm-plugkit JS fixes (wrapper+supervisor) do NOT hot-deploy - the bun x reboot refreshes the on-disk installed wrapper but a HEALTHY running watcher keeps its old in-memory wrapper (the new daemon defers via peer-stale-takeover). To deploy a wrapper fix you must KILL the specific running watcher pid so the supervisor respawns it loading fresh wrapper. The SUPERVISOR process is even longer-lived (shared across projects) - its code change is on-disk-pending its own restart, not killed by --kill-stale-watchers. installed != running, twice over (watcher in-memory wrapper, supervisor in-memory code).
