---
key: mem-1782203120785-3-521
ns: default
created: 1782203121574
updated: 1782203390592
---

## Resolved mutable: browser-blank-fix-design

plugkit-wasm-wrapper.js:1993-2013 parses timeout= then capture= prefixes off evalBody, then :2015 passes evalBody to the runner -e. Mirror: parse a 'url=<url>' prefix (and a bare ^https?://\S+$ body) and prepend `await page.goto(url,{waitUntil:'load',timeout})` to the user script, BEFORE the capture wrap so capture also records the navigation. Default path (no url=/bare-URL) byte-unchanged. Line 1018 about:blank launch stays (per-dispatch navigation is the right layer).
