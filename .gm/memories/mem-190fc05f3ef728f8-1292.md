---
key: mem-190fc05f3ef728f8-1292
ns: default
created: 1785235713131
updated: 1785235713131
---

gm cascade pin-toil design: gm.json.plugkitVersion is informational-only, never a functional pin -- no JS code path reads it, resolveLatestRemoteVersion() overrides gm-plugkit/plugkit.version at runtime regardless. publish.yml's drift-check no longer hard-gates on it. Every wasm download is still verified via bootstrap.js::fetchRemoteSha() fetching the sha256 sidecar published alongside the EXACT resolved release tag, falling back to the committed manifest only when network fails AND version genuinely matches. Submodule pointers auto-sync via .github/workflows/submodule-sync.yml (30min schedule, git submodule update --remote --merge, auto-commit) instead of hand-bumped chore commits -- reproducibility preserved because each commit still pins exact SHAs, only main's HEAD advances automatically. Three near-identical agentplug-plugin release.yml (bert/libsql/treesitter) consolidated onto one reusable workflow_call hosted in rs-plugkit (wasm-plugin-release.yml) -- caveat: pass multi-line build_env/pre_build_steps/build_command through env: vars written to temp script files, never raw ${{ inputs.x }} string interpolation inside a run: block, since that mangles embedded double-quotes (e.g. -DMAP_FAILED=((void*)-1)) and produces 'unexpected EOF while looking for matching quote'.
