---
key: mem-fc54ef09000e8049-1278
ns: default
created: 1779987776318
updated: 1779988119673
---

long-gap gate fix (rs-plugkit ec6a751, 2026-05-28): the long-gap-no-instruction gate was the dominant deviation (29/66 over 24h, almost all verb=browser, gaps 300k-635k) because gates.rs reset its clock only on orchestrator verbs (instruction/transition/prd-*/mutable-*), so a rapid succession of active work verbs (browser/exec_js/codesearch) tripped the 300s gate even though the agent was working, not idle. FIX: gates.rs now stamps .gm/last-dispatch-ts on every gated work verb (capturing the PRIOR value first) and long_gap_should_fire(last_instruction,prev_dispatch,now,300k) fires only when BOTH >300s since last instruction AND >300s since the previous dispatch of any verb. Active back-to-back work keeps prev_dispatch fresh -> gate quiet; genuine idle (prev==0 or >300s, e.g. version download/overnight/CI-watch) still fires. Pure predicate + 4 unit tests. Also hardened prd-resolve: recover_truncated_envelope brace-free scan recovers the id from a truncated/malformed JSON body (LLM cut off mid-witness_evidence) that serde rejects, fixing the prd-resolve-unknown-id class (20/66, from imapsync/portabox). AGENTS.md prose updated: the first-verb-after-wait=instruction rule applies to genuine IDLE only; do not inject defensive instruction between active work verbs.
