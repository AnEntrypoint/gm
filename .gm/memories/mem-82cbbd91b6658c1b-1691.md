---
key: mem-82cbbd91b6658c1b-1691
ns: default
created: 1781869289392
updated: 1781869510670
---

{"facts":["INVERTED RC CHECK PATTERN (2026-06-19): host_kv_put returns 0 on success in all wasm crates. Both rs-learn/crates/wasm/src/wasm_host.rs (CRITICAL: rc!=0 was Ok -- every write silently failed) and rs-plugkit/src/wasm_dispatch.rs close+feedback verbs had inverted checks. Canonical: if rc == 0 { Ok(()) } else { Err(...) }. Audit all host_kv_put callers for this inversion.","FORGET VERB TOMBSTONE BUG FIXED (2026-06-19): rs-plugkit/src/wasm_dispatch.rs forget() was writing a tombstone string (__deleted__<ts>) to the KV store instead of calling host_kv_delete. host_kv_delete IS available in the extern block (line 17). Fixed: now calls host_kv_delete directly.","RS-PLUGKIT EMBED.RS DUPLICATE EXTERN (2026-06-19): embed.rs had a private host_now_ms extern block declaring i64, conflicting with wasm_dispatch.rs public block declaring u64. Fix: remove private block, use crate::wasm_dispatch::host_now_ms() as i64. Pattern: never redeclare a host function in a secondary extern block when the primary extern is already pub.","RS-LEARN ROUTER PERSIST TRAINED-ON-MISMATCH (2026-06-19): load_router was setting r.trained=blob.trained even when weights were NOT loaded due to dimension mismatch. Fixed: r.trained = blob.trained && weights_match. Also added empty-targets guard before Router::new to prevent wasm panic on corrupted blobs.","RS-EXEC IS_REJECTED STUB (2026-06-19): wasm_spool.rs had fn is_rejected(_lang) -> bool { false } that was never called, falsely implying rejection logic. #![allow(dead_code)] on line 1 suppressed the compiler warning. Removed both. Pattern: stubs that always return false or hardcoded values violate nothing-fake and hide real behavior gaps."]}
