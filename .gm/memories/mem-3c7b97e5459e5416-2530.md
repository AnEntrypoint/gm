---
key: mem-3c7b97e5459e5416-2530
ns: default
created: 1780736835943
updated: 1780737000463
---

Audit-loop fire (2026-06-06 ~09:00, watcher-tz): caught + fixed a REAL local version-drift bug, NOT foreign drift. Running watcher reported instance_version=0.1.634 + version_drifted=true while plugkit.version text / npm plugkit-wasm / gm.json all = 0.1.635. ROOT CAUSE: a stale 634-build wasm (size 149696367, sha b0612df2..., embeds 0.1.634) overwrote the active ~/.gm-tools/plugkit.wasm while the plugkit.version TEXT file was bumped to 635 (by ensureReady's remote-latest override bootstrap.js:845-851). The active wasm+sha were internally self-consistent (634 build + its own sha), so only the version TEXT lied. ensureReady's versionDrift check (bootstrap.js:832) compares installedVersionAtTools() = the TEXT file (635) vs remote (635) -> no drift -> isReady()&&!versionDrift early-returns (line 834) reusing the 634 wasm forever. npm plugkit-wasm@0.1.635 DOES ship a correct 635 wasm (size 149649759, sha eaef35fa..., embeds 0.1.635) = matched local .bak; so 635 published fine, the decoupling was local. FIX (committed): added a version-drift recycle to checkWatcherHealth in BOTH supervisor variants (gm-plugkit/supervisor.js + bin/plugkit-supervisor.js, copy-drift rule) mirroring the existing wrapper-sha-drift block: on status.version_drifted===true (the watcher's OWN embedded-instance-version-vs-file-version compare, already exposed in .status.json), evict stale gmTools plugkit.wasm/.version/.sha256 then kill child so the next bootstrap fails isReady() and redownloads the correct build. Net-smaller: reuses the already-computed version_drifted signal, no new dep, ~25 lines each. node --check both OK. IMMEDIATE operational restore done + VALIDATED LIVE: copied .bak (635) over active wasm, rebooted watcher -> instance_version=0.1.635 version_drifted=false fresh heartbeat. STANDING AUDIT RULE: a version_drifted=true in .status.json means the wasm BINARY embeds a different version than the plugkit.version TEXT file -- the text can lie; trust the embedded instance_version. Restore via .bak (verified-correct prior download) or evict+redownload, never trust the text file alone. Also clean steady-state otherwise: gmsniff --since 30m/2h = 0 deviations (~10th consecutive clean fire), gm conformance epsilon=0 prd-pend=0, all rs-* repos porcelain=0 ahead=0 (rs-plugkit bb480a7, rs-exec 1eb3803, rs-codeinsight 4371395, rs-search a3c8f46, rs-learn 47f2804). Watcher boot-time codesearch index sync on 149MB wasm blocks heartbeat ~5min (busy_until honored correctly; a freeze inside that window is not a crash).
