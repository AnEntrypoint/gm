---
key: mem-c89ce9999848af5a-1276
ns: default
created: 1787382637917
updated: 1787382637917
---

gm-method: discipline fiber lifecycle detail (drained from AGENTS.md, part 1). .gm/disciplines/<name>/fiber-state.json (gitignored, Inactive/Active/Unloading), advanced one transition per active_policies() call -- Active->Unloading the instant satisfaction is lost, Unloading->Inactive on the following call, giving dependents a real one-dispatch window where a leaving provider is still visible. discipline-check-removal names every other enabled discipline whose fiber has reached Active and whose requires still resolves to the target's provides -- withdrawal-ordering guard (Cordis Theorem 63). discipline-audit checks preservation (no two Active disciplines share a provides capability, enforced via ActiveFiberSet's type-safe insert), recovery exactness/ordering (via proof-carrying types fiber_lifecycle::WithdrawalComplete/SafeToWithdraw), progress, fail-closed access control. requires.json may set realm:<name> for coeffect isolation (Cordis Section 3.2.3). fiber_lifecycle.rs is kind-agnostic: disciplines, sibling wasm plugins, memory namespaces, codeinsight namespaces all instantiate it with zero module changes. check_confluence brings Theorem 73 into a live per-audit check: forward and reversed fiber transitions from the same start reach the same Active set.
