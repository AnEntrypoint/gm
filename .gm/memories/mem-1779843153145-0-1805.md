---
key: mem-1779843153145-0-1805
ns: default
created: 1779843153147
updated: 1779843769208
---

iter72 (2026-05-27, 0.1.600): fixed TWO predictability deviations in gm-plugkit/plugkit-wasm-wrapper.js. (1) AUTO_RECALL STALE-PROMPT RACE: tryAutoRecallForTurnEntry ran at wrapper:2747 reading .gm/last-prompt.txt BEFORE the wasm dispatch() at 2764 writes the fresh prompt from the instruction bodys {prompt} field - so auto_recall surfaced the PRIOR turns prompt every fire (witnessed: this fire got query=gravitational constant planetary orbital mechanics, the iter71 synthetic-test query, instead of the audit prompt). FIX: added promptFromInstructionBody(body) that parses the bodys prompt field; tryAutoRecallForTurnEntry now takes a bodyPrompt param and PREFERS it over the stale file, falling back to readUserPromptForRecall only when body has no usable prompt. Edge-cases unit-tested: empty {}, malformed json, non-string, whitespace all fall back. (2) UNBOUNDED INSTRUCTION RESPONSE: instruction-1722.json was 603KB / 240K tokens - the recall_hits + auto_recall.hits packs embed full memory text unbounded, blowing the agents single-Read budget (256KB/25K tok). FIX: capHitText(360ch, 8hits) + capInstructionPacks applied to instruction AND transition responses (both auto_recall-merge path and the else-branch); preserves key/namespace/score, only truncates text with a […+Nch] marker. Unit-tested: small hits untouched, keys preserved, count capped. Both ship via gm-plugkit npm publish (JS wrapper, not wasm cascade); installed ~/.gm-tools wrapper refreshes via ensureWrapperInstalled on next bun x gm-plugkit boot. LESSON: turn-entry hooks that read a side-channel file race the body that carries the same data - prefer the in-band body field; and every orientation pack the agent must Read in one call MUST be bounded, or predictability-regardless-of-llm breaks at the context-window edge.
