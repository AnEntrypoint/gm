---
key: mem-617c9010e61f612e-1339
ns: default
created: 1779875550046
updated: 1779875838229
---

GIT VERBS (shipped 2026-05-27, this session): git is a first-class spool surface, never a shell command. 12 wasm verbs in rs-plugkit wasm_dispatch.rs: inspect git_status/git_log/git_diff/git_show/git_branch; mutate git_add/git_commit/git_checkout/git_fetch/git_rm/git_revert/git_reset; push git_push (porcelain-gated, rebase-retry); BUNDLE git_finalize {message} = add-all->commit->porcelain-clean-gate->git_push in ONE dispatch, the COMPLETE-phase surface replacing 4+ Bash events. All route through host_git import (wrapper plugkit-wasm-wrapper.js) which now (a) resolves git->git.exe via resolveWindowsExeLocal so windowsHide propagates (no .cmd-shim terminal flash on Windows [[windows-spawn-cmd-shim-flash]]), (b) accepts a JSON argv array so multi-word commit messages are not shattered by space-split. A bash/sh/shell/powershell verb with a git-dominant command (first token git, */git, or starts-with git ) is GATED at gates.rs check_dispatch (mirrored lib/spool-dispatch.js checkDispatchGates), emits deviation.bash-git-bypass, names the equivalent verb. Ships via cascade: push rs-plugkit -> plugkit.wasm; wrapper change ships via gm publish.yml. CANNOT validate the wasm verbs live until the next plugkit version builds+deploys (no local cargo build, push and let CI). Legacy lib/git.js (bash-routed) REMOVED as dead/superseded.
