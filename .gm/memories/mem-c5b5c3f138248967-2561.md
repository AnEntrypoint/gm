---
key: mem-c5b5c3f138248967-2561
ns: default
created: 1782066254321
updated: 1782066361485
---

DNA-SWEEP iter-21 (2026-06-21, /loop /gm-skill re-fire 'make a workflow, fan out subagents, context-engineering + looper-bloat focus'). Ran .claude/workflows/dna-sweep.js fresh (wf_3f956561-3a3, 86 agents, 2.83M tokens): 71 candidates -> 9 verified -> 9 implement-now. NEW GROUND despite 20 prior fires (the shifted looper-bloat + context-engineering focus surfaced it). SHIPPED gm: (1) lib/spool-dispatch.js:342-353 isWorktreeDirty/hasUnpushedCommits FAIL-OPEN fixed -> available:false (git timeout/error) now pushes a hard 'git state UNKNOWN' residual (fail-CLOSED, Principle 9; was silently passing the COMPLETE gate on a dirty/unpushed tree); (2) :62 dead BROWSER_FILE_EXT_RE removed, replaced by BROWSER_FILE_ALWAYS_RE + BROWSER_FILE_DIRGATED_RE used by name (was dead union const + duplicated regex literals that could drift); (3) skills/gm-skill/index.js git_rm'd (dead export loadCanonicalSkill/SKILL_MD_PATH, zero importers, SKILL.md frontmatter loads no .js); (4) :163-169 unsolicitedDocs now applies TOPLEVEL_DOC_ALLOWLIST by BASENAME for nested+toplevel both (was asymmetric: nested skipped only a hardcoded prefix list, so docs/README.md false-flagged). SHIPPED rs-search context.rs:23 deleted dead get_file_total_lines (zero callers, std::fs unusable in wasm32). SHIPPED rs-codeinsight: analyzer.rs traverse() got const MAX_TRAVERSE_DEPTH=2000 + truncated:bool field (was unbounded recursion -> stack-blow on deep AST; now fail-degraded partial); lib.rs:217 wasm digest tag v1:->v1w: so wasm/native freshness digests (native folds GIT_HEAD+DIRTY, wasm doesn't) can never silently compare-equal under the same version tag (Principle 10 honest interface; digest is opaque equality-only key so one-time cache-miss only, no contract break). ADDED gm test.js checkAgentsMdBudget() 36000-byte ceiling + recall:-pointer assertion -- the STRUCTURAL guard the looper failure mode lacked (looper AGENTS.md=79610 bytes, 2.9x gm's 27546, DESPITE having .gm/rs-learn.db = bloat happened because no guard enforced the bidirectional-drain back-pressure). DEFERRED blockedBy:external (correct, not avoidance): rs-codeinsight scanner.rs TODO comment-node-walk (changes index output, needs eval), gm bootstrap-triplication (3 divergent boot impls, needs multi-host boot-test harness). gmsniff own:0 + ccsniff all-7-disciplines zero C--dev-gm (foreign hits gate-positive). LESSON: a fresh sweep with a NEW focus (looper-bloat-prevention) finds real critical/high gm bugs (fail-open gate) the 20 same-lens fires missed; the convergence wall is per-lens, not absolute.
