---
key: mem-db98a892e6fb993b-1164
ns: default
created: 1780815431793
updated: 1780815845757
---

Audit-loop WORKFLOW scan:ccsniff self-deviation (iter28, 2026-06-07): the workflow's scan:ccsniff subagent ran native Bash `find C:/dev/gm -iname '*ccsniff*'` at 06:52:45 to LOCATE the ccsniff binary -> ccsniff --search-discipline flagged it (own-session 7d37a7ed, gate-correct). This is the SAME class as iter26 (scan:version-source used Grep) but a DIFFERENT subagent (scan:ccsniff), which the iter26 fix did not touch. ROOT FIX (durable, same as the iter23 auditor no-lookup rule applied to the workflow): the scan:ccsniff prompt must instruct the subagent to invoke ccsniff via the stable published form `npx ccsniff@latest` / `bun x ccsniff@latest` which needs ZERO binary lookup, and NEVER native find/grep/Glob to locate the tool. PATTERN: every workflow subagent that runs an external tool (ccsniff, gmsniff) must invoke it via the stable npx/bun-x form, never search for its binary first - the binary-location reflex is the recurring leak across the workflow's audit subagents (iter26 scan:version-source, iter28 scan:ccsniff). Fix shipped iter28. The 10 [spoint] session-9b281073 search flags this window are FOREIGN (different project), not audit-owned.
