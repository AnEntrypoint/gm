---
key: mem-1779702492642-0-1641
ns: default
created: 1779702492644
updated: 1779702538550
---

Loop iter (2026-05-25, plugkit 0.1.556 live): shipped watcher auto-update (gm 2e82cb62). ROOT GAP: a long-running healthy watcher never self-updated — checkForUpdate in plugkit-wasm-wrapper.js detected a newer plugkit-bin release and wrote .update-available.json but only NOTIFIED; the version-drift self-respawn compares instance-version vs the disk plugkit.version file, but that file only moved on a fresh boot (ensureReady), which never ran while the watcher held the lock. Dead loop: detect->notify->nothing->stale until a human kills it. FIX: checkForUpdate now bumps GM_TOOLS_ROOT/plugkit.version to latest (network path requires the plugkit.wasm asset present; shared-cache path also arms; deduped via _autoUpdateArmedFor). The existing drift-check tick then sees instance!=file and self-respawns via `bun x gm-plugkit@latest spool`, whose ensureReady downloads the matching wasm. UPDATE MECHANISM LESSONS confirmed this iter: (1) `bun x gm-plugkit spool` defers to a running healthy watcher and does NOT fetch a newer wasm in place — only a fresh boot (after killing the watcher) runs ensureReady's download. (2) --kill-stale-watchers only kills when disk-wasm-version > instance-version (drift); if disk hasn't been updated yet there's nothing 'stale' to kill (killed:[], stale_found:0). (3) To force an update manually: kill ALL bun+node plugkit-wasm-wrapper procs (Get-CimInstance), clear .watcher.lock, boot — the boot's ensureReady fetches latest. Validated live on 0.1.556: prd-add reject (empty subject -> ok:false), kill-stale bun fix, fresh-prompt COMPLETE->PLAN reset, complete-chain-poll + idle_spanned all working.
