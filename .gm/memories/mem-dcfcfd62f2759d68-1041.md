---
key: mem-dcfcfd62f2759d68-1041
ns: default
created: 1780561395877
updated: 1780561620116
---

JANK finding (codeinsight full-rebuild thrash): the codesearch verb emits codeinsight_rebuild reason=digest-mismatch and then re-indexes the ENTIRE project (root=. files=98, ~280s in-wasm bge embedding) on every digest change, blocking the watcher heartbeat for minutes (witnessed multiple times 2026-06-04: ts frozen 200s+, busy_until pushed out; concurrent loop sessions thrash it because each others edits flip the digest). The index/digest logic lives in rs-plugkit code_index.rs (search() ~:410, codeinsight verb wasm_dispatch.rs ~:501) NOT rs-codeinsight (which has analyzer/scanner/project but no code_index.rs). Digest is a single whole-tree hash (stored_then_current like e6134f...:2919452322) so any one changed file forces a full rebuild+re-embed of all 98 files. FIX (rs-plugkit, ships via cascade, never built locally): make reindex incremental - per-file content-hash map, re-embed only changed files, or debounce/skip rebuild when another session holds it. This is a focused rs-plugkit chain, external to the gm repo worktree.
