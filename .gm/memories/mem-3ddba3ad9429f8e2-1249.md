---
key: mem-3ddba3ad9429f8e2-1249
ns: default
created: 1785410636005
updated: 1785410636005
---

rs-plugkit VERIFY/DECIDE gate design fix: browser-witness-coverage only checks .turn-browser-edits.json (files edited this turn), so a zero-edit confirmation/audit turn trivially satisfied it without ever dispatching browser. Fixed by adding an unconditional app-loads-witnessed gate on DECIDE->COMPLETE: any project with .gm/browser-config.json present must record a same-turn browser dispatch with ok:true, zero pageErrors, zero console error-level lines (crate::browser_witness::record_app_loads_witness_unconditional_on_edits/app_loads_witnessed_this_stop_window, gated regardless of .turn-browser-edits.json content). Vacuously true for projects with no browser entrypoint declared. The gate cleared at session_start/session_end/prompt_submit (stop-window boundary) so a prior turn's pass never satisfies the current turn's claim. Both the rs-plugkit compiled default (fsm.rs/transitions.rs) and the gm-config SourceRepo override graph.json needed the same edge+gate addition since gm-config's override is what this project actually serves. Never fix this class of gap via a project-local .gm/instructions/ vendor override -- that freezes the project off the auto-update cascade; always edit the upstream compiled default or config-source repo.
