---
key: mem-1779959952562-0-647
ns: default
created: 1779959952564
updated: 1779960203251
---

AGENTS.md drain (2026-05-28): Dead-watcher recovery uses bun x gm-plugkit@latest spool (npm-fetch path), NOT node ~/.gm-tools/plugkit-wasm-wrapper.js spool. Direct-node runs the installed wrapper as-is and skips ensureWrapperInstalled, so a just-published wrapper fix stays undeployed (running watcher carries stale wrapper code even though npm has the fix). The bun x path sha-checks and refreshes the installed wrapper before booting. After any wrapper-level fix is published, verify deployment by grepping the installed ~/.gm-tools/plugkit-wasm-wrapper.js for the new code, not just the npm version - npm-published is not installed-and-running.
