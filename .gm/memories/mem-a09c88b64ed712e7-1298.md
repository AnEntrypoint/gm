---
key: mem-a09c88b64ed712e7-1298
ns: default
created: 1780048211258
updated: 1780048288188
---

BROWSER CROSS-RUN OVERLAP — ROOT CAUSE + FIX (2026-05-29, supersedes pruned mem-1779983209753). The cross-talk was NOT the wrapper registry (that is correctly per-(cwd,sid): profile sessionProfileDir wrapper:686, eval -s pwSessionId wrapper:1864, browser-ports/sessions.json per-cwd) and NOT a shared watcher (each cwd has its OWN watcher+spool+.watcher.lock; agentgui lock-rejection pid was its own concurrent boot, not gms). ROOT: runBrowserRunner set env.PLAYWRITER_HOME = GM_TOOLS_ROOT/pw-sock (machine-global ~/.gm-tools), so playwriter (external npm pkg playwriter@0.2.0, NOT in rs-exec/rs-plugkit) kept ALL runs sessions in ONE shared home/registry/socket = cross-route. FIX (gm ce8a4f64): playwriterHomeFor(cwd,sid)=<cwd>/.gm/pw-sock-<sessionProfileSlug(sid)>, runBrowserRunner(pw,args,timeoutMs,cwd,claudeSessionId) sets PLAYWRITER_HOME from it, all 5 call-sites thread cwd+sid, env PLAYWRITER_HOME still overrides, dir auto-gitignored. Each run = isolated playwriter session namespace. rs-exec has NO browser code (lib/wasm_host/wasm_spool only); the browser stack is wrapper + external playwriter + chromium. To validate cross-run isolation in practice: two concurrent browser dispatches from different cwds/sids get distinct PLAYWRITER_HOME dirs and distinct playwriter session lists.
