---
key: mem-ac13ba561f1f46f8-610
ns: default
created: 1783699862139
updated: 1783699862139
---

## Resolved mutable: mut-prd-clear-semantics-delete-vs-archive

rs-plugkit/src/gates.rs:186 prd_has_open_items already filters by status!=completed (prd.rs:12), and durable memory already lives in memorize-fire/.gm/memories -- so completed rows carry zero correctness value once bundled into a commit message; decision: DELETE the row from prd.yml at bundle time (git_commit/git_finalize time), not archive-to-separate-file. The commit message itself becomes the durable historical record via git log, matching the existing pattern where git_finalize summary already reads git log -1 for its own summary field.
