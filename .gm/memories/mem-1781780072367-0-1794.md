---
key: mem-1781780072367-0-1794
ns: default
created: 1781780072370
updated: 1781780202620
---

Rule-enforcement fix (2026-06-18, user: '200-line single-file test rule not enforced; move important rules into the skillfile'). A dstate agent rationalized that a 24-file mock test/ unit suite does NOT violate the rule because it only literally names test.js. FIX (1): the gm test rule now unambiguously states the test SURFACE is a SINGLE <=200-line real-services integration witness (test.js at root), mock-free -- a mock-heavy multi-file unit-suite sprawl is the conventional-testing tell-tale gm REPLACES, never a coexisting exemption, and test.js-being-capped does NOT bless a parallel suite; more than the single witness is a re-scope to justify. Clarified in gm-plugkit/instructions/verify.md AND added as a standing guaranteed-read rule in skills/gm-skill/SKILL.md (the test discipline was VERIFY-phase-prose-only before, so an agent writing tests in EXECUTE never saw it -- 'move important rules into the skillfile'). FIX (2) ROOT-FIX for the recurring .md<->.rs const-drift (flagged iter prior, recurred this task): sync-instruction-consts.mjs now reads the .md with .replace(/\r\n/g,'\n') -- the bundle .md is CRLF on Windows so the local sync emitted CRLF consts while CI emits LF, perpetually re-drifting; now deterministic LF, local+CI byte-identical (6 consts one-time normalized, entry.rs CRLF 94->0). SKILL.md ships via gm publish (gm-skill package + AnEntrypoint/gm-skill mirror, NOT a synced const) and ensureReady sha256-rewrites stale installed SKILL.md copies; the verify.rs/etc consts ship via cascade. Did NOT touch foreign dstate's suite (its design) -- fixed the RULE so every agent applies it. This turn also eager-upgraded to 0.1.649 (the prior synced-const push, live+boots-clean) and confirmed the resume-inherited-rows directive is LIVE in the served PLAN prose.
