---
key: mem-1779843629602-1-1276
ns: default
created: 1779843629603
updated: 1779843769303
---

iter72 VERIFY self-report finding (2026-05-27): the agent-as-llm training loop has a single-namespace friction. record_outcome (FastGRNN router) is unconditional and landed (count 3, quality_milli 611). But the MicroLoRA scoring-head feedback errors instant not initialized; call init_instant first, and init_instant requires targets (the namespace array it ranks over). In a default-only session (enabled.txt empty) the scoring head has nothing to rank, so feedback is a CORRECT no-op, not a failure - it is meaningful only with >=2 enabled disciplines. ALSO FOUND: verify.rs line 57 still carried the STALE embedding-space-refine framing the user corrected in iter62 (the LoRA is a scoring head emitting B·(A·embedding) logits, NOT a vector reshape) - it had been fixed in AGENTS.md+paper but verify.rs was missed. FIXED both in rs-plugkit verify.rs (4d4e4ee->745ca19): conditional feedback gated on >=2 disciplines, names init_instant{targets} prerequisite, scoring-head framing. LESSON: when a framing correction lands, grep ALL instruction surfaces (plan/execute/verify/emit/complete .rs) for the old wording - a correction in AGENTS.md+paper does not propagate to the rust instruction prose automatically; the agent reads verify.rs prose, not AGENTS.md, during VERIFY.
