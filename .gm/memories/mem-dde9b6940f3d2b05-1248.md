---
key: mem-dde9b6940f3d2b05-1248
ns: default
created: 1779832659767
updated: 1779832813986
---

iter62 (2026-05-26, 0.1.592->0.1.594): CORRECTED the MicroLoRA understanding (iter61 overclaimed). rs-learn apply_adapter outputs LOGITS (logit[k]+=B·(A·embedding) per target) - it is a rank-2 learned SCORING HEAD over bge-small embeddings, NOT an embedding-space transform. Trains on trajectory-quality via feedback (sona-style Hebbian+EWC, norm-bound). iter61 said it refines the embedding space - WRONG; it scores targets from embeddings. Corrected paper s40 (browser-witnessed) + AGENTS.md rule. KEY OPEN GAP: recall does NOT use the adapter at all - wasm_learn recall calls vec_search(raw query), the trained head is never consumed, so the LoRA loop is OPEN at the consumption end. Closing it = re-rank recall candidates by the heads learned target-relevance score (NOT transform the query vector). The head trains today (feedback validated iter61); wiring its score into recall ranking is the remaining integration. Also: deviations_30m=6 = 2 agent in-session-heavy-work long-gaps (recurring, interleave instruction more) + 4 aged empty-sess (resolve-before-add + gate-deny, rolling off, no live source). LESSON: verify a cores OUTPUT type (logits vs vector) before framing what it does; apply_adapter=scoring head, not embedding transform.
