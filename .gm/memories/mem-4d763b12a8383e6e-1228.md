---
key: mem-4d763b12a8383e6e-1228
ns: default
created: 1784801127498
updated: 1784801127498
---

orchestrator/instructions/mod.rs architecture (rs-plugkit) handle_instruction: "Valid phase" now means "a state in the ACTIVE graph" (plus the two always-legal pseudo-phases ENTRY/ORCHESTRATOR and the BROWSER prose pseudo-phase, none of which are real FSM states a project's graph declares) rather than a fixed compiled list -- a custom-graph project's own phase names are accepted without a Rust rebuild. The fresh-prompt-reset-to-initial-phase and terminal-phase-check logic is graph-policy-driven via policy.initial_phase/policy.terminal_phase, not hardcoded PLAN/COMPLETE literals -- a project overriding graph.json's policy gets correct fresh-prompt-reset behavior for its own custom phase names; an unconfigured project sees byte-identical PLAN/COMPLETE behavior via the policy defaults. codeinsight_overview attachment: proactive delivery of the already-indexed codeinsight overview (file/symbol counts, top kinds, largest files) on every instruction response instead of requiring a separate codesearch dispatch just to learn "what's in this codebase" -- purely a query over the EXISTING index (never triggers scan/parse/embed), cheap on every dispatch; null when no index exists yet (first-ever session in a fresh repo).
