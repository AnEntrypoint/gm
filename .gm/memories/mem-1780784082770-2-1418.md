---
key: mem-1780784082770-2-1418
ns: default
created: 1780784085120
updated: 1780784292865
---

RECURRING audit-session self-deviation (iter22 AND iter23, 2026-06-06/07) - the iter22 memorize did NOT durably stop it, so this is the reinforced rule: the recurring gm/rs-* audit session keeps reaching for native Glob/Grep to "locate the ccsniff/gmsniff binary" (iter22: `**/ccsniff*`+grep ccsniff; iter23: `**/ccsniff*` again PLUS grep -iE rs-search|gm filtering ccsniff output - caught live by the audit-loop workflow's ccsniff scan both times). ROOT FIX (behavioral, durable): NEVER locate the snifftools at all. Invoke them via the stable published form `bun x gmsniff@latest ...` / `bun x ccsniff@latest ...` which needs ZERO file lookup. NEVER pipe their output through native grep to filter by cwd/discipline - instead pass the tool's own flags (--git-discipline/--search-discipline/--since/--stats) and read the structured output directly, OR route any genuine code/file/symbol lookup in the [gm] cwd through the codesearch verb. The recall hit for this rule surfaces at cos~0.79 on every audit turn yet the reflex still fired in iter23 - the fix is to remove the NEED for the lookup (stable invocation form), not just to remember not to do it. The audit-loop workflow catching the same self-deviation two fires running is the paper's predictability-regardless-of-LLM turned inward and is itself the proof the discipline gate works; the gap is auditor compliance, now closed by the no-lookup invocation rule.
