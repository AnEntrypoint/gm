---
key: mem-1780596014498-3-1309
ns: default
created: 1780596014499
updated: 1780596183916
---

CODEINSIGHT INCREMENTAL-REBUILD FIX LOCATION (rs-plugkit, for next focused fire): the multi-minute spool-freeze on every post-edit codesearch is rooted at C:/dev/rs-plugkit/src/code_index.rs:335 in fn index(root,max_files) - line `let _ = clear_codeinsight();` UNCONDITIONALLY deletes ALL codeinsight keys (clear_codeinsight at code_index.rs:38 iterates fv_query('codeinsight','') and fv_delete each) then re-embeds all 99 files via in-wasm bge. The digest (current_digest/store_digest at code_index.rs:401-402) only gates WHETHER to rebuild (sync-before-emit), not WHICH files. FIX: per-file incremental rebuild - store a per-file content hash alongside each file's chunk keys, on index() compare each file's current hash to stored, fv_delete+re-embed only changed files, keep unchanged keys. Removes the clear-all + full re-embed. CAVEAT: rs-plugkit is wasm-only, CI is the ONLY type-gate (mem-1779742294534), so after the change push and gh-watch Build-WASM to GREEN before declaring done; a botched index change ships via cascade to ALL sessions and breaks codesearch globally - this is why it is a focused-fire change not a rushed audit-fire edit. clear_codeinsight_if_dim_mismatch (code_index.rs:60) stays (embed-dim change is a real full-clear case). PRD row codeinsight-incremental-rebuild-rs-plugkit.
