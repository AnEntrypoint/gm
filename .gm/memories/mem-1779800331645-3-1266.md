---
key: mem-1779800331645-3-1266
ns: default
created: 1779800331646
updated: 1779801429303
---

iter44 (2026-05-26): GitHub codeload/Actions RECOVERED (the iter41-43 infra outage cleared). gm gh-pages re-run of 5eea62a SUCCEEDED -> the home-page visual fixes are now LIVE: raw-HTML fetch of https://anentrypoint.github.io/gm/ confirms .cli{display:block} (install instructions stack) + .work-detail-chips{display:flex;align-items:center} (Star CTA aligned, browser-witnessed all chip tops 448). IMPORTANT witness caveat that cost time: a reused chrome browser session served a STALE cached copy showing .cli display:flex even with ?cb= cache-bust param - the authoritative check was the raw https.get of the deployed HTML (.cli{display:block} present; the flex hits are all .cli .cli-line inner prompt+cmd rows which are correctly flex). LESSON: for deploy-witness, fetch raw HTML (https.get) as source-of-truth, not a reused browser session that caches across goto() calls; or use a fresh session per witness. With codeload back, re-triggered the rs-plugkit backlog (classifier 4283091 + browser-gate 09148c2) via empty commit 7bbb3b9 push (push-triggers should fire now). 5 deviations 30m were 1-mine-recovered + 4-external (tv8/planet prd-resolve + foreign-browser-wedge gate-deny which the undeployed 09148c2 fixes). store 80/80, AGENTS.md 9/9, recall clean.
