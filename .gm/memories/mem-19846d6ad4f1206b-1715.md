---
key: mem-19846d6ad4f1206b-1715
ns: default
created: 1785274237979
updated: 1785274237979
---

gm SKILL.md browser verb GL-capture mechanism detail: debugSetup patches HTMLCanvasElement.prototype.getContext pre-navigation via page.addInitScript (never page.evaluateOnNewDocument, a Puppeteer-only method name absent on playwriter's real Playwright Page -- silently no-ops if reintroduced) to wrap every drawArrays/drawElements/drawArraysInstanced/drawElementsInstanced call with a post-call gl.getError() drain. window.__gmGlLastDrainedError ({fn,error,errorName,drawCallIndex}) exposes the wrapper's own most recently drained GL error code -- a user script's own post-draw gl.getError() call always reads NO_ERROR because this wrapper's drain already ran first inside the wrapped draw function (WebGL's error state is a single-slot FIFO, only the first reader after a draw sees a real code), so a debugging script should read this global instead of re-calling gl.getError() itself. This capture replaces hand-rolling the same gl.*=function(){...gl.getError()...} monkeypatch ad hoc every session, and is the standing capability for GPU rendering-bug root-causing (stale buffer/VAO bindings, sampler-unit collisions, index-buffer/GL-type-constant mismatches). The capture/profile/trace prefixes remain for their original purpose (CPU sampling, CDP GPU/compositor tracing) -- not required just for console/network/GL visibility, which is unconditional as of gm-plugkit >=2.0.1916. The profile prefix's culprits array pairs with a gpu_hint field: when the top culprit is the unattributed (program)/(native) bucket at >=40% self-time, gpu_hint names the trace prefix (browser surface, real gpu_us/viz_us/cc_us via CDP Tracing) or an accurate node-specific note (exec_js surface, no GPU-tracing follow-up applies).
