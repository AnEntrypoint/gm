---
key: mem-f780f0bde247c3bd-495
ns: default
created: 1784800609176
updated: 1784800609176
---

code_index.rs history (rs-plugkit): git_commit_rank_fallback is filename-token-overlap ranking, used ONLY when embedding is unavailable (embed model failed to load, or git_commit_vectors table has zero usable rows for the query) -- never the primary ranking path; git_commit_rank itself ranks by cosine similarity of a diff+message embedding against the query embedding, with git_commit_vectors synced incrementally (wall-budgeted) on each call so a large backlog never blocks a single dispatch.
