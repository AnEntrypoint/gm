---
key: mem-6b7ab55c5b5f1806-1271
ns: default
created: 1781107533576
updated: 1781107563030
---

MANIFEST CACHE FINALLY WORKS - the TRUE root cause of codesearch 5-min reindex (2026-06-10, third and final fix in the chain): bb480a7 added the manifest cache, 059dfc0 canonicalized path forms, but reused stayed 0 because the HOST KV STORE SANITIZES ROW KEYS INTO FILENAMES: wrapper safeName() replaces [^A-Za-z0-9._-] with _, so fv_put(ns, "agents/gm.md") writes agents_gm.md.json and host_kv_query returns the MANGLED filename as the row key. load_manifests keyed its map by row key, so prior.get(real-path) could never hit for any subdirectory file. FIX 9b60607 (rs-plugkit, wasm 0.1.640): embed "path" in the manifest VALUE, key the loaded map by embedded path, skip legacy pathless manifests; write/reap unchanged since safeName is deterministic. LIVE VALIDATED: pass2 digest-mismatch rebuild embedded=0 reused=372 reused_files=54 in ~15s (was 240-270s full re-embed). DURABLE RULE for all wasm kv use in rs-plugkit: NEVER round-trip a key containing / (or any non [A-Za-z0-9._-] char) through fv_query row keys - the host returns sanitized filenames, not the written key; embed the real identifier in the value. DIAGNOSIS LESSON: the giveaway was on-disk filenames agents_gm.md.json (underscore where the path has a slash) in .gm/disciplines/codeinsight-manifest/.
