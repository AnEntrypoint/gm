---
key: mem-1782740240451-0-540
ns: default
created: 1782740241282
updated: 1782740895886
---

## Resolved mutable: browser-getorcreate-toctou-race

ELIMINATED as the live cause: live PowerShell Win32_Process shows each managed chromium has exactly ONE top-level process per distinct user-data-dir (parent=a watcher pid); the extra chrome.exe are children of the one launch (gpu/renderer/utility, same port, no own --user-data-dir). No two top-level chromiums share one profile -> the read-check-spawn-write path did not double-spawn within a single cwd. The race is a latent hardening target but NOT what opened the multiple browsers.
