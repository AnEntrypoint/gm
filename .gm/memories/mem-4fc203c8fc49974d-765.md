---
key: mem-4fc203c8fc49974d-765
ns: default
created: 1787293108441
updated: 1787293108441
---

## Resolved mutable: exec-js-shell-exec-plain-text-body-contract

cargo check -p rs-plugkit --target wasm32-wasip1 clean compile. 7 live spool dispatches against a locally-built slim wasm sideloaded into the running agentplug-runner daemon: exec_js JSON-object body rejected (error_code invalid_args, received_keys+supported_shapes named); exec_js timeoutMs=5000-prefixed raw body executed (stdout '2'); exec_js bare body with no timeoutMs rejected as missing timeoutMs; bash timeoutMs=5000-prefixed raw body executed (stdout 'hello-from-bash'); bash JSON-object body rejected matching exec_js; exec_js empty body rejected; exec_js timeoutMs=0 rejected as below the 100ms floor. Sideload reverted, shared daemon relaunched with the restored stable build afterward.
