---
key: mem-b52266083516fdcd-1350
ns: default
created: 1784800597577
updated: 1784800597577
---

code_index.rs history (rs-plugkit) index() prune step: was gated on deferred_files==0 (a fully complete pass) but on any repo exceeding INDEX_WALL_BUDGET_MS in one call (1500+ files) deferred_files is essentially always >0, so prune never ran in practice -- code_chunks accumulated rows for files deleted commits ago (gm-plugkit/plugkit-wasm-wrapper.js, supervisor.js, git-rm'd well before the session that found this) still surfaced by codeinsight_overview.likely_orphaned as if live. Fix: a file missing from `seen` is safe to prune UNCONDITIONALLY as long as it is ALSO absent from the full files enumeration computed up front before the wall-budget loop truncates -- distinguishes "deferred this pass, still exists" from "genuinely gone", preserving legitimate multi-pass resume while still pruning dead files on the first pass that notices them. current_digest() is the cheap mtime-only stat-walk "has anything changed" check codesearch runs before every index() call (no file content read) -- prior content-hash version read+crc32'd every file on every search causing daemon-wedging on large trees; matches codebasesearch reference impl (mtime-based change detection). Must mirror index()'s >256KB size-exclusion using stat size (no content read) or the two digests carry different file sets and permanently mismatch, forcing perpetual rebuild.
