---
key: mem-636397d3f30d67bb-1522
ns: default
created: 1779804855204
updated: 1779804970012
---

iter47 (2026-05-26): found+fixed a PACKAGING bug while validating the iter46 fresh-boot fix shipped. gm-plugkit/package.json files[] OMITTED supervisor.js AND lang-host-runner.js, so they never published to npm. Verified by npm pack gm-plugkit@2.0.1442: the package contained only cli/index/bootstrap/plugkit-wasm-wrapper.js - supervisor.js absent (the grep for resolveNode came back blank because the FILE was missing, not my edit). CONSEQUENCE: every published consumer hit fs.existsSync(path.join(__dirname,supervisor.js))=false in bootstrap.js startSpoolDaemon, so the SUPERVISED watcher path (unplanned-restart recovery + idle-teardown coordination that bootstrap explicitly wants) was PERMANENTLY DORMANT for everyone since gm-plugkit shipped; they silently fell to the no-supervisor branch. (Good news: iter46 ALSO fixed the no-supervisor branch cmd=resolveNodeRuntime(), and bootstrap.js DID ship, so the fresh-boot-death fix reached published consumers via that path regardless.) FIX: added supervisor.js + lang-host-runner.js to gm-plugkit files[]; now the supervisor (carrying the iter46 node-runtime fix) + the lang-verb host loader actually ship. LESSON: always verify a fix REACHED the published artifact by npm pack + inspecting package contents, not just that the source commit landed - a files[] omission silently drops whole modules from the package even when the source + publish.yml succeed. The iter32 published!=installed lesson generalizes: committed != published-in-package != installed != running.
