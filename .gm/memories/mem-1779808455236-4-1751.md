---
key: mem-1779808455236-4-1751
ns: default
created: 1779808455236
updated: 1779808558139
---

iter49 (2026-05-26, 0.1.582): MAJOR validation - the rs-learn bi-temporal graph core works END-TO-END through the spool. Exercised via the learn verb over PlugkitKv: insert_edge {id:iter49-test-edge,src:plugkit,dst:rs-learn,relation:dispatches,valid_at,created_at} returned {ok:true,edge_id}; query_at {src:plugkit,t:after-valid_at} returned {count:1, edges:[the full edge with all temporal fields]}; invalidate_edge returned ok (bi-temporal: invalidates not deletes). The edge PERSISTED to .gm/disciplines/rs-learn_graph_edges/<id>.json + rs-learn_graph_edges_by_src/ + _by_dst/ indexes via host_kv_put, and query_at RETRIEVED it point-in-time via host_kv_get/host_kv_query. This is the deepest proof that the learning system works 100% as intended: the rs-learn cores do not just return health - they COMPUTE and PERSIST real graph state through the host KV backend, round-trip, live on 0.1.582. The iter33 bridge + this iter49 round-trip together prove the cores are genuinely functional via the spool. REMAINING FRONTIER (now de-risked since the capability is proven): automatic PER-TURN wiring - nothing yet calls insert_edge on every memorize (to build the temporal knowledge graph from session facts) nor init_router/route/record_outcome on model/temp/context selection. That agent-lifecycle integration is the multi-fire advance; the cores themselves are validated working. Cleaned the test edge files from tracked .gm/disciplines. The graph persists under rs-learn_graph_edges/by_src/by_dst disciplines (tracked, travel with the project in git). LESSON: validate a wired capability with a real compute+persist round-trip (insert then query, check on-disk state), not just a health ping - health proves reachable, round-trip proves functional.
