---
key: mem-1779700675079-0-1202
ns: default
created: 1779700675081
updated: 1779700763634
---

Loop iter (2026-05-25, plugkit 0.1.555 live): two fixes + validation. (1) prd-add hardening (rs-plugkit 7817c54): prd.rs now HARD-REJECTS when no usable id and unslugifiable subject — was a soft warn that inserted an item-<ms> row (recurred 5x/30m). item-<ms> ids cannot be referenced by intent in recall/prd-resolve, so they are never admitted now. (2) kill-stale-watchers bun bug (gm 7cfef128): cli.js killStaleWatchers Windows branch filtered Get-WmiObject to name='node.exe' only, but the default boot `bun x gm-plugkit spool` runs under bun.exe — so bun-hosted watchers were invisible to kill-stale and survived, blocking wasm version updates. Witnessed: 0.1.554 watcher pid 151052 (bun) survived --kill-stale-watchers; 0.1.555 only landed after manually killing all bun plugkit-wasm-wrapper processes. Fixed to filter node.exe OR bun.exe. UPDATE PROCEDURE LESSON: when an update won't land, kill ALL bun+node plugkit-wasm-wrapper processes via Get-CimInstance, clear .watcher.lock, then boot — multiple stale watchers fight the lock. Validated live on 0.1.555: DEFECT A complete-chain-poll fires (4x/30m), DEFECT B idle_spanned stamped on turns, DEFECT C fresh-prompt resets COMPLETE->PLAN.
