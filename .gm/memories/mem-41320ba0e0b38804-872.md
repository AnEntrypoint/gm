---
key: mem-41320ba0e0b38804-872
ns: default
created: 1785248535233
updated: 1785248535233
---

agentplug-runner runtime-state path split (gm-plugkit launcher edge vs agentplug-host runtime): bun x gm-plugkit@latest spool re-execs agentplug-runner staged at ~/.gm-tools/agentplug-runner and exits, but the executable's own runtime state lives at ~/.agentplug (install_dir()), not ~/.gm-tools -- plugin wasm/version markers, daemon-status.json, daemon-registry.txt, daemon-config.json all live under ~/.agentplug, independent of gm-plugkit's own launcher-edge files under ~/.gm-tools. Always check ~/.agentplug/plugins/gm.version, never ~/.gm-tools/plugins/gm.version or ~/.gm-tools/plugkit.version, to see what agentplug-runner actually serves -- checking the wrong directory is the top false-alarm source diagnosing update-path issues. plugkit-slim.wasm being absent at the ~/.gm-tools top level is expected once tryDelegateToRunner succeeds, not a bootstrap failure.
