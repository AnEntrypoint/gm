---
key: mem-6f017d73ee98c0cb-1167
ns: default
created: 1779839575748
updated: 1779839698010
---

iter69 (2026-05-27, 0.1.596): FOUND + FIXED the LoRA re-rank calibration bug (the iter63 re-rank ran but never reordered). Synthetic 2-namespace test (ltest-alpha/beta near-identical gravity facts; baseline beta 0.9095>alpha 0.8996): trained the instant head to favor alpha (apply_adapter logits [0,1.3e-5,0] - alpha slot nonzero, training+apply confirmed), but recall order UNCHANGED. ROOT CAUSE: the heads quality-trained logits are tiny (~1e-5); the iter63 blend 1+0.25*(sigmoid(logit)-0.5)*2 -> sigmoid(1e-5)~=0.5 -> factor~=1.0, negligible vs cosine. FIX (e4ec625, build green 50bc31a): temperature-scaled SOFTMAX over candidate-namespace logits so RELATIVE preference drives the blend independent of absolute magnitude, centered on uniform 1/n (untrained=no-op exactly, trained reorders), lambda=1.0. LESSON: validate that a re-rank actually REORDERS (not just that it runs no-op-safe) - a blend can be arithmetically present but mis-scaled to zero effect; test with a real trained head against a near-tie. The mechanism (wrapper fans recall across enabled disciplines, hits carry namespace, re-rank scores by head logits) is sound; the calibration was the bug.
