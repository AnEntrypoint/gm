---
key: mem-1779858373972-13-1382
ns: default
created: 1779858373973
updated: 1779858451923
---

iter94 (2026-05-27, 0.1.603): FIXED a real predictability gap in SKILL.md L23 from the recurring iter92/94 observation. The prose claimed a fresh /loop prompt resets phase to PLAN on the next instruction dispatch - but iter92 AND iter94 both observed the first instruction after a fresh prompt returning phase=COMPLETE + UPDATE-DOCS, NOT PLAN (the auto-reset is non-deterministic, likely tied to gap/turn-state timing). The prose overstated reliability and omitted the recovery, so a different LLM would poll instruction (firing complete-chain-poll) instead of reopening. FIXED: amended SKILL.md L23 to state the reset is not always automatic and to dispatch transition to=PLAN ONCE on intended-new work if instruction still returns COMPLETE - explicitly distinguishing this admissible reopen from the re-poll deviation. Ships via publish.yml mirror + bootstrap auto-refresh. rs-plugkit update_docs.rs:35 covers the complementary opposite case (re-poll with NO new prompt = deviation) and is correct as-is. LESSON: a recurring operational observation across >=2 fires that the docs do not match is a real instructional-perfection fix, not steady-state noise - the user explicitly wants predictability-regardless-of-llm, and a doc that overstates auto-behavior + omits the recovery is exactly the predictability gap to close. cores 122/122, router count 26 quality 915, deviations 0.
