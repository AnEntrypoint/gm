---
key: mem-16f47170a2f90a37-685
ns: default
created: 1782165438855
updated: 1782165762238
---

## Resolved mutable: bundle-edit-sync-requirement

gm-plugkit/scripts/sync-instruction-consts.mjs:6-33 reads gm-plugkit/instructions/<key>.md (CRLF->LF), writes rs-plugkit/src/orchestrator/instructions/<key>.rs as 'pub const TEXT: &str = r#"<md>"#;' for keys entry/plan/execute/emit/verify/update_docs/browser (rsDir default ../../../rs-plugkit or RS_PLUGKIT_DIR). The wasm serves the .rs const (prose.rs) with .md fallback, so a bundle-only edit ships nothing until the .rs const is re-synced and rs-plugkit pushed (cascade rebuild). REQUIREMENT: edit .md -> run sync -> commit gm-plugkit .md AND rs-plugkit .rs -> push both. residual/* and gates/* are NOT in the sync list (md-only).
