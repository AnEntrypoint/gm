---
key: mem-1781596587017-1-1587
ns: default
created: 1781596587018
updated: 1781596998840
---

Audit-loop fire (2026-06-16 iter-4+5 recovery + CORRECTED finding, replaces pruned mem-1781595379779). RECOVERY (iter-4): a ~6.7h idle gap idle-tore-down the watcher (teardown reason=idle, idle_limit 1h); bun x gm-plugkit@latest hit a transient FailedToOpenSocket; improvising a fallback to cached bun x gm-plugkit (no @latest) booted a STALE 0.1.616 watcher; network tested UP so killed-stale + rebooted correct 0.1.645. LESSON: on a transient @latest socket failure RETRY @latest (blips resolve in seconds), never the no-@latest cache-fallback (lands stale); the supervisor version-drift self-heal also re-downloads once network recovers. CORRECTED DIAGNOSIS (iter-5, supersedes the iter-4 'spool-dir-removal race'): the spool-write ENOENT is NOT dir removal. The platform Write tool writes in/<verb>/<N>.txt then stats it to confirm; a fast watcher (fresh 0.1.645 6044) consumes+unlinks the file before that post-write stat, so Write errors ENOENT on a dispatch that ACTUALLY SUCCEEDED -- proven: 4 ENOENT'd prd-add writes all produced out/ responses. runSpoolWatcher (walkDir + 5s poll + retention) has NO verb-dir rmdir; retention only recreates top in/out if missing. PREDICTABILITY-CRITICAL: on a spool-write ENOENT the dispatch likely happened -- Read the out/ response, NEVER blind-retry (a non-idempotent verb like git_finalize double-fires). A Bash heredoc 'cat > in/<verb>/<N>.txt' has no post-write stat and is the race-safe spool-write. Fixed in SKILL.md dispatch section (ships via cascade). The long-lived iter-1-3 watcher 9032 never surfaced it (slower fs.watch timing).
