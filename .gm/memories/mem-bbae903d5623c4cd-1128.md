---
key: mem-bbae903d5623c4cd-1128
ns: default
created: 1779842044790
updated: 1779842304586
---

iter71 (2026-05-27, 0.1.599): WITNESSED the LoRA re-rank REORDERS recall - the culmination of the iter61-71 LoRA work. Synthetic 2-ns test on 0.1.599 (label-mapping live): baseline beta#0(0.9095)>alpha#1(0.8996); trained the head to favor alpha (feedback_count 8, apply_adapter [0,1.76e-5,0]+targets labels); the instruction recall_hits pack then PROMOTED alpha to #0 ABOVE beta despite alphas lower cosine. The LoRA loop is materially effective END-TO-END: train->persist->label-map->softmax-reorder, all proven. FOUND+FIXED a consistency gap: the re-rank ran in the instruction recall_hits pack but NOT the explicit recall verb (fn recall routes separately in wasm_dispatch); fixed 4ae1ac8 (fn recall now calls rerank_by_adapter, made pub) so both surfaces re-rank. LESSON: when a feature works on one surface but not another, check that ALL entry points to the capability route through the shared implementation - the recall verb and the instruction recall_hits pack are two separate recall paths. The full LoRA arc (iter61 self-report train, iter63 re-rank, iter69 softmax, iter70 label-map, iter71 witnessed) is now closed.
