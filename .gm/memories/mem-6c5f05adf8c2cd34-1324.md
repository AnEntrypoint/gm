---
key: mem-6c5f05adf8c2cd34-1324
ns: default
created: 1779697221703
updated: 1779697267823
---

Loop-audit iter (2026-05-25): fixed THREE plugkit defects, all instances of the paper-spirit-drift pattern (prose claims behavior code lacks). DEFECT C (load-bearing): SKILL.md promised 'a fresh prompt resets phase to PLAN on a COMPLETE chain' but handle_instruction (rs-plugkit/src/orchestrator/instructions/mod.rs) only wrote last-prompt.txt — never reset phase. This is why chains repeatedly wedged at COMPLETE serving UPDATE-DOCS. Fix (commit 003a80c): compute fresh_prompt = prompt differs from read_last_prompt(); when phase==COMPLETE && no phase= override && fresh_prompt, set phase=PLAN + write_state. DEFECT A: same path with empty/unchanged prompt + prd_pending==0 now emits deviation.complete-chain-poll via new idev() helper (mirrors gates.rs log_deviation). DEFECT B (gm commit e098e9e6): turnTick idle-split ran only for verb==instruction, so a non-instruction verb after overnight idle stamped t.lastTs forward without splitting -> turn.end dur_ms ballooned to 9.7h with 16 dispatches. Fix: idle-split runs for ALL verbs via shared endTurn() with idle_spanned flag; non-instruction verb after idle opens no turn. Hunt method that found these: read gmsniff turn.end events for absurd dur_ms + COMPLETE-phase instruction-only turns, then grep the claimed reset behavior against actual handle_instruction code.
