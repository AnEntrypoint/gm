---
key: mem-1f9f52c6aa921f06-1117
ns: default
created: 1784800563380
updated: 1784800563380
---

code_index.rs history (rs-plugkit): MANIFEST_VERSION=5 added commit_overview (short sha+subject+shortstat diffstat for a file's most recent touching commit, computed once per content-change via single git log -1 --shortstat call, cached on manifest so unchanged files never re-pay the git subprocess cost); v4 added mtime_ms enabling a stat-only skip before content read/hash for the common unchanged-file case. Manifests failing the version check are purged (one-time re-index, same cost as any manifest-schema migration). SUBMODULE_DIRS (rs-plugkit/rs-codeinsight/rs-search/agentplug/agentplug-bert/agentplug-libsql/agentplug-treesitter): a path under one of these has no history in the PARENT repo's git log (parent only tracks the submodule commit-pointer gitlink) so compute_commit_overview skips these paths explicitly rather than silently returning wrong/misleading data -- live-confirmed a codesearch hit for agentplug/.../exec_js.rs carried no overview despite running unconditionally. Proper fix needs host_git cwd-join semantics it does not currently have (explicit cwd bypasses the per-project root join).
