---
key: mem-1781787433018-0-1491
ns: default
created: 1781787433019
updated: 1781787474134
---

Audit fire iter-10 (2026-06-18): the no-surprise keystone's first transform VALIDATED in practice -- the .gitattributes LF-pin ended the recurring const-drift: sync-instruction-consts.mjs now reports 'done: 0/7 consts rewritten' (every prior fire it re-wrote verify.rs/entry.rs from the CRLF/LF autocrlf churn). 0.1.651 SHIPPED (the 40d04d8 cascade: entry.rs cwd-scope const + .gitattributes compiled clean, no dark cascade; the watcher recycled onto it). gm/rs-* clean (own:0, ccsniff [gm] bash+search 0; 2 foreign gate-positives). KEYSTONE HUNT: the clear tool-surprise transforms are done (codesearch-cwd->prose, const-drift->.gitattributes); remaining advice-memos are prose-covered or mixed-with-architecture-facts (kept) -- the keystone now governs future memos so accumulation stops at the source. VERSION-DRIFT RECYCLE investigated + concluded NOT-a-defect: supervisor checkWatcherHealth (POLL 10s, STATUS_STALE 30s, BURST_BACKOFF 60s, VERSION_DRIFT_COOLDOWN 60s); a ~109s drifted-watcher window is the EXPECTED version-upgrade cost (evict the stale on-disk wasm + redownload the new ~149MB build + respawn + stale-detect + possible burst-backoff), the redownload is necessary (on-disk bytes were the old version), recovery is documented (boot-prose update_available->eager-upgrade, which recovered it). OPERATIONAL LESSON: prefer Read-by-path over codesearch right after a watcher recycle -- a cold codesearch triggers a full reindex (busy_until ~321s) that queues subsequent verbs.
