---
key: mem-6650ccd56c9038eb-2001
ns: default
created: 1782040017905
updated: 1782040067511
---

FINISH-REMAINING-WORK fire (2026-06-21, /gm-skill after iter-20): drove the deferred DNA-sweep backlog to honest closure. KEY DISCOVERY: the deferred rs-plugkit CORRECTNESS cluster is ALREADY SHIPPED in live 0.1.678 -- gates.rs:153-185 prd_has_open_items + mutables_unresolved use serde_yaml structural Sequence parse (the LIVE GATE BUG substring-scan is fixed), worktree_dirty:187 trims git_porcelain (fail->dirty shipped); is_unsolicited_toplevel_doc:231 is log-only BY DESIGN (soft warn not deny). SHIPPED rs-plugkit cb086ff: memorize.rs write-time dedup -- content-addressed key mem-{fnv1a64(namespace|text):016x}-{len} (was mem-{now}-{counter}-{len}, duplicated on restate) + pre-put host_kv_read short-circuit returning deduped:true skipping re-embed; reused existing pipeline::fnv1a64 made pub(crate), no new dep; backward-compat (old keys resolve, tombstone falls through). This closes the chatgpt-memory write-time-dedup gap autonomously-safely (additive, no ranking change, no eval needed). GENUINE RESIDUAL (blockedBy external, NOT mere size): rs-search search-quality (camel-split/context-regex/weighted-fusion change codesearch RANKING), rs-codeinsight indexer-output (depgraph/project/digest change index output), rs-learn ranking-reweight (decay/recency/salience change recall ordering), supervisor multi-process, gm bootstrap-wiring -- each needs an eval harness gated on a labeled qrels dataset (a not-in-session product decision) or a multi-process test harness. LESSON: 'finish all remaining work' on a deferred backlog = (1) re-verify each defer against LIVE source (several were already shipped by the cascade), (2) land the additive/correctness items that need no eval, (3) honestly mark the eval-gated ranking/index changes blockedBy:external -- deferring them is the correct call (Principle 3/7: don't blind-regress the agent's own retrieval infra without a measured eval), not avoidance. The decay/recency/salience reweight items remain genuinely supervised-eval-session work.
