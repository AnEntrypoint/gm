---
key: mem-3c99e67f404ecdde-2139
ns: default
created: 1780381279931
updated: 1780386845040
---

CASCADE OUTAGE ROOT-CAUSED + FIXED (2026-06-02, audit fire ~06:11). SYMPTOM: plugkit-bin latest GH release stuck at v0.1.616 (published 2026-05-28, built from 3cbb933) while rs-plugkit Cargo.toml had advanced to 0.1.622 (fac4468) over 5 days and 6 version bumps - update_available=null was CORRECT (no newer release exists), so the absent-digest fix (fac4468/540c32c) + codeinsight digest resync (dba4606) sat in source but NEVER shipped to any running wasm. ROOT CAUSE: rs-plugkit Release+Build workflows failed at the Build-WASM step (run 844/572, 12s = compile error not timeout) on every push since 0.1.616. Found via: GH API releases/latest (stuck version) -> actions/runs (conclusion:failure) -> runs/<id>/jobs (build job, step 9 Build-WASM failure, steps 10-12 skipped, publish-binaries skipped). Log endpoint 403 (needs admin) so reproduced locally: cargo check --no-default-features --features wasm --lib emitted error[E0428] the name handle_fire is defined multiple times at src/orchestrator/memorize.rs:219. CAUSE: memorize.rs:56 handle_fire (wasm real-kv/embed path) lacked #[cfg(target_arch=wasm32)] while :219 had #[cfg(not(target_arch=wasm32))]; un-gated :56 collided with :219. FIX (rs-plugkit 4c4ed3f): added #[cfg(target_arch=wasm32)] to :56 so the two variants are mutually exclusive on every target; E0428 confirmed gone in native check. CAVEAT: native check also shows 9 E0433 cannot-find-wasm_dispatch - these are native-only artifacts (wasm_dispatch/code_index/embed are #[cfg(target_arch=wasm32)] modules per lib.rs:1-11, absent off-wasm, present on the wasm CI target) - NOT the build blocker. PUSH triggered CI: Release+Build now in_progress on auto-bumped d0fb5ff (0.1.623). VALIDATION PENDING: confirm Build-WASM reaches success + publish-binaries ships plugkit-bin > v0.1.616 + gm.json plugkitVersion auto-bumps. METHODOLOGY: cargo check (native, --features wasm) is a legit DIAGNOSTIC to read a compile error CI hides behind a 403 log - distinct from the forbidden local artifact BUILD. This is the iter21 (mem-1779742294534) failure class recurring: version-bump-without-wasm-publish from a CI compile error.
