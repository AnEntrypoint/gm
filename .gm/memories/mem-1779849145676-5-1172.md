---
key: mem-1779849145676-5-1172
ns: default
created: 1779849145677
updated: 1779849228505
---

iter79 (2026-05-27, 0.1.602): drift hunt extended to rs-* sibling repos. Found rs-plugkit/bash.exe.stackdump (Cygwin/MSYS bash crash dump) present in the git index despite *.stackdump being in .gitignore line 3 (gitignore does not untrack already-indexed files). Removed via git rm --cached + rm. RESOLUTION NUANCE: after removal the tree was CLEAN with nothing to commit and the file was NOT in HEAD - it was a staged-but-never-committed index artifact, not committed dead code, so clearing index+disk left a clean tree needing no commit. Scanned all 5 repos for .stackdump/.core/.orig/.rej/DS_Store/Thumbs/nul junk - this was the ONLY one. rs-* repos have no src/bin daemon entrypoints (wasm cdylib only, consistent with WASM-only). LESSON: gitignore rules added AFTER a file is committed/indexed do not retroactively untrack it - git rm --cached is required; periodically scan git ls-files (not just status) for junk patterns that slipped in before their ignore rule. cores 105/105, router count 10 quality 784 (steadily climbing 611->784 across fires, agent-as-llm loop accumulating), deviations_35m=0 (instruction-first after the >300s boot gap prevented a long-gap).
