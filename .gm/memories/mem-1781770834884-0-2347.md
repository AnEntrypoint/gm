---
key: mem-1781770834884-0-2347
ns: default
created: 1781770834886
updated: 1781771352375
---

DURABLE FIX for the recurring audit-fire false own deviation.mid-chain-stall: in gm-plugkit/plugkit-wasm-wrapper.js, turnTick (updates a turn's lastTs/stall clock) is reached ONLY via emitOrchestratorEvents which returns early on !ORCHESTRATOR_VERBS.has(verb) -- so wasm-direct verbs (recall/codesearch/fetch/exec_js/git_*/health/filter) never warmed the stall clock, and a Bash-free stretch of pure recall/codesearch still tripped scanStalledTurns at idle>=STALL_MS(300s). FIX (this fire): added touchActiveTurn(sess) {refresh open turn lastTs + clear stallEmitted, never create/split a turn}; emitOrchestratorEvents now calls it for every successful non-orchestrator verb. Result: ANY spool dispatch keeps the mid-chain-stall clock warm; the documented 'route investigation through codesearch to keep the watcher seeing activity' becomes TRUE. node --check passes; logic test: post-touch a +301s genuine idle still stalls (real detection preserved). Wrapper-only, ships via gm-plugkit cascade; the long-gap-no-instruction rs-plugkit gate is separate and unchanged. The JS checkDispatchGates bash-operation gates (spool-poll/native-search) are dormant in Claude Code by design (no PreToolUse hook, .claude/settings.json={}); the universal enforcement layers are SKILL prose + ccsniff --bash-discipline post-hoc (which DOES catch spool-polling: sleep-poll + bad-leading-cmd) + rs-plugkit in-wasm verb gates -- so the durable stall fix correctly lives in the watcher layer, not the dormant JS gate. Foreign sessions (spoint, dstate) bash-poll the synchronous spool (until/for + sleep + cat out/<verb>-N.json) which only ccsniff catches post-hoc; correct behavior is Read the out/ response (first not-found normal, re-Read next message). This-fire validations IN PRACTICE: cascade source-of-truth aligned (origin gm.json plugkitVersion 0.1.647 == live wasm 0.1.647; local was merely behind origin); supervisor resilience holding (only a version-drift self-heal, no giving-up/restart-burst); GLSL commit fbd6a9ad live (code_index.rs:114 maps .glsl/.vert/.frag/etc -> tree_sitter_c, tv8 code_index skipped_no_embed=0); recall demonstrably active (50 dispatches avg 237ms, 548 auto_recall); chains DO complete (looper prd 25/25, streaming-gltf 210 dispatches full P-E-E-V-C). idle-teardown after 1h is by-design (frees resources, cron/session reboots).
