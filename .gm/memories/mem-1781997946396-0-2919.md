---
key: mem-1781997946396-0-2919
ns: default
created: 1781997946396
updated: 1781998049455
---

DNA-SWEEP DEFERRED FINDINGS (wf_ccb66342-a22, 2026-06-21, 44 confirmed defer-prd findings needing cargo builds/deeper work). CONTEXT-ROT + RLM cluster (rs-plugkit, ~12): recall_hits returns FULL untruncated memo text (5 front-loaded every instruction dispatch); full instruction prose re-injected verbatim every turn even mid-phase; no snippet+expand handle on recall (codesearch already snippets to 400ch but offers no full-chunk fetch); route_hint+8 diagnostic drift blocks embedded every payload regardless of phase need; prd_items injected as full list AND ready_wave (overlap). Fix direction: snippet+expand handles, phase-gated payload sections, recall truncation guard -- matches arxiv 2512.24601 RLM (drilldown over context-dump) + trychroma context-rot (irrelevant context degrades retrieval). CHATGPT-MEMORY cluster (rs-plugkit memorize.rs/recall.rs + rs-learn pipeline.rs): no write-time dedup (memorize-fire mints unique key per call, restating a fact duplicates); no decay/recency weighting in recall ranking (created_at stored but unused); no salience scoring at write time (every memo equal weight); no access-frequency reinforcement (recall hits emitted as telemetry never written back); forgetting fully manual/agent-judged (no automated decay eviction); consolidation pipeline exists for long memos but never merges semantically-equivalent ones. RS-CODEINSIGHT (8): freshness digest keys mtime+path only (same-second edits skip reindex); WASM digest omits GIT_HEAD+dirty-count the native/README promise; unbounded tree recursion stack-overflows on deep files; max_file_size 200KB silently drops files no record; digest walks tree then analyze re-walks+re-reads ALL; per-byte format!("{:02x}") allocates String per byte in digest hot path; rayon par_iter no per-file panic isolation (one bad parse aborts batch); secret/SQL scanner line-based 20-clause exclusion ladder high false-positive. RS-PLUGKIT CORRECTNESS: 3 incompatible defs of 'open PRD item' across closure path; CRITICAL git_porcelain swallows git failure as clean worktree -> COMPLETE gate passes on broken git; closed-status allowlist duplicated inline 4+ times; is_unsolicited_toplevel_doc only logs never denies (no-op gate); kv_key in pending_step_full mismatches key actually read. RS-SEARCH: RRF fetches only k per retriever then truncates to k (starves cross-retriever recall); 3 host searches strictly serial (worst-case latency=sum not max); fusion_search swallows host errors to empty list+log only; payload-merge clones every id+payload across 3 passes; camel-split drops single-char segments. RS-EXEC: exec_js parse-failure path inverts synchronous contract (loses timed_out/exit_code); no upper bound on timeoutMs (floor enforced no ceiling); exec_js defaults ok=false when 'ok' absent (masks field-name drift); error JSON written to stdout not stderr (dishonest channel). Full per-finding file:line in task output w1xzrslqg.
