iter60 (2026-05-26, 0.1.592, deviations clean): the LORA-OPTIMIZATION frontier is architecturally complete but autonomously BLOCKED. The MicroLoRA/EWC instant core works (iter50 validated: feedback grows adapter, apply_adapter reflects, cross-dispatch persist) but has NO lifecycle auto-train - feedback is manual-dispatch-only. Auto-training the instant adapter hits the SAME blocker as the router (iter53): instant feedback needs {model}=the run-model, and the harness does NOT expose the running model to wasm. So both weight-bearing cores (router FastGRNN + instant MicroLoRA) are 100% functional + reachable but cannot autonomously REINFORCE - blockedBy:external on harness model-exposure. The graph core auto-learns (no model needed, auto-edge); the deep EWC needs only a loss signal. HONEST learning-100% state: 5 cores validated+reachable; autonomous weight-core training waits on the one fact the harness hides. OPTION (owner decision): a model-agnostic shared adapter trained on turn-quality would sidestep attribution but lose per-model specialization (different objective, not shipped). query_at_bounded tree-stability re-witnessed on real data (default hub limit:3 -> count:3). LESSON: the agent-as-LLM autonomous-training ceiling is set by harness model-opacity, not by the cores.