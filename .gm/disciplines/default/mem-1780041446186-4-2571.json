Audit-loop iter19 (2026-05-29 ~07:50, watcher 0.1.616): DURABLE BEHAVIORAL LESSON + resume-state. Channel degraded mid-EXECUTE again (Bash/Read intermittently empty; watcher ALIVE, spool verbs land). FINDINGS THIS FIRE (all confirmed before channel degraded): (1) all 6 repos clean ahead=0, prior commits live. (2) 8 deviations_30m all benign/understood: 4x deviation.prd-resolve-unknown (MY prior self-deviation), 2x long-gap browser (concurrent tv8/sandcastle idle), 2x push-dirty+gate-deny (my own correctly-gated COMPLETE). Zero new gm/rs-* code defects. (3) cascade for git-verb cwd fix b98eb6b STILL pending: rs-plugkit Cargo.toml=0.1.616 == gm.json plugkitVersion=0.1.616, update_available null = CI rebuild in-flight not stalled; cwd-fix in-practice validation deferred to post-deploy. (4) 1 gm ccsniff search-discipline hit is a FALSE POSITIVE (my own 'bun x gmsniff|grep deviation\\.' stdout pipe-filter, grep in 2nd pipe segment - same class as bc1cfb9 fix); ccsniff source NOT on this machine (no /c/dev/ccsniff, not in bun/npm caches) so detector fix is blockedBy:external. THE DURABLE LESSON (highest value, applies to every future fire, every LLM): (a) NEVER batch a fragile call that can parse-error (PowerShell with $LASTEXITCODE/operators, complex quoting) in the SAME parallel tool block as spool Writes — a single cancel takes the WHOLE block down, so the prd-add Writes silently never dispatch. (b) NEVER dispatch prd-resolve for a row whose prd-add you have not READ back as ok:true {added:<id>} — the prd-resolve-unknown gate correctly catches resolve-before-confirmed-add (it is NOT a plugkit bug, it is agent discipline). This caused the 4 prd-resolve-unknown deviations last iter. RESUME-STATE for next fire: 3 prd-adds dispatched this fire (prd-resolve-before-confirm-add-selfdev, steady-state-clean-iter19, ccsniff-pipe-filter-fp-external) but responses unconfirmed due to dead channel - NEXT FIRE FIRST ACTION: boot-probe, phase-status, prd-list to see which of those 3 actually landed; resolve the landed ones with the witnesses in this memo; for ccsniff-pipe-filter-fp set blockedBy:external (ccsniff repo absent locally); then residual-scan + COMPLETE. Do NOT re-add rows that already landed (would duplicate). Cron faa9e4ab every 30min. LESSON-OF-LESSONS reinforced across iter17/18/19: this Windows harness host channel (Bash/PowerShell/Read) degrades intermittently for stretches while the plugkit watcher stays alive - when it does, record resume-state to memory + let next cron fire recover, never fabricate, never resolve-blind."