Cross-session deviation diagnosed + fixed 2026-05-20 ~09:57: sibling agents in tv8 produced 11 orchestrator.error events per 30m window dispatching mutable-resolve with body shape {mutable_id, witness_evidence, witness_method}. Orchestrator's handle_resolve in rs-plugkit/src/orchestrator/mutables.rs:138 was treating the entire JSON-stringified body as the id, so every fuzzy-match check failed even though the id was right there in the body. The fuzzy hint correctly suggested 'did you mean: X' where X IS the mutable_id field; the agent was structurally correct, the orchestrator wasn't extracting. Fixed at rs-plugkit 9ad3e66: parser distinguishes JSON object (extract mutable_id|id + inline witness_evidence), JSON string, and raw text. When the yml row lacks witness_evidence but the body provides it, the inline evidence is written into the row before the status flip — making single-dispatch resolve (id + evidence) the audited path. Self-discipline this iteration: 4 bash-discipline violations (ls/grep on JSON outputs). Tools/discipline check: 3 watchers alive on v0.1.438, no drift currently, plugkit-wasm@0.1.439 live with the generalized prose.