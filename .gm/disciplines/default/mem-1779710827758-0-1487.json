Loop iter (2026-05-25): MAJOR learning-system finding. rs-learn has a full advanced architecture in rs-learn/crates/wasm/src — graph/attention, graph/temporal_core (temporal edges insert_edge/query_at/invalidate_edge/contradict), learn/deep_core (EWC: consolidate/ewc_penalty/record_loss), learn/instant_core (instant-LoRA: init_instant/feedback/apply_adapter/reset_adapter), router/core (FastGRNN: init_router/route/record_outcome), attention (init_attention/attend). dispatch.rs exposes ALL as verbs. BUT: rs-learn is NOT a Cargo dependency of rs-plugkit, and NONE of these verbs are in the gm plugkit spool ABI — the agent's only memory surface is plugkit's simple file-vec recall/memorize/memorize-prune. So rs-learn's 'massive advances' run in a STANDALONE crate ORPHANED from the live gm loop; nothing bridges plugkit->rs-learn, which is exactly why the tree-stability/tree-optimization/lora-optimization 'scheduled internal processes require manual work' — there is no bridge, so they never fire in the gm loop. Second finding: AGENTS.md-as-staging-ground -> learning migration is NOT implemented — .gm/disciplines/AGENTS.md/ frozen at May 21, no code path ingests AGENTS.md into the store (update_docs.rs prose claims it but no mechanism exists). PRDs: rslearn-pathways-orphaned-from-plugkit (large: bridge or document), agentsmd-migration-not-implemented (large: implement migration or fix prose). Deploy note: 0.1.559 landed clean via kill-all + rm wasm/version + boot.