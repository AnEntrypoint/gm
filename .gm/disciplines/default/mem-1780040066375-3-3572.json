Audit-loop iter17c RESUME-STATE (2026-05-29 ~07:33, 0.1.616): MAJOR DEFECT FOUND + FIX APPLIED (verification pending dead channel). The harness host inspection channel (Bash + PowerShell + Read-of-spool-out + fs_read-via-spool-out) is DOWN this turn — all return EMPTY — while the watcher heartbeat stays ALIVE and instruction/prd/memorize spool verbs land+respond. So I can WRITE edits and dispatch verbs but CANNOT read back host file state to verify. ROOT-CAUSE DEFECT (grounded in rs-plugkit/src/wasm_dispatch.rs source, the real reason prior cross-repo git_finalize {cwd:X} falsely reported success against gm): git verbs inconsistently honor the cwd field. BROKEN: git_status L1069 was `fn git_status(_body: &Value)` ignoring body, calling git_porcelain()=git_call(...,None) -> always watcher cwd (gm). branch_status L1096 used exec_git (no repo). git_finalize L1253 + git_push L1128 read ONLY body.get(\"repo\"), NOT cwd. WORKING (reference): git_add L1206 + git_commit L1226 already read body.get(\"cwd\").or(repo). FIX APPLIED THIS TURN via Edit to wasm_dispatch.rs (5 edits, ALL need source-integrity verification next iter because a grep showed possible line-duplication 1069+1070 and 1097+1098 — MUST confirm the file is not corrupted before commit): (1) git_status: `fn git_status(body: &Value)` + `let cwd = body.get(\"cwd\").or(repo); let porcelain = git_porcelain_in(cwd);`. (2) branch_status: added `let cwd = body.get(\"cwd\").or(repo)` and switched exec_git->exec_git_in(cwd,...) for rev-parse, config, fetch, rev-list (4 call sites). (3) git_push L1128: `let repo = body.get(\"cwd\").or(body.get(\"repo\")).map(String::from)`. (4) git_finalize L1253: same cwd.or(repo). NEXT-ITER FIRST ACTIONS (when channel healthy): (a) boot-probe + dispatch fs_read or raw Read on rs-plugkit/src/wasm_dispatch.rs lines 1068-1102 to CONFIRM NO DUPLICATION from my edits (if duplicated, the grep showing 1069+1070 both with the cwd line means a doubled insert — revert+reapply cleanly). (b) confirm it still compiles conceptually (5 edits are mechanical cwd-threading). (c) commit rs-plugkit via git_finalize {repo:\"C:/dev/rs-plugkit\"} (use `repo` field which the CURRENT shipped verb DOES read for finalize, since the cwd fix is not yet deployed to the running wasm) with both gates.rs (dead-code cleanup, still ' M') AND wasm_dispatch.rs (this cwd fix). VERIFY landing with raw git -C C:/dev/rs-plugkit log after. (d) commit rs-learn .gitignore via git_finalize {repo:\"C:/dev/rs-learn\"}. (e) resolve PRD rows reship-rslearn-gitignore, reship-rsplugkit-gates, git-verb-cwd-not-honored-confirmed with the landed shas. (f) the cwd fix only takes effect after the rs-plugkit cascade rebuilds the wasm and gm.json plugkitVersion bumps — so even after commit, this-running-watcher still has the OLD broken git verbs; use `repo` field not `cwd` for any finalize until the new wasm deploys. CRITICAL LESSON (memorize as durable rule): git_finalize/git_push read `repo` NOT `cwd`; git_status/branch_status (pre-fix) ignore both — to target a non-cwd repo with the CURRENTLY-DEPLOYED verbs, pass `repo` not `cwd`, and ALWAYS verify the commit landed with raw git ground-truth, never trust finalize's committed:true/pushed:true alone (it reports success even when it ran against the wrong/clean repo). Open PRD rows: reship-rslearn-gitignore, reship-rsplugkit-gates, git-verb-cwd-not-honored-confirmed. Cron faa9e4ab fires every 30min. validate-longgap-gate-live-catch already resolved (gate confirmed working on a concurrent tv8 session's genuine 14min idle)."