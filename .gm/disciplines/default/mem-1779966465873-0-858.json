iter (2026-05-28, 0.1.611): per-session browser profile fix DEPLOYED+VALIDATED LIVE. Killed running watcher (in-memory wrapper was pre-fix despite on-disk wrapper carrying it - the installed!=running caveat), supervisor respawned fresh watcher loading the updated wrapper, browser verb then created .gm/browser-profile-default (per-session form) not the shared .gm/browser-profile. Confirms acquireProfileDir(cwd,claudeSessionId) -> sessionProfileDir works end-to-end; concurrent agents get isolated chromium. DEPLOY METHOD for wrapper fixes: kill the running watcher pid, supervisor respawns with fresh on-disk wrapper (bun x reboot refreshes disk but a healthy running watcher keeps old in-memory code). Also supervisor clean-exit classification fixed (c42c2d14: code 0 / process-exit = planned severity:info, was flooding critical at version transitions).