Cascade repos involved (reference, drained from AGENTS.md 2026-06-06): pushing to any of these 6 AnEntrypoint repos triggers the cascade. Per-repo role: rs-exec = exec runner, browser sessions, idle cleanup, session task isolation. rs-codeinsight = code search backend, symbol indexing. rs-search = file search backend, embedding + sweep. rs-plugkit = CLI entry point, spool watcher dispatcher, version source-of-truth in Cargo.toml. rs-learn = memory backend, recall/ingest via HTTP RPC. gm = gm.json holds plugkitVersion, CI publishes the gm-skill npm package. Recall key: 'cascade repos involved roles'.