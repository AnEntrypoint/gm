Loop iter (2026-05-25): validated slug-alias fix live on plugkit 0.1.562 — prd-add {id:X} then prd-resolve {slug:X} returned resolved:X ok:true (was not-found before 5304958). CASCADE ORDERING FINDING: 0.1.562 HAS the slug fix (5304958) but NOT the codesearch dual-write/autobuild (e17696d) — codesearch returned {data:[]} pre-autobuild shape, no code_index log, codeinsight-vec empty. So commits land in plugkit-wasm versions in roughly push order, and a fix pushed late validates 1-2 cascade versions later than an earlier same-session fix. Practical: when validating a multi-commit session, check WHICH version carries WHICH commit, don't assume the latest version has all session commits. codesearch autobuild re-validation deferred to next fire after version > 0.1.562. BOOT NOTE: bun x gm-plugkit can emit 'ENOENT: failed opening cache/package/version dir' as a non-fatal warning — the wasm still downloads; if the spool-watcher-running line is absent, just re-run the boot (succeeds on retry). STILL OPEN out-of-reach: rslearn-rlib-bridge, residual-scan-browser-gate-watcher-global, codesearch-autobuild-cascade-lagged.