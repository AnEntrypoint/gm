FALSE-POSITIVE dead-spooler diagnosis + fix (babspoint 'spooler not working' that blocked browser debugging and made the agent ask the user). Root cause is NOT mechanical: the spooler is sound by construction -- .status.json is written atomically via atomicWriteJson (plugkit-wasm-wrapper.js:594 temp+renameSync), readJsonFile (:617) is parse-safe, and every long verb advertises busy_until (codesearch witnessed stamping busy_until ~360s on index rebuild; runBrowserRunner _writeStatusBusy((timeoutMs||30000)+5000)). The flat 15s stale threshold (test.js:21, bin/gm-validate.js:55) is safe because supervisor.js:272 checkWatcherHealth honors busy_until before the age check. The ONLY real trigger is a ~1.5s respawn/idle-teardown window (supervisor spawnWatcher setTimeout 1500) where status reads stale. ROOT CAUSE = no structural/prose guard stopped the LLM, on that transient/genuine dead-watcher, from asking the user + a/b-testing instead of the prescribed mechanical recovery. FIX (instructional, the surface reaching every LLM): SKILL.md dead-watcher-recovery section + AGENTS.md now carry an unmissable hard rule -- apparent tooling failure is mechanical self-service (honor busy_until, else boot+re-dispatch; on FailedToOpenSocket retry @latest never the non-@latest cache), NEVER ask the user, NEVER a/b-test; debug the live page via window.* globals + page.evaluate as process-of-elimination. Reinforces the core gm method on its OWN tooling: record all mutables, eliminate by witness, discover more, keep going. Ships via cascade SKILL.md refresh.