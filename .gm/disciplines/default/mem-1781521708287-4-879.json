IMPROVEMENT CANDIDATE (recurring, witnessed across 2026-06-15 fires): the close-of-chain forces a second git_finalize because resolving the final close-row writes .gm/prd.yml AFTER the first finalize, so residual-scan then refuses on a prd.yml-only dirty tree. .gm/prd.yml is plugkit's own state file, not user work. Candidate: the COMPLETE gate / residual-scan could treat a worktree whose ONLY dirty path is .gm/prd.yml (and other plugkit-owned managed state) as auto-committable plugkit-state rather than a blocking dirty tree, collapsing the mandatory double-finalize to one. Touches the core safety gate (gates.rs / residual scan), so it needs deliberate design + cascade CI validation, not a rushed mid-fire edit; the current double-finalize is cheap and safe, so this is an efficiency/predictability refinement, not a defect. Evaluate in a dedicated fire with its own PRD.