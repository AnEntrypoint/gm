LIVE GATE BUG witnessed 2026-06-21: the COMPLETE stop-gate (rs-plugkit gates.rs:152-155) detects open PRD items via a raw content.contains substring search over prd.yml for the literal pending-status marker (with a space). It false-positives on ANY PRD row whose description PROSE contains that literal -- e.g. the row 'fix-complete-gate-string-search' (itself marked completed) documents this exact bug and its quoted example self-blocked COMPLETE this session even though handle_list reported 0 pending rows. Structured instruction view said prd_pending=0 while the gate denied 'PRD has open items' -- the 'three incompatible definitions of open PRD item' finding manifesting. WORKAROUND used: rephrased the row description to drop the bare literal. REAL FIX (row exists, captured in defer-rs-plugkit-correctness): gates.rs must call orchestrator::prd::handle_list() and check structured status, not substring-scan the YAML. The row being marked 'completed' is STALE -- the deployed wasm still has the bug; do not trust the completed status.