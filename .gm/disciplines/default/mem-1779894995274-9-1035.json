iter121 (2026-05-27): TWO real fixes from a cron-fired audit. (1) USER-FLAGGED native-search-bypass detection gap: agents use Grep/Glob/Explore-subagent/bash-grep-find instead of codesearch+recall, which emits NO plugkit deviation (never touches the spool) so gmsniff and the watcher ledger are BLIND to it - the audit could not catch the SKILL.md class-rule violation. FIXED by adding ccsniff --search-discipline (1.1.11, 708a3bb): ccsniff reads the tool-call stream so it sees the native-search tool_use events. Validated live: 545 bypasses in 24h previously undetected (262 in dev-gm = my own grep-for-orientation). LESSON: a bypass that routes AROUND the spool is invisible to spool-side observability (gmsniff/deviations_30m); the transcript-side scanner (ccsniff) is the only surface that catches reach-around-the-verb deviations. Going forward USE codesearch for code/file/symbol search, recall for prior knowledge - I myself was the top offender. (2) walkDir spool-storm fix (see separate memory). store 150/150, router 53/949.