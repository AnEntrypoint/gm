## Resolved mutable: edge-case-process-kill-unverified

codesearch found no process.kill(pidStr) at line 2646 matching the mutable's exact description -- likely a stale line-number reference from an earlier wrapper revision (file has grown/shifted). Real kill call sites witnessed this session (killPidQuiet, killPid, gracefulCloseBrowser) all follow SIGTERM-then-verify-then-escalate pattern: process.kill(pid,'SIGTERM') followed by isProcessAliveSync polling with deadline, then taskkill/SIGKILL escalation if still alive -- the verification the mutable claims is missing IS present at the call sites this session actually witnessed. Line-number drift means the original finding's exact site could not be re-verified; the general failure mode (unverified single-shot kill) is not present in the kill helpers actually witnessed.