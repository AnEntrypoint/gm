## Resolved mutable: rs-exec-spool-side-effects-idempotent

rs-exec/src/wasm_spool.rs:16-32 write_result does kv_put("outbox", task_id, body) -- overwrite-by-id, not append; re-dispatch of same task_id overwrites same outbox key with same result. Plumbing is idempotent; the executed user code's own side effects are outside the spool contract. f∘f≡f holds for the exec spool layer.