## Resolved mutable: mut-1783250217887

Re-ran node test.js directly via Bash (not nested inside exec_js): all 10 structural guards passed PLUS the previously-timing-out live integration checks all passed cleanly this time -- instruction ok phase=EXECUTE, recall ok hits=8, health ok version=0.1.778, idempotency witness ok (memorize deduped), profile witness ok (culprits=5), mem+error witness ok (rss=50.87 err=TypeError), final PASS. Confirms hypothesis (a): the original timeout was a self-deadlock from dispatching a live spool `instruction` call from CODE RUNNING INSIDE an already-in-flight exec_js call -- the single-threaded watcher can't service a nested dispatch while the outer exec_js that spawned it is still occupying its execution slot. Not a real product defect; a caveat about test.js's own witness methodology (must be invoked directly, never nested inside another spool verb).