Loop iter (2026-05-25): LEARNING SYSTEM root cause + fix. The recall and codesearch SPOOL VERBS were routed to the LEGACY libsql backend (recall_libsql/codesearch_libsql reading stale .gm/rs-learn.db, ~12 days old, returns rows:[]) while memorize-fire and auto_recall use the FILE-VEC discipline store (.gm/disciplines/<ns>-vec/mem-*.json, actively written). Witnessed: a freshly memorize-fired sentinel was NOT retrievable via the recall verb (mode:vector_top_k rows:[] — note 'rows' key = recall_libsql shape, vs 'hits' key = file-vec recall() shape). FIX rs-plugkit 1d21ac7 wasm_dispatch.rs: 'recall'=>recall(&body), 'codesearch'=>codesearch(&body), 'memorize'=>memorize_with_raw — primary names now use file-vec; legacy reachable under _libsql suffix. host_vec_search (wrapper) reads <ns>-vec dirs fresh, scores cos*recency (HALF_LIFE 30d, RECENCY_FLOOR 0.4), jaccard-dedup 0.7. SECOND GAP: codesearch needs codeinsight_index to populate .gm/disciplines/codeinsight-vec (currently EMPTY — index never built); routing fixed but index-build trigger is a separate PRD codeinsight-index-autobuild-trigger. STILL OPEN (carry forward): bad-memory-pruning (no pruning exists, append-only store, TOP priority per user), tree/lora optimization pathway validation, temporal-recency validation, AGENTS.md-staging migration, paper sections wasm/rslearn/exec. OPERATIONAL: the watcher process DIES under browser-verb (chromium) load — saw 164s + 297s stalls with process gone; PRD watcher-dies-under-browser-load. Browser verb body is JS code executed as (async()=>{ <body> })(), NOT a {url,op} object — use 'await page.goto(...); return await page.evaluate(...)'.