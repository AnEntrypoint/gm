Loop iter (2026-05-25): MAJOR finding — cascade build-provenance bug. plugkit-wasm 0.1.562 (built from commit 5304958, which descends from e17696d in git) does NOT contain e17696d's codesearch autobuild, despite git ancestry guaranteeing it should. Proof: codesearch on empty index returns {data:[]} in 124ms with ZERO code_index/files_scanned/chunk log lines — code_index::index never called, so the committed autobuild branch (verified present via git show HEAD:src/wasm_dispatch.rs) is NOT in the running binary. Ruled out: sibling-crate-pull (rs-plugkit Cargo.toml has no git/rev pins, code is crate-local), embedding failure (memorize probe embedded:true), git ancestry (5304958 > e17696d). Conclusion: rs-plugkit release.yml publishes a wasm that does not reflect the committed source — likely a cached target/ or the wasm not regenerated per-commit. This is serious: fixes appear shipped (CI green) but are absent from the artifact, so prior 'pushed + CI-built' validations may be false. PRD cascade-build-omits-pushed-commit blockedBy out-of-reach (needs CI-log + local build cycle). VALIDATED LIVE on 0.1.562: slug-alias (prd-resolve {slug:X} works) AND memorize-prune (explicit-key delete removed probe mem+file). Version timeline: 0.1.561=codesearch Release@13:40, 0.1.562=slug Release@13:58.