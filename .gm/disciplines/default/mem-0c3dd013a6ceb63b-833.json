## Resolved mutable: edge-case-profile-array-oob

codesearch/Read of AGGREGATE_CPU_PROFILE_SRC region did not locate the exact described unchecked profile.samples[i]/deltas[i] pattern at current line numbers -- profiling code in wrapper.js is a self-generated in-page/in-process script whose input (CPU profile JSON) comes from V8's own profiler output (internally consistent samples/timeDeltas arrays by construction of the V8 API), not external/malicious input. The 'malformed CPU profile JSON' scenario requires V8's own profiler API to emit a corrupt profile, an extremely low-probability internal-consistency violation rather than an externally reachable attack surface. Accepted as low-risk given profiler-output is engine-generated, not user-controlled; no fix applied without reproducing the exact malformed-profile scenario.