iter33 browser-witness wedge (2026-05-26): dispatching the browser verb for a paper.html witness left an orphaned chrome - playwriter lost its session handle (session reset -> "Session default not found") but the chrome process (pid 310716) kept running, AND the watcher kept the entry in .gm/exec-spool/browser-ports.json + browser-sessions.json, so open_browser_sessions stayed non-empty and BLOCKED the residual-scan/COMPLETE gate. Recovery: Stop-Process the chrome pid directly + overwrite both tracking json files with {}. Re-check via instruction showed open_browser_sessions:[]. DEFECT (scoped watcher-reap-dead-browser-sessions): the watcher should reap sessions whose chrome pid is dead or whose playwriter handle is gone, so an orphaned/crashed witness browser cannot wedge COMPLETE. Also kill-port verb rejected a body with timeoutMs ("received":null despite body carrying it) - schema/parse mismatch. PRACTICAL: when a browser witness orphans, close via killing the tracked chrome_pid (from open_browser_sessions) + clearing browser-ports/sessions.json (runtime/gitignored files), not via playwriter session reset which fails once the handle is lost. The browser-ports.json also still had a dead paper-witness pid 138012 from an earlier session - confirming sessions accumulate without reaping.