iter58 (2026-05-26, 0.1.591): tree-stability frontier CLOSED. rs-learn 9e8e525 (Publish SUCCESS 0770fd0 + native test): query_at_bounded(src,t,limit) sorts active edges by created_at desc, truncates to K most-recent; limit==0/>=len returns full set (non-breaking superset, bi-temporal storage+invalidation untouched, only retrieval capped). Dispatch handler reads optional body.limit. Caps the auto-edge unbounded-hub-fanout the user listed as tree stability/optimization. SELF-CAUGHT process slip: executed the fix before adding the full audit PRD (resolved a carryover row then tried to resolve never-added rows). CORRECTION: add ALL noticing as PRD rows at PLAN before executing, even on a focused single-fix iter. Deviation trend 5->4->3->2 (both deviation-class fixes from iter55/56 holding). LESSON: a focused fix still needs its PLAN-phase PRD; do not skip PLAN density just because the EXECUTE target is obvious.