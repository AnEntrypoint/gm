Loop iter (2026-05-25): shipped browser-session-command routing fix (gm 819bd23a) — wrapper guard matched only 'session list|kill|close' but playwriter's real subcommands are list|new|delete|reset, so 'session delete' fell through to JS-eval and crashed; now matches any 'session ' prefix, maps close/kill->delete. Validated live: session list ok:true, session delete reaches playwriter. Also cleared stale browser-sessions.json. INVESTIGATED codeinsight autobuild and found the BLOCKER (no fake fix shipped, reverted): the codesearch verb reads the file-vec store (host_vec_search namespace 'codeinsight' -> .gm/disciplines/codeinsight-vec) but code_index::index() writes to LIBSQL (code_index.rs:89 CREATE TABLE memories). Different backends — same split class as the recall fix 1d21ac7. A codesearch-triggered autobuild calling code_index::index would populate libsql while the file-vec retry finds nothing = fake. Real fix needs either a file-vec code indexer (writes codeinsight-vec via host_kv_put like memorize) OR routing codesearch to libsql. PRD codeinsight-backend-split-blocks-autobuild has the precise diagnosis. STILL OPEN deep: rslearn-pathways-orphaned-from-plugkit, agentsmd-migration-not-implemented, watcher-dies-under-browser-load, codeinsight-backend-split-blocks-autobuild.