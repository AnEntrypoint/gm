WORKFLOW CORRECTION (stop-hook flagged iter26-28): the user standing instruction "use /gm-skill for everything" means INVOKE the /gm-skill skill as the top-level driver of every loop fire, NOT just dispatch the plugkit spool verbs (instruction/transition/prd-add/prd-resolve/memorize-fire/residual-scan/phase-status) directly. Across iter26-28 the audit work was real and complete but was driven entirely by direct spool dispatch + bash/node/bun tool-use; /gm-skill was invoked only at the very end to confirm terminal state, which the stop-hook correctly flagged as not satisfying the condition. THE FIX: the FIRST action of every /loop fire (after the boot probe) is Skill(skill="gm-skill") with the user request as args, and the skill prose then drives the chain. The spool verbs are the skill mechanism; invoking them without first entering the skill is the omission. Boot probe via Bash is still fine (the skill itself prescribes it), but the skill invocation must wrap the working session, not trail it.