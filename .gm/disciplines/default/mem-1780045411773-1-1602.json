SKILL.md AUTO-REFRESH — VALIDATED + LIVE BUG FOUND (2026-05-29). The refresh logic exists in TWO places, both correct (sha256-compare bundled-vs-installed + atomic tmp+rename overwrite-on-mismatch): gm-plugkit/bootstrap.js::ensureSkillMdFresh() (lines 668-726) and lib/skill-bootstrap.js::ensureSkillMdCurrent() (lines 293-337). Both target ~/.agents/skills/gm-skill/SKILL.md and ~/.claude/skills/gm-skill/SKILL.md; bundled source resolved from skills/gm-skill/SKILL.md. LIVE BUG WITNESSED via node snapshot: installed copies were sha c7a5c368af24 (29786 bytes) while bundled was 84ac09fa11ff (29875 bytes) — DIVERGENT, i.e. the installed SKILL.md was STALE and auto-refresh had NOT fired against the newer bundled file in the running watcher (booted 0.1.616). Running the refresh logic by hand overwrote both installed copies to match bundled. ROOT GAP (user's pt2 'occasionally check + update'): ensureSkillMd* runs only at bootstrap/session-start, NOT on a timer, so a long-lived watcher whose bundled SKILL.md changed mid-life (e.g. cascade shipped a new wrapper+SKILL.md) never re-refreshes the installed copy until next cold boot. Also SKILL.md freshness is gated on WRAPPER freshness: the bundled SKILL.md only updates when bootstrap fetches a new gm-plugkit. FIX directions: (a) call ensureSkillMd* on the watcher's periodic update-check tick (where update_available is recomputed) not just at boot, and (b) ensure the update-check tick itself runs on a timer in the watcher loop. Editsite: gm-plugkit/bootstrap.js + the watcher update-check path in plugkit-wasm-wrapper.js / supervisor.js.