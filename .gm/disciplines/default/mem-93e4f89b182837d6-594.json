## Resolved mutable: edge-case-yaml-parse-empty-file

FIXED lib/spool-dispatch.js:319-345 checkDispatchGates: empty/whitespace-only prd.yml or mutables.yml now pushes an explicit residual ('...is empty/truncated -- cannot confirm... done; restore from git or re-scope') instead of silently treating an empty status-list as zero-pending. Witnessed live: node -e invocation truncated .gm/prd.yml to empty, called checkDispatchGates(sess,'stop',{}), result allowed=false with the new residual present alongside pre-existing mutables/dirty-tree residuals; original file content restored after test.