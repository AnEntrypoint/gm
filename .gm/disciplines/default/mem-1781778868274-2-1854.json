PICK-UP-UNDONE-PRDs fix (2026-06-18, user task): agents that abandon (turn-stop without COMPLETE) leave PRD rows pending (chronic foreign open-PRD: wasivst 30, acptoapi 13, sandcastle 10, corpus 7 -- dead-watcher cwds sitting forever, on stale wasm). GAP: the prose had NO directive to resume inherited open rows (plan.md/entry.md resume_hits=0); the orchestrator returns ready_wave+prd_pending but no prose acted on them, so a session entering a cwd with a prior session's open rows could orphan them or plan a disjoint fresh cover. FIX (prose bundle -- ships live via the gm-plugkit .md no-rebuild + the .rs const via cascade): plan.md Cover gained '**Inherited rows resume first** -- a non-empty ready_wave/prd_pending>0 at entry is undone work to resume to prd-resolve/re-scope BEFORE new rows, never orphan/plan-over; a finishing agent leaving inherited rows pending stopped mid-transform, not completed'; entry.md continuation-invariant gained 'inherited open rows are undone work to resume, never orphan -- the chain is not done while an inherited row sits pending.' Decided AGAINST a Rust orchestrator banner (prose in BOTH the standing entry + phase plan surfaces achieves the prominence without cascade-rebuild risk; the response already carries prd_pending+ready_wave). FIX-ON-SIGHT during the sync: sync-instruction-consts.mjs realigned ALL 7 .rs consts (not just the 2 edited) -- the consts had drifted stale from the .md (sync not re-run after the 2026-06-14 prose compression; runtime unaffected since it serves the .md, but the compiled-const FALLBACK was stale); now byte-aligned. gm itself never orphans (COMPLETE gate refuses open PRD + single-finalize resolves-all-first; instruction-302 showed gm prd_pending 0). FOLLOW-UP (low-priority): a CI/publish step running the sync + failing on drift would prevent the const-drift recurring.