## Resolved mutable: version-bump-cascade-idempotent

.github/workflows/publish.yml: npm publish guarded by grep 'cannot publish over|previously published' -> skip (idempotent re-run); plugkit.version commit-back step L149-161 is gated by `git status --porcelain gm-plugkit/plugkit.version` (no-op when aligned) -- the prior drift incident is FIXED. Auto-bump (L30-62) always increments patch per push by design (every cascade push = new publishable version) with [skip ci] preventing recursion -- intentional, not a defect. Pipeline is idempotent at the publish boundary.