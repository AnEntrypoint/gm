CODEINSIGHT STALE/NOISY FIX (2026-05-31, reported by c:/dev/looper). ROOT CAUSES + FIXES, all shipped: (1) STALENESS PRIMARY: wasm_dispatch.rs::codesearch only rebuilt on explicit rebuild:true or empty-index; a non-empty-but-stale index (tree changed) was served forever (repro: 6-day-old ci-1779728* gm-tree keys). FIX: code_index.rs gains current_digest()/stored_digest()/store_digest() (git HEAD + porcelain-crc32); index() stamps digest at completion and now clear_codeinsight() BEFORE rebuild (replace not append); codesearch verb compares stored vs current digest and clear+rebuild+retry(auto_indexed) on mismatch. VALIDATED LIVE via clear-before-rebuild: codesearch returned fresh ci-1780234020375 keys, no stale survivors. (2) NOISE: host_vec_search had no cosine floor (irrelevant queries returned cos 0.48-0.53 noise; embeddings cluster tightly ~0.5). FIX: plugkit-wasm-wrapper.js COS_FLOOR=0.55 for codeinsight ns only (0 elsewhere so recall untouched), continue-below-floor. Dedup already existed (jaccard 0.7 line 1746). (3) THRASH (discovered during validation, REAL pre-existing defect): cold codesearch (133MB bge-small load + index) blocks the 5s heartbeat >30s -> supervisor STATUS_STALE_MS=30000 reaps the watcher mid-index -> respawn cold-loads again -> 16-orphan thrash, index never completes. supervisor ALREADY honors busy_until (line 214) but wrapper only stamped it for git verbs. FIX: added 'codesearch' to the busy_until set (180s) at plugkit-wasm-wrapper.js:2906. VALIDATED LIVE: busy_until stamped, watcher survived multi-minute cold index without respawn. DURABLE OPERATIONAL CAVEAT: local wrapper source edits do NOT persist - every bun-x bootstrap runs ensureWrapperFresh() copying the PUBLISHED package wrapper into ~/.gm-tools/, clobbering local edits. So wrapper fixes must ship via gm publish (cascade); validate by code-read + post-cascade, the cp-to-gmTools workaround holds only until next bootstrap. rs-plugkit changes need the wasm cascade (never local cargo).