GM_LOG SINK PATH (correcting a grep-error notice, 2026-05-29): plugkit lifecycle events from logEvent(sub,event,fields) (wrapper:409) write to ~/.claude/gm-log/<YYYY-MM-DD>/<sub>.jsonl where GM_LOG_ROOT=~/.claude/gm-log — NOT to <cwd>/.gm/exec-spool/.watcher.log (that file carries the [plugkit-wasm] stdout/dispatch lines + evt: JSON, a different stream). To verify a plugkit event (browser.idle-closed, browser.attached, browser.stale-reclaimed, instruction.served, etc) grep ~/.claude/gm-log/<day>/plugkit.jsonl; for deviations grep hook.jsonl in the same dir. gmsniff/ccsniff read these gm-log jsonl files. Witnessed: chrome-fix browser.idle-closed{cwd:C:/dev/gm,sess:claude-loop-iter,idle_ms:665495,child_pid:24556} present in plugkit.jsonl at 12:06 (56 browser.attached events too), while .watcher.log had 0 - proving the sink is gm-log not .watcher.log. ALSO: the chrome-lifecycle fix (2.0.1505) is live STACK-WIDE - browser.stale-reclaimed fired for C:/dev/spoint and C:/dev/anentrypoint-design cwds (their own watchers on 2.0.1505 reaping orphan chromes), confirming every project's watcher picked up the fix via self-stale auto-update, not just gm.