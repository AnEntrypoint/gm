CASCADE STALLED RELEASE finding (iter33, 2026-06-07, caught by audit-loop workflow scan:cascade + verified live via fetch verb): rs-plugkit Cargo.toml is at 0.1.636 but npm plugkit-wasm latest is still 0.1.635 - the 0.1.636 publish never landed because the rs-plugkit Release run (27021235843) FAILED at the 'Fetch embedding weights' step (publish-binaries skipped). The SAME-COMMIT Build run (27021235799) SUCCEEDED, so the WASM compiles and there is NO code regression. My local triad is internally consistent and healthy: .status.json version/instance_version/file_version all 0.1.635, version_drifted=false (correctly - no local drift, the install matches published 0.1.635). DURABLE LESSON: the rs-plugkit Release workflow's 'Fetch embedding weights' step is a cascade fragility point - it fetches the bge-small-en-v1.5 embedding weights from an external source, and a transient network/asset fetch failure there blocks the wasm publish while Build (compile-only) still passes. This decouples Cargo.toml-version-bumped from npm-published, leaving source one patch ahead. The version-drift gate does NOT fire (correct - it only compares the LOCAL instance vs on-disk wasm, both 0.1.635). CORRECTIVE: re-run the rs-plugkit Release workflow (cascade is push/CI-driven, never local cargo build/publish per AGENTS.md); self-resolves when 0.1.636 lands on npm and the auto-bump propagates gm.json plugkitVersion. NOT a gm-local fix - rs-plugkit's CI surface. Worth flagging to the user since a stuck publish persists until re-triggered. The audit-loop workflow's scan:cascade row correctly caught this (the first real cascade finding of the campaign) - validating the workflow's cascade-health check in practice.