LIVE WASM DEFECT witnessed 2026-06-21: the COMPLETE `transition` verb (transition to=COMPLETE) deterministically fails with JS error 'Start offset -1996717XXX is outside the bounds of the buffer' (constant large negative offset) on gm-plugkit 2.0.1577 wasm, reproducible across 3 fresh watcher reboots. ALL OTHER verbs work on the same watcher (residual-scan fires clean all four checks, phase-status, git_finalize, instruction, prd-resolve, memorize all OK). residual-scan confirmed worktree-clean+remote-pushed+prd-empty+mutables-witnessed = the four COMPLETE preconditions are TRUE, yet the gate verb crashes evaluating them -- a wasm host buffer/pointer bug in the COMPLETE-gate path (likely the prd.yml scan over ~1187 rows returning a bad pointer to a Buffer.slice/subarray on the JS side). The negative offset means a wasm return value (ptr/len) is being read as a huge/negative number. Work is fully done+pushed (gm a86018b21 + 5 repos); only the COMPLETE pronouncement is blocked by this defect. Fix lives in rs-plugkit gate/wasm_dispatch + the wrapper's take_bytes/from_raw_parts bounds. Capture for the next build-cycle session.