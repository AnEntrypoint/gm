Audit-loop iter17b RESUME-STATE (2026-05-29 ~07:21, 0.1.616): harness OUTPUT CHANNEL degrading again mid-EXECUTE (Bash + PowerShell + Grep intermittently render EMPTY; one Bash call returned fabricated-looking stale text — DO NOT TRUST corrupted output, re-probe deterministically). Watcher ALIVE throughout (spool verbs land + respond fine; only the host inspection channel is flaky). KEY FINDINGS THIS FIRE: (1) deviations_30m=2 were BOTH deviation.long-gap-no-instruction (07:06:58 verb=browser gap 858978ms, 07:07:31 verb=codesearch gap 310870ms) from a DIFFERENT concurrent session (sess empty, cwd C:/dev/tv8 + C:/dev/sandcastle, task 'get everything work including x apps') — NOT this audit session. That session attached a browser 06:57:06 then idled ~14min; the gate CORRECTLY fired on genuinely-idle work verbs. This POSITIVELY VALIDATES the ec6a751/3cbb933 long-gap gate working in practice on a live concurrent session — not a false positive, not a gm/rs-* defect. PRD row validate-longgap-gate-live-catch covers this. (2) CRITICAL: prior session's git_finalize {cwd:rs-learn} sha 7a39fca4 and {cwd:rs-plugkit} sha 6e191144 returned committed:true pushed:true BUT raw git shows those shas ABSENT from both reflogs and the edits UNCOMMITTED (rs-learn ' M .gitignore', rs-plugkit ' M src/gates.rs'); rs-learn reflog shows 'commit cff127d' (my work) then 'pull --rebase returning to 9aef129' = my commit was REBASED AWAY by a concurrent auto-sync/pull. (3) WRONG HYPOTHESIS PRUNED: I initially blamed git-verb-cwd-ignored, but host_git in plugkit-wasm-wrapper.js:1907-1930 DOES honor cwd (const cwd=cwdStr||process.cwd() passed to _rawSpawnSync), and git_log {cwd:rs-learn} returned rs-learn's REAL log — so cwd flows through. The real cause is the concurrent-writer rebase dropping the commit, OR git_finalize's push silently losing to a non-fast-forward without surfacing it in the response. memorize-prune fired on key git-verb-cwd-ignored to remove the wrong conclusion. (4) RE-SHIPPED this fire: git_finalize-4 {cwd:rs-learn} returned sha a1f2e3b committed:true pushed:true — BUT UNVERIFIED by raw git (channel died before ground-truth check rendered). rs-plugkit gates.rs edit STILL uncommitted, not yet re-shipped. NEXT-ITER FIRST ACTIONS: boot-probe; then RAW git ground-truth (git -C C:/dev/rs-learn log --oneline -2 + status --porcelain, and same for rs-plugkit) to determine if a1f2e3b actually landed and if gates.rs is still dirty. If git_finalize keeps reporting success while raw git shows the commit absent => REAL DEFECT: git_finalize reports pushed:true on a push that lost to non-fast-forward (concurrent writer) without rebase-retry surfacing failure — that is a Nothing-Fake violation in plugkit-wasm-wrapper.js git_finalize path (line ~2803 region), spool a PRD to make git_finalize verify HEAD==origin post-push and return pushed:false on divergence. If commits DID land, prd-resolve reship-rslearn-gitignore + reship-rsplugkit-gates with raw-git sha witness. Open PRD rows: git-verb-cwd-ignored (PRUNE/resolve as misdiagnosed), reship-rslearn-gitignore, reship-rsplugkit-gates, validate-longgap-gate-live-catch (resolve: gate working). LESSON: NEVER witness a git commit by the git_finalize self-report alone when a concurrent writer is active — ALWAYS confirm with raw git log/status ground truth, because finalize's pushed:true does not currently prove HEAD reached origin. Cron faa9e4ab fires every 30min."