WRAPPER ESM require() FORBIDDEN (drained from AGENTS.md 2026-06-04): plugkit-wasm-wrapper.js is ESM and runs under both node and bun; the supervisor's resolveRuntime() prefers bun. Under bun's ESM, require is not a global, so an inline const x=require('crypto'|'net'|'http'|'https'|'child_process') throws 'require is not defined' - and because those calls sit in catch(_){} blocks the failure is SILENT: it broke _ownWrapperSha12 (status.wrapper_sha stayed null, leaving the supervisor wrapper-sha-drift recycle inert), _wrapperShaAtBoot and its self-drift-restart, the synthetic-session cwd-hash, and the file-index sha - all only under the bun watcher (node-run watchers have require via CJS interop, which is why it hid). RULE: every node builtin is imported once at module scope (import crypto from 'crypto', etc.); inline require of a builtin is forbidden. Full incident: recall 'wrapper require not defined under bun'.