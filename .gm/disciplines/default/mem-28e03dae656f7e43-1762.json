CONTINUOUS-UPDATE-WARNING fire (2026-06-21, /gm-skill 'running runtime must warn continuously when a newer one is out but not running'). The machinery already existed: plugkit-wasm-wrapper.js checkUpdateViaNpm polls registry.npmjs.org/plugkit-wasm/latest every 5min -> writes .gm/exec-spool/.update-available.json {installed,latest,update_url} when latest!==installed (applyUpdateCheckResult, unlinks on catch-up) -> surfaced in turn-summary every 5s. GAP: it was NOT on the agent-facing instruction response. SHIPPED: added injectUpdateWarning(parsed) reading .update-available.json -> sets parsed.data.update_available + a loud parsed.data.update_warning restart imperative; wired into the verb post-process block (3090-3105) for instruction/transition/phase-status AND the autoRecall branch, injected AFTER capInstructionPacks so the cap cannot strip it. Continuous by construction (re-reads the file every dispatch; auto-stops when applyUpdateCheckResult unlinks on catch-up). Witnessed via standalone harness: stale 0.1.682-vs-0.1.690 -> warned with full payload, same-version -> no warning. test.js checkUpdateWarningWired guard (injectUpdateWarning defined + reads .update-available.json + update_warning + >=3 refs). ALSO FIXED the perpetual version-drift the consistency-guard kept catching: gm-plugkit/plugkit.version drifts because the cascade bumps gm.json+bin/plugkit.version but never commits the gm-plugkit copy back -> added a publish.yml step that commits gm-plugkit/plugkit.version back to main [skip ci] after publish, closing the loop; aligned to 0.1.682. LESSON: 'warn continuously' = inject on the highest-frequency agent-facing surface (instruction response), re-read the source file every dispatch so it's self-clearing, never a one-shot.