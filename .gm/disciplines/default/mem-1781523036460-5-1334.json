AUDIT-TOOL FIX + METHODOLOGY (2026-06-15, user caught inline auditor using native Grep/Glob): ccsniff src/cli.js runs each --*-discipline as a one-shot report ending process.exit(0) in source order (bash/git/verb-bypass/spool/search/glyph/continuation), so combining flags in ONE invocation silently runs only the first and drops the rest = false-clean audit. This hid the auditor's own native-search violations for the whole loop. FIXES landed in ccsniff (separate repo C:/dev/ccsniff): (1) cli.js guard - if >1 --*-discipline flag, fail loud stderr + exit 2 instead of silent single-run; (2) discipline-helpers.js targetsOutsideCwd - normalize backslashes (stripQuoted(line).replace(/\\/g,'/')) so a Grep/Glob tool whose path is a Windows-absolute sibling-repo path (C:\dev\ccsniff\...) is correctly exempted as cross-repo instead of false-flagged; +2 unit tests (14 total pass). METHODOLOGY RULE: ccsniff runs ONE discipline per invocation - run --git-discipline, --search-discipline, --glyph-discipline, --bash-discipline, --verb-bypass-discipline, --spool-discipline, --continuation-discipline as SEPARATE `bun x ccsniff@latest --<one> --since 35m` calls, NEVER combined. audit-loop.js scan:ccsniff prompt hardened to require seven separate invocations (was 'four disciplines', ambiguous + under-covering bash/verb-bypass/spool).