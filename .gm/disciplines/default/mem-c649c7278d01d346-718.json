rs-learn routing feedback loop is not closed: dispatch.rs:334-362 handle_route calls a no-op adapter route, router weights never mutate (record_outcome updates side stats only), so trained:true reports on frozen seed-random projections and feedback never influences routing. No delete/compaction path exists (host_kv_delete exported by wrapper but never imported by rs-learn). rs-search fuse_n (fusion.rs:37) silently discards caller weights on the non-identifier branch so IDENTIFIER_BOOST evaporates for natural-language queries, and lowercase tokenization treats only -_. as separators, gluing foo::bar into foobar -- query-side vs doc-side tokenization disagree for exactly the identifier shapes codesearch serves.