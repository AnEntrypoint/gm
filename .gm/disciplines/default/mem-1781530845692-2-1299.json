SUPERVISOR CONVERGENCE DONE (2026-06-15 audit fire, closes the dual-supervisor divergence from mem-1781525379333): the two divergent supervisors are unified to ONE. Evidence it was safe: gm-plugkit/supervisor.js imports only node builtins (fs/path/os/crypto/child_process) so it is standalone-runnable exactly like the old bin/plugkit-supervisor.js, and it is the more capable one (restart-burst backoff, version-drift cooldown, granular watcher-exited-clean/idle/for-update/lock-rejected/unexpectedly events, and the boot_reason isPlanned fix); it is already the implementation that runs in practice. CHANGE: lib/skill-bootstrap.js ensureSupervisorInstalled now resolves gm-plugkit/supervisor.js (require('gm-plugkit') base + bundled ../gm-plugkit/supervisor.js fallback, mirroring the proven ensureWrapperInstalled), installing it to ~/.gm-tools/plugkit-supervisor.js (target filename unchanged); bin/plugkit-supervisor.js deleted (its only consumer was ensureSupervisorInstalled; node --check passes; gm files[] ships gm-plugkit/ + depends on gm-plugkit so both resolution paths work). Both boot paths (gm-skill bootstrap + `bun x gm-plugkit spool`) now run the same supervisor + one event schema, closing the observability blind spot. Ships via gm-skill publish.yml (lib/ is a non-ignored path).