BOTTOM-UP CPU PROFILE AFFORDANCE (2026-06 /gm-skill, plugkit-wasm-wrapper.js, wrapper-only like the capture/url precedent): both surfaces now return worst-20 culprits by file:line with a capture timeframe spanning init+exec. (1) exec_js: opts.profile:true (must be nested under body.opts -- rs-plugkit wasm_dispatch.rs exec_js forwards body.opts verbatim, so top-level profile is dropped; opts is the pass-through, no Rust change needed). Wrapper writes user code to a temp .js, requires it under a node inspector Session (Profiler.enable/setSamplingInterval/start BEFORE the require so module-init+node-internal load frames are sampled), Profiler.stop, aggregate; returns {result,profile:{timeframe:{start_us,end_us,total_us,sample_count},culprits:[{location,function,self_us,self_pct,hits}]},profile_error,user_error,duration_ms}. (2) browser: 'profile\n<script>' prefix (split out of the capture branch -- the captureMatch regex already matched 'profile'), opens page.context().newCDPSession(page)+Profiler.start BEFORE the gotoPrefix IIFE so page navigation+init is sampled, stop+aggregate; same {timeframe,culprits} shape plus debug{console,pageErrors,network,performance}. CDP/inspector failure -> profile:null+profile_error, result still returns (additive). Shared aggregator source AGGREGATE_CPU_PROFILE_SRC injected into both. page.evaluate anonymous frames bucket to (program)/(native); real .js-file scripts carry file:line. LOCAL-WITNESS MECHANISM: the running watcher loads ~/.gm-tools/plugkit-wasm-wrapper.js, and bootstrap.js ensureWrapperFresh() copies node_modules/gm-plugkit/plugkit-wasm-wrapper.js OVER ~/.gm-tools on every boot -- so to witness a local wrapper edit before the cascade republishes, copy the edited wrapper to BOTH the npm node_modules source AND ~/.gm-tools, then restart so wrapper_sha matches the edit and survives. Docs in instructions/browser.md+execute.md, synced to rs-plugkit consts via sync-instruction-consts.mjs.