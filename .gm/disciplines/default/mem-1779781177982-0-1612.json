Loop iter31 (2026-05-26, 3rd consecutive Skill(gm-skill)-driven fire): FIXED the recurring watcher-dies-on-cascade-publish fragility (documented iter25/28/30, never fixed until now). Boot probe showed empty .status.json + dead watcher pid 194748: my iter30 push cascaded to publish 0.1.580 which triggered the drift-self-respawn, and the respawn left no live watcher. ROOT CAUSE: plugkit-wasm-wrapper.js drift-self-respawn (~line 2292) spawned bun x gm-plugkit@latest spool, then exited after only 2000ms AND deleted .status.json — but the replacement needs far longer (149MB wasm download via ensureReady) to produce a live watcher, so there was a window with NO watcher and NO .status.json, forcing a manual agent boot every cascade. FIX (gm 14b3eec8->8f0473e6): keep the old heartbeat alive, poll .status.json for a fresh ts from a DIFFERENT pid (bounded 90s for the download), exit only once the replacement is confirmed live; never delete .status.json before confirmation; timeout-exit fail-safe preserves old behavior on the unlucky path. This makes paper s41 system-stability "clean self-respawn" claim operationally true. LESSON: spawn-then-blind-exit with a fixed short timeout is wrong when the replacement has a long cold-start (download); the handoff must be heartbeat-confirmed, not time-boxed. The respawn-gap was the single recurring operational fragility across the whole iter26-31 loop. Also iter31: learn-debug validated REAL on 0.1.580 ([default 64/64 balanced orphans:0]); 0 deviations 30m (3rd clean window); store 64/64; AGENTS.md 9/9; rs-learn 5 cores still unwired (named open advance).