browser cross-talk CORRECTED state (2026-05-28 audit, supersedes+prunes the iter-0.1.611 "per-session chromium = no crosstalk, validated" memories which were WRONG): the per-session .gm/browser-profile-<sid> profile fix did NOT eliminate cross-talk. This session witnessed cross-talk 4x (browser-3/5/7/8) despite explicit isolated sessionId: dispatches returned session_id:83 with a CONCURRENT agent's eval output. TWO deeper root causes remain: (1) rs-plugkit wasm_dispatch.rs browser() read sessionId ONLY from the verb body, defaulting to empty -> wrapper mapped to shared per-cwd 'default' playwriter session -> concurrent agents in one cwd collided. FIXED e88a47e (now live 0.1.613): sessionId falls back to host_read('.gm/exec-spool/.session-current'). (2) STILL OPEN: the playwriter cdp-relay (shared per-cwd, ~port 19988) cross-routes responses - a concurrent session's eval result lands in the wrong out-file regardless of sessionId. Also codesearch of getOrCreateBrowserSession shows the indexed wrapper still uses a single path.join(cwd,'.gm','browser-profile') keyed only by cwd, not per-sid - so the profile-isolation claim is unreliable. FIX SPEC for the relay: per-session relay/chromium OR strict session-keyed request->response correlation + out-file carries dispatcher request id. Until shipped, concurrent-agent browser-witness in the same cwd is unreliable; single-agent is fine.