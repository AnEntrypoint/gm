Loop iter (2026-05-25): codesearch-empty bug LAYER 6 fixed + layer 7 pre-instrumented. Full saga (each layer masked the next, only visible after deploying the prior fix): L1 routing to libsql not file-vec (1d21ac7); L2 backend-split, indexer wrote libsql but searcher reads file-vec, added dual-write+autobuild (e17696d); L3 build-provenance, rust-cache constant key shipped stale wasm omitting the fix (0905378: rm wasm + cargo clean -p); L4 libsql-gate, index() aborted at ensure_schema before file-vec write (4689b7d: best-effort + entry log); L5 list_dir parsed bare strings but host returns {name,is_dir,is_file} objects -> files=0 (6a45c67); L6 read_path prepended '/' making ./src/lib.rs -> /src/lib.rs which fs.readFileSync resolved against fs root -> all 101 reads failed -> 0 chunks (23e3c2e: try relative-as-is first). L7 pre-instrumented (d8be2c7: done-summary log chunks/embedded/skipped) so if chunks still 0 after >0.1.565 the cause (extract_chunks vs embed) is instantly visible. METHODOLOGY that works for layered hidden bugs: deploy fix, observe next symptom via entry/summary logs, fix next layer; instrument PROACTIVELY (a log per stage) to collapse the diagnose-fire count. Validate codesearch on cascade > 0.1.565. Validated live this session: slug-alias, memorize-prune.