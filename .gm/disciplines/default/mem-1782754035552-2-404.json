## Resolved mutable: opts-passthrough-exec-js

rs-plugkit/src/wasm_dispatch.rs:291-298 exec_js(): let mut opts_obj = body.get("opts").cloned().unwrap_or(json!({})); inserts only timeoutMs; opts_obj.to_string() -> host_exec_js verbatim. New opts (profileTopN/sampleIntervalUs/profile) ride opts with NO Rust change. browser() :880 passes raw code through. Cascade = wrapper-only + served-prose const sync.