PRIOR-COMMIT VALIDATION (2026-06-04 ~18:30 fire): the version-resolution fix from gm 385d10c0 (prior fire) is LIVE-VALIDATED in the deployed wrapper. Running watcher self-updated to 0.1.634 via cascade, update_available=null (was falsely v0.3.6 before the fix). On-disk ~/.gm-tools/plugkit-wasm-wrapper.js: checkForUpdate() is the new minimal form (resolveVersion->cache->checkUpdateViaNpm, NO GH releases/latest block), selfHeal references registry.npmjs.org/plugkit-wasm/latest (count=1), and grep 'releases/latest' on the deployed wrapper = 0. The plugkit-bin releases/latest contamination (v0.3.6 rs-codeinsight sibling outranking plugkit v0.1.x) is fully purged from the running update path. Fix confirmed working in practice, not just in source.