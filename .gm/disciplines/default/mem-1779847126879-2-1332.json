iter76 (2026-05-27, 0.1.602): removed retired rtk binary-fetch dead code from bootstrap. AGENTS.md says the in-wasm filter verb replaces the legacy detached rtk binary download, but bin/bootstrap.js still had ~58 rtk refs (rtkBinaryName, readRtkVersion, rtkCacheDir, bootstrapRtk curl-download, spawnDetachedRtkFetch + 3 call sites, --rtk-only CLI handler, resolveCachedRtk export, pruneOldVersions rtk arm) + bin/rtk.sha256 + gm-plugkit/bootstrap.js rtk arm. CONFIRMED dead (fetched never executed; filter verb does the job in-wasm, witnessed). Removed all + orphaned platformKey + updated AGENTS.md. node -c both OK, zero refs remain. Removes a wasteful detached download on every bootstrap heal/install. LESSON 1: when AGENTS.md says X replaces Y, grep for Y - documented replacement does not delete old code; steady-state fires should hunt this drift. LESSON 2 (self-caught this fire): the rtk-removal EXECUTE stretch (many edits+greps) ran 378s between instruction dispatches and TRIPPED deviation.long-gap-no-instruction on the memorize-fire (threshold 300s). Heavy EXECUTE work MUST interleave an instruction dispatch every <5min to reset the gap clock - a long edit/grep sequence without re-anchoring is the canonical long-gap trigger. Recovery: dispatch instruction (resets last_instruction_ts) then re-fire the gated verb.