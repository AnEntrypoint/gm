## Resolved mutable: browser-handler-wrap-point

plugkit-wasm-wrapper.js:1935 host_browser_exec; the eval path (1989-2014) runs runBrowserRunner(pw,['-s',sid,'--timeout',ms,'-e',evalBody]) and returns {ok,stdout,stderr,exit_code,session_id,timeout_ms_used}. The 'timeout=<ms>\n' prefix parse at :1993-2000 is the exact pattern to mirror for an opt-in 'capture'/'profile' prefix that wraps evalBody (page.on capture preamble + async IIFE + {result,debug} return). Opt-in => default byte-unchanged, non-breaking.