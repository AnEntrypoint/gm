iter98 (2026-05-27, 0.1.603): FIXED THE ROOT CAUSE of the recurring COMPLETE-on-fresh-prompt (iter92/94/97/98 - became dominant). rs-plugkit mod.rs computed fresh_prompt = prompt present AND p.trim() != prev_prompt.trim(). Recurring /loop fires re-send the SAME prompt verbatim, so p==prev_prompt -> fresh_prompt=false -> COMPLETE chain never reset to PLAN, forcing the manual transition to=PLAN every fire. FIXED (5a147f6): fresh_prompt = prompt_opt present AND non-empty (dropped the text-inequality clause + the now-unused prev_prompt read). The poll case carries empty body (prompt_opt=None) so complete-chain-poll detection still fires correctly. Ships next wasm. The iter94 SKILL.md transition-to-PLAN doc becomes a fallback, not the primary path. LESSON: when a doc-workaround (iter94) for a recurring behavior keeps being needed every fire, the behavior is a ROOT-CAUSE bug, not an operational quirk - escalate from documenting the symptom to fixing the cause. The text-inequality freshness test is wrong for any recurring-identical-prompt driver; presence of a prompt field IS the new-work signal. cores 126/126, router count 31 quality 928, deviations 0.