iter53 (2026-05-26, 0.1.587->0.1.589): advanced the learning system from reachable to AUTONOMOUSLY-LEARNING (partial). (1) SHIPPED auto-insert-edge-on-memorize (rs-plugkit c49d583+615d1d0 i64-fix, CI build SUCCESS aff12c6, npm 0.1.589): memorize-fire now also inserts a bi-temporal graph edge over the already-computed embedding (src=ns,dst=key,relation=memorize,created_at=now), so the graph core auto-populates from real memory at zero extra embed cost. PlugkitKv made pub. (2) FINDING: only route_hint was auto-wired pre-iter53; the router/instant/deep cores cannot auto-TRAIN because the harness does NOT expose the actually-run model to wasm (env has AI_AGENT,CLAUDE_EFFORT,session-id but NO model name). Auto-record-outcome would train the router on its own recommendation = self-referential no-op. blockedBy:external, NOT shipped as a fake trainer. (3) Temporal sensitivity CONFIRMED complete: host_vec_search recency=0.4+0.6*exp(-ageMs/30day-halflife)*cos, matches paper. (4) Build failed first (i64/u64 mismatch on now), fixed by cast - push-and-let-CI-build caught it. (5) Browser crashed all session (STATUS_STACK_BUFFER_OVERRUN) - reverted the paper.html edit to respect the witness gate rather than bypass; paper-dormant-to-learning paragraph deferred to a stable-browser turn. LESSON: the agent-as-LLM router learns to ADVISE per-turn but cannot autonomously REINFORCE under a harness that hides model identity - that is the true boundary.