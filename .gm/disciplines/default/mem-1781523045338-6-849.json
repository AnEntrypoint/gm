INLINE-AUDITOR SEARCH REFLEX (reinforced 2026-06-15, user-caught regression): the inline gm-skill audit session binds to the codesearch-reflex rule identically to workflow subagents - ALL code/file/symbol lookup in the [gm] cwd routes through the codesearch verb (prior knowledge through recall, known-path content through Read/fs verb), NEVER the platform Grep/Glob tools. The auditor regressed this loop (Glob .claude/workflows/*.js + Grep audit-loop.js, both flagged native-search-glob/grep in C--dev-gm by ccsniff --search-discipline once actually run). Cross-repo Grep/Glob on a sibling repo outside the [gm] cwd (e.g. C:/dev/ccsniff while fixing it) IS exempt - no codesearch index exists for siblings - so reading/searching another project's source directly is allowed; the rule binds only within the conversation cwd that codesearch indexes.