## Resolved mutable: browser-runner-rust-involvement

plugkit-wasm-wrapper.js:2015 runBrowserRunner(pw,['-s',pwSessionId,'--timeout',...,'-e',evalBody]) -- the external agent-browser runner evaluates evalBody (raw JS) in the session page. A page.goto rides inside evalBody, so the start-URL fix is purely wrapper-side JS; rs-exec/rs-plugkit are NOT involved. No Rust change.