Loop iter (2026-05-25): codesearch-empty bug reached LAYER 5 + fixed (rs-plugkit 6a45c67). On 0.1.564 the autobuild FIRED for the first time (log 'code_index: indexing root=. files=0 libsql_ok=true') — confirming layers 1-4 (routing 1d21ac7, backend-split/dual-write e17696d, build-provenance 0905378, libsql-decouple 4689b7d) ALL work. But files=0. Root cause layer 5: code_index list_dir parsed host_fs_readdir expecting bare strings via x.as_str(), but the gm-plugkit wrapper returns rich entry OBJECTS {name,is_dir,is_file} — every object failed as_str() -> empty list -> files=0 -> empty index. Fix: list_dir extracts name/path/file from objects + accepts bare strings. The 'autobuild fires but files=0 with an entry log' was the breakthrough — the log I added in layer 4 made layer 5 instantly visible (lesson confirmed: add entry logs to silent functions). Next layer if files still 0 after >0.1.564: walk_posix's entry.contains('.') dot-heuristic for file-vs-dir, or host readdir path resolution. Validate codesearch full chain on cascade > 0.1.564. The codesearch saga is the canonical example of layered hidden bugs each masking the next — only fixable by deploying each fix and observing the next symptom.