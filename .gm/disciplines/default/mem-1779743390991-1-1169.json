Loop iter22 (2026-05-25, plugkit 0.1.577 live with all accumulated fixes): (1) Browser pid-dead is NOT a collision — gm-plugkit wrapper getOrCreateBrowserSession was firing deviation.browser-profile-collision for reason pid-dead (4x from tv8 in 8min), but a dead-pid stale entry is routine lifecycle cleanup, not an anomaly. Fix: pid-dead now logs informational plugkit/browser.stale-reclaimed; only cdp-dead (pid alive but CDP unreachable) and profile-drift (path mismatch) remain deviations. Cleanup path unchanged. (2) Validated in practice on 0.1.577: prd-add field-fallback live (title-only prd-add admitted, slug derived from title); the widened changelog classifier is live and now bounces the historical-framing shape that previously leaked. Memory store balanced 55/55, recall temporal-scored. NOTE the classifier is aggressive enough that a memory which QUOTES a changelog-trigger phrase gets self-rejected — phrase session learnings without embedding literal commit-fix-reference substrings. The full accumulated fix stack (prd-resolve nested unwrap, gates time-dedup, prd-add field-fallback, classifier-widen, kill-stale fallback) is all confirmed live.