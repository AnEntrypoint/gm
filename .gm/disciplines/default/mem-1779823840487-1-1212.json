iter55 (2026-05-26, 0.1.590): orphan-edge fix WITNESSED + long-gap-on-boot fix shipped. (1) WITNESSED orphan-edge-invalidate end-to-end on 0.1.590: memorize-fire (ns itertest55) graph_edge:true; query_at count=1; memorize-prune; query_at count=0 (edge invalidated, point-in-time excludes it). Bad-memory pruning now COMPLETE across file-vec AND graph layers - prune removes the recall hit and invalidates its graph edge together. (2) FIXED the recurring long-gap-no-instruction-on-boot deviation: every version-upgrade boot (kill->extract-149MB->new-watcher) took minutes with no dispatch, tripping the 300s gate on the first post-boot verb. FIX (gm plugkit-wasm-wrapper.js runSpoolWatcher): a fresh watcher now writes .gm/last-instruction-ts=now + clears long-gap-retry-state on boot, so the boot gap does not penalize the agent (who did not cause it). This is the systemic per-iter deviation, now structurally fixed. (3) Browser stable on 0.1.590 (lockfile-clear fix holding, eval 42). (4) complete-chain-poll deviations with empty sess are cron/stop-hook on the terminal chain, not agent action. LESSON: a fresh watcher boot must reset the long-gap clock or the upgrade mechanism is blamed for the agent idle.