Audit-loop fire (2026-06-16 iteration-2, gm-skill inline /loop 30m cron 6874e20b): the recurring own deviation.mid-chain-stall on clean audit fires (turn_idx-1, ended PLAN, prd_pending 0, idle ~310s, 1 dispatch; seen 06-12,06-14,06-15x3) is ROOT-CAUSED and FIXED. Root cause: the audit orient's ccsniff scans were ~29s EACH (7-discipline = ~203s), blowing the 300s long-gap-no-instruction budget -- the clock measures since-any-SPOOL-VERB and Bash/Read do NOT count, so a multi-minute bun x scan block with no interleaved spool verb trips the gate (live-reproduced TWICE this fire: codesearch denied 335192ms, git_finalize denied 351651ms). ccsniff was slow for two reasons, both fixed in ccsniff 944062f (c:/dev/ccsniff, reachable sibling): (1) vault() re-copied all ~2609 backed-up files every call because the skip condition dstStat.mtimeMs >= srcStat.mtimeMs fails on Windows (utimesSync->statSync loses sub-ms precision, dst a fraction < src) -- fixed by a 2s mtime tolerance; (2) replay() read+parsed all 5362 history files even with --since -- fixed by skipping files whose mtimeMs precedes the --since cutoff by >5min (drops only window-independent timestamp-less metadata, preserves every timestamped in-window tool event; proven by a CONSTANT ~4020 old-vs-new count delta across 30m/6h windows). 7-discipline --since 30m audit now ~9s (was ~203s); ccsniff tests pass; 7 disciplines still 0 for gm. The benign stall's session always walked the full P E E V C to COMPLETE (43 dispatches, prd 13/13) -- the loop finishes work, no regression. PREVENTION RULE: until the fast ccsniff is published, interleave a spool verb (or dispatch instruction) around multi-minute bun x scan blocks; gmsniff (~4s, no vault copy) is unaffected. codeinsight+search flawless (10 fresh hits recency 0.9999997); cascade 0.1.645 aligned; foreign tv8 PRD-pend 13 gate-positive.