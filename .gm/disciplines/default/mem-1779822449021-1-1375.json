iter54 (2026-05-26, 0.1.589): graph core autonomous-learning WITNESSED + orphan-edge gap closed + browser-crash root-caused. (1) auto-insert-edge WITNESSED end-to-end on 0.1.589: memorize-fire returns graph_edge:true, query_at src=default returns count:1 with the exact edge (dst=mem key, created_at match, embedding populated). Graph auto-populates from real memory. (2) FOUND+FIXED orphan-edge gap (rs-plugkit 8a1257a, build green 24662ab): memorize-prune deleted text+vec but left the auto-created graph edge orphaned; now calls invalidate_memory_edge(key) -> bi-temporal invalidate_edge (invalidate-not-delete). Pruned memories no longer leave dangling edges. (3) ROOT-CAUSED the iter53-54 browser crash (STATUS_STACK_BUFFER_OVERRUN): stale .gm/browser-profile/lockfile + 17 orphan chrome procs locked the profile. RECOVERY: kill orphan chrome + rm lockfile/Singleton*, then session works (witnessed eval=4). Durable fix (gm): isProfileLocked now also clears lowercase lockfile on dead-holder. Live-orphan reap left manual (cross-platform process-attribution too fragile to auto-kill). (4) Re-added the iter53-deferred paper dormant-to-learning paragraph + browser-witnessed it (browser fixed enabled the witness). LESSON: when a browser verb wedges with STATUS_STACK_BUFFER_OVERRUN, check .gm/browser-profile for stale lockfile + orphan chrome procs holding the profile.