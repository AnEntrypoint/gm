CLOSE-ORDERING CORRECTIVE (2026-06-15, supersedes pruned mem-1781521708287 which wrongly proposed a risky residual-scan/COMPLETE gate change): the recurring prd.yml double-finalize at chain close is eliminated by ORDERING, not a gate change. Resolve EVERY PRD row - including the final memorize-and-close row - BEFORE the single final git_finalize, witnessing the close row with the fired memo keys + close-action completeness rather than the post-finalize push sha. git_finalize then commits the complete prd.yml in one shot; the subsequent transitions (EMIT/VERIFY/COMPLETE) and residual-scan write only phase/marker files (.gm/next-step.md, .gm/residual-check-fired), never prd.yml, so the worktree stays clean through VERIFY->COMPLETE and no second finalize is needed. Zero infra risk - purely the agent's close sequencing. The push itself is the validation dispatch (verify.rs); the close row does not need the sha as witness. Demonstrated this fire: a single git_finalize closed the chain clean.