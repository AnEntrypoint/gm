Runtime-file freshness checks during /loop or gm-skill use the Read tool on .gm/exec-spool/ root files (runtime-data exception) OR the EXACT prescribed combined boot probe (cat .status.json + .turn-summary.json + date in one whitelisted Bash event) -- never an ad-hoc 'cat .turn-summary.json' Bash variant. ccsniff --bash-discipline flags off-pattern variants as bad-leading-cmd (gate-positive: the status-leading prescribed probe is whitelisted, a turn-summary-leading variant is not). Witnessed 2026-06-16 iter /loop: an ad-hoc cat freshness check run during /loop dynamic-mode handling before entering the skill tripped bad-leading-cmd (cc7f2714) while gmsniff --list-deviations showed total 0 -- COMPLEMENTARY-SURFACE rule: gmsniff deviations_30m reads plugkit's emitted deviation event-stream, ccsniff bash/git/search-discipline reads the Claude tool-call vault; a bad-leading-cmd / bash-usage issue is ccsniff-only and invisible to gmsniff, so an audit fire must consult BOTH surfaces, never trust deviations_30m=0 alone as all-clear. Do NOT widen the ccsniff whitelist to accept the variant -- that erodes the Read-over-cat discipline; the correction is behavioral.