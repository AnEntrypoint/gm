AUTO-UPDATE CONVERGENCE MAP (2026-06-11) - how every run gets new SKILL.md + plugkit. FOUR convergence paths: (1) NEW SESSION bootstrap -> ensureReady (gm-plugkit/bootstrap.js:792): resolveLatestRemoteVersion -> if versionDrift, killRunningDaemons + redownload wasm; always ensureWrapperFresh + ensureSkillMdFresh. (2) SUPERVISOR checkWatcherHealth (gm-plugkit/supervisor.js, POLL interval): on stale-status>30s SIGTERM-recycle, on wrapper_sha drift recycle, on version_drifted evict-wasm+recycle. (3) WATCHER self-drift loops (plugkit-wasm-wrapper.js, 60s): version-drift + wrapper-sha-drift; unsupervised=self-respawn-and-wait, supervised=exit-for-respawn. (4) BOOT PROBE on session entry = the backstop (catches stale/update_available -> kill-stale+reboot). GAPS FIXED THIS SESSION: (a) the SUPERVISED watcher self-exited on drift assuming the supervisor respawns it, but a DEAD supervisor meant it exited into nothing = dead spool. FIX: added _supervisorIsDead() (reads .supervisor.pid + isProcessAliveSync), OR-ed into BOTH drift loops unsupervised conditions, so a watcher whose supervisor died self-respawns-and-waits instead. (b) SKILL.md sha-compare mismatched forever on CRLF-vs-LF (content identical) -> rewrote every bootstrap (thrash). FIX: _norm(\r\n->\n) before hashing in all 3 sites (gm-plugkit/bootstrap.js, lib/skill-bootstrap.js, bin/bootstrap.js). (c) SKILL.md description still said cost,bounds,direction -> updated to witness,single-writer,direction. WITNESSED: skill CONTENT was never stale (installed==repo ignoring CRLF); convergence works. CRITICAL iter37 LESSON STILL HOLDS: do NOT kill a healthy watcher to force-upgrade (149MB flaky-TLS download); all fixes use self-respawn-and-wait (confirm replacement heartbeat before exit), never kill-before-download. Shipped gm direct-push main (no PR); ships via gm-plugkit publish + ensureWrapperFresh refreshes the installed wrapper.