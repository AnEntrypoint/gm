## Resolved mutable: migration-source-inventory

exec_js-9043 + exec_js-9042: rs-learn.db = 50 tables, ~all 0 rows (episodes=23 the only content; nodes/edges/trajectories/patterns/router_weights/ewc_fisher all 0). Real memory store is ALREADY file-per-key JSON: .gm/disciplines/<namespace>/mem-*.json with <namespace>-vec/ embedding siblings (default, AGENTS.md, codeinsight*, rs-learn_graph_* namespaces). Migration = mem-*.json -> md (text) + derived vec index, plus 23 sqlite episodes; far smaller than a full db export.