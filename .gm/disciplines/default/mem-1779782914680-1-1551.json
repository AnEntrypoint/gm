Loop iter32 (2026-05-26, plugkit 0.1.580, 4th consecutive Skill(gm-skill)-driven fire): caught a PUBLISHED-NOT-DEPLOYED gap. The iter31 respawn-confirm wrapper fix shipped to npm gm-plugkit 2.0.1439 (grep confirmed 3 matches in the published tarball) BUT the installed ~/.gm-tools/plugkit-wasm-wrapper.js did NOT have it (sha 052e790c, grep 0). ROOT CAUSE: iter31 dead-watcher recovery booted via "node ~/.gm-tools/plugkit-wasm-wrapper.js spool" (the direct-node form), which runs the installed wrapper as-is and SKIPS ensureWrapperInstalled; only "bun x gm-plugkit@latest spool" sha-checks+refreshes the installed wrapper. So a just-published wrapper fix can stay undeployed while the running watcher carries stale code. FIX: ran bun x gm-plugkit@latest spool -> installed wrapper refreshed to e6ca55eb with drift-respawn-confirmed present; the iter31 respawn fix is now actually deployed. Codified in AGENTS.md "Dead-watcher recovery prefers bun x gm-plugkit@latest spool, not direct-node boot" + verify-deployment-by-grepping-installed-wrapper-not-just-npm-version. LESSON: npm-published != installed-and-running; after any wrapper-level fix, grep the installed file, not the npm version, to confirm deployment. The 2 gmsniff deviations this window (long-gap-no-instruction verb=browser) were EXTERNAL (C:/dev/thebird, not gm) - correct gate behavior on another project, blockedBy external. gm/rs-plugkit had 0 deviations. learn-debug real [default 65/65] on 0.1.580; store 65/65; AGENTS.md 9/9; rs-learn 5 cores still unwired (named open advance).