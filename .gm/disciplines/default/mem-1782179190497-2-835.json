## Resolved mutable: terrain-false-completion

DIAGNOSED via c:/dev/terrain/.gm/prd.yml: NOT a gate bug -- rows are genuinely status:completed individually. The mechanism is SELF-CANCELLING re-plans: rows that DO work (shash3-upgrade: write Murmur3 into terrain.glsl; jsElev-gpu-probe) coexist with rows that REVERT the same work (revert-tv8-terrain-glsl: 'We incorrectly wrote ... revert to HEAD'; revert-tv8-planet-html; revert-tv8-package), each marked completed. The gate sees all-rows-done while the deliverable oscillates (do -> revert -> redo across runs), so it 'pretends finished'. Witnesses are NARRATIVE ('diff -> exit 0','executed prior turn') not captured exec output -- the witness_evidence string is unverified by the gate. Root is convergence/honesty (contradictory re-cuts + narrative witnesses), not a code gate hole.