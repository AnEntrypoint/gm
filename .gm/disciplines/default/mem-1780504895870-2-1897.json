Audit-loop fire (2026-06-03 ~16:38): CLEAN STEADY-STATE for gm/rs-*. gmsniff 35m: ZERO deviation.* tokens. ccsniff all 4 disciplines empty. All 6 repos porcelain=0 ahead=0: gm bb78f8d8 (CI auto-bump past my d7a65ee4 = prior clean-fire commit live), rs-plugkit ee481ee, rs-exec 1eb3803, rs-codeinsight 4371395, rs-search a3c8f46, rs-learn 47f2804. ABOUT:BLANK HEADED-FIX DEPLOY STATUS: gm-plugkit npm latest is now 2.0.1522 (the cascade PUBLISHED the about:blank wrapper - confirmed: repo gm-plugkit/package.json=2.0.1522 == npm view gm-plugkit version=2.0.1522). The about:blank fix IS shipped at the artifact level. BUT the running watcher (pid 15808, wrapper_sha 0b0c6a6d2612, uptime ~64min) predates it and still runs the OLD wrapper. KEY MECHANISM FINDING: update_available=null because the supervisor compares the wasm version-pin (0.1.631, unchanged), NOT the gm-plugkit npm wrapper bump - so a wrapper-only change (like about:blank) is NOT surfaced as update_available and does NOT trigger the auto-recycle. bun x gm-plugkit@latest --kill-stale-watchers returned stale_found=0 (the live watcher is within idle window = fresh, not force-killed) and the single-instance guard blocks a 2nd watcher, so a manual reboot does NOT replace the live watcher. The running-watcher pickup happens on the supervisor's natural recycle (wrapper-sha-drift check against the on-disk 2.0.1522 wrapper, or idle-limit 3600000ms expiry). Do NOT force-kill a live watcher to chase this (violates do-not-fight-process-churn). NOTICING-TO-PRD for next consideration: the supervisor's update-detection keys on wasm version-pin only; a wrapper-only npm bump (no wasm change) is invisible to update_available - if faster wrapper-pickup is wanted, the supervisor should ALSO compare the on-disk wrapper sha against the running wrapper_sha and recycle on drift. Spooled as audit-supervisor-wrapper-only-drift-detection.